---
phase: 09-duy-t-n-th-ng-b-o-theo-d-i-n
plan: 05
type: execute
wave: 2
depends_on: ["09-02", "09-04"]
files_modified:
  - apps/api/src/modules/notifications/channels/sms-channel.ts
  - apps/api/src/modules/notifications/channels/console-sms-channel.ts
  - apps/api/src/modules/notifications/channels/esms-channel.ts
  - apps/api/src/modules/notifications/channels/email-channel.ts
  - apps/api/src/modules/notifications/channels/sms-channel.test.ts
  - apps/api/src/modules/notifications/outbox-rules.ts
  - apps/api/src/modules/notifications/outbox-rules.test.ts
  - apps/api/src/modules/notifications/notification-outbox.service.ts
  - apps/api/src/modules/notifications/outbox-poller.ts
  - apps/api/src/modules/notifications/notifications.module.ts
  - apps/api/src/app.module.ts
  - .env.example
autonomous: true
requirements: [REQ-N]

must_haves:
  truths:
    - "Đơn còn WAITING sau `escalate_sms_after_s` (mặc định 90) → SMS bắn tới `notify_sms_recipients`"
    - "Duyệt/từ chối trước ngưỡng → hàng outbox L2 chuyển CANCELLED, SMS KHÔNG bao giờ gửi"
    - "Đổi `SMS_DRIVER` console↔esms KHÔNG sửa dòng logic nào — cùng interface, cùng contract test"
    - "Poller quét đúng `status='PENDING' AND scheduled_at <= now`, chạy mỗi 15s bằng @Cron in-process"
    - "KHÔNG còn lớp L4 auto-OFF (D-12 ghi đè M2.D-60) — không có code nào tự đổi công tắc nhận đơn"
    - "SMS gửi cho NHÂN SỰ QUÁN, không gửi cho khách (D-10); nội dung SMS không chứa PII của khách"
  artifacts:
    - path: "apps/api/src/modules/notifications/channels/sms-channel.ts"
      provides: "Interface SmsChannel + DI token + factory theo SMS_DRIVER"
      exports: ["SmsChannel", "SMS_CHANNEL", "buildEscalationSms"]
    - path: "apps/api/src/modules/notifications/outbox-rules.ts"
      provides: "Hàm thuần: lịch enqueue L1/L2/L3 + quyết định retry/fail"
      exports: ["planOutboxRows", "nextAttemptDecision", "OUTBOX_MAX_ATTEMPTS"]
    - path: "apps/api/src/modules/notifications/notification-outbox.service.ts"
      provides: "enqueueForNewRequest / cancelPendingForRequest / claimDue / markSent / markFailed"
    - path: "apps/api/src/modules/notifications/outbox-poller.ts"
      provides: "@Cron 15s quét + dispatch"
      contains: "@Cron"
  key_links:
    - from: "apps/api/src/modules/notifications/outbox-poller.ts"
      to: "notification_outbox (status/scheduled_at)"
      via: "claimDue() trong transaction có FOR UPDATE SKIP LOCKED"
      pattern: "SKIP LOCKED"
    - from: "apps/api/src/modules/notifications/notifications.module.ts"
      to: "SMS_CHANNEL provider"
      via: "useFactory đọc process.env.SMS_DRIVER"
      pattern: "SMS_DRIVER"
---

<objective>
Dựng hạ tầng thông báo: bảng outbox có người ghi và người đọc. `NotificationOutboxService` xếp hàng
L1/L3 ngay lúc submit và L2 (SMS) hẹn giờ `+escalate_sms_after_s`, huỷ L2 khi đơn được duyệt kịp;
`OutboxPoller` chạy `@Cron` mỗi 15s quét và bắn qua `SmsChannel`/`EmailChannel`.

Purpose: Sau khi D-12 bỏ auto-OFF, **SMS 90s là lớp DUY NHẤT còn lại tới được người đang không ngồi
trước máy** (D-15 nói thẳng: "không được cắt"). Đây là criterion 4 của ROADMAP. Outbox tồn tại để
SMS fail thì retry được và audit được, không bắn thẳng trong request handler (spec §4.6).

Output: 1 module `notifications` với 2 channel SMS hoán đổi bằng env, 1 email channel, 1 service, 1
poller, và 2 file test hàm-thuần/contract.
</objective>

<execution_context>
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-CONTEXT.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-RESEARCH.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-PATTERNS.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-VALIDATION.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-02-SUMMARY.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-04-SUMMARY.md

<interfaces>
<!-- Trích sẵn. KHÔNG đi khám phá lại. -->

apps/api/src/modules/notifications/entities/notification-outbox.entity.ts (plan 09-04):
  { id uuid, request_id varchar(36), channel 'SSE'|'SMS'|'EMAIL', recipient varchar(255),
    level 'L1'|'L2'|'L3', status 'PENDING'|'SENT'|'FAILED'|'CANCELLED', attempts int,
    last_error varchar(500)|null, scheduled_at number(epoch ms), sent_at number|null, created_at number }
  Index: `idx_outbox_scheduled_status` (scheduled_at, status) · `idx_outbox_request` (request_id)
  ⚠ KHÔNG có level `'L4'` — D-12 bỏ hẳn auto-OFF.

apps/api/src/modules/settings/settings.service.ts (phase 8):
  `readAll(): Promise<StoreSettingsMap>` — có `escalate_sms_after_s` (int, mặc định 90),
  `notify_sms_recipients` (string[]), `notify_email_recipients` (string[]), `store_phone`.
  `getOrderingStatus(nowMs)` — KHÔNG dùng ở plan này.

apps/api/src/modules/settings/settings.module.ts — export `SettingsService` (đã được `PublicModule` import).

apps/api/src/modules/maintenance/maintenance-cron.service.ts (plan 09-02) — KHUÔN MẪU `@Cron`:
  `@Injectable()` + `@Cron(...)` + `try/catch` nuốt lỗi + `Logger`, inject qua `@InjectDataSource`.
  `ScheduleModule.forRoot()` đã bật ở `app.module.ts`.

apps/api/src/modules/audit/audit.interceptor.ts dòng 108-128 — KHUÔN MẪU handler nuốt lỗi:
  `@OnEvent(name, { async: true })` + `try { ... } catch { console.error(...) }` — không bao giờ throw.

M2.D-63: `SMS_DRIVER=console` (chưa có brandname) / `SMS_DRIVER=esms` (production). Đổi driver
  **không** được sửa logic gọi.
M2.D-38: Email CHỈ dùng cho tổng hợp cuối ngày — KHÔNG dùng cho đơn mới. Ở phase 9, `EmailChannel`
  chỉ cần tồn tại đúng interface và ghi log; job tổng hợp 23:30 là phase 10.
D-10: KHÔNG bắn SMS cho khách khi bị từ chối. Outbox chỉ gửi cho nhân sự quán.
D-15: giữ nguyên SMS ở 90s.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SmsChannel interface + ConsoleSmsChannel + EsmsChannel + EmailChannel (M2.D-63)</name>
  <files>apps/api/src/modules/notifications/channels/sms-channel.ts, apps/api/src/modules/notifications/channels/console-sms-channel.ts, apps/api/src/modules/notifications/channels/esms-channel.ts, apps/api/src/modules/notifications/channels/email-channel.ts, apps/api/src/modules/notifications/channels/sms-channel.test.ts, .env.example</files>
  <read_first>
    - apps/api/src/modules/public/ip-hash.ts (khuôn mẫu module đọc env có hàm `resolve...Salt()` — bắt chước cách đọc + validate env)
    - apps/api/src/modules/audit/audit.interceptor.ts dòng 108-128 (khuôn mẫu nuốt lỗi)
    - .env.example (xem quy ước đặt tên + comment tiếng Việt của các biến hiện có)
    - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-CONTEXT.md D-10, D-15, D-16
  </read_first>
  <behavior>
    `sms-channel.test.ts` là **contract test dùng chung**: 1 hàm `describeSmsChannelContract(name, factory)`
    chạy y hệt cho `ConsoleSmsChannel` và `EsmsChannel` (với `fetch` giả). Đây chính là bằng chứng cho
    "đổi driver không sửa logic". Phủ:
    - `send({ to, message })` trả `{ ok: true }` khi thành công
    - `to` rỗng hoặc không phải SĐT hợp lệ → trả `{ ok: false, error }`, **không throw**
    - `message` dài > 300 ký tự → cắt còn 300, vẫn `ok: true` (không để 1 tin SMS khổng lồ)
    - Lỗi mạng/exception bên trong → trả `{ ok: false, error }` (KHÔNG throw ra ngoài — poller phải
      còn sống để xử lý hàng kế tiếp)
    Riêng cho `EsmsChannel` (ngoài contract chung):
    - Gọi đúng 1 lần `fetch` với method POST và JSON body chứa `Phone`, `Content`, `ApiKey`,
      `SecretKey`, `Brandname` lấy từ env
    - HTTP 200 nhưng body `{ CodeResult: '99' }` → `{ ok: false }` (eSMS trả lỗi trong body, không qua status)
    - HTTP 200 body `{ CodeResult: '100' }` → `{ ok: true }`
    - **`ApiKey`/`SecretKey` KHÔNG xuất hiện trong `error` trả về và không được `console.log`** —
      test assert chuỗi secret giả (`'SECRET-XYZ'`) không có mặt trong bất kỳ tham số nào của spy logger
    Riêng cho `ConsoleSmsChannel`:
    - Ghi log 1 dòng có tiền tố nhận biết được (vd `[SMS:console]`) chứa `to` + `message`
    `buildEscalationSms`:
    - `buildEscalationSms({ waitingSeconds: 95, pendingCount: 1 })` trả chuỗi tiếng Việt có số 95 và
      **không chứa** tên khách, SĐT khách, địa chỉ khách (test truyền input không có các field đó và
      assert chuỗi kết quả khớp regex whitelist)
    - Chuỗi kết quả ≤ 160 ký tự (1 tin SMS tiếng Việt có dấu là 70 ký tự/segment — chấp nhận ≤ 3 segment)
  </behavior>
  <action>
    **`channels/sms-channel.ts`** — module hợp đồng, không phụ thuộc implementation:
    - `export interface SmsChannel { readonly name: string; send(msg: { to: string; message: string }): Promise<{ ok: true } | { ok: false; error: string }>; }`
    - `export const SMS_CHANNEL = Symbol('SMS_CHANNEL')` — DI token.
    - `export const SMS_MAX_LENGTH = 300`.
    - `export function buildEscalationSms(input: { waitingSeconds: number; pendingCount: number }): string`
      — nội dung mẫu: `"Quán Ba Lún: có ${pendingCount} đơn online chờ duyệt quá ${waitingSeconds}s. Mở trang Hàng chờ duyệt để xử lý."`
      ⚠ **Không nội suy bất kỳ thông tin khách nào** (tên/SĐT/địa chỉ): SMS đi tới máy nhân viên, có
      thể lọt lên màn hình khoá — giảm bề mặt rò PII và cũng không cần thiết để hành động.
    - `export function isValidSmsRecipient(raw: string): boolean` — chỉ chấp nhận `^\+?\d{9,15}$` sau khi
      bỏ khoảng trắng.

    **`channels/console-sms-channel.ts`** — `@Injectable()`, implement `SmsChannel`, `name = 'console'`.
    `send()` validate `to`, cắt `message` về `SMS_MAX_LENGTH`, `Logger.log('[SMS:console] → {to}: {message}')`,
    trả `{ ok: true }`. Đây là driver mặc định khi chưa có brandname (M2.D-63).

    **`channels/esms-channel.ts`** — `@Injectable()`, `name = 'esms'`. Đọc env
    `ESMS_API_KEY`, `ESMS_SECRET_KEY`, `ESMS_BRANDNAME` (+ `ESMS_ENDPOINT` có mặc định
    `https://rest.esms.vn/MainService.svc/json/SendMultipleMessage_V4_post_json/`).
    Thiếu env → `send()` trả `{ ok: false, error: 'ESMS chưa cấu hình' }` **ngay**, không gọi mạng,
    không throw. Dùng `globalThis.fetch` (Node 20 có sẵn — KHÔNG thêm `axios`/`node-fetch`), timeout
    10s bằng `AbortSignal.timeout(10_000)`. Đọc `CodeResult` trong body JSON: `'100'` = thành công,
    khác = lỗi (đưa `CodeResult` + `ErrorMessage` vào `error`, **không** đưa key/secret vào).
    Nhận `fetchFn` qua constructor tuỳ chọn (mặc định `globalThis.fetch`) để contract test bơm giả
    được mà không cần mock global.

    **`channels/email-channel.ts`** — `@Injectable()`, interface tương tự (`send({ to, subject, body })`).
    Phase 9 chỉ cần bản `ConsoleEmailChannel` ghi log. Docblock ghi rõ M2.D-38: **Email chỉ dùng cho
    tổng hợp cuối ngày (phase 10), KHÔNG dùng cho đơn mới** — hàng L3 trong outbox tồn tại để phase 10
    có sẵn đường ống, ở phase 9 nó chỉ ghi log và đánh dấu SENT.

    **`.env.example`** — thêm khối có comment tiếng Việt:
    `SMS_DRIVER=console` (giá trị hợp lệ: `console` | `esms`; M2.D-63 — đổi driver không sửa code),
    `ESMS_API_KEY=`, `ESMS_SECRET_KEY=`, `ESMS_BRANDNAME=`. Ghi chú: để trống ở local; `SMS_DRIVER=esms`
    mà thiếu key thì SMS trả lỗi có log, **không** làm sập poller.

    Viết `sms-channel.test.ts` với hàm contract dùng chung như mô tả ở `<behavior>`.
  </action>
  <verify>
    <automated>cd apps/api && ./node_modules/.bin/vitest run src/modules/notifications/channels/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "describeSmsChannelContract" apps/api/src/modules/notifications/channels/sms-channel.test.ts` ≥ 3 (định nghĩa + 2 lần gọi cho 2 driver)
    - `grep -cE "axios|node-fetch" apps/api/src/modules/notifications/channels/esms-channel.ts` = 0
    - `grep -cE "console\.log\(.*(ApiKey|SecretKey)|logger\..*(ApiKey|SecretKey)" apps/api/src/modules/notifications/channels/esms-channel.ts` = 0
    - `grep -v '^\s*[/*]' apps/api/src/modules/notifications/channels/sms-channel.ts | grep -cE "customer_name|customer_phone|customer_address"` = 0
    - `grep -c "SMS_DRIVER" .env.example` = 1
    - `cd apps/api && ./node_modules/.bin/vitest run src/modules/notifications/channels/` exit 0 với ≥ 14 test pass
  </acceptance_criteria>
  <done>2 implementation SMS cùng qua 1 contract test — criterion 4 phần "đổi driver không sửa logic" có bằng chứng tự động.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: outbox-rules.ts (hàm thuần) + NotificationOutboxService (enqueue / cancel / claim / mark)</name>
  <files>apps/api/src/modules/notifications/outbox-rules.ts, apps/api/src/modules/notifications/outbox-rules.test.ts, apps/api/src/modules/notifications/notification-outbox.service.ts</files>
  <read_first>
    - apps/api/src/modules/notifications/entities/notification-outbox.entity.ts (plan 09-04)
    - apps/api/src/modules/settings/settings.service.ts (`readAll()` và `StoreSettingsMap`)
    - apps/api/src/modules/public/public-orders.service.ts (khuôn mẫu service inject `@InjectDataSource` + raw query có `FOR UPDATE`)
    - apps/api/src/modules/public/store-status.ts (khuôn mẫu module thuần)
    - docs/MILESTONE-02-ONLINE-ORDERING-SPEC.md dòng 464-469 (lịch L1/L2/L3 — **bỏ L4 theo D-12**)
  </read_first>
  <behavior>
    `outbox-rules.test.ts` (hàm thuần, không DB) phủ:
    - `planOutboxRows({ requestId, nowMs, escalateSmsAfterS: 90, smsRecipients: ['0900000001'], emailRecipients: ['a@b.c'] })`
      trả đúng 3 hàng: L1/SSE `scheduled_at = nowMs`; L3/EMAIL `scheduled_at = nowMs`;
      L2/SMS `scheduled_at = nowMs + 90_000`
    - `smsRecipients` có 2 số → sinh **2** hàng L2 (mỗi người 1 hàng, retry độc lập)
    - `smsRecipients` rỗng → **không** sinh hàng L2 nào (không tạo rác)
    - `emailRecipients` rỗng → không sinh L3
    - `escalateSmsAfterS: 0` → L2 `scheduled_at = nowMs` (bắn ngay, hợp lệ)
    - **Không hàng nào có `level = 'L4'`** — assert tường minh, đây là bằng chứng D-12 đã được thi hành
    - Mọi hàng trả về đều `status: 'PENDING'`, `attempts: 0`, `sent_at: null`
    - `nextAttemptDecision({ ok: false, attempts: 1 })` → `{ status: 'PENDING' }` (còn thử lại)
    - `nextAttemptDecision({ ok: false, attempts: OUTBOX_MAX_ATTEMPTS })` → `{ status: 'FAILED' }`
    - `nextAttemptDecision({ ok: true, attempts: 1 })` → `{ status: 'SENT' }`
    - `OUTBOX_MAX_ATTEMPTS` = 3
  </behavior>
  <action>
    **`outbox-rules.ts`** — module thuần (không Nest/TypeORM):
    - `export const OUTBOX_MAX_ATTEMPTS = 3`
    - `export type PlannedOutboxRow = { request_id, channel, recipient, level, status: 'PENDING', attempts: 0, last_error: null, scheduled_at: number, sent_at: null }`
    - `export function planOutboxRows(input): PlannedOutboxRow[]` — theo `<behavior>`.
      Docblock: "Spec §7 dòng 465-469 liệt kê 4 lớp L1..L4. **L4 (AUTOOFF) đã bị D-12 xoá hẳn** —
      không có cơ chế nào tự đổi trạng thái công tắc nhận đơn nữa. Ai định thêm lại L4 phải sửa
      OVERRIDE-DEBT trước."
    - `export function nextAttemptDecision(input: { ok: boolean; attempts: number }): { status: 'SENT'|'FAILED'|'PENDING' }`

    **`notification-outbox.service.ts`** — `@Injectable()`, inject `@InjectDataSource() ds: DataSource`
    và `SettingsService`:
    - `enqueueForNewRequest(requestId: string, nowMs: number, mgr?: EntityManager): Promise<void>` —
      đọc settings, gọi `planOutboxRows`, `insert` nhiều hàng. Nhận `mgr` tuỳ chọn để plan 09-09 gọi
      **bên trong transaction submit** (outbox pattern: hàng thông báo và hàng đơn cùng commit hoặc
      cùng rollback — không được có đơn mà không có lịch SMS).
    - `cancelPendingForRequest(requestId: string, mgr?: EntityManager): Promise<number>` —
      `UPDATE notification_outbox SET status='CANCELLED' WHERE request_id=? AND status='PENDING'`,
      trả số dòng. Gọi khi confirm/reject (spec §7 dòng 489). Docblock: giữ dòng CANCELLED thay vì
      xoá để còn chứng minh được "đã duyệt kịp nên không bắn SMS".
    - `claimDue(nowMs: number, limit: number): Promise<NotificationOutbox[]>` — trong 1 transaction
      NGẮN: `SELECT id FROM notification_outbox WHERE status = 'PENDING' AND scheduled_at <= ? ORDER BY scheduled_at ASC LIMIT ? FOR UPDATE SKIP LOCKED`
      rồi `UPDATE ... SET attempts = attempts + 1 WHERE id IN (...)`, commit, trả về các hàng đã lấy.
      ⚠ **KHÔNG** giữ transaction mở trong lúc gọi mạng — dispatch nằm ngoài (poller, Task 3).
      `SKIP LOCKED` để 2 tick chồng nhau không lấy trùng hàng.
    - `markSent(id, nowMs)` / `markFailed(id, attempts, error)` — dùng `nextAttemptDecision` để chọn
      `status`; `last_error` cắt còn 500 ký tự.
    - `pendingSmsCount(nowMs)` — số hàng L2 đang PENDING đã tới hạn, dùng cho nội dung SMS
      (`buildEscalationSms.pendingCount`) và cho log.

    Viết `outbox-rules.test.ts` phủ đủ `<behavior>`. Service không cần unit test riêng ở plan này —
    hành vi DB của nó được chứng minh ở plan 09-08 (integration MySQL thật).
  </action>
  <verify>
    <automated>cd apps/api && ./node_modules/.bin/vitest run src/modules/notifications/outbox-rules.test.ts && ../../node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'L4'" apps/api/src/modules/notifications/outbox-rules.ts` = 0
    - `grep -c "SKIP LOCKED" apps/api/src/modules/notifications/notification-outbox.service.ts` = 1
    - `grep -c "escalate_sms_after_s" apps/api/src/modules/notifications/notification-outbox.service.ts` = 1
    - `grep -c "CANCELLED" apps/api/src/modules/notifications/notification-outbox.service.ts` ≥ 1
    - `grep -cE "from '@nestjs|from 'typeorm'" apps/api/src/modules/notifications/outbox-rules.ts` = 0
    - `cd apps/api && ./node_modules/.bin/vitest run src/modules/notifications/outbox-rules.test.ts` exit 0 với ≥ 11 test pass
  </acceptance_criteria>
  <done>Quy tắc xếp hàng/huỷ/retry có bằng chứng tự động; service có đủ 5 phương thức cho poller và cho luồng duyệt đơn.</done>
</task>

<task type="auto">
  <name>Task 3: OutboxPoller @Cron 15s + NotificationsModule + đăng ký app.module</name>
  <files>apps/api/src/modules/notifications/outbox-poller.ts, apps/api/src/modules/notifications/notifications.module.ts, apps/api/src/app.module.ts</files>
  <read_first>
    - apps/api/src/modules/maintenance/maintenance-cron.service.ts (plan 09-02 — khuôn `@Cron` + try/catch + Logger)
    - apps/api/src/modules/notifications/notification-outbox.service.ts (Task 2)
    - apps/api/src/modules/notifications/channels/sms-channel.ts (Task 1 — token `SMS_CHANNEL`)
    - apps/api/src/modules/settings/settings.module.ts (cách export service cho module khác import)
    - apps/api/src/app.module.ts (đã có `ScheduleModule.forRoot()` từ plan 09-02)
    - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-RESEARCH.md § Pattern 3
  </read_first>
  <action>
    **`outbox-poller.ts`** — `@Injectable()`:
    - `@Cron('*/15 * * * * *')` trên method `tick()` (D-19: mỗi 15s).
    - **Chống chồng lấn:** field `private running = false`; đầu `tick()` nếu `running` thì `return`
      ngay (log `debug`), cuối đặt lại trong `finally`. Lý do: 1 tick gọi mạng có thể lâu hơn 15s;
      không có guard thì 2 tick cùng dispatch. Kết hợp với `SKIP LOCKED` ở `claimDue` là đủ cho
      triển khai 1 container (D-19 chốt in-process). Ghi comment: nếu sau này scale nhiều instance
      thì `SKIP LOCKED` là lớp bảo vệ chính, guard này chỉ là lớp thứ hai.
    - Mỗi tick: `rows = await outbox.claimDue(Date.now(), 20)`; với mỗi row:
      · `channel === 'SMS'` → `smsChannel.send({ to: row.recipient, message: buildEscalationSms({...}) })`
      · `channel === 'EMAIL'` → `emailChannel.send(...)` (phase 9 chỉ log — M2.D-38)
      · `channel === 'SSE'` → **KHÔNG gửi lại gì**; event SSE đã được emit ngay lúc submit (plan 09-09).
        Hàng L1 chỉ tồn tại để audit "đã có thông báo tức thời". Đánh dấu `SENT` luôn. Ghi comment
        nói rõ để người sau không nghĩ SSE đang bị bỏ sót.
      · Kết quả → `markSent`/`markFailed`.
    - Toàn bộ `tick()` bọc `try/catch`, log lỗi, **không throw** (khuôn `AuditEventHandler`).
    - Log 1 dòng mỗi tick CÓ xử lý (`rows.length > 0`); tick rỗng không log để không ngập log
      (4 tick/phút × 24h).

    **`notifications.module.ts`**:
    - `imports: [TypeOrmModule.forFeature([NotificationOutbox]), SettingsModule]`
    - `providers: [NotificationOutboxService, OutboxPoller, ConsoleSmsChannel, EsmsChannel, EmailChannel,
      { provide: SMS_CHANNEL, useFactory: (c: ConsoleSmsChannel, e: EsmsChannel) =>
        (process.env.SMS_DRIVER ?? 'console').toLowerCase() === 'esms' ? e : c,
        inject: [ConsoleSmsChannel, EsmsChannel] }]`
      Giá trị `SMS_DRIVER` lạ → rơi về `console` (fail-safe: thà log còn hơn im lặng không gửi gì).
      Log 1 dòng lúc khởi động cho biết driver nào đang hoạt động.
    - `exports: [NotificationOutboxService]` — plan 09-06 (huỷ L2 khi duyệt) và 09-09 (enqueue lúc
      submit) đều cần.
    Thêm `NotificationsModule` vào `imports` của `app.module.ts`.

    **Kiểm bằng tay có kịch bản** (ghi kết quả vào SUMMARY): chèn 1 hàng outbox SMS đến hạn bằng
    MySQL, chờ ≤ 20s, xác nhận log `[SMS:console]` xuất hiện và `status` chuyển `SENT`.
  </action>
  <verify>
    <automated>cd apps/api && ../../node_modules/.bin/tsc --noEmit && ./node_modules/.bin/vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@Cron('\*/15 \* \* \* \* \*')" apps/api/src/modules/notifications/outbox-poller.ts` = 1
    - `grep -c "private running" apps/api/src/modules/notifications/outbox-poller.ts` = 1
    - `grep -c "SMS_DRIVER" apps/api/src/modules/notifications/notifications.module.ts` = 1
    - `grep -c "NotificationsModule" apps/api/src/app.module.ts` = 2
    - `grep -c "exports: \[NotificationOutboxService\]" apps/api/src/modules/notifications/notifications.module.ts` = 1
    - **Chạy thật, console driver**: khởi động API; `mysql -h127.0.0.1 -P3307 -uorder_app -porder_app_pass order_quan_balun -e "INSERT INTO notification_outbox (id, request_id, channel, recipient, level, status, attempts, scheduled_at, created_at) VALUES (UUID(), 'test-req-1', 'SMS', '0900000001', 'L2', 'PENDING', 0, UTC_TIMESTAMP(6), UTC_TIMESTAMP(6));"` → trong ≤ 20s log API in dòng `[SMS:console]`, và `SELECT status, attempts FROM notification_outbox WHERE request_id='test-req-1'` → `SENT`, `attempts = 1`. Xoá dòng test sau khi kiểm.
    - **Đổi driver không sửa code**: đặt `SMS_DRIVER=esms` (không có key) rồi lặp lại → hàng chuyển `PENDING` (attempts 1) rồi sau 3 lần thành `FAILED` với `last_error` chứa "ESMS chưa cấu hình"; **không** có dòng code nào phải sửa giữa 2 lần chạy. Trả `SMS_DRIVER` về `console`, xoá dòng test.
    - `cd apps/api && ./node_modules/.bin/vitest run` full suite exit 0
  </acceptance_criteria>
  <done>Outbox có người đọc chạy thật mỗi 15s; đổi `SMS_DRIVER` bằng env đổi được driver mà không sửa dòng code nào.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dữ liệu đơn (nội bộ) → nội dung SMS gửi ra mạng viễn thông | SMS đi qua bên thứ ba (eSMS) và hiện trên màn hình khoá điện thoại nhân viên |
| env secret (`ESMS_*`) → log / message lỗi | Secret rò qua log là đường rò phổ biến nhất của tích hợp SMS |
| job nền → bảng outbox | Nhiều tick / nhiều instance cùng đọc 1 hàng |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-19 | Information Disclosure | PII khách (tên/SĐT/địa chỉ) lọt vào nội dung SMS gửi ra ngoài | mitigate | `buildEscalationSms` chỉ nhận `waitingSeconds`/`pendingCount`; test assert chuỗi kết quả không chứa PII; acceptance criteria grep field khách trong `sms-channel.ts` = 0 |
| T-09-20 | Information Disclosure | `ESMS_API_KEY`/`ESMS_SECRET_KEY` rò qua log hoặc qua `error` trả về | mitigate | `error` chỉ ghép `CodeResult` + `ErrorMessage`; test bơm secret giả `SECRET-XYZ` và assert không xuất hiện ở logger spy; grep trong acceptance criteria |
| T-09-21 | Spoofing | `notify_sms_recipients` bị nhồi giá trị không phải SĐT (SMS đi sai người / injection vào payload eSMS) | mitigate | `isValidSmsRecipient` regex `^\+?\d{9,15}$`; `send()` trả `{ok:false}` thay vì gọi mạng |
| T-09-22 | Tampering | 2 tick / 2 instance cùng lấy 1 hàng → gửi SMS trùng | mitigate | `FOR UPDATE SKIP LOCKED` khi claim + guard `running` in-process. **Chấp nhận rủi ro còn lại:** triển khai nhiều instance đồng thời chưa được kiểm; D-19 chốt in-process 1 container, ghi lại giới hạn này trong SUMMARY |
| T-09-23 | Denial of Service | 1 hàng lỗi vĩnh viễn bị thử lại vô hạn, hoặc exception làm chết `@Cron` | mitigate | `OUTBOX_MAX_ATTEMPTS = 3` rồi `FAILED`; `tick()` bọc try/catch nuốt lỗi; channel không bao giờ throw (contract test khẳng định) |
| T-09-24 | Elevation of Privilege | Code auto-OFF (L4) lén quay lại và tự tắt công tắc nhận đơn của quán | mitigate | Union `level` không có `'L4'`; `planOutboxRows` có test assert không sinh L4; docblock chỉ tới D-12 + OVERRIDE-DEBT |
| T-09-25 | Information Disclosure | SMS gửi cho **khách** khi đơn bị từ chối (trái D-10) | mitigate | Recipient chỉ lấy từ `notify_sms_recipients` (cấu hình quán); không có đường code nào đọc `customer_phone` trong module này — grep trong acceptance criteria |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | Plan này KHÔNG cài package mới. eSMS gọi bằng `globalThis.fetch` của Node 20; acceptance criteria grep `axios|node-fetch` = 0 |
</threat_model>

<verification>
1. `cd apps/api && ./node_modules/.bin/vitest run src/modules/notifications/` — xanh (≥ 25 test)
2. `cd apps/api && ./node_modules/.bin/vitest run` — full suite xanh
3. `cd apps/api && ../../node_modules/.bin/tsc --noEmit` — sạch
4. 2 kịch bản chạy thật ở acceptance criteria Task 3 (console gửi được; esms thiếu key thì FAILED có log, không sập)
5. `SELECT COUNT(*) FROM notification_outbox WHERE level = 'L4'` = 0
</verification>

<success_criteria>
- 3 lớp L1/L2/L3 xếp hàng đúng lịch; không tồn tại L4
- Poller `@Cron` 15s chạy thật, có chống chồng lấn, không bao giờ throw ra ngoài
- `SMS_DRIVER` đổi console↔esms bằng env, có contract test chung chứng minh không cần sửa logic
- Secret eSMS không rò ra log/error; SMS không chứa PII khách; không gửi SMS cho khách
- Full suite xanh
</success_criteria>

<output>
Create `.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-05-SUMMARY.md` when done
</output>
