---
phase: 09-duy-t-n-th-ng-b-o-theo-d-i-n
plan: 11
type: execute
wave: 6
depends_on: ["09-09"]
files_modified:
  - apps/shop/src/components/BannerNotice.tsx
  - apps/shop/src/components/OrderStepper.tsx
  - apps/shop/src/lib/order-update.ts
  - apps/shop/src/lib/order-update.test.ts
  - apps/shop/src/pages/OrderTrackPage.tsx
  - packages/schemas/src/public-orders.ts
  - apps/api/src/modules/public/public-orders.controller.ts
  - apps/api/src/modules/public/cancel-order.ts
  - apps/api/src/modules/public/cancel-order.test.ts
autonomous: true
requirements: [REQ-O]

must_haves:
  truths:
    - "Khách mở `/o/<token>` thấy % lớn + 5 mốc trạng thái ngang + nhãn mốc hiện tại, poll 5-10s tự cập nhật"
    - "% không bao giờ tụt trên màn hình (BE đã đơn điệu; FE không tự tính lại %)"
    - "Đơn bị từ chối: THAY HẲN stepper bằng banner đỏ + lý do nguyên văn + nút gọi quán; ẩn số %"
    - "Món bị huỷ: banner tone info 'Quán đã cập nhật đơn của bạn' hiện phía TRÊN stepper (M2.D-21)"
    - "Quán sửa đơn khi khách đang mở trang → banner cập nhật + danh sách món và tổng tiền mới (M2.D-47)"
    - "Sau khi đơn được xác nhận, khách bấm 'Muốn sửa đơn?' thấy SĐT quán + nút gọi, không có ô tự sửa (M2.D-45, M2.D-46)"
    - "Đơn còn WAITING: khách tự huỷ được (M2.D-44 nửa huỷ) — nút Huỷ đơn + hộp xác nhận, huỷ xong chuyển sang CANCELLED_BY_CUSTOMER"
    - "Đơn đã CONFIRMED: nút Huỷ đơn BIẾN MẤT — khách chỉ còn SĐT + nút gọi, khớp M2.D-45/46"
    - "Khách bấm Huỷ đúng lúc admin bấm Xác nhận: đúng MỘT bên thắng, bên thua nhận lỗi rõ ràng, không bao giờ có đơn vừa CONFIRMED vừa CANCELLED"
    - "`bash scripts/check-shop-bundle.sh` vẫn OK — KHÔNG nới ngưỡng 370 kB, KHÔNG thêm npm package cho apps/shop"
  artifacts:
    - path: "apps/shop/src/components/OrderStepper.tsx"
      provides: "Stepper 5 mốc ngang, SVG tự vẽ, chỉ dùng design token"
    - path: "apps/shop/src/lib/order-update.ts"
      provides: "Hàm thuần phát hiện quán vừa sửa đơn giữa 2 lần poll"
      exports: ["detectOrderUpdate"]
    - path: "apps/shop/src/pages/OrderTrackPage.tsx"
      provides: "Trang theo dõi đơn đầy đủ REQ-O"
    - path: "apps/api/src/modules/public/cancel-order.ts"
      provides: "Huỷ đơn phía khách khi còn WAITING (M2.D-44 nửa huỷ), cùng row lock với 09-06"
      exports: ["cancelOrderByCustomer"]
  key_links:
    - from: "apps/shop/src/pages/OrderTrackPage.tsx"
      to: "OrderStepper"
      via: "import + render tại điểm chèn đã đánh dấu sẵn"
      pattern: "OrderStepper"
    - from: "apps/shop/src/components/BannerNotice.tsx"
      to: "tone 'info' + InfoGlyph"
      via: "mở rộng union Tone + TONE_STYLES"
      pattern: "info"
---

<objective>
Điền nội dung REQ-O vào `/o/:token` — mặt trận B của 09-UI-SPEC: khách trên di động thấy % tiến độ,
5 mốc, cảnh báo món huỷ, trạng thái từ chối, và biết gọi quán khi muốn sửa đơn.

Purpose: Đây là mặt khách của criterion 5 và là nơi G-1 ("khách yên tâm mà không thấy trạng thái
từng món") thành hình. Phase 8 đã để sẵn comment `{/* ── Chỗ chèn phase 9 (REQ-O) ── */}` đúng vị
trí — chèn vào đó, **không viết lại trang**.

**Ràng buộc cứng:** `apps/shop` chỉ còn ~18 kB trong ngưỡng 370 kB của `scripts/check-shop-bundle.sh`.
Không npm package mới (không chart lib, không animation lib, không icon package). Chỉ CSS + SVG tự vẽ
theo khuôn `CheckGlyph` đã có trong chính file này.

Output: 1 tone banner mới, 1 component stepper, 1 hàm thuần + test, 1 trang được mở rộng.
</objective>

<execution_context>
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-CONTEXT.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-PATTERNS.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-UI-SPEC.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-09-SUMMARY.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-UI-SPEC.md

<interfaces>
<!-- Trích sẵn. KHÔNG đi khám phá lại. -->

`PublicOrderStatus` sau plan 09-09 (16 field):
  order_token · status ('WAITING'|'CONFIRMED'|'REJECTED'|'CANCELLED_BY_CUSTOMER') ·
  fulfillment_type · items[{name,qty,unit_price}] · subtotal · submitted_at_ms · store_phone ·
  reject_reason (string|null) · stage · stage_label · percent (0..100) · cancelled_count ·
  cancelled_note (string|null) · eta_min (number|null) · eta_max (number|null) · updated_at_ms
  ⚠ **Không có** trạng thái từng item (M2.D-23) — đừng đi tìm.

apps/shop/src/pages/OrderTrackPage.tsx — điểm chèn CÓ SẴN ở dòng ~64-67:
```tsx
{/* ── Chỗ chèn phase 9 (REQ-O): banner % tiến độ + 5 mốc trạng thái +
    banner "quán vừa cập nhật đơn" đặt NGAY TẠI ĐÂY, phía trên danh sách
    món bên dưới. ... ── */}
```
Trang hiện dùng `useApi('/api/public/orders/${token}', PublicOrderStatus, { skip: !token })` →
`{ data, loading, error, reload }`. `masked = token.slice(0,4).toUpperCase() + '…'` — **giữ nguyên**,
không bao giờ render token đầy đủ (OD-02, C-INFRA-03).
`CheckGlyph` (dòng ~97-114) — KHUÔN SVG tự vẽ: `stroke="var(--herb-600)"`, `strokeWidth={2}`,
`strokeLinecap="round"`, `aria-hidden="true"`. Icon mới phải theo đúng khuôn này.
`ctaButton` — style nút gọi quán đã có, tái dùng.
Mọi style là `const X: CSSProperties = {...}` dùng `var(--...)`.

apps/shop/src/components/BannerNotice.tsx (166 dòng):
```typescript
type Tone = 'brand' | 'warn' | 'danger';                       // → thêm 'info'
const TONE_STYLES: Record<Tone, { bg: string; text: string }> = {
  brand:  { bg: 'var(--brand-100)',  text: 'var(--text-strong)' },
  warn:   { bg: 'var(--warn-100)',   text: 'var(--warn-600)' },
  danger: { bg: 'var(--danger-100)', text: 'var(--danger-600)' },
};
```
`renderIcon()` (dòng ~73-77) cần nhánh thứ 4. **`InfoGlyph` ĐÃ TỒN TẠI trong file (dòng ~79-86)
nhưng chưa tone nào dùng** — chỉ cần nối dây, không vẽ mới.
Nhánh `role="alert"` (danger) vs `role="status"` (còn lại) ở dòng ~59-70: `'info'` rơi vào nhánh
`role="status"`, không cần thêm nhánh.
Props: `{ tone, title, body?, action?: { label, href } | { label, onClick } }`.

apps/shop/src/styles/tokens.css — token cần dùng (đã có sẵn, KHÔNG thêm token mới):
  `--ok-600`/`--ok-100` · `--warn-600`/`--warn-100` · `--danger-600`/`--danger-100` ·
  `--info-600`/`--info-100` · `--border-default` · `--bg-surface` · `--text-faint` ·
  `--fs-3xl` (36px) · `--fs-lg` (20px) · `--fs-base` · `--fs-sm` · `--fw-heavy` (800) · `--fw-bold` (700) ·
  `--sp-1..--sp-16`

09-UI-SPEC.md § "B. /o/:token" — hợp đồng thị giác:
  - Stepper **NGANG kể cả ở 375px** (Giả định #4), 5 node tròn nối bằng đường kẻ `flex: 1`
  - Node đã qua/hiện tại 20px, node chưa tới 14px, đường nối cao 2px
  - Node đã qua: fill `--ok-600` + icon check trắng nhỏ; node hiện tại: warn nếu còn `RECEIVED`,
    ok nếu từ `CONFIRMED` trở lên, có pulse nhẹ (**tôn trọng `prefers-reduced-motion`**);
    node chưa tới: viền `--border-default`, nền `--bg-surface`, icon mờ `--text-faint`
  - **Không** hiện 5 nhãn cùng lúc — chỉ 1 dòng `stage_label` (`--fs-lg`/`--fw-bold`) dưới stepper
  - Số `{percent}%` to (`--fs-3xl`/`--fw-heavy`/`--ok-600`), ETA phụ `--fs-sm`/`--text-muted`
  - `REJECTED` → **thay hẳn** stepper bằng banner `danger`, ẩn %; danh sách món + tổng tiền vẫn hiện
  - Banner món huỷ đặt **NGAY TRÊN** stepper/banner rejected

scripts/check-shop-bundle.sh — 2 gate: 11 chuỗi cấm + `MAX_JS_KB=370`. Hiện dùng ~352 kB.
  Chạy: `cd apps/shop && ./node_modules/.bin/vite build && cd ../.. && bash scripts/check-shop-bundle.sh`
  ⚠ File script ghi rõ: "muốn nâng phải sửa số này VÀ ghi lý do ngay tại đây" — plan này **KHÔNG** nâng.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BannerNotice tone 'info' + OrderStepper + hàm thuần phát hiện quán sửa đơn</name>
  <files>apps/shop/src/components/BannerNotice.tsx, apps/shop/src/components/OrderStepper.tsx, apps/shop/src/lib/order-update.ts, apps/shop/src/lib/order-update.test.ts</files>
  <read_first>
    - apps/shop/src/components/BannerNotice.tsx (toàn bộ 166 dòng — `Tone`, `TONE_STYLES`, `renderIcon`, `InfoGlyph` chưa dùng)
    - apps/shop/src/pages/OrderTrackPage.tsx dòng 90-270 (khuôn `CheckGlyph` + các `const X: CSSProperties`)
    - apps/shop/src/styles/tokens.css (danh sách token có sẵn — KHÔNG thêm token mới)
    - apps/shop/src/lib/cart-store.ts và file test hiện có của `apps/shop` (khuôn test hàm thuần)
    - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-UI-SPEC.md § "B. /o/:token"
  </read_first>
  <behavior>
    `order-update.test.ts` (hàm thuần):
    - `detectOrderUpdate(null, next)` = `false` (lần poll đầu không phải "quán vừa sửa")
    - `updated_at_ms` không đổi, items giống nhau → `false`
    - `updated_at_ms` tăng nhưng items + subtotal y hệt → `false` (đổi trạng thái món **không** phải
      "sửa đơn" — nếu không thì banner sẽ nhảy liên tục mỗi lần bếp chuyển trạng thái)
    - Bớt 1 món → `true`; thêm 1 món → `true`; đổi `qty` → `true`; đổi `subtotal` → `true`
    - `cancelled_count` tăng → `true`
    - `status` đổi WAITING→CONFIRMED nhưng items y hệt → `false` (đã có stepper diễn đạt việc này)
  </behavior>
  <action>
    **`BannerNotice.tsx`** — mở rộng tối thiểu, không thiết kế lại:
    thêm `'info'` vào union `Tone`; thêm
    `info: { bg: 'var(--info-100)', text: 'var(--info-600)' }` vào `TONE_STYLES`; thêm nhánh
    `if (tone === 'info') return <InfoGlyph />` trong `renderIcon()` (component đã có sẵn trong file,
    chỉ chưa được dùng). Không đụng logic `role="alert"`/`role="status"`.

    **`OrderStepper.tsx`** (mới) — component thuần trình bày:
    ```
    props: { stage: OrderStage; fulfillmentType: 'PICKUP' | 'DELIVERY' }
    ```
    5 node theo thứ tự: `RECEIVED` → `CONFIRMED` → `COOKING` →
    (`DELIVERY` ? `DELIVERING` : `READY_FOR_PICKUP`) → `COMPLETED`.
    - Layout: `display: flex; align-items: center;` — node là `<span>` tròn, giữa mỗi cặp node là
      `<span style={{ flex: 1, height: 2, background: ... }}/>`.
    - Trạng thái node suy từ vị trí `stage` trong mảng: index < current → "đã qua"; = current →
      "hiện tại"; > current → "chưa tới". Dùng đúng kích thước/màu ở `<interfaces>`.
    - Icon check trong node đã qua: SVG tự vẽ theo khuôn `CheckGlyph` (`stroke="currentColor"`,
      `strokeWidth={2}`, `aria-hidden="true"`), 10px. **Không** import icon package.
    - Pulse node hiện tại bằng CSS `@keyframes` inline `<style>` (khuôn `SKELETON_CSS` đã có trong
      `OrderTrackPage.tsx`), bọc trong `@media (prefers-reduced-motion: reduce) { animation: none }`.
    - Accessibility: bọc `role="list"`, mỗi node `role="listitem"` + `aria-label` là tên mốc; thêm
      `aria-current="step"` cho node hiện tại. Chỉ dòng `stage_label` bên dưới là chữ nhìn thấy được
      (09-UI-SPEC: không hiện 5 nhãn cùng lúc).
    - **Mọi màu/khoảng cách là `var(--...)`** — `apps/shop` cấm hardcode hex/px màu (quy ước đã khoá
      từ phase 7-8).
    - Ở `< 400px` giữ nguyên kích thước, không thu nhỏ (09-UI-SPEC Giả định #4). Ghi giả định này
      thành comment đầu file để nghiệm thu biết đây là giả định của UI researcher, chưa hỏi chủ quán.

    **`order-update.ts`** — `export function detectOrderUpdate(prev: PublicOrderStatus | null, next: PublicOrderStatus): boolean`
    theo `<behavior>`: so sánh `subtotal`, `cancelled_count`, và chữ ký danh sách món
    (`items.map(i => `${i.name}|${i.qty}|${i.unit_price}`).join(';')`). **Không** so `updated_at_ms`
    đơn lẻ (nó đổi mỗi lần bếp chuyển trạng thái món → banner sẽ nhảy sai).
    Docblock dẫn M2.D-47 và giải thích vì sao không dùng `updated_at_ms` làm điều kiện duy nhất.
  </action>
  <verify>
    <automated>cd apps/shop && ./node_modules/.bin/vitest run && ../../node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'info'" apps/shop/src/components/BannerNotice.tsx` ≥ 2 (union + TONE_STYLES/renderIcon)
    - `grep -c "InfoGlyph" apps/shop/src/components/BannerNotice.tsx` ≥ 2 (định nghĩa + sử dụng)
    - `grep -oE "#[0-9a-fA-F]{3,6}" apps/shop/src/components/OrderStepper.tsx | wc -l` = 0 (không hex, chỉ token)
    - `grep -c "prefers-reduced-motion" apps/shop/src/components/OrderStepper.tsx` = 1
    - `grep -cE "from 'react-icons|lucide|@heroicons|framer-motion|recharts" apps/shop/src/components/OrderStepper.tsx` = 0
    - `git diff apps/shop/package.json` — trống (không thêm dependency nào)
    - `cd apps/shop && ./node_modules/.bin/vitest run` exit 0 với ≥ 30 test pass (22 cũ + ≥ 8 mới)
    - `cd apps/shop && ../../node_modules/.bin/tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>3 mảnh ghép sẵn sàng, không thêm dependency nào, không hex lạ trong `apps/shop`.</done>
</task>

<task type="auto">
  <name>Task 2: OrderTrackPage — % + stepper + 3 banner + nhánh REJECTED + "Muốn sửa đơn?" (M2.D-45/46) + gate bundle</name>
  <files>apps/shop/src/pages/OrderTrackPage.tsx</files>
  <read_first>
    - apps/shop/src/pages/OrderTrackPage.tsx (TOÀN BỘ — điểm chèn, `useApi`, `ctaButton`, các CSSProperties)
    - apps/shop/src/lib/use-api.ts (chữ ký `useApi` + có hỗ trợ poll không; nếu chưa thì tự `setInterval` + `reload()`)
    - apps/shop/src/components/OrderStepper.tsx và BannerNotice.tsx (Task 1)
    - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-UI-SPEC.md § "B. /o/:token" + § Copywriting Contract Mặt B
    - scripts/check-shop-bundle.sh (đọc phần ghi chú ngưỡng — KHÔNG sửa file này)
  </read_first>
  <action>
    Chèn vào **đúng vị trí comment đã đánh dấu**, giữ nguyên phần header/danh sách món/tổng tiền/nút
    gọi quán ở trên và dưới. Thứ tự đọc từ trên xuống: **banner cập nhật → banner rejected hoặc
    (% + stepper) → danh sách món**.

    1. **Poll 5-10s**: thêm `setInterval(() => reload(), 8000)` trong `useEffect`, dọn khi unmount và
       **dừng poll** khi `status` là `REJECTED`/`CANCELLED_BY_CUSTOMER` hoặc `stage === 'COMPLETED'`
       (đơn đã kết thúc, poll tiếp là lãng phí pin + băng thông của khách).
       Giữ `prevRef` = `PublicOrderStatus | null` để gọi `detectOrderUpdate(prevRef.current, data)`;
       `true` → bật cờ hiện banner cập nhật (giữ hiện trong ~30s hoặc tới khi khách chạm vào trang).

    2. **Banner "Quán đã cập nhật đơn của bạn"** (`tone="info"`) — hiện khi
       `detectOrderUpdate` báo true **hoặc** `cancelled_count > 0`:
       - Có món huỷ: body = `cancelled_note` **nguyên văn từ API** (BE đã soạn theo M2.D-21). Nếu
         muốn liệt kê tên món thì KHÔNG được — response không trả tên món đã huỷ (đúng thiết kế), nên
         dùng đúng câu BE trả.
       - Không có món huỷ (chỉ đổi món/tổng tiền): title "Quán đã cập nhật đơn của bạn",
         body "Danh sách món và tổng tiền bên dưới đã là bản mới nhất."

    3. **Nhánh `status === 'REJECTED'`**: **thay hẳn** khối %+stepper bằng
       `<BannerNotice tone="danger" title="Đơn đã bị từ chối" body={data.reject_reason ?? ''}
       action={{ label: 'Gọi quán', href: 'tel:...' }} />`. **Ẩn số %**, **không** vẽ node dở dang.
       Danh sách món + tổng tiền vẫn hiện (khách cần xem lại đã đặt gì).
       `status === 'CANCELLED_BY_CUSTOMER'` dùng cùng khối nhưng title lấy từ `stage_label`.

    4. **Nhánh còn lại**: khối tiến độ
       - `{percent}%` (`--fs-3xl`/`--fw-heavy`/`--ok-600`)
       - `<OrderStepper stage={data.stage} fulfillmentType={data.fulfillment_type} />`
       - `stage_label` 1 dòng (`--fs-lg`/`--fw-bold`) — **render nguyên văn từ API**, không map lại
         chữ ở FE (BE là nguồn sự thật của nhãn mốc)
       - ETA phụ: chỉ khi `eta_min != null && eta_max != null` →
         "Dự kiến còn khoảng {eta_min}–{eta_max} phút" (`--fs-sm`/`--text-muted`)
       - `aria-live="polite"` cho khối % + `stage_label` để trình đọc màn hình thông báo khi tiến độ đổi

    5. **"Muốn sửa đơn?" (M2.D-45, M2.D-46)** — đặt dưới tổng tiền, chỉ hiện khi
       `status === 'CONFIRMED'`: 1 nút text mở rộng ra dòng chữ
       "Đơn đã vào bếp, vui lòng gọi quán để đổi." + nút gọi 1 chạm (tái dùng `ctaButton`).
       **Không** có ô sửa nào — khách không tự sửa sau khi xác nhận.
       Khi `status === 'WAITING'` thì **không** hiện khối này (đơn chưa duyệt là trường hợp M2.D-44,
       nằm ngoài phạm vi phase 9 — xem ghi chú ở SUMMARY).

    6. **Sửa tiêu đề trang theo trạng thái**: hiện tại luôn là "Đã gửi đơn thành công!" —
       giữ nguyên khi `WAITING`, đổi thành `stage_label` khi đã `CONFIRMED` trở đi để không mâu thuẫn
       với stepper.

    **Gate bundle — phần bắt buộc của task này, không phải việc dọn sau:**
    Chạy `cd apps/shop && ./node_modules/.bin/vite build` rồi `bash scripts/check-shop-bundle.sh`.
    Nếu vượt 370 kB: **cắt bớt code trong task này** (gộp style, bỏ animation, đơn giản hoá SVG) cho
    tới khi đậu. **TUYỆT ĐỐI KHÔNG** sửa `MAX_JS_KB` trong script. Ghi số kB đo được (trước/sau) vào
    SUMMARY.
  </action>
  <verify>
    <automated>cd apps/shop && ../../node_modules/.bin/tsc --noEmit && ./node_modules/.bin/vite build && cd ../.. && bash scripts/check-shop-bundle.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "OrderStepper" apps/shop/src/pages/OrderTrackPage.tsx` = 2 (import + render)
    - `grep -c "stage_label" apps/shop/src/pages/OrderTrackPage.tsx` ≥ 1 và `grep -c "Đang chuẩn bị" apps/shop/src/pages/OrderTrackPage.tsx` = 0 (nhãn mốc lấy từ API, không gõ lại)
    - `grep -c "tone=\"info\"" apps/shop/src/pages/OrderTrackPage.tsx` ≥ 1
    - `grep -c "Đơn đã vào bếp" apps/shop/src/pages/OrderTrackPage.tsx` = 1
    - `grep -c "aria-live" apps/shop/src/pages/OrderTrackPage.tsx` ≥ 1
    - `grep -oE "#[0-9a-fA-F]{3,6}" apps/shop/src/pages/OrderTrackPage.tsx | wc -l` = 0
    - `git diff scripts/check-shop-bundle.sh` — **trống** (không nới ngưỡng)
    - `bash scripts/check-shop-bundle.sh` in `OK: bundle JS <N> kB (ngưỡng 370 kB)` với N ≤ 370, và `OK: 11 forbidden strings clean`
    - **Chạy thật (ghi kết quả + ảnh mô tả vào SUMMARY)**, `cd apps/shop && ./node_modules/.bin/vite`
      rồi mở `/o/<token>` ở viewport 375px:
      (a) đơn `WAITING` → 0%, node 1 sáng, không có ETA sai lệch;
      (b) duyệt đơn ở màn admin → trong ≤ 10s trang khách tự đổi sang `stage_label` mới và % tăng;
      (c) huỷ 1 món ở bàn → banner info hiện + danh sách món và tổng tiền đổi theo;
      (d) từ chối 1 đơn khác → banner đỏ + lý do soạn sẵn + nút gọi quán, **không** thấy stepper, **không** thấy %;
      (e) stepper vẫn nằm ngang, không tràn ngang ở 375px.
    - `curl -s http://localhost:3001/api/public/orders/<token> | grep -c '"state"'` = 0 (G-1 vẫn giữ sau khi FE đổi)
    - `cd apps/shop && ./node_modules/.bin/vitest run` exit 0
  </acceptance_criteria>
  <done>REQ-O hoàn chỉnh ở mặt khách, đúng hợp đồng thị giác, và bundle vẫn dưới ngưỡng 370 kB mà không nới gate.</done>
</task>

<task type="auto">
  <name>Task 3: Khách tự huỷ đơn khi còn WAITING (M2.D-44 nửa huỷ) — DELETE endpoint + nút Huỷ đơn</name>
  <files>packages/schemas/src/public-orders.ts, apps/api/src/modules/public/cancel-order.ts, apps/api/src/modules/public/cancel-order.test.ts, apps/api/src/modules/public/public-orders.controller.ts, apps/shop/src/pages/OrderTrackPage.tsx</files>
  <read_first>
    - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-06-PLAN.md — hàm `confirm()` và cách nó lấy row lock trên `online_order_requests`. Task này PHẢI dùng CÙNG một lock, không được tự nghĩ cơ chế riêng.
    - apps/api/src/modules/public/public-orders.controller.ts — chữ ký `GET /:token` hiện có, cách nó tra `order_token`
    - apps/api/src/modules/public/submit-order.ts — cách map mã lỗi sang HTTP status (mẫu cho lỗi của DELETE)
    - apps/shop/src/pages/OrderTrackPage.tsx — sau Task 2, để biết chèn nút vào đâu
    - docs/MILESTONE-02-ONLINE-ORDERING-SPEC.md §5.1 (M2.D-44) và M2.D-45/D-46
  </read_first>
  <action>
    **Bối cảnh phạm vi:** chủ dự án chốt 2026-07-31 chỉ làm **nửa huỷ** của M2.D-44. Nửa sửa đơn
    (`PATCH /api/public/orders/:token`) hoãn sang phase 10 — plan 09-13 ghi nợ việc này. KHÔNG thi
    công `PATCH` ở đây.

    **1. Schema** — thêm response type cho huỷ đơn vào **file ĐÃ CÓ** `packages/schemas/src/public-orders.ts`
    (số nhiều). ⚠ KHÔNG tạo `public-order.ts` số ít bên cạnh: file số nhiều đã được barrel export ở
    `packages/schemas/src/index.ts:9`, thêm file gần trùng tên là bẫy cho người sau. Không cần request
    body — `order_token` nằm trên URL.

    **2. `cancel-order.ts`** — hàm thuần + hàm chạy trong transaction:
    - Nhận `order_token`, mở transaction, `SELECT ... FOR UPDATE` trên hàng
      `online_order_requests` tương ứng — **cùng dòng, cùng kiểu lock** mà `confirm()` ở 09-06 dùng.
      Đây là toàn bộ cách giải race, không thêm cờ nào ở tầng ứng dụng.
    - `status = 'WAITING'` → đổi sang `CANCELLED_BY_CUSTOMER`, ghi `cancelled_at`, commit.
      Enum `CANCELLED_BY_CUSTOMER` **đã tồn tại** nhưng tới nay chưa có đường nào tạo ra nó — đây
      chính là chỗ nối vào, không phải thêm giá trị enum mới.
    - `status` khác `WAITING` → trả mã lỗi rõ ràng, KHÔNG đổi gì:
      · `CONFIRMED` → `ORDER_ALREADY_CONFIRMED` (409)
      · `REJECTED` → `ORDER_ALREADY_REJECTED` (409)
      · `CANCELLED_BY_CUSTOMER` → trả 200 idempotent (khách bấm 2 lần / mạng chập chờn không được
        báo lỗi — huỷ 2 lần vẫn là đã huỷ)
    - Không tìm thấy token → 404 `ORDER_NOT_FOUND`. **Không** phân biệt "token sai" với "token
      không tồn tại" trong thông báo lỗi.

    **3. Race khách-Huỷ vs admin-Xác nhận — quy tắc chốt: AI VÀO TRƯỚC THẮNG.**
    Cả `confirm()` (09-06) và `cancelOrderByCustomer()` đều `SELECT ... FOR UPDATE` cùng hàng, nên
    InnoDB tự xếp hàng: bên thứ hai chỉ chạy sau khi bên thứ nhất commit, và lúc đó nó đọc được
    `status` đã đổi nên tự rơi vào nhánh lỗi ở trên. KHÔNG cần cờ hay kiểm tra thời gian.
    - Admin thắng → khách nhận `409 ORDER_ALREADY_CONFIRMED`, trang tự reload và hiện trạng thái đã
      xác nhận. Copy: "Quán vừa xác nhận đơn của bạn nên không huỷ được nữa — gọi {SĐT} nếu bạn cần
      đổi." Không dùng chữ "lỗi".
    - Khách thắng → admin nhận lỗi từ `confirm()` (09-06 đã có nhánh này cho đơn không còn
      `WAITING`); hàng đơn biến mất khỏi màn hàng chờ ở lần SSE/reload kế tiếp.
    Viết **1 test integration 2 connection MySQL thật** chứng minh điều này, theo đúng khuôn
    `apps/api/src/modules/public/open-order-lock.integration.test.ts` mà 09-08 dùng lại.

    **4. Controller**: `@Delete(':token')` trên `public-orders.controller.ts`. Đi qua đúng
    `CsrfOriginGuard` + rate limit như `POST` — huỷ đơn là thao tác ghi, không được lỏng hơn đặt đơn.

    **5. FE** (`OrderTrackPage.tsx`): nút "Huỷ đơn" **chỉ render khi `status === 'WAITING'`**.
    - Bấm → hộp xác nhận 2 nút ("Không huỷ nữa" / "Huỷ đơn"), vì bấm nhầm là mất đơn thật.
      Dùng lại pattern nút + `--danger-600` đã có; **không** thêm thư viện dialog.
    - Huỷ xong → dừng poll (Task 2 đã dừng poll ở trạng thái kết thúc, thêm
      `CANCELLED_BY_CUSTOMER` vào danh sách đó), hiện trạng thái đã huỷ + nút "Xem menu" để khách
      đặt lại.
    - Sau khi `CONFIRMED` nút này **biến mất hoàn toàn**, không phải disable — khớp M2.D-45/46
      (chỉ còn SĐT + nút gọi).
  </action>
  <verify>
    <automated>cd apps/api && ./node_modules/.bin/vitest run src/modules/public/cancel-order.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `DELETE /api/public/orders/:token` khi `WAITING` → 200, DB đổi sang `CANCELLED_BY_CUSTOMER`
    - Gọi lại lần 2 → vẫn 200 (idempotent), không đổi gì thêm
    - Đơn đã `CONFIRMED` → 409 `ORDER_ALREADY_CONFIRMED`, DB **không** đổi
    - Token không tồn tại → 404, thông báo không tiết lộ token có tồn tại hay không
    - Bỏ header `Origin` → 403 như `POST` (cùng `CsrfOriginGuard`)
    - Test integration 2 connection song song (1 confirm, 1 cancel): đúng 1 bên thành công, bên kia
      nhận lỗi đúng loại; `SELECT status` cuối cùng chỉ ra **một** giá trị
    - `grep -c "CANCELLED_BY_CUSTOMER" apps/api/src/modules/public/cancel-order.ts` ≥ 1
    - `git diff apps/shop/package.json` trống — không thêm dependency
    - `cd apps/shop && ./node_modules/.bin/vite build && cd ../.. && bash scripts/check-shop-bundle.sh` OK,
      **ghi số kB vào SUMMARY** để thấy nút này ăn bao nhiêu trong ~18 kB còn lại
    - `cd apps/api && ../../node_modules/.bin/tsc --noEmit` sạch
  </acceptance_criteria>
  <done>Khách đặt nhầm tự huỷ được khi quán chưa duyệt, không phải gọi điện; race với admin giải sạch ở tầng DB và có test chứng minh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL `/o/<token>` | `order_token` là bearer credential duy nhất bảo vệ đơn — ai có link là xem được |
| dữ liệu API → màn hình khách | Mọi thứ hiện ở đây là "công khai với người giữ link" |
| kích thước bundle | Hợp đồng hiệu năng với khách dùng 3G (M2.D-64 + gate 370 kB) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-59 | Information Disclosure | Render `order_token` đầy đủ ra màn hình / rò qua `Referer` khi khách bấm link ngoài | mitigate | Giữ nguyên `masked` 4 ký tự đầu của phase 8; `Referrer-Policy: no-referrer` đã đặt cho block `order.` (OD-02). Không thêm link ngoài nào vào trang này |
| T-09-60 | Information Disclosure | FE tự suy diễn trạng thái từng món từ % rồi hiện lên (vi phạm tinh thần M2.D-23) | mitigate | FE **không** tính lại gì: `percent`, `stage`, `stage_label`, `cancelled_note` render nguyên văn từ API; grep khẳng định không gõ lại nhãn mốc ở FE |
| T-09-61 | Tampering | Che việc quán huỷ món (lừa khách) | mitigate | Banner `info` hiện bắt buộc khi `cancelled_count > 0`, dùng `cancelled_note` nguyên văn (M2.D-21 — ngoại lệ bắt buộc của G-1) |
| T-09-62 | Denial of Service | Poll 8s chạy vĩnh viễn kể cả khi đơn đã kết thúc → hao pin khách + tải API vô ích | mitigate | Dừng poll khi `REJECTED`/`CANCELLED_BY_CUSTOMER`/`COMPLETED`; dọn interval khi unmount |
| T-09-63 | Denial of Service | Thêm thư viện đồ hoạ/icon làm bundle vượt ngưỡng 3G | mitigate | Cấm dependency mới (grep import + `git diff package.json` trống); gate `check-shop-bundle.sh` là acceptance criteria của chính task, không phải bước dọn sau |
| T-09-64 | Tampering | Nới `MAX_JS_KB` để "cho đậu" | mitigate | `git diff scripts/check-shop-bundle.sh` phải trống — nếu vượt thì cắt code, không nới gate |
| T-09-80 | Elevation of Privilege | `DELETE /api/public/orders/:token` chỉ cần `order_token` — ai nhặt được link (khách chuyển tiếp tin nhắn, người mượn điện thoại, log proxy) là **huỷ được đơn của người khác** | mitigate | Chấp nhận có kiểm soát: token là bearer credential duy nhất của trang này, giống hệt `GET /:token` đã ship ở phase 8 — huỷ không lộ thêm dữ liệu nào mà `GET` chưa lộ. Giới hạn thiệt hại: chỉ huỷ được khi còn `WAITING` (cửa sổ hẹp, đơn chưa vào bếp, chưa mất tiền), thao tác **không thể thực hiện lại** sau khi `CONFIRMED`, và mọi lần huỷ ghi audit log kèm `ip_hash` để truy được. `Referrer-Policy: no-referrer` (OD-02) chặn token rò qua `Referer`. **Không** thêm lớp xác thực nào — bắt khách nhập SĐT để huỷ sẽ phá đúng mục tiêu "khách tự huỷ thoải mái, không cần xin phép" của M2.D-44 |
| T-09-81 | Denial of Service | Gọi `DELETE` hàng loạt bằng token đoán mò để dò đơn tồn tại | mitigate | Đi qua cùng rate limit và `CsrfOriginGuard` với `POST`; 404 không phân biệt "token sai" với "token không tồn tại" nên không dùng làm oracle được. `order_token` 64 hex — không gian đoán không khả thi |
| T-09-82 | Tampering | Race khách-huỷ vs admin-xác nhận để lại đơn ở trạng thái nhập nhằng (vừa CONFIRMED vừa CANCELLED, hoặc cấp bàn rồi mà đơn đã huỷ) | mitigate | Cả 2 đường dùng CÙNG `SELECT ... FOR UPDATE` trên cùng hàng — InnoDB xếp hàng, bên thứ hai đọc được `status` đã đổi và tự rơi vào nhánh lỗi. Test integration 2 connection thật là acceptance criteria, không phải tuỳ chọn |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | Plan này KHÔNG cài package nào cho `apps/shop`; acceptance criteria kiểm `git diff apps/shop/package.json` trống |
</threat_model>

<verification>
1. `cd apps/shop && ../../node_modules/.bin/tsc --noEmit` — sạch
2. `cd apps/shop && ./node_modules/.bin/vitest run` — xanh (≥ 30 test)
3. `cd apps/shop && ./node_modules/.bin/vite build && cd ../.. && bash scripts/check-shop-bundle.sh` — OK cả 2 gate, ghi số kB vào SUMMARY
4. 5 kịch bản chạy thật ở viewport 375px (WAITING · duyệt · huỷ món · từ chối · stepper không tràn)
5. `curl` grep `"state"` = 0 — G-1 vẫn giữ
6. `cd apps/api && ./node_modules/.bin/vitest run src/modules/public/cancel-order.test.ts` — xanh, gồm test race 2 connection MySQL thật
7. Chạy tay: đơn `WAITING` → bấm Huỷ đơn → xác nhận → DB thành `CANCELLED_BY_CUSTOMER`; duyệt 1 đơn khác rồi mở lại `/o/:token` → nút Huỷ đơn **không còn**
</verification>

<success_criteria>
- `/o/:token` hiện % lớn + stepper 5 mốc ngang + nhãn mốc + ETA (khi có)
- Nhánh REJECTED thay hẳn stepper bằng banner đỏ, ẩn %
- Banner món huỷ / quán sửa đơn hiện đúng lúc, dùng câu của API
- "Muốn sửa đơn?" chỉ hiện SĐT + nút gọi sau khi CONFIRMED
- Khách huỷ được đơn còn WAITING; nút biến mất sau khi CONFIRMED; race với admin có test chứng minh đúng 1 bên thắng
- Không hex, không token mới, không dependency mới, bundle ≤ 370 kB, gate không bị nới
</success_criteria>

<output>
Create `.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-11-SUMMARY.md` when done
</output>
