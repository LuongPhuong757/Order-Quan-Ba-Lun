---
phase: 09-duy-t-n-th-ng-b-o-theo-d-i-n
plan: 12
type: execute
wave: 7
depends_on: ["09-11"]
files_modified:
  - apps/api/src/modules/settings/settings.defaults.ts
  - apps/api/src/modules/settings/settings.controller.ts
  - packages/schemas/src/public-store.ts
  - apps/api/src/modules/public/public-store.controller.ts
  - apps/api/src/modules/public/order-guard.ts
  - apps/api/src/modules/public/order-guard.test.ts
  - apps/api/src/modules/public/submit-order.ts
  - apps/api/src/modules/public/store-status.ts
  - apps/web/src/pages/AdminSettingsPage.tsx
  - apps/shop/src/pages/CheckoutPage.tsx
  - apps/shop/src/pages/OrderTrackPage.tsx
autonomous: true
requirements: [REQ-M, REQ-N, REQ-O]

must_haves:
  truths:
    - "Công tắc còn đúng 2 trạng thái Mở / Đóng cửa, và CẢ HAI ĐỀU NHẬN ĐƠN BÌNH THƯỜNG (D-11)"
    - "Gọi `POST /api/public/orders` lúc Đóng cửa và ngoài giờ mở cửa đều trả 201, KHÔNG còn 409 ONLINE_ORDERING_DISABLED / STORE_CLOSED"
    - "4 lớp chống lạm dụng của phase 8 VẪN XANH: 1 đơn mở/SĐT · blacklist SĐT · rate limit · hash IP (D-18)"
    - "Không còn cơ chế nào tự đổi trạng thái công tắc — `escalate_autooff_after_s` bị xoá hẳn (D-12)"
    - "Đúng 2 câu chữ mới sửa được ở `/admin/settings`, lưu DB, ăn ngay không cần build lại (D-14)"
    - "Trang khách lúc Đóng cửa hiện banner tone info với `closed_banner_text`, nút gửi đơn KHÔNG bị khoá"
  artifacts:
    - path: "apps/api/src/modules/public/order-guard.ts"
      provides: "Guard chỉ còn 4 lớp chống lạm dụng, không còn nhánh công tắc"
    - path: "apps/api/src/modules/settings/settings.defaults.ts"
      provides: "2 key mới, bỏ escalate_autooff_after_s"
      contains: "closed_banner_text"
    - path: "apps/web/src/pages/AdminSettingsPage.tsx"
      provides: "2 textarea không giới hạn độ dài trong card 'Công tắc nhận đơn'"
  key_links:
    - from: "apps/shop/src/pages/CheckoutPage.tsx"
      to: "store.data.closed_banner_text"
      via: "BannerNotice tone info, không khoá nút"
      pattern: "closed_banner_text"
    - from: "apps/api/src/modules/public/public-store.controller.ts"
      to: "PublicStoreStatus.strict().parse"
      via: "whitelist 13 field"
      pattern: "strict\\(\\)"
---

<objective>
Sửa lại phần Phase 8 đã ship theo D-11/D-12/D-14/D-17: công tắc nhận đơn **đổi ngữ nghĩa hoàn toàn**
từ "chặn 2 lớp" sang "chỉ đổi chữ hiển thị".

Purpose: Đây là **rework có chủ đích của code đã verify**, do chủ dự án chốt ngày 2026-07-31 trong
buổi discuss phase 9 — không phải scope creep (D-17). Để code mâu thuẫn với tài liệu qua một phase
là rủi ro lớn hơn việc phase 9 phình to. Đặt ở wave 7 để không làm mất ổn định các wave xây tính
năng mới.

⚠ **`order-guard.test.ts` SẼ ĐỎ** khi nhánh chặn bị gỡ — đó là **dự kiến, không phải hồi quy**.
Plan này viết lại chính file test đó. Executor tuyệt đối **không** được "sửa lại cho xanh" bằng cách
khôi phục nhánh chặn.

⚠ **4 lớp chống lạm dụng phải sống nguyên vẹn (D-18)** — chúng độc lập với công tắc. Có task riêng
chứng minh chúng vẫn xanh.

Output: 2 key cấu hình mới, 1 key bị xoá, guard rút gọn, test viết lại, 3 màn hình cập nhật.
</objective>

<execution_context>
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-CONTEXT.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-RESEARCH.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-PATTERNS.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-UI-SPEC.md
@OVERRIDE-DEBT.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-CONTEXT.md

<interfaces>
<!-- Trích nguyên văn code hiện tại. KHÔNG đi khám phá lại. -->

apps/api/src/modules/public/order-guard.ts — nhánh PHẢI XOÁ (dòng 26-31):
```typescript
export function checkOrderGuard(input: OrderGuardInput): GuardErrorCode | null {
  if (!input.ordering.enabled) {
    return input.ordering.blocking_reason === 'OUTSIDE_HOURS' ? 'STORE_CLOSED' : 'ONLINE_ORDERING_DISABLED';
  }
  if (input.isBlacklisted) return 'PHONE_BLACKLISTED';        // ← 4 lớp này PHẢI GIỮ NGUYÊN (D-18)
  if (input.isRateLimited) return 'TOO_MANY_REQUESTS';
  if (input.hasOpenOrder) return 'ORDER_ALREADY_OPEN_FOR_PHONE';
  if (input.unavailableItemCodes.length > 0) return 'MENU_ITEM_UNAVAILABLE';
  return null;
}
```
`OrderGuardInput.ordering: OrderingStatus` trở thành field không ai đọc → **bỏ khỏi type**; kiểm
mọi caller trước khi bỏ.
`GuardErrorCode` mất 2 thành viên `'ONLINE_ORDERING_DISABLED'` và `'STORE_CLOSED'`.

apps/api/src/modules/public/submit-order.ts — 2 case chết trong `buildGuardMessage()` (dòng 88-94):
```typescript
case 'ONLINE_ORDERING_DISABLED': return settings.online_ordering_off_reason ? ... : ...;
case 'STORE_CLOSED': return `Quán đang ngoài giờ mở cửa hôm nay. ...`;
```
Xoá cả 2 case. Chỗ gọi `getOrderingStatus(nowMs)` trong `submitOrder` cũng thành thừa nếu không còn
ai đọc — kiểm rồi bỏ khỏi `Promise.all` và khỏi `SubmitDeps` nếu thật sự không còn dùng.
⚠ **KHÔNG** đụng 4 lớp còn lại và các lệnh fetch nuôi chúng (blacklist / rate-limit / open-order /
menu lookup, dòng ~132-141) — chúng không đọc `ordering` (D-18).

apps/api/src/modules/public/order-guard.test.ts (94 dòng) — 2 `describe` (dòng 34-49) test đúng
nhánh bị gỡ → xoá 2 `describe` đó; `baseInput()` (dòng 17-26) bỏ field `ordering: ENABLED`.
**Giữ nguyên** mọi `describe` còn lại.

apps/api/src/modules/public/store-status.ts — `evaluateOrderingStatus()` **GIỮ NGUYÊN CƠ CHẾ**
(OD-07 tính-lúc-đọc vẫn dùng được), chỉ đổi **ngữ nghĩa**: `enabled === false` nay nghĩa là
"đang Đóng cửa (vẫn nhận đơn)", không còn nghĩa "chặn submit". Sửa docblock, **không** sửa logic.

apps/api/src/modules/settings/settings.defaults.ts — mẫu entry (dòng 17-43) + type
`StoreSettingsMap` (dòng 47-68). `SETTINGS_DEFAULTS_MAP` và `SETTINGS_KIND_BY_KEY` tự suy ra.
Dòng 36 hiện có `{ key: 'escalate_autooff_after_s', kind: 'int', default: 1800 }` → **XOÁ**.

apps/api/src/modules/settings/settings.controller.ts — `UpdateSettingsDto` (dòng ~58-78) +
vòng lặp allowlist `for (const key of [...] as const)` (dòng ~118-130).
⚠ `SettingsService.updateMany()` có `if (!kind) continue` → key không khai báo bị **nuốt lặng lẽ**,
trả 200 mà không ghi gì. Đây là **round-trip 3 chỗ**: (1) `SETTINGS_DEFAULTS` + `StoreSettingsMap`,
(2) `UpdateSettingsDto` + mảng allowlist ở controller, (3) nơi trả ra response. Thiếu 1 chỗ = nút
Lưu im lặng không làm gì.

packages/schemas/src/public-store.ts — `PublicStoreStatus` hiện 11 field.
apps/api/src/modules/public/public-store.controller.ts — dựng payload thủ công rồi
`PublicStoreStatus.strict().parse(payload)`. **Thêm field vào schema mà quên controller (hoặc ngược
lại) = 500 ngay.** Sửa cả 2 trong cùng task.

apps/web/src/pages/AdminSettingsPage.tsx dòng 297-371 — card "Công tắc nhận đơn" + ô textarea
"Lý do hiện cho khách" (`maxLength={255}` + đếm ký tự). ⚠ 2 ô MỚI **không** giới hạn độ dài
(09-UI-SPEC: chủ quán tự nhập, layout phải co giãn, không ellipsis, không khoá 1 dòng).
Toast: `toast.push('success', '...'); await onRefresh();`

apps/shop/src/pages/CheckoutPage.tsx dòng 176-177:
```typescript
const storeOff = store.data ? store.data.ordering_enabled === false : false;
const ctaDisabled = storeOff || hasFieldErrors || submitting;     // ← bỏ storeOff
```
dòng 266-281 — banner có ternary `blocking_reason === 'OUTSIDE_HOURS' ? 'warn' : 'brand'` + 2 chuỗi
cứng → thay bằng 1 banner `tone="info"` dùng `closed_banner_text` nguyên văn.
dòng 94-99 — `errorAction()` chứa cả `ONLINE_ORDERING_DISABLED`/`STORE_CLOSED` (nay chết) lẫn
`NO_TABLE_AVAILABLE`/`PHONE_BLACKLISTED` (còn sống) → sửa **một phần**, không xoá cả khối.

apps/shop/src/components/BannerNotice.tsx — tone `'info'` đã được thêm ở plan 09-11.

09-CONTEXT.md `<specifics>` — giá trị mặc định NGUYÊN VĂN của 2 câu chữ (chép đúng từng chữ).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 2 key cấu hình mới (round-trip 3 chỗ) + xoá `escalate_autooff_after_s` + đưa `closed_banner_text` ra `/api/public/store`</name>
  <files>apps/api/src/modules/settings/settings.defaults.ts, apps/api/src/modules/settings/settings.controller.ts, packages/schemas/src/public-store.ts, apps/api/src/modules/public/public-store.controller.ts</files>
  <read_first>
    - apps/api/src/modules/settings/settings.defaults.ts (toàn bộ 77 dòng)
    - apps/api/src/modules/settings/settings.service.ts (`updateMany` — đặc biệt `if (!kind) continue`)
    - apps/api/src/modules/settings/settings.controller.ts (`UpdateSettingsDto` + `update()` + `buildResponse()`)
    - packages/schemas/src/public-store.ts (toàn bộ 38 dòng)
    - apps/api/src/modules/public/public-store.controller.ts (toàn bộ 53 dòng)
    - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-CONTEXT.md D-12, D-14 + `<specifics>`
    - apps/web/src/pages/AdminSettingsPage.tsx — grep `escalate_autooff_after_s` để xác nhận lại lần cuối là UI không render key này trước khi xoá
  </read_first>
  <action>
    **`settings.defaults.ts`:**
    - Thêm 2 entry `kind: 'string'` với default chép **nguyên văn** từ 09-CONTEXT `<specifics>`:
      · `closed_banner_text` = `'Hiện chúng tôi đang đóng cửa, đơn của quý khách cứ tiếp tục đặt và chúng tôi sẽ xử lý sớm nhất có thể'`
      · `closed_submit_confirm_text` = `'Chúng tôi đã tiếp nhận đơn, và sẽ liên hệ khi quán mở lại'`
      Comment ngay trên: D-14, chủ quán sửa được ở `/admin/settings`, **độ dài không giới hạn**.
    - **XOÁ** entry `escalate_autooff_after_s` và field tương ứng trong `StoreSettingsMap`.
      **Quyết định đã chốt: xoá hẳn, không giữ no-op** (D-12 discretion). Lý do ghi thành comment tại
      chỗ: PATTERNS.md đã grep toàn bộ `AdminSettingsPage.tsx` (722 dòng) và xác nhận **không** có UI
      nào render key này; giữ lại một setting không ai đọc là mời người sau cài lại auto-OFF. Dòng cũ
      trong DB (nếu admin từng ghi) sẽ bị `readAll()` bỏ qua tự nhiên (`if (!kind) continue`) — không
      cần xoá tay, ghi rõ điều này vào SUMMARY.
      ⚠ Trước khi xoá, `grep -rn "escalate_autooff_after_s" apps/ packages/` — nếu còn nơi nào đọc
      thì phải xử lý nơi đó trước.
    - Thêm đúng 2 field mới vào `StoreSettingsMap`.

    **`settings.controller.ts`** (chỗ thứ 2 của round-trip):
    - `UpdateSettingsDto`: `@IsOptional() @IsString() closed_banner_text?: string;` và
      `@IsOptional() @IsString() closed_submit_confirm_text?: string;`
      **KHÔNG** thêm `@MaxLength` (D-14: độ dài không giới hạn). Cột `store_settings.value` là text
      nên chịu được; nếu cột là `varchar` ngắn thì báo blocker thay vì âm thầm cắt chuỗi.
    - Thêm 2 key vào mảng allowlist trong `update()`.
    - `buildResponse()` trả `settings` từ `readAll()` nên 2 key tự có mặt — xác nhận bằng test round-trip
      ở acceptance criteria, không suy đoán.

    **`packages/schemas/src/public-store.ts` + `public-store.controller.ts`** (sửa **cùng lúc**):
    - Thêm `closed_banner_text: z.string()` **và** `closed_submit_confirm_text: z.string()` vào
      `PublicStoreStatus`, và thêm cả 2 vào payload của controller.
      ⚠ **Quyết định đã chốt tại plan này, lệch gợi ý của PATTERNS.md** (PATTERNS đề nghị KHÔNG đưa
      `closed_submit_confirm_text` lên endpoint này): trang `/o/:token` cần câu xác nhận sau khi
      **tải lại trang** — nếu truyền qua router state thì refresh là mất. Đưa cả 2 lên
      `GET /api/public/store` (1 request không poll, payload vài trăm byte) cho phép
      `OrderTrackPage` đọc lại bất cứ lúc nào và tự đúng nếu quán đã mở lại. Ghi lệch này + lý do
      vào SUMMARY.
    - Cập nhật docblock của `public-store.controller.ts`: whitelist nay 13 field.
    - `cd packages/schemas && ../../node_modules/.bin/tsc`
  </action>
  <verify>
    <automated>cd packages/schemas && ../../node_modules/.bin/tsc && cd ../../apps/api && ../../node_modules/.bin/tsc --noEmit && ./node_modules/.bin/vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "escalate_autooff_after_s" apps/ packages/ --include=*.ts --include=*.tsx | wc -l` = 0
    - `grep -c "closed_banner_text" apps/api/src/modules/settings/settings.defaults.ts` = 2 (entry + type)
    - `grep -c "closed_banner_text" apps/api/src/modules/settings/settings.controller.ts` = 2 (DTO + allowlist)
    - `grep -c "closed_banner_text" packages/schemas/src/public-store.ts` = 1 và `grep -c "closed_banner_text" apps/api/src/modules/public/public-store.controller.ts` = 1
    - `grep -cE "MaxLength\(\s*\d+\s*\)\s*closed_" apps/api/src/modules/settings/settings.controller.ts` = 0
    - **Round-trip thật (bắt lỗi nuốt-lặng-lẽ)**:
      `curl -X PUT http://localhost:3001/admin/settings -H 'Content-Type: application/json' -H 'Origin: http://localhost:5173' --cookie "<admin>" -d '{"closed_banner_text":"ZZ-BANNER-TEST","closed_submit_confirm_text":"ZZ-CONFIRM-TEST"}'`
      → 200; rồi `curl -s .../admin/settings --cookie "<admin>" | grep -c ZZ-BANNER-TEST` = 1 và
      `grep -c ZZ-CONFIRM-TEST` = 1. **Nếu 0 → thiếu 1 trong 3 chỗ round-trip, phải sửa.**
    - **Chuỗi dài không bị cắt**: PUT 1 chuỗi 500 ký tự vào `closed_banner_text`, GET lại → đủ 500 ký tự
    - `curl -s http://localhost:3001/api/public/store | grep -c closed_banner_text` = 1
    - Trả 2 giá trị về mặc định sau khi kiểm
    - `cd apps/api && ./node_modules/.bin/vitest run` full suite exit 0
  </acceptance_criteria>
  <done>Đúng 2 key runtime-editable hoạt động đầu-cuối; setting auto-OFF chết bị xoá sạch khỏi codebase.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gỡ nhánh chặn khỏi order-guard + submit-order, viết lại test, chứng minh 4 lớp chống lạm dụng vẫn xanh (D-18)</name>
  <files>apps/api/src/modules/public/order-guard.ts, apps/api/src/modules/public/order-guard.test.ts, apps/api/src/modules/public/submit-order.ts, apps/api/src/modules/public/store-status.ts</files>
  <read_first>
    - apps/api/src/modules/public/order-guard.ts (toàn bộ 37 dòng)
    - apps/api/src/modules/public/order-guard.test.ts (toàn bộ 94 dòng)
    - apps/api/src/modules/public/submit-order.ts (toàn bộ — `buildGuardMessage` + `Promise.all` fetch)
    - apps/api/src/modules/public/store-status.ts (docblock — sẽ sửa chữ, KHÔNG sửa logic)
    - apps/api/src/modules/public/public-orders.service.ts (`makeDeps` — nếu bỏ `getOrderingStatus` khỏi `SubmitDeps` thì phải sửa ở đây)
    - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-CONTEXT.md D-11, D-18
  </read_first>
  <behavior>
    `order-guard.test.ts` sau khi viết lại phải:
    - **XOÁ** 2 `describe` về `MANUAL_OFF` và `OUTSIDE_HOURS` (chúng khẳng định hành vi mà D-11 vừa bỏ)
    - **THÊM** 1 `describe('D-11 — công tắc không còn chặn submit')` với các case:
      · Trước đây `ordering.enabled = false` sẽ chặn; nay `checkOrderGuard` **không nhận** field
        `ordering` nữa và với input hợp lệ khác thì trả `null`
      · Input có đủ 4 lớp đều "sạch" → `null` (đơn đi qua được)
    - **GIỮ NGUYÊN** và bổ sung khẳng định cho **4 lớp D-18**, mỗi lớp ≥ 1 case:
      · `isBlacklisted = true` → `'PHONE_BLACKLISTED'`
      · `isRateLimited = true` → `'TOO_MANY_REQUESTS'`
      · `hasOpenOrder = true` → `'ORDER_ALREADY_OPEN_FOR_PHONE'`
      · `unavailableItemCodes = ['abc']` → `'MENU_ITEM_UNAVAILABLE'`
    - **Thứ tự ưu tiên còn lại** vẫn cố định: blacklist > rate limit > open order > hết món
      (≥ 2 case bật đồng thời 2 cờ và khẳng định cái ưu tiên cao hơn thắng)
    - 1 case "hồi quy ngược": `GuardErrorCode` **không còn** chứa `'ONLINE_ORDERING_DISABLED'`/
      `'STORE_CLOSED'` — khẳng định bằng cách gọi guard với mọi tổ hợp cờ và assert kết quả luôn nằm
      trong tập 4 mã còn lại hoặc `null`
  </behavior>
  <action>
    **`order-guard.ts`:**
    - Xoá nhánh `if (!input.ordering.enabled) {...}`.
    - Bỏ `ordering` khỏi `OrderGuardInput` **sau khi** grep xác nhận không caller nào còn cần
      (`grep -rn "checkOrderGuard" apps/api/src`).
    - Bỏ `'ONLINE_ORDERING_DISABLED'` và `'STORE_CLOSED'` khỏi union `GuardErrorCode`
      (TypeScript **không** báo lỗi khi để thành viên union thừa — phải chủ động xoá, đây là thứ chỉ
      review bắt được).
    - Cập nhật docblock đầu file: thứ tự kiểm tra nay chỉ còn 4 lớp; ghi rõ **D-11 ghi đè M2.D-26 và
      M2.D-27**, và **D-18: 4 lớp này độc lập với công tắc, không được gỡ theo**.
      Trỏ tới `OVERRIDE-DEBT.md` (entry sẽ được thêm ở plan 09-13).

    **`submit-order.ts`:**
    - Xoá 2 `case` trong `buildGuardMessage()`.
    - Nếu `getOrderingStatus` không còn ai đọc: bỏ khỏi `Promise.all`, bỏ khỏi `SubmitDeps`, và bỏ
      cài đặt tương ứng trong `makeDeps()` của `public-orders.service.ts`.
      ⚠ Nếu còn nơi khác dùng (vd để ghi `blocking_reason` vào log) thì **giữ lại** và ghi rõ lý do —
      đừng xoá mù.
    - **KHÔNG đụng** 4 lệnh fetch nuôi 4 lớp chống lạm dụng (`isPhoneBlacklisted`,
      `countRecentByPhone`, `hasOpenOrderForPhoneLocked`, `findMenuItemsByIds`) và
      `hashIpFn` — D-18.
    - **KHÔNG** xoá 2 mã `'ONLINE_ORDERING_DISABLED'`/`'STORE_CLOSED'` khỏi
      `packages/schemas/src/errors.ts`: chúng vẫn nằm trong hợp đồng lỗi lịch sử và
      `.planning/phases/09-*/09-CONTEXT.md § Deferred` để ngỏ khả năng thêm lại trạng thái
      "TẮT HẲN". Ghi comment tại `errors.ts`... **không**, đừng sửa `errors.ts` ở task này; chỉ ghi
      nhận trong SUMMARY rằng 2 mã trở thành **không còn được phát ra** nhưng vẫn giữ trong enum.

    **`store-status.ts`:** chỉ sửa **docblock/comment**: `enabled === false` nay nghĩa là "quán đang
    Đóng cửa (VẪN nhận đơn, chỉ đổi chữ hiển thị)", không còn nghĩa "chặn submit". Cơ chế
    tính-lúc-đọc của OD-07 giữ nguyên, chỉ đổi cách gọi tên trạng thái "OFF đến hết hôm nay" thành
    "Đóng cửa đến hết hôm nay". **Không đổi một dòng logic nào** — `store-status.test.ts` (16 test)
    phải xanh y nguyên, không sửa file test đó.

    Viết lại `order-guard.test.ts` phủ đủ `<behavior>`.
  </action>
  <verify>
    <automated>cd apps/api && ./node_modules/.bin/vitest run src/modules/public/ && ../../node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ordering" apps/api/src/modules/public/order-guard.ts` = 0
    - `grep -cE "ONLINE_ORDERING_DISABLED|STORE_CLOSED" apps/api/src/modules/public/order-guard.ts` = 0
    - `grep -cE "ONLINE_ORDERING_DISABLED|STORE_CLOSED" apps/api/src/modules/public/submit-order.ts` = 0
    - `grep -c "isBlacklisted" apps/api/src/modules/public/order-guard.ts` = 1 và tương tự cho `isRateLimited`, `hasOpenOrder`, `unavailableItemCodes` (4 lớp còn nguyên — D-18)
    - `git diff apps/api/src/modules/public/store-status.ts` — **chỉ** có dòng comment/docblock thay đổi, 0 dòng logic
    - `cd apps/api && ./node_modules/.bin/vitest run src/modules/public/store-status.test.ts` exit 0 với 16 test pass (không sửa file test này)
    - `cd apps/api && ./node_modules/.bin/vitest run src/modules/public/order-guard.test.ts` exit 0 với ≥ 10 test pass
    - **Chạy thật — D-11**: tắt công tắc ở Dashboard rồi
      `curl -i -X POST http://localhost:3001/api/public/orders -H 'Content-Type: application/json' -H 'Origin: http://localhost:5174' -d '<đơn hợp lệ>'` → **201**, KHÔNG phải 409. Đơn xuất hiện ở hàng chờ duyệt.
    - **Chạy thật — ngoài giờ mở cửa cũng nhận đơn**: đặt `open_hours` sao cho hiện tại là ngoài giờ, submit → **201**
    - **Chạy thật — D-18 (4 lớp vẫn chặn), làm đủ 4 kịch bản, dán output vào SUMMARY**:
      (a) submit 2 lần cùng SĐT → lần 2 **409 `ORDER_ALREADY_OPEN_FOR_PHONE`**
      (b) thêm SĐT vào blacklist rồi submit → **409 `PHONE_BLACKLISTED`**, message không chứa "chặn"/"blacklist"
      (c) submit 11 lần/phút từ 1 IP → request thứ 11 **429**
      (d) `SELECT LENGTH(ip_hash) FROM online_order_requests ORDER BY created_at DESC LIMIT 1` = 64 và
          `SELECT COUNT(*) FROM online_order_requests WHERE ip_hash LIKE '%127.0.0.1%' OR ip_hash LIKE '%::1%'` = 0
    - `cd apps/api && ./node_modules/.bin/vitest run` full suite exit 0
  </acceptance_criteria>
  <done>Công tắc không còn chặn gì; 4 lớp chống lạm dụng của phase 8 được chứng minh còn nguyên bằng cả unit test lẫn 4 kịch bản HTTP thật.</done>
</task>

<task type="auto">
  <name>Task 3: 3 màn hình — `/admin/settings` 2 ô chữ, `/checkout` bỏ khoá nút + banner info, `/o/:token` câu xác nhận lúc Đóng cửa</name>
  <files>apps/web/src/pages/AdminSettingsPage.tsx, apps/shop/src/pages/CheckoutPage.tsx, apps/shop/src/pages/OrderTrackPage.tsx</files>
  <read_first>
    - apps/web/src/pages/AdminSettingsPage.tsx dòng 280-380 (card "Công tắc nhận đơn" + ô "Lý do hiện cho khách" + nút Lưu + toast)
    - apps/shop/src/pages/CheckoutPage.tsx dòng 75-200 và 255-315 (storeOff, ctaDisabled, banner, errorAction)
    - apps/shop/src/pages/OrderTrackPage.tsx (sau plan 09-11 — tiêu đề trang + `useApi`)
    - apps/shop/src/components/BannerNotice.tsx (tone `'info'` đã có từ plan 09-11)
    - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-UI-SPEC.md § Copywriting Contract Mặt B (2 chuỗi runtime-editable)
  </read_first>
  <action>
    **`AdminSettingsPage.tsx`** — thêm **đúng 2** `<textarea>` vào **card "Công tắc nhận đơn" đã có**
    (cạnh ô "Lý do hiện cho khách"), không dựng card/tab mới:
    - "Câu hiển thị trên trang khách khi Đóng cửa" → `closed_banner_text`
    - "Câu hiển thị sau khi khách gửi đơn lúc Đóng cửa" → `closed_submit_confirm_text`
    Chép khuôn container/label/style của ô `off-reason` **nhưng BỎ** `maxLength={255}`, BỎ
    `.slice(0, 255)` và **BỎ** dòng đếm "N ký tự còn lại" (D-14: độ dài không giới hạn; hiện bộ đếm
    ngụ ý có giới hạn là sai). `rows={3}`, `style` co giãn theo nội dung (`width: '100%'`), không
    `text-overflow: ellipsis`, không `white-space: nowrap`.
    Thêm chú thích nhỏ dưới mỗi ô (12px `#6b7280`): "Khách đọc nguyên văn câu này." và
    "Đổi chữ là ăn ngay, không cần build lại."
    Nối vào cùng nút Lưu + `toast.push('success', ...)` + `await onRefresh()` đang có của card đó.
    ⚠ `git diff apps/web/src/pages/AdminSettingsPage.tsx` trước khi sửa — chủ dự án đang có thay đổi
    chưa commit ở `apps/web/src/pages/*`; nếu file này có thay đổi chưa commit thì **dừng và báo**,
    không đè lên.

    **`CheckoutPage.tsx`** (D-11 — bỏ chặn):
    - Dòng ~177: `const ctaDisabled = hasFieldErrors || submitting;` — **bỏ `storeOff`**.
    - Dòng ~266-281: thay cả khối banner bằng **một** banner không điều kiện phụ:
      ```tsx
      {storeOff && store.data && (
        <BannerNotice tone="info" title="Quán đang đóng cửa" body={store.data.closed_banner_text} />
      )}
      ```
      Xoá ternary `blocking_reason === 'OUTSIDE_HOURS' ? 'warn' : 'brand'` và **2 chuỗi cứng**.
      Bỏ luôn `action={{ label: 'Gọi quán' }}` nếu câu chữ do chủ quán tự soạn đã đủ (giữ nút gọi
      cũng được — ghi lựa chọn vào SUMMARY). Banner phải **co giãn chiều cao** theo độ dài chuỗi:
      không `WebkitLineClamp`, không `textOverflow: 'ellipsis'`, không `whiteSpace: 'nowrap'`.
    - Dòng ~94-99 `errorAction()`: bỏ `ONLINE_ORDERING_DISABLED`/`STORE_CLOSED` khỏi điều kiện,
      **giữ** `NO_TABLE_AVAILABLE`/`PHONE_BLACKLISTED` (sửa một phần, không xoá cả khối).
    - Giữ nguyên biến `storeOff` (vẫn cần để biết có hiện banner không) — chỉ đổi ý nghĩa của nó.

    **`OrderTrackPage.tsx`** — câu xác nhận sau submit lúc Đóng cửa:
    - Thêm 1 lời gọi `useApi('/api/public/store', PublicStoreStatus)` (**không poll**, chỉ 1 lần).
    - Khi `orderStatus === 'WAITING'` **và** `store.ordering_enabled === false` → tiêu đề trang dùng
      `store.closed_submit_confirm_text` thay cho "Đã gửi đơn thành công!". Các trường hợp còn lại
      giữ nguyên hành vi của plan 09-11.
    - Layout tiêu đề phải chịu được chuỗi dài (nhiều dòng), dùng `--fs-lg` thay `--fs-3xl` nếu chuỗi
      dài — chọn cách đơn giản: luôn cho phép xuống dòng, không giới hạn số dòng.
    - Kiểm lại ngưỡng bundle sau khi thêm (task này thêm rất ít code, nhưng vẫn phải đo).
  </action>
  <verify>
    <automated>cd apps/web && ../../node_modules/.bin/tsc --noEmit && cd ../shop && ../../node_modules/.bin/tsc --noEmit && ./node_modules/.bin/vite build && cd ../.. && bash scripts/check-shop-bundle.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "closed_banner_text" apps/web/src/pages/AdminSettingsPage.tsx` ≥ 1 và `grep -c "closed_submit_confirm_text" apps/web/src/pages/AdminSettingsPage.tsx` ≥ 1
    - `grep -c "ký tự còn lại" apps/web/src/pages/AdminSettingsPage.tsx` = 1 (chỉ còn của ô `off-reason` cũ, 2 ô mới không có bộ đếm)
    - `grep -c "storeOff || hasFieldErrors" apps/shop/src/pages/CheckoutPage.tsx` = 0
    - `grep -c "closed_banner_text" apps/shop/src/pages/CheckoutPage.tsx` = 1
    - `grep -cE "ellipsis|nowrap|WebkitLineClamp" apps/shop/src/pages/CheckoutPage.tsx` = 0
    - `grep -c "closed_submit_confirm_text" apps/shop/src/pages/OrderTrackPage.tsx` = 1
    - `bash scripts/check-shop-bundle.sh` → OK, ≤ 370 kB, và `git diff scripts/check-shop-bundle.sh` trống
    - **Chạy thật, chuỗi dài (nghiệm thu 09-VALIDATION § Manual-Only hàng 4)**: nhập chuỗi ~300 ký tự
      vào `closed_banner_text` ở `/admin/settings`, tắt công tắc, mở `/checkout` ở viewport **375px** →
      chữ **xuống dòng đủ**, không ellipsis, không tràn ngang, và **nút gửi đơn vẫn bấm được**
    - **Chạy thật, luồng đầy đủ**: submit đơn lúc Đóng cửa → 201 → `/o/<token>` hiện đúng
      `closed_submit_confirm_text`; bật công tắc lại rồi refresh `/o/<token>` → hiện tiêu đề bình thường
    - `cd apps/shop && ./node_modules/.bin/vitest run` và `cd apps/web && ./node_modules/.bin/vitest run` đều exit 0
  </acceptance_criteria>
  <done>Chủ quán đổi được 2 câu chữ và thấy ăn ngay ở trang khách; công tắc Đóng cửa chỉ còn đổi chữ, không khoá nút nào.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gỡ lớp chặn công tắc ↔ 4 lớp chống lạm dụng | Cùng nằm trong 1 hàm `checkOrderGuard` — gỡ nhầm là mở toang chống lạm dụng của phase 8 |
| chuỗi chủ quán tự nhập → HTML trang khách | Nội dung do người nhập được render cho mọi khách |
| key cấu hình mới → `updateMany()` | `if (!kind) continue` nuốt lặng lẽ key chưa khai báo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-65 | Elevation of Privilege | Gỡ nhánh công tắc kéo theo gỡ nhầm 1 trong 4 lớp chống lạm bụng → mở lại bom đơn / lách "1 đơn mở/SĐT" (**HIGH**, D-18) | mitigate | Task 2 có `<behavior>` bắt buộc ≥ 1 case cho **mỗi** lớp + 4 kịch bản HTTP thật (open-order, blacklist, rate limit, ip_hash) trong acceptance criteria; grep khẳng định 4 tên biến còn nguyên trong guard |
| T-09-66 | Tampering | Executor "sửa cho test xanh" bằng cách khôi phục nhánh chặn → code lại mâu thuẫn quyết định chủ dự án | mitigate | `<objective>` nói thẳng test SẼ đỏ và đó là dự kiến; acceptance criteria grep `ONLINE_ORDERING_DISABLED` trong guard = 0 |
| T-09-67 | Information Disclosure / Tampering | Chuỗi chủ quán nhập chứa mã HTML/JS → XSS trên trang khách | mitigate | React escape mặc định khi render chuỗi qua `{}`; **cấm** `dangerouslySetInnerHTML` — kiểm bằng grep = 0 trong 3 file FE sửa ở task 3. Chuỗi chỉ do tài khoản admin đã đăng nhập đặt, không phải input công khai |
| T-09-68 | Tampering | Key cấu hình thiếu 1 trong 3 chỗ round-trip → admin bấm Lưu, nhận 200, không gì được ghi | mitigate | Acceptance criteria có bài PUT-rồi-GET với chuỗi mồi `ZZ-BANNER-TEST`; grep 2 chỗ khai báo |
| T-09-69 | Denial of Service | Chuỗi rất dài phá vỡ layout trang khách ở 375px (chữ tràn / bị cắt mất thông tin quan trọng) | mitigate | Cấm `ellipsis`/`nowrap`/`line-clamp` (grep = 0); kịch bản nghiệm thu tay với chuỗi ~300 ký tự ở 375px |
| T-09-70 | Tampering | Xoá `escalate_autooff_after_s` khi vẫn còn nơi đọc → runtime `undefined` | mitigate | Bắt buộc `grep -rn` toàn `apps/` + `packages/` = 0 trước và sau khi xoá |
| T-09-71 | Repudiation | Ghi đè 5 quyết định LOCKED (M2.D-26/27/33/36/60) mà không có vết trong `OVERRIDE-DEBT.md` | mitigate | Plan 09-13 thêm entry OD-11..OD-15; docblock của `order-guard.ts` trỏ tới file đó. Plan này KHÔNG được coi là xong nếu 09-13 bị bỏ |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Plan này KHÔNG cài package nào |
</threat_model>

<verification>
1. `cd packages/schemas && ../../node_modules/.bin/tsc` — build lại
2. `cd apps/api && ./node_modules/.bin/vitest run` — full suite xanh (`order-guard.test.ts` đã viết lại, `store-status.test.ts` 16/16 nguyên)
3. `cd apps/web && ../../node_modules/.bin/tsc --noEmit` và `cd apps/shop && ../../node_modules/.bin/tsc --noEmit` — sạch
4. `bash scripts/check-shop-bundle.sh` — OK, gate không bị nới
5. 4 kịch bản D-18 chạy thật + 2 kịch bản D-11 (Đóng cửa và ngoài giờ đều 201)
6. Round-trip 2 key cấu hình bằng `ZZ-BANNER-TEST` / chuỗi 500 ký tự
7. Nghiệm thu tay chuỗi 300 ký tự ở 375px
</verification>

<success_criteria>
- Công tắc còn 2 trạng thái, cả hai đều nhận đơn; không còn 409 do công tắc
- 4 lớp chống lạm dụng chứng minh còn nguyên bằng unit test + 4 kịch bản HTTP
- `escalate_autooff_after_s` bị xoá sạch, không còn nơi nào đọc
- 2 key chữ mới round-trip đúng, không giới hạn độ dài, không cắt chữ ở 375px
- `store-status.ts` chỉ đổi comment, 16 test cũ xanh nguyên
- Bundle shop vẫn dưới ngưỡng, gate không bị nới
</success_criteria>

<output>
Create `.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-12-SUMMARY.md` when done
</output>
