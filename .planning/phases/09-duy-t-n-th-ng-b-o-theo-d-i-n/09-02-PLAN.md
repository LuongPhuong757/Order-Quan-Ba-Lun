---
phase: 09-duy-t-n-th-ng-b-o-theo-d-i-n
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/api/src/app.module.ts
  - apps/api/src/modules/maintenance/maintenance.module.ts
  - apps/api/src/modules/maintenance/retention-queries.ts
  - apps/api/src/modules/maintenance/retention-queries.test.ts
  - apps/api/src/modules/maintenance/maintenance-cron.service.ts
autonomous: true
requirements: [REQ-N]

must_haves:
  truths:
    - "`@nestjs/schedule@6.1.3` cài được và `import { Cron } from '@nestjs/schedule'` chạy thật trên máy này dù pnpm hỏng"
    - "`ScheduleModule.forRoot()` đăng ký trong app.module — poller của plan 09-05 có chỗ cắm"
    - "2 cron chết (`cron-audit-retention`, `cron-jti-cleanup`) CHẠY THẬT trong process API, không còn là script không ai gọi (C-CRON-01)"
    - "Điều kiện xoá của 2 cron y hệt bản CLI cũ — không viết lại logic, không nới cutoff"
    - "Cron mới dùng connection pool của Nest, KHÔNG tạo DataSource thứ hai (chống Anti-Pattern dual DataSource lifecycle)"
  artifacts:
    - path: "apps/api/src/modules/maintenance/retention-queries.ts"
      provides: "Hàm thuần tính cutoff + hàm xoá nhận EntityManager (test được)"
      exports: ["auditRetentionCutoffMs", "pruneAuditLogs", "pruneOrderActivityLogs", "pruneRevokedJti"]
    - path: "apps/api/src/modules/maintenance/maintenance-cron.service.ts"
      provides: "2 @Cron job gọi lại đúng query cũ qua DI"
      contains: "@Cron"
    - path: "apps/api/src/modules/maintenance/maintenance.module.ts"
      provides: "Module đăng ký cron + forFeature 3 entity"
  key_links:
    - from: "apps/api/src/app.module.ts"
      to: "ScheduleModule.forRoot()"
      via: "imports"
      pattern: "ScheduleModule\\.forRoot"
    - from: "apps/api/src/modules/maintenance/maintenance-cron.service.ts"
      to: "@InjectDataSource / @InjectRepository"
      via: "Nest DI — không AppDataSource.initialize()"
      pattern: "@Inject(DataSource|Repository)"
---

<objective>
Cài `@nestjs/schedule@6.1.3` bằng đường không-pnpm (pnpm hỏng trên máy này), bật
`ScheduleModule.forRoot()`, và **hồi sinh 2 cron đang chết** (`cron-audit-retention.ts`,
`cron-jti-cleanup.ts`) thành `@Cron` chạy trong process API (D-19, C-CRON-01).

Purpose: Toàn bộ lớp thông báo của phase (poller outbox 15s ở plan 09-05) đứng trên gói này. Đồng
thời đóng một nợ kỹ thuật đã ghi ở `.planning/codebase/CONCERNS.md:14-17`: 2 file cron tồn tại dạng
CLI nhưng **không có entry nào trong `docker-compose*.yml` gọi tới** — tức là tính năng có code
nhưng chưa bao giờ chạy. Thêm cron thứ ba (outbox) mà không sửa 2 cái này là nhân bản điểm chết im lặng.

Output: 1 dependency mới đã verify chạy được, 1 module `maintenance` với 2 job + test hàm thuần.
</objective>

<execution_context>
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-CONTEXT.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-RESEARCH.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-PATTERNS.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-VALIDATION.md

<interfaces>
<!-- Đã trích sẵn. KHÔNG đi khám phá lại. -->

apps/api/src/cli/cron-audit-retention.ts — LOGIC PHẢI GIỮ NGUYÊN:
  cutoffDays mặc định 90 · `cutoff_ts_ms = Date.now() - cutoffDays * 86_400_000`
  · xoá `AuditLog` WHERE `ts_ms < cutoff_ts_ms`   (cột là số epoch ms)
  · xoá `OrderActivityLog` WHERE `created_at < new Date(cutoff_ts_ms)`  (cột là Date)
  ⚠ hai cột KHÁC KIỂU — đừng "thống nhất" lại, sẽ xoá sai.

apps/api/src/cli/cron-jti-cleanup.ts — LOGIC PHẢI GIỮ NGUYÊN:
  xoá `RevokedJti` WHERE `expires_at_ms < Date.now()`

apps/api/src/app.module.ts (53 dòng) — `imports` hiện có: ConfigModule.forRoot({isGlobal:true}),
  TypeOrmModule.forRoot(dataSourceOptions), EventEmitterModule.forRoot(), ThrottlerModule.forRoot([...]),
  AuthModule, AuditModule, AdminModule, SetupModule, MenuModule, TablesModule, OrdersModule,
  PublicModule, SettingsModule.

Entity đã đăng ký sẵn trong `data-source.ts` (không cần thêm gì cho plan này):
  AuditLog · OrderActivityLog · RevokedJti

apps/api/src/modules/orders/orders.module.ts — KHUÔN MẪU khai báo module (`TypeOrmModule.forFeature`).

RESEARCH.md § Package Legitimacy Audit: `@nestjs/schedule` verdict **[OK]** (npm registry check +
  slopcheck), disposition Approved. Cây phụ thuộc đã xác minh:
  `@nestjs/schedule@6.1.3` → `cron@4.4.0` → `luxon@~3.7.0` + `@types/luxon@~3.7.0`. peerDeps
  `@nestjs/common|core: ^10 || ^11` khớp `^10.4.0` đang dùng. **Không có [ASSUMED]/[SUS]/[SLOP]** nên
  KHÔNG cần checkpoint xác minh thủ công trước khi cài.

RESEARCH.md § Anti-Patterns: "Copy `AppDataSource.initialize()`/`.destroy()` pattern của CLI script
  vào code chạy trong HTTP server" — cấm tuyệt đối. Cron mới PHẢI dùng DI.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cài @nestjs/schedule@6.1.3 không qua pnpm + đăng ký ScheduleModule</name>
  <files>apps/api/package.json, apps/api/src/app.module.ts</files>
  <read_first>
    - apps/api/package.json (khối `dependencies`, thứ tự alphabet của các gói `@nestjs/*`)
    - apps/api/src/app.module.ts (toàn bộ, 53 dòng)
    - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-RESEARCH.md § Environment Availability + § Package Legitimacy Audit
  </read_first>
  <action>
    **Bước 1 — khai báo dependency.** Thêm `"@nestjs/schedule": "6.1.3"` vào `dependencies` của
    `apps/api/package.json`, đặt sau `"@nestjs/platform-express"` để giữ thứ tự alphabet. Ghim đúng
    version, KHÔNG dùng `^` (máy này không chạy được `pnpm install` nên lockfile sẽ không khớp; ghim
    cứng làm việc "cài lại trên máy khác" tất định).

    **Bước 2 — cài thật, đường không-pnpm.** `pnpm` hỏng trên máy này (Node 20 vs `node:sqlite` →
    `ERR_UNKNOWN_BUILTIN_MODULE`) và `npm install` chạy thẳng trong repo sẽ thất bại vì
    `package.json` dùng protocol `workspace:*` cho `@order/schemas`/`@order/utils`. Cách đã chọn —
    cài ở thư mục tạm rồi copy vào `apps/api/node_modules` (nơi Node phân giải trước tiên khi chạy
    từ `apps/api/src`):

    ```
    TMP="$(mktemp -d)"
    cd "$TMP" && npm init -y >/dev/null
    npm install --omit=peer --no-audit --no-fund @nestjs/schedule@6.1.3
    ```
    rồi copy **4 thư mục** (đúng cây phụ thuộc đã xác minh, không copy thừa) từ
    `$TMP/node_modules` sang `<repo>/apps/api/node_modules/`:
    `@nestjs/schedule` · `cron` · `luxon` · `@types/luxon`
    (dùng `cp -R`; `@nestjs/schedule` là thư mục THẬT, không phải symlink — cố ý, vì store pnpm
    không dựng lại được khi `pnpm` hỏng).

    Nếu máy không có mạng: dừng lại và báo blocker, KHÔNG tự viết `setInterval` thay thế
    (`@nestjs/schedule` là quyết định đã chốt ở D-19 và RESEARCH § Don't Hand-Roll).

    Ghi lại **nguyên văn** các lệnh đã dùng và output vào `09-02-SUMMARY.md` — 09-VALIDATION.md Wave 0
    yêu cầu ghi rõ cách cài đã dùng, vì người sau trên máy khác sẽ chỉ cần `pnpm install`.

    **Bước 3 — đăng ký.** Trong `apps/api/src/app.module.ts`, thêm
    `import { ScheduleModule } from '@nestjs/schedule';` và `ScheduleModule.forRoot(),` vào `imports`,
    đặt ngay sau `EventEmitterModule.forRoot()`. Thêm comment 3 dòng ngay trên: đây là scheduler
    in-process của D-19, thay cho OS cron mà repo chưa bao giờ wire được (C-CRON-01); mọi `@Cron`
    trong app đều phụ thuộc dòng này — xoá nó là làm chết im lặng cả outbox poller lẫn 2 job retention.
    Cũng thêm `MaintenanceModule` vào `imports` (file tạo ở Task 2) — nếu Task 2 chưa xong thì để lại
    bước này cho cuối Task 2, KHÔNG commit app.module ở trạng thái import module chưa tồn tại.
  </action>
  <verify>
    <automated>cd apps/api && node -e "import('@nestjs/schedule').then(m=>{if(typeof m.Cron!=='function')process.exit(1);console.log('Cron OK');})" && ../../node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"@nestjs/schedule": "6.1.3"' apps/api/package.json` = 1
    - `ls apps/api/node_modules/@nestjs/schedule/package.json` tồn tại
    - `node -p "require('./apps/api/node_modules/@nestjs/schedule/package.json').version"` in ra `6.1.3`
    - `ls apps/api/node_modules/cron/package.json apps/api/node_modules/luxon/package.json` đều tồn tại
    - `grep -c "ScheduleModule.forRoot()" apps/api/src/app.module.ts` = 1
    - `cd apps/api && ../../node_modules/.bin/tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>`@Cron` import được và biên dịch được trong `apps/api`; scheduler đã bật ở app.module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hồi sinh 2 cron chết thành @Cron dùng DI (C-CRON-01)</name>
  <files>apps/api/src/modules/maintenance/retention-queries.ts, apps/api/src/modules/maintenance/retention-queries.test.ts, apps/api/src/modules/maintenance/maintenance-cron.service.ts, apps/api/src/modules/maintenance/maintenance.module.ts, apps/api/src/app.module.ts</files>
  <read_first>
    - apps/api/src/cli/cron-audit-retention.ts (toàn bộ — nguồn logic phải chép đúng)
    - apps/api/src/cli/cron-jti-cleanup.ts (toàn bộ)
    - apps/api/src/modules/orders/orders.module.ts (khuôn mẫu module + forFeature)
    - apps/api/src/modules/audit/audit.service.ts (đã có sẵn `prune(cutoffDays, dryRun)` — cân nhắc tái dùng thay vì viết lại phần AuditLog)
    - apps/api/src/modules/public/store-status.test.ts (khuôn mẫu test hàm thuần)
  </read_first>
  <behavior>
    `retention-queries.test.ts` (hàm thuần, KHÔNG cần DB) phủ:
    - `auditRetentionCutoffMs(nowMs, 90)` = `nowMs - 90 * 86_400_000`
    - `auditRetentionCutoffMs(nowMs, 1)` = `nowMs - 86_400_000` (đổi được số ngày, không hardcode)
    - `auditRetentionCutoffMs` không tự đọc `Date.now()` — gọi 2 lần với cùng `nowMs` cho cùng kết quả
    - 3 hàm `prune*` nhận `EntityManager` giả (object có `getRepository()` trả fake repo ghi lại
      điều kiện `where`) và assert:
      · `pruneAuditLogs` dùng điều kiện trên cột `ts_ms` với giá trị **số** = cutoff ms
      · `pruneOrderActivityLogs` dùng điều kiện trên cột `created_at` với giá trị **Date** = `new Date(cutoff)`
      · `pruneRevokedJti` dùng điều kiện trên `expires_at_ms` với giá trị **số** = nowMs
    - Mỗi hàm trả về `{ deleted_rows: number }` lấy từ `result.affected ?? 0` (fake trả `affected: 7` → 7;
      fake trả `affected: undefined` → 0, không NaN)
  </behavior>
  <action>
    Tạo thư mục `apps/api/src/modules/maintenance/`.

    **`retention-queries.ts`** — tách phần "quyết định + câu query" ra khỏi Nest để test được:
    - `export function auditRetentionCutoffMs(nowMs: number, cutoffDays: number): number`
    - `export async function pruneAuditLogs(mgr: EntityManager, cutoffMs: number): Promise<{ deleted_rows: number }>`
    - `export async function pruneOrderActivityLogs(mgr: EntityManager, cutoffMs: number): Promise<{ deleted_rows: number }>`
    - `export async function pruneRevokedJti(mgr: EntityManager, nowMs: number): Promise<{ deleted_rows: number }>`
    Chép **nguyên điều kiện** từ 2 file CLI (xem `<interfaces>`): `AuditLog.ts_ms < cutoffMs` (số),
    `OrderActivityLog.created_at < new Date(cutoffMs)` (Date), `RevokedJti.expires_at_ms < nowMs` (số).
    Docblock đầu file: "Logic gốc ở `src/cli/cron-audit-retention.ts` và `cron-jti-cleanup.ts` —
    plan 09-02 chỉ NỐI DÂY lại, không đổi điều kiện xoá. Sửa số ngày giữ log là quyết định vận hành,
    phải sửa cả 2 nơi hoặc bỏ hẳn file CLI."
    Import `EntityManager` bằng `import type { EntityManager } from 'typeorm'` (type-only) để file vẫn
    test được mà không kéo runtime TypeORM vào test.

    **`maintenance-cron.service.ts`** — `@Injectable()`, inject `@InjectDataSource() private readonly ds: DataSource`:
    - `@Cron(CronExpression.EVERY_DAY_AT_3AM)` → `auditRetention()`: đọc số ngày từ
      `process.env.AUDIT_RETENTION_DAYS` (mặc định **90**, giữ nguyên mặc định CLI), gọi
      `pruneAuditLogs` + `pruneOrderActivityLogs`, log 1 dòng qua `Logger` với số dòng đã xoá.
    - `@Cron(CronExpression.EVERY_HOUR)` → `jtiCleanup()`: gọi `pruneRevokedJti(this.ds.manager, Date.now())`,
      log số dòng.
      Lý do chọn 2 lịch này (ghi thành comment): retention là dọn dữ liệu 90 ngày → 1 lần/ngày lúc
      quán đóng cửa là đủ; JTI hết hạn tích luỹ theo phiên đăng nhập nên 1 lần/giờ, rẻ vì bảng nhỏ.
      Đây là discretion đã chốt tại plan (RESEARCH Assumption A3 không chốt tần suất).
    - Cả 2 method bọc `try/catch`, log lỗi rồi **nuốt** — job lỗi KHÔNG được làm sập app (đúng khuôn
      `AuditEventHandler` ở `audit.interceptor.ts`).
    - ⚠ TUYỆT ĐỐI không gọi `AppDataSource.initialize()`/`.destroy()` trong file này — đó là pattern
      của CLI script, dùng lại trong HTTP server sẽ tạo dual DataSource lifecycle (Anti-Pattern đã ghi
      ở `ARCHITECTURE.md`). Ghi cảnh báo này thành comment ngay trên constructor.

    **`maintenance.module.ts`** — `@Module({ imports: [TypeOrmModule.forFeature([AuditLog, OrderActivityLog, RevokedJti])],
    providers: [MaintenanceCronService] })`. Thêm `MaintenanceModule` vào `imports` của `app.module.ts`.

    **KHÔNG xoá** `src/cli/cron-audit-retention.ts`, `src/cli/cron-jti-cleanup.ts` hay 2 script
    `cron:*` trong `package.json` — giữ để chạy tay/`--dry-run` khi cần (RESEARCH Pattern 3).
    Thêm 1 dòng comment ở đầu MỖI file CLI: "Từ phase 9 việc chạy định kỳ do
    `modules/maintenance/maintenance-cron.service.ts` đảm nhiệm. File này giữ lại cho chạy tay/dry-run."

    Viết `retention-queries.test.ts` phủ đủ `<behavior>`, không dùng `@nestjs/testing`, không cần MySQL.
  </action>
  <verify>
    <automated>cd apps/api && ./node_modules/.bin/vitest run src/modules/maintenance/retention-queries.test.ts && ../../node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@Cron(" apps/api/src/modules/maintenance/maintenance-cron.service.ts` = 2
    - `grep -cE "AppDataSource" apps/api/src/modules/maintenance/*.ts` = 0
    - `grep -c "ts_ms" apps/api/src/modules/maintenance/retention-queries.ts` ≥ 1 và `grep -c "expires_at_ms" ...` ≥ 1 và `grep -c "created_at" ...` ≥ 1
    - `grep -c "MaintenanceModule" apps/api/src/app.module.ts` = 2 (import + imports array)
    - `grep -c "cron:audit-retention" apps/api/package.json` = 1 (script CLI cũ vẫn còn)
    - `cd apps/api && ./node_modules/.bin/vitest run src/modules/maintenance/` exit 0 với ≥ 8 test pass
    - **Chạy thật**: khởi động API (`cd apps/api && node --import @swc-node/register/esm-register src/main.ts`), log khởi động không có lỗi DI, và `grep -i "schedule\|cron"` trong log không có exception. Tắt server sau khi kiểm.
    - `cd apps/api && ./node_modules/.bin/vitest run` full suite exit 0
  </acceptance_criteria>
  <done>2 job retention chạy trong process API bằng `@Cron` với đúng điều kiện xoá cũ; C-CRON-01 đóng phần "cron có code nhưng không ai gọi".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → `apps/api/node_modules` | Cài gói mới đầu tiên của phase 9; cài tay nên không có lockfile bảo vệ |
| job nền → dữ liệu audit/JTI | Job xoá dữ liệu tự động, không có người bấm nút — sai điều kiện = mất bằng chứng audit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Tampering | Cron retention xoá quá tay → mất audit log (mất khả năng truy vết ai duyệt đơn — chính là kiểm soát bù trừ của D-02) | mitigate | Điều kiện xoá chép nguyên văn CLI cũ (`ts_ms < now - 90d`), số ngày đọc từ env với mặc định 90; test hàm thuần assert đúng cutoff và đúng kiểu dữ liệu từng cột |
| T-09-07 | Denial of Service | Job lỗi ném exception làm sập process API | mitigate | Cả 2 method bọc `try/catch` + log, nuốt lỗi — khuôn `AuditEventHandler` đã chạy production |
| T-09-08 | Tampering | Dual DataSource lifecycle (copy pattern CLI) làm rò connection/khoá pool 50 | mitigate | Cấm `AppDataSource` trong `modules/maintenance/*`; acceptance criteria grep = 0; dùng `@InjectDataSource` |
| T-09-09 | Repudiation | Job chạy nhưng không ai biết đã xoá bao nhiêu | mitigate | Mỗi lần chạy log 1 dòng `Logger` kèm `deleted_rows` + cutoff |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | `@nestjs/schedule@6.1.3` — RESEARCH § Package Legitimacy Audit: slopcheck **[OK]**, org chính thức NestJS, repo `github.com/nestjs/schedule`, disposition **Approved**. Không có `[ASSUMED]`/`[SUS]`/`[SLOP]` nên không cần checkpoint chặn. Cây phụ thuộc ghim đúng 4 gói (`@nestjs/schedule`, `cron`, `luxon`, `@types/luxon`), version ghim cứng không `^`, và acceptance criteria xác minh version thật trên đĩa |
</threat_model>

<verification>
1. `node -p "require('./apps/api/node_modules/@nestjs/schedule/package.json').version"` → `6.1.3`
2. `cd apps/api && ./node_modules/.bin/vitest run src/modules/maintenance/` — xanh
3. `cd apps/api && ./node_modules/.bin/vitest run` — full suite xanh
4. `cd apps/api && ../../node_modules/.bin/tsc --noEmit` — sạch
5. Khởi động API thật 1 lần: không có lỗi DI của `ScheduleModule`/`MaintenanceModule`
</verification>

<success_criteria>
- `@nestjs/schedule@6.1.3` cài được và import được, cách cài ghi nguyên văn vào SUMMARY
- `ScheduleModule.forRoot()` có mặt trong app.module — plan 09-05 cắm poller vào được
- 2 job retention chạy bằng `@Cron`, dùng DI, giữ nguyên điều kiện xoá, log số dòng
- 2 file CLI cũ + 2 script `cron:*` vẫn còn để chạy tay
- Full suite xanh, typecheck sạch
</success_criteria>

<output>
Create `.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-02-SUMMARY.md` when done
</output>
