---
phase: 09-duy-t-n-th-ng-b-o-theo-d-i-n
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/modules/orders/entities/order.entity.ts
  - apps/api/src/modules/public/entities/online-order-request.entity.ts
  - apps/api/src/modules/notifications/entities/notification-outbox.entity.ts
  - apps/api/src/data-source.ts
  - apps/api/src/cli/verify-schema.ts
autonomous: true
requirements: [REQ-M, REQ-N, REQ-O]

must_haves:
  truths:
    - "Bảng `orders` có đủ 9 cột §4.5; `ship_fee` mặc định 0 nên đơn tại quán không đổi hành vi"
    - "Bảng `notification_outbox` TỒN TẠI THẬT trong MySQL — chứng minh bằng truy vấn information_schema, không phải bằng typecheck"
    - "`online_order_requests` có cột ghi chú nội bộ (D-09) tách hẳn khỏi `reject_reason`"
    - "Mọi entity mới/đổi đều có mặt trong mảng `entities` tường minh của `data-source.ts`"
    - "`schema:verify` fail (exit 1) nếu thiếu bất kỳ bảng/cột nào của phase 9"
  artifacts:
    - path: "apps/api/src/modules/notifications/entities/notification-outbox.entity.ts"
      provides: "Entity §4.6 + index (scheduled_at, status) cho câu quét poller"
      contains: "notification_outbox"
    - path: "apps/api/src/data-source.ts"
      provides: "Đăng ký NotificationOutbox trong mảng entities tường minh"
      contains: "NotificationOutbox"
    - path: "apps/api/src/cli/verify-schema.ts"
      provides: "Gate schema mở rộng cho phase 9"
      contains: "notification_outbox"
  key_links:
    - from: "apps/api/src/data-source.ts"
      to: "modules/notifications/entities/notification-outbox.entity.js"
      via: "import + mảng entities"
      pattern: "NotificationOutbox"
    - from: "apps/api/src/cli/verify-schema.ts"
      to: "information_schema.COLUMNS"
      via: "truy vấn MySQL thật"
      pattern: "information_schema"
---

<objective>
Thêm toàn bộ schema phase 9 (**chỉ ADD cột/bảng, không rename gì**) và chứng minh nó đã hạ cánh
thật xuống MySQL, không chỉ biên dịch được.

Purpose: `data-source.ts` dùng **mảng entities TƯỜNG MINH, không autoload** (comment dòng 40-43 nói
rõ). Entity thiếu trong mảng đó thì `synchronize` bỏ qua hoàn toàn bảng của nó **mà `tsc` vẫn xanh** —
đây đúng là cái bẫy đã được ghi thành gate ở phase 8. RESEARCH Pitfall 4 và 5 cũng khẳng định
`Order` chưa có cột nào của §4.5 và `notification_outbox` chưa tồn tại. 5 plan phía sau đứng trên đây,
nên bước xác nhận bằng truy vấn MySQL thật là **[BLOCKING]**, không phải báo cáo cho vui.

Output: 9 cột mới trên `orders`, 1 cột mới trên `online_order_requests`, 1 bảng mới, 1 gate mở rộng.
</objective>

<execution_context>
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-CONTEXT.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-RESEARCH.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-PATTERNS.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-02-SUMMARY.md

<interfaces>
<!-- Trích sẵn. KHÔNG đi khám phá lại. -->

apps/api/src/data-source.ts dòng 44-50 — mảng cần sửa:
```typescript
entities: [
  User, AuditLog, RevokedJti, RecoveryCode, MenuItem, MenuGroup, RestaurantTable, Order,
  OrderItem, OrderActivityLog,
  StoreSetting,
  PhoneBlacklist,
  OnlineOrderRequest,
],
```
`synchronize: true`, `migrations` khai nhưng KHÔNG dùng (M2.D-07 + C-SCHEMA-07). **Không có
`typeorm migration:run` trong dự án này** — thêm cột là đủ, rename cột = mất dữ liệu im lặng.

apps/api/src/modules/orders/entities/order.entity.ts (73 dòng) — cột hiện có:
  id · table_id · table_code · opened_at · first_kitchen_at · closed_at · is_paid ·
  customer_name · customer_address · customer_phone · created_by_user_id · created_by_full_name ·
  checked_out_by_user_id · checked_out_by_full_name · updated_at · items (OneToMany)
  Quy ước cột nullable: `@Column({ type: 'varchar', length: 128, nullable: true }) x!: string | null;`
  Quy ước cột thời gian: `@Column({ type:'datetime', precision:6, transformer: dateToMsTransformer })`
  (import từ `../../auth/entities/user.entity.js`).

apps/api/src/modules/public/entities/online-order-request.entity.ts (124 dòng) — ĐÃ CÓ:
  order_token · customer_token · status · fulfillment_type · customer_name · customer_phone ·
  customer_address · customer_lat/lng (decimal → **string** qua mysql2) · customer_map_link ·
  distance_km (decimal → string) · customer_note · items_snapshot (json) · subtotal ·
  submitted_at · reviewed_at · reviewed_by_user_id · reviewed_by_full_name · reject_reason ·
  order_id · max_progress_shown · ip_hash · user_agent · created_at
  → CHỈ THIẾU cột ghi chú nội bộ của D-09.

docs/MILESTONE-02-ONLINE-ORDERING-SPEC.md §4.5 (dòng 327-344) — 9 cột thêm vào `orders`.
docs/MILESTONE-02-ONLINE-ORDERING-SPEC.md §4.6 (dòng 346-363) — bảng `notification_outbox`.

apps/api/src/cli/verify-schema.ts — gate đã có từ phase 8, mảng `CHECKS` gồm 3 bảng
  (`store_settings`, `phone_blacklist`, `online_order_requests`); exit 1 nếu thiếu.
  Chạy (pnpm hỏng): `cd apps/api && node --import @swc-node/register/esm-register src/cli/verify-schema.ts`

MySQL local: `mysql -h127.0.0.1 -P3307 -uorder_app -porder_app_pass order_quan_balun` (theo `.env`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 9 cột §4.5 trên `orders` + cột ghi chú nội bộ trên `online_order_requests` + entity `notification_outbox`</name>
  <files>apps/api/src/modules/orders/entities/order.entity.ts, apps/api/src/modules/public/entities/online-order-request.entity.ts, apps/api/src/modules/notifications/entities/notification-outbox.entity.ts</files>
  <read_first>
    - apps/api/src/modules/orders/entities/order.entity.ts (toàn bộ 73 dòng — quy ước `@Column`)
    - apps/api/src/modules/public/entities/online-order-request.entity.ts (toàn bộ — quy ước decimal→string, `dateToMsTransformer`, docblock cảnh báo rename)
    - docs/MILESTONE-02-ONLINE-ORDERING-SPEC.md dòng 327-363
    - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-PATTERNS.md § notification-outbox.entity.ts và § order.entity.ts
  </read_first>
  <action>
    **`order.entity.ts` — thêm ĐÚNG 9 cột §4.5, tất cả additive, không đụng cột cũ:**
    - `source` varchar(16) default `'STAFF'` — comment: `'STAFF' | 'ONLINE'`. Mặc định `'STAFF'` để
      mọi đơn cũ và mọi đơn tạo tại quán giữ nguyên nghĩa.
    - `fulfillment_type` varchar(16) nullable — `'PICKUP' | 'DELIVERY' | null` (null = dine-in)
    - `online_request_id` varchar(36) nullable + `@Index()` — trỏ ngược `online_order_requests.id`
    - `order_token` varchar(64) nullable + `@Index({ unique: true })` — copy từ request để `/o/<token>`
      đọc được order thật sau khi duyệt
    - `customer_lat` / `customer_lng` decimal(10,7) nullable → **khai type TS là `string | null`**
      (mysql2 trả decimal dạng chuỗi — cùng lý do đã ghi trong `online-order-request.entity.ts`)
    - `customer_map_link` varchar(512) nullable
    - `distance_km` decimal(6,2) nullable → `string | null`
    - `ship_fee` int default 0 — comment M2.D-62: **không** vào doanh thu món; `PAID_SQL` ở
      `orders.service.ts` tính tiền từ `order_items` nên mặc định 0 tự động vô hại với đơn tại quán
    - `payment_method` varchar(16) default `'CASH'` — M2.D-58, chỗ ngỏ cho chuyển khoản sau này
    Thêm docblock ngắn trên cụm 9 cột: "Phase 9 (§4.5). CHỈ THÊM cột. `synchronize: true`, không
    migration — rename bất kỳ cột nào ở đây về sau là mất dữ liệu im lặng (C-SCHEMA-07)."

    **`online-order-request.entity.ts` — thêm ĐÚNG 1 cột:**
    - `internal_reject_note` varchar(500) nullable. Docblock ngay trên: "D-09 — ghi chú tự do của
      admin khi từ chối. CHỈ lưu DB + audit log. **TUYỆT ĐỐI không** đưa vào `PublicOrderStatus` hay
      bất kỳ response `/api/public/*` nào; `reject_reason` (câu soạn sẵn) mới là thứ khách đọc."
    Không đụng cột nào khác.

    **`modules/notifications/entities/notification-outbox.entity.ts` (mới)** — đúng §4.6, theo khuôn
    `online-order-request.entity.ts`:
    ```
    @Entity('notification_outbox')
    @Index('idx_outbox_scheduled_status', ['scheduled_at', 'status'])   // câu quét của poller
    @Index('idx_outbox_request', ['request_id'])                        // huỷ L2 khi duyệt
    ```
    Cột: `id` uuid PK · `request_id` varchar(36) · `channel` varchar(16) (`'SSE'|'SMS'|'EMAIL'`) ·
    `recipient` varchar(255) · `level` varchar(4) (`'L1'|'L2'|'L3'`) · `status` varchar(16)
    (`'PENDING'|'SENT'|'FAILED'|'CANCELLED'`) · `attempts` int default 0 · `last_error` varchar(500)
    nullable · `scheduled_at` datetime(6) + `dateToMsTransformer` · `sent_at` datetime(6) nullable +
    transformer · `created_at` `@CreateDateColumn` + transformer.
    ⚠ Ghi rõ trong docblock 2 điểm lệch spec đã chốt:
    1. Thêm giá trị `'CANCELLED'` cho `status` — spec §4.6 chỉ liệt kê PENDING/SENT/FAILED, nhưng
       §7 dòng 489 yêu cầu "huỷ các outbox còn PENDING" khi duyệt/từ chối. Giữ `'CANCELLED'` thay vì
       xoá row để còn audit được "đã duyệt kịp trước 90s nên không bắn SMS".
    2. **KHÔNG có `L4`** — D-12 bỏ hẳn auto-OFF, nên chỉ còn L1 (SSE), L2 (SMS 90s), L3 (EMAIL).
       Union type chỉ khai 3 giá trị để ai đó cài lại L4 sẽ vấp typecheck.
    ⚠ `recipient` KHÔNG được chứa số điện thoại KHÁCH — outbox chỉ gửi cho nhân sự quán
    (`notify_sms_recipients`/`notify_email_recipients`), D-10 đã chốt không SMS cho khách. Ghi comment.
  </action>
  <verify>
    <automated>cd apps/api && ../../node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^\s+(source|fulfillment_type|online_request_id|order_token|customer_lat|customer_lng|customer_map_link|distance_km|ship_fee|payment_method)!" apps/api/src/modules/orders/entities/order.entity.ts` = 10
    - `grep -c "internal_reject_note" apps/api/src/modules/public/entities/online-order-request.entity.ts` = 1
    - `grep -c "notification_outbox" apps/api/src/modules/notifications/entities/notification-outbox.entity.ts` = 1
    - `grep -c "idx_outbox_scheduled_status" apps/api/src/modules/notifications/entities/notification-outbox.entity.ts` = 1
    - `grep -c "'L4'" apps/api/src/modules/notifications/entities/notification-outbox.entity.ts` = 0
    - `git diff --stat apps/api/src/modules/orders/entities/order.entity.ts` — chỉ có dòng THÊM, 0 dòng cột cũ bị xoá/đổi tên
    - `cd apps/api && ../../node_modules/.bin/tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>3 file entity đúng §4.5/§4.6 + cột D-09, tất cả additive.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Đăng ký entity vào data-source + mở rộng gate `schema:verify` + chứng minh bằng MySQL thật</name>
  <files>apps/api/src/data-source.ts, apps/api/src/cli/verify-schema.ts</files>
  <read_first>
    - apps/api/src/data-source.ts (toàn bộ 59 dòng — đặc biệt comment dòng 40-43 về mảng tường minh)
    - apps/api/src/cli/verify-schema.ts (toàn bộ — mảng `CHECKS` và cách exit 1)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-02-SUMMARY.md (tiền lệ task BLOCKING này ở phase 8)
  </read_first>
  <action>
    **`data-source.ts`:** thêm `import { NotificationOutbox } from './modules/notifications/entities/notification-outbox.entity.js';`
    và thêm `NotificationOutbox,` vào cuối mảng `entities`. `Order` và `OnlineOrderRequest` đã có sẵn
    trong mảng nên cột mới của chúng tự được `synchronize` — nhưng vẫn phải chứng minh ở bước dưới,
    không được tin.

    **`verify-schema.ts`:** mở rộng mảng `CHECKS`:
    - thêm bảng `notification_outbox` với `requiredColumns`: `id`, `request_id`, `channel`,
      `recipient`, `level`, `status`, `attempts`, `last_error`, `scheduled_at`, `sent_at`, `created_at`
    - thêm bảng `orders` với `requiredColumns`: 10 cột mới (`source`, `fulfillment_type`,
      `online_request_id`, `order_token`, `customer_lat`, `customer_lng`, `customer_map_link`,
      `distance_km`, `ship_fee`, `payment_method`)
    - bổ sung `internal_reject_note` vào `requiredColumns` của `online_order_requests` đã có
    Cập nhật docblock đầu file: gate này nay phủ cả phase 8 lẫn phase 9.

    **Chứng minh bằng DB thật (đây là phần BLOCKING):** khởi động API 1 lần để `synchronize` chạy
    (`cd apps/api && node --import @swc-node/register/esm-register src/main.ts`, chờ log sẵn sàng rồi
    tắt), sau đó chạy gate và truy vấn tay. Nếu bảng/cột chưa có: KHÔNG được sửa gate cho dễ đậu —
    quay lại kiểm mảng `entities` và tên bảng trong `@Entity()`.

    Dán **output thật** của cả 3 lệnh ở acceptance criteria vào `09-04-SUMMARY.md`. Mọi plan từ 09-05
    trở đi đều giả định bước này đã xanh.
  </action>
  <verify>
    <automated>cd apps/api && node --import @swc-node/register/esm-register src/cli/verify-schema.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "NotificationOutbox" apps/api/src/data-source.ts` = 2 (import + mảng)
    - `grep -c "notification_outbox" apps/api/src/cli/verify-schema.ts` = 1
    - `grep -c "internal_reject_note" apps/api/src/cli/verify-schema.ts` = 1
    - `cd apps/api && node --import @swc-node/register/esm-register src/cli/verify-schema.ts` exit **0**
    - `mysql -h127.0.0.1 -P3307 -uorder_app -porder_app_pass order_quan_balun -e "SHOW TABLES LIKE 'notification_outbox';"` → có đúng 1 dòng
    - `mysql -h127.0.0.1 -P3307 -uorder_app -porder_app_pass order_quan_balun -e "SHOW COLUMNS FROM orders;"` → thấy đủ 10 cột mới, `ship_fee` có `Default 0`, `source` có `Default STAFF`, `payment_method` có `Default CASH`
    - `mysql -h127.0.0.1 -P3307 -uorder_app -porder_app_pass order_quan_balun -e "SHOW COLUMNS FROM online_order_requests LIKE 'internal_reject_note';"` → có đúng 1 dòng
    - `mysql -h127.0.0.1 -P3307 -uorder_app -porder_app_pass order_quan_balun -e "SHOW INDEX FROM notification_outbox WHERE Key_name='idx_outbox_scheduled_status';"` → có dòng (index quét poller tồn tại thật)
    - `mysql ... -e "SELECT COUNT(*) FROM orders WHERE ship_fee IS NULL;"` = 0 (đơn cũ nhận default, không NULL)
    - `cd apps/api && ./node_modules/.bin/vitest run` full suite exit 0 (đơn cũ không bị ảnh hưởng)
  </acceptance_criteria>
  <done>Schema phase 9 đã hạ cánh thật xuống MySQL và có gate tự động chặn hồi quy; 5 plan phía sau được phép giả định bảng/cột tồn tại.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| entity TypeScript → bảng MySQL thật | `synchronize` là bước duy nhất; typecheck KHÔNG chứng minh gì ở ranh giới này |
| ghi chú nội bộ (cột mới) → response công khai | Cột mới đầu tiên của hệ thống được thiết kế để KHÔNG BAO GIỜ ra khỏi hàng rào nội bộ |
| `order_token` trên bảng `orders` | Bearer credential nay tồn tại ở 2 bảng — bề mặt rò tăng gấp đôi |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-14 | Denial of Service | Entity thiếu trong mảng `entities` → bảng không bao giờ được tạo, poller chết im lặng ở runtime dù CI xanh | mitigate | Task 2 là `[BLOCKING]`: `verify-schema.ts` truy vấn `information_schema` thật + 4 truy vấn `mysql` tay trong acceptance criteria |
| T-09-15 | Information Disclosure | `internal_reject_note` bị spread vào response công khai bởi plan sau | mitigate | Cột đặt tên có tiền tố `internal_`, docblock cảnh báo tại chỗ; whitelist `.strict().parse()` của `PublicOrderStatus` (09-09) là lưới thứ hai; test assert ở 09-09 là lưới thứ ba |
| T-09-16 | Spoofing | Đoán `order_token` để xem đơn người khác (nay có thêm bản trên `orders`) | mitigate | Token vẫn là `randomBytes(32)` từ phase 8 (256-bit CSPRNG), chỉ **copy** sang `orders`, không sinh lại; cột khai `UNIQUE` để 2 đơn không đụng token |
| T-09-17 | Tampering | Rename/đổi kiểu cột cũ → mất dữ liệu im lặng (không có migration để rollback) | mitigate | Chỉ ADD; acceptance criteria đọc `git diff --stat` khẳng định 0 dòng cột cũ bị đổi |
| T-09-18 | Tampering | `ship_fee` mặc định NULL làm hỏng mọi phép cộng doanh thu cũ | mitigate | `default: 0` + truy vấn `COUNT(*) WHERE ship_fee IS NULL` = 0 trong acceptance criteria |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Plan này KHÔNG cài package nào |
</threat_model>

<verification>
1. `cd apps/api && node --import @swc-node/register/esm-register src/cli/verify-schema.ts` → exit 0
2. 4 truy vấn `mysql` ở acceptance criteria — dán output thật vào SUMMARY
3. `cd apps/api && ./node_modules/.bin/vitest run` — full suite xanh
4. `cd apps/api && ../../node_modules/.bin/tsc --noEmit` — sạch
</verification>

<success_criteria>
- 10 cột mới trên `orders`, 1 cột mới trên `online_order_requests`, bảng `notification_outbox` tồn tại **trong MySQL thật**
- `schema:verify` phủ đủ phase 9 và exit 1 khi thiếu
- Không có cột nào bị rename/đổi kiểu; đơn cũ không bị ảnh hưởng
- Output truy vấn MySQL được dán vào SUMMARY làm bằng chứng
</success_criteria>

<output>
Create `.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-04-SUMMARY.md` when done
</output>
