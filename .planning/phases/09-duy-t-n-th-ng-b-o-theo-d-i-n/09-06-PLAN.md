---
phase: 09-duy-t-n-th-ng-b-o-theo-d-i-n
plan: 06
type: execute
wave: 3
depends_on: ["09-01", "09-03", "09-04", "09-05"]
files_modified:
  - packages/schemas/src/errors.ts
  - apps/api/src/modules/admin-online-orders/admin-online-orders.service.ts
  - apps/api/src/modules/admin-online-orders/admin-online-orders.module.ts
autonomous: true
requirements: [REQ-M]

must_haves:
  truths:
    - "Xác nhận đơn → cấp bàn trống đầu tiên theo `code` ASC đúng `kind` (PICKUP→takeaway, DELIVERY→delivery)"
    - "Hết bàn → TỰ TẠO bàn mới trong cùng transaction + ghi audit log; khách KHÔNG BAO GIỜ bị chặn vì hết bàn (M2.D-05)"
    - "Bàn được chọn bị khoá bằng `FOR UPDATE` nên 2 admin duyệt song song không cấp trùng bàn (M2.D-06)"
    - "Món trong đơn vào `orders`/`order_items` với `state='KITCHEN'` — bếp thấy ngay; giá lấy từ `items_snapshot` (giá đã chốt lúc submit)"
    - "Món đã hết hàng lúc duyệt bị chặn hoặc bị admin tick bỏ; bỏ hết món thì KHÔNG cho xác nhận (M2.D-61)"
    - "Đơn `WAITING` không bao giờ có mặt trong `orders` — chỉ có sau khi CONFIRMED"
    - "Duyệt xong thì hàng outbox L2 (SMS) của đơn đó chuyển CANCELLED, SMS không bắn"
  artifacts:
    - path: "apps/api/src/modules/admin-online-orders/admin-online-orders.service.ts"
      provides: "confirm() — transaction cấp bàn + tạo Order + items KITCHEN + cập nhật request"
      contains: "FOR UPDATE"
      min_lines: 180
    - path: "apps/api/src/modules/admin-online-orders/admin-online-orders.module.ts"
      provides: "Module đăng ký service + forFeature 5 entity"
  key_links:
    - from: "apps/api/src/modules/admin-online-orders/admin-online-orders.service.ts"
      to: "restaurant_tables (SELECT ... ORDER BY code ASC LIMIT 1 FOR UPDATE)"
      via: "raw query trong ds.transaction"
      pattern: "FOR UPDATE"
    - from: "apps/api/src/modules/admin-online-orders/admin-online-orders.service.ts"
      to: "runWithRetry"
      via: "import từ common/run-with-retry.js (M2.D-06)"
      pattern: "runWithRetry"
    - from: "apps/api/src/modules/admin-online-orders/admin-online-orders.service.ts"
      to: "NotificationOutboxService.cancelPendingForRequest"
      via: "gọi trong cùng transaction confirm"
      pattern: "cancelPendingForRequest"
---

<objective>
Viết `AdminOnlineOrdersService.confirm()` — thao tác phức tạp nhất của phase: trong **một**
transaction, khoá và cấp bàn trống đầu tiên đúng `kind`, tự tạo bàn khi hết, dựng `orders` +
`order_items` ở trạng thái `KITCHEN`, re-check tồn kho, ghi `ship_fee`, cập nhật request, và huỷ
hàng outbox SMS.

Purpose: Đây là criterion 2 của ROADMAP và là ranh giới M2.D-01 (đơn `WAITING` không được lẫn vào
`orders` — có 48 điểm query doanh thu/history/sơ đồ bàn/bếp phụ thuộc điều này). Sai ở đây thì hoặc
2 khách chung 1 bàn, hoặc khách bị chặn vì hết bàn, hoặc doanh thu nhiễm.

**Quyết định kỹ thuật đã chốt tại plan này (lệch pseudo-code spec §7 dòng 480, có lý do):**
Spec ghi `order = getOrCreateOpenOrder(bàn.id, creator)`. **KHÔNG dùng được** vì
`OrdersService.getOrCreateOpenOrderImpl` mở `this.ds.transaction(...)` **riêng** (connection khác);
gọi từ trong transaction của ta thì (a) nó không nhìn thấy bàn ta vừa INSERT chưa commit → ném
`NOT_FOUND: Bàn không tồn tại`, và (b) mở đường deadlock 2 connection. Thay bằng tạo `Order` trực
tiếp trên `mgr` của transaction hiện tại — an toàn vì bàn vừa được khoá `FOR UPDATE` và câu chọn bàn
đã loại mọi bàn còn đơn mở. Đây là lệch **pseudo-code minh hoạ**, không lệch quyết định LOCKED nào
(M2.D-02/04/05/06 vẫn được thoả) — ghi thành comment tại chỗ và nêu trong SUMMARY.

Output: 1 service + 1 module + 2 mã lỗi mới. Controller/SSE ở plan 09-07; bằng chứng row lock ở 09-08.
</objective>

<execution_context>
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-CONTEXT.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-RESEARCH.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-PATTERNS.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-VALIDATION.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-03-SUMMARY.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-04-SUMMARY.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-05-SUMMARY.md

<interfaces>
<!-- Trích sẵn. KHÔNG đi khám phá lại. -->

@order/schemas (plan 09-01):
  ConfirmOnlineOrderBody = { ship_fee?: int 0..1_000_000; drop_menu_item_ids?: string[] }
  RejectOnlineOrderBody = { reason_code: RejectReasonCode; reason_other_text?: string; internal_note?: string }
  REJECT_REASON_TEXT: Record<RejectReasonCode, string>
  AdminOnlineOrderRow / AdminOnlineOrderList / AdminOnlineOrderItem

apps/api/src/modules/tables/table-kind.ts (plan 09-03):
  kindForFulfillment('PICKUP') = 'takeaway' · kindForFulfillment('DELIVERY') = 'delivery'  (M2.D-14)
  formatTableCode(kind, num) · formatTableName(kind, num)
apps/api/src/modules/admin-online-orders/table-assign.ts (plan 09-03):
  pickFreeTable(candidates) · nextTableCode(kind, existingCodes)
apps/api/src/common/run-with-retry.ts (plan 09-03):
  runWithRetry(fn, maxAttempts, { onRetry?, sleepFn? })

apps/api/src/modules/notifications/notification-outbox.service.ts (plan 09-05):
  cancelPendingForRequest(requestId, mgr?) → Promise<number>
  (module `NotificationsModule` đã `exports: [NotificationOutboxService]`)

Entity đã có (plan 09-04):
  Order: + source, fulfillment_type, online_request_id, order_token, customer_lat/lng (string|null),
    customer_map_link, distance_km (string|null), ship_fee (int, default 0), payment_method
  OnlineOrderRequest: + internal_reject_note; đã có reviewed_at / reviewed_by_user_id /
    reviewed_by_full_name / reject_reason / order_id / max_progress_shown / items_snapshot
  OrderItem: { order_id, menu_item_id|null, menu_item_name, menu_item_price (int), is_note,
    qty, state (default 'PENDING'), is_priority, note, created_by_user_id, created_by_full_name, ... }
  RestaurantTable: { id, code varchar(16) UNIQUE, name, kind, x, y, is_active, kiotviet_locked }
  OrderActivityLog: { order_id, item_id|null, table_id|null, table_code, order_opened_at,
    event_kind, message, actor_id, actor_name, created_at }  — immutable, chỉ insert

apps/api/src/modules/orders/orders.service.ts dòng 112-134 — khuôn `writeActivity` (chép cách dựng
  row `OrderActivityLog`, KHÔNG gọi method private đó từ ngoài).
apps/api/src/modules/orders/orders.service.ts dòng 279-287 — khuôn xử lý lỗi: re-throw
  `NotFoundException`/`BadRequestException`/`ConflictException` nguyên vẹn, log + rethrow phần còn lại.

apps/api/src/modules/public/public-orders.service.ts — khuôn raw query có `FOR UPDATE` trong
  `ds.transaction` (phase 8, `hasOpenOrderForPhoneLocked`).

packages/schemas/src/errors.ts — `ErrorCode` là `z.enum` ĐÓNG (34 giá trị). Mã lỗi mới phải thêm
  vào đây, nếu không `ErrorEnvelope.parse` ở FE sẽ fail.
  ⚠ Sau khi sửa: `cd packages/schemas && ../../node_modules/.bin/tsc`

docs/MILESTONE-02-ONLINE-ORDERING-SPEC.md dòng 472-505 — pseudo-code cấp bàn + M2.D-61 + M2.D-62.
M2.D-42: giá trong `items_snapshot` là giá ĐÃ CHỐT lúc submit — khi tạo `order_items` phải dùng
  `unit_price` của snapshot, **không** query lại giá menu hiện tại.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 2 mã lỗi mới + khung service + confirmImpl phần cấp bàn (FOR UPDATE + tự tạo bàn)</name>
  <files>packages/schemas/src/errors.ts, apps/api/src/modules/admin-online-orders/admin-online-orders.service.ts</files>
  <read_first>
    - packages/schemas/src/errors.ts (toàn bộ — enum đóng)
    - apps/api/src/modules/public/public-orders.service.ts (khuôn `ds.transaction` + raw `FOR UPDATE`)
    - apps/api/src/modules/orders/orders.service.ts dòng 175-290 (khuôn retry + xử lý lỗi + tạo Order)
    - apps/api/src/modules/admin-online-orders/table-assign.ts và apps/api/src/modules/tables/table-kind.ts (plan 09-03)
    - docs/MILESTONE-02-ONLINE-ORDERING-SPEC.md dòng 472-487
  </read_first>
  <action>
    **`packages/schemas/src/errors.ts`** — thêm vào `ErrorCode` enum, nhóm comment "M2 phase 09":
    - `'ORDER_EMPTY_AFTER_DROP'` — bỏ hết món khi duyệt (M2.D-61: buộc dùng Từ chối)
    - `'ROLE_FORBIDDEN'` — code mà `RequireRoles` đã ném từ Milestone 1 nhưng chưa từng có trong enum;
      plan 09-07 dùng guard này nên bổ sung luôn cho `ErrorEnvelope` parse được ở FE.
    Rồi `cd packages/schemas && ../../node_modules/.bin/tsc`.

    **`admin-online-orders.service.ts`** — `@Injectable()`, inject
    `@InjectDataSource() private readonly ds: DataSource`, `NotificationOutboxService`, `EventEmitter2`.
    Docblock đầu file phải ghi 3 điều: (1) đây là nơi duy nhất đơn online chuyển từ bảng staging
    `online_order_requests` sang `orders` (ranh giới M2.D-01); (2) lệch pseudo-code spec dòng 480 —
    KHÔNG gọi `OrdersService.getOrCreateOpenOrder` vì nó mở transaction riêng (xem `<objective>`);
    (3) D-02 — cả 3 role duyệt được, nên `actor` phải được ghi vào `reviewed_by_*` + audit, đó là
    kiểm soát bù trừ thay cho lớp chặn role cũ.

    Public API của service ở task này:
    ```
    confirm(requestId: string, actor: { id: string; full_name: string }, body: ConfirmOnlineOrderBody)
      : Promise<{ order_id: string; table_code: string; table_created: boolean; dropped_count: number }>
    ```
    cài bằng `return runWithRetry(() => this.confirmImpl(requestId, actor, body), 2, { onRetry: ... })`
    (M2.D-06).

    **`confirmImpl` — bước 1..3 ở task này**, toàn bộ trong `this.ds.transaction(async (mgr) => {...})`:

    1. **Khoá request**: raw
       `SELECT * FROM online_order_requests WHERE id = ? FOR UPDATE` (qua `mgr.query`).
       Không có → `NotFoundException({ code: 'NOT_FOUND', message: 'Không tìm thấy đơn này.' })`.
       `status !== 'WAITING'` → `ConflictException({ code: 'ORDER_ALREADY_CONFIRMED',
       message: 'Đơn này đã được xử lý rồi.' })`. Khoá ở đây chặn 2 admin cùng duyệt **một** đơn.

    2. **Re-check tồn kho + áp `drop_menu_item_ids`** (M2.D-61):
       - `keptItems` = `items_snapshot` bỏ những dòng có `menu_item_id ∈ body.drop_menu_item_ids`.
       - `keptItems.length === 0` → `ConflictException({ code: 'ORDER_EMPTY_AFTER_DROP',
         message: 'Đơn không còn món nào. Hãy dùng nút Từ chối thay vì Xác nhận.' })`.
       - Query `mgr.getRepository(MenuItem).find({ where: { id: In(keptIds) } })`; món **không tìm
         thấy / `is_active = false` / `is_out_of_stock = true`** → `ConflictException({
         code: 'MENU_ITEM_UNAVAILABLE', message: 'Còn N món đã hết hàng: <tên>. Tick bỏ các món đó
         rồi xác nhận lại.' })`. Không tự ý bỏ giúp — admin phải quyết (M2.D-61).
       - `dropped_count = items_snapshot.length - keptItems.length`.

    3. **Cấp bàn** (M2.D-04/05/06/14):
       - `kind = kindForFulfillment(request.fulfillment_type)`.
       - Raw query trong `mgr`:
         ```sql
         SELECT t.id, t.code, t.name FROM restaurant_tables t
         WHERE t.kind = ? AND t.is_active = 1 AND t.kiotviet_locked = 0
           AND t.id NOT IN (SELECT o.table_id FROM orders o WHERE o.closed_at IS NULL)
         ORDER BY t.code ASC LIMIT 1 FOR UPDATE
         ```
         Ghi comment: `FOR UPDATE` ở đây là **record lock** trên hàng bàn tìm được (khác gap lock của
         phase 8 ở `online_order_requests`) — transaction thứ hai chọn cùng bàn sẽ bị chặn tới khi
         transaction đầu commit, rồi câu `NOT IN (open orders)` của nó loại bàn đó ra.
       - Có kết quả → dùng `pickFreeTable([...])` để lấy hàng đầu (lưới an toàn thứ hai cho quy tắc
         `code` ASC).
       - **Không có bàn nào → TỰ TẠO** (M2.D-05, khách không bao giờ bị chặn):
         `SELECT code FROM restaurant_tables WHERE kind = ?` → `nextTableCode(kind, codes)` →
         `INSERT` bàn mới `{ code, name: formatTableName(...), kind, x: 0, y: 0, is_active: true,
         kiotviet_locked: false }`. Đặt cờ `table_created = true`.
         Nếu INSERT vấp `Duplicate entry` (2 transaction cùng sinh 1 code) → **để lỗi ném ra**:
         `runWithRetry` không retry lỗi này (không transient) nên phải bắt riêng và ném
         `ConflictException({ code: 'CONFLICT', message: 'Có admin khác vừa tạo bàn cùng lúc, bấm Xác nhận lại.' })`
         — thông điệp phải nói rõ hành động tiếp theo, không để admin đứng nhìn lỗi lạ.
       - **KHÔNG BAO GIỜ** ném `NO_TABLE_AVAILABLE` từ luồng này — mã đó tồn tại trong enum từ phase 8
         nhưng M2.D-05 đã loại bỏ khả năng "hết bàn". Ghi comment nói rõ.
       - Sau commit (bên ngoài transaction), nếu `table_created` → emit
         `this.emitter.emit('audit.write', { actor_id, actor_name, ip: 'system', ts_ms, action_kind:
         'online_order.table_autocreated', target_kind: 'table', target_id: <table id>,
         after_json: { code, kind, reason: 'Hết bàn trống khi duyệt đơn online', request_id } })`
         — dùng đúng khuôn `AuditEvent` của `audit.service.ts`, handler `AuditEventHandler` đã có sẵn.
  </action>
  <verify>
    <automated>cd packages/schemas && ../../node_modules/.bin/tsc && cd ../../apps/api && ../../node_modules/.bin/tsc --noEmit && ./node_modules/.bin/vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ORDER_EMPTY_AFTER_DROP" packages/schemas/src/errors.ts` = 1 và `grep -c "ROLE_FORBIDDEN" packages/schemas/src/errors.ts` = 1
    - `ls packages/schemas/dist/errors.js` mới hơn file `src/errors.ts` (đã build lại)
    - `grep -c "FOR UPDATE" apps/api/src/modules/admin-online-orders/admin-online-orders.service.ts` = 2 (khoá request + khoá bàn)
    - `grep -c "ORDER BY t.code ASC LIMIT 1" apps/api/src/modules/admin-online-orders/admin-online-orders.service.ts` = 1
    - `grep -c "runWithRetry" apps/api/src/modules/admin-online-orders/admin-online-orders.service.ts` = 1
    - `grep -c "getOrCreateOpenOrder" apps/api/src/modules/admin-online-orders/admin-online-orders.service.ts` = 0
    - `grep -c "NO_TABLE_AVAILABLE" apps/api/src/modules/admin-online-orders/admin-online-orders.service.ts` = 0
    - `grep -c "online_order.table_autocreated" apps/api/src/modules/admin-online-orders/admin-online-orders.service.ts` = 1
    - `cd apps/api && ../../node_modules/.bin/tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Phần khó nhất của cấp bàn (khoá, chọn theo code ASC, tự tạo khi hết) đã có, biên dịch sạch, có audit khi tự tạo bàn.</done>
</task>

<task type="auto">
  <name>Task 2: confirmImpl phần tạo Order + items KITCHEN + cập nhật request + huỷ outbox; reject(); module</name>
  <files>apps/api/src/modules/admin-online-orders/admin-online-orders.service.ts, apps/api/src/modules/admin-online-orders/admin-online-orders.module.ts</files>
  <read_first>
    - apps/api/src/modules/orders/orders.service.ts dòng 112-134 (dựng row `OrderActivityLog`)
    - apps/api/src/modules/orders/orders.service.ts dòng 371-465 (`addItemsBulk` — cách dựng `OrderItem` từ menu, tên field snapshot)
    - apps/api/src/modules/orders/entities/order-item.entity.ts
    - apps/api/src/modules/orders/orders.module.ts (khuôn module + forFeature)
    - apps/api/src/modules/notifications/notifications.module.ts (plan 09-05 — export `NotificationOutboxService`)
    - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-CONTEXT.md D-08, D-09
  </read_first>
  <action>
    **Tiếp `confirmImpl`, bước 4..7 (vẫn trong cùng transaction):**

    4. **Tạo `Order` trực tiếp trên `mgr`** (không gọi `OrdersService` — xem `<objective>`):
       `{ table_id: table.id, table_code: table.code, first_kitchen_at: nowMs, closed_at: null,
       is_paid: false, source: 'ONLINE', fulfillment_type: request.fulfillment_type,
       online_request_id: request.id, order_token: request.order_token,
       customer_name: request.customer_name, customer_phone: request.customer_phone,
       customer_address: request.customer_address, customer_lat/lng, customer_map_link,
       distance_km: request.distance_km, ship_fee: body.ship_fee ?? 0, payment_method: 'CASH',
       created_by_user_id: actor.id, created_by_full_name: actor.full_name }`.
       `first_kitchen_at` đặt luôn vì items vào thẳng `KITCHEN` (khớp ngữ nghĩa cột: "lần đầu báo bếp").

    5. **Tạo `order_items` từ `keptItems`**, mỗi dòng:
       `{ order_id, menu_item_id: it.menu_item_id, menu_item_name: it.name,
       menu_item_price: it.unit_price, is_note: false, qty: it.qty, state: 'KITCHEN',
       is_priority: false, note: it.note ?? null, created_by_user_id: actor.id,
       created_by_full_name: actor.full_name }`.
       ⚠ `menu_item_price` lấy từ **snapshot**, KHÔNG query lại giá menu (M2.D-42 — giá đã chốt với
       khách lúc submit; giá menu có thể đã đổi giữa chừng). Ghi comment tại chỗ.
       `state: 'KITCHEN'` ngay từ đầu → bếp thấy ngay, không cần bấm "báo bếp" (spec §7 dòng 485).

    6. **Ghi `OrderActivityLog`** (1 dòng, chép khuôn `writeActivity`):
       `event_kind: 'order_created'`, `message: 'Duyệt đơn online — cấp bàn {code}' + (table_created
       ? ' (bàn tự tạo)' : '')`, `actor_id`/`actor_name` = actor, `table_code`, `order_opened_at`.
       Bọc try/catch riêng — log hoạt động fail KHÔNG được làm rollback cả giao dịch duyệt đơn.

    7. **Cập nhật request + huỷ outbox**:
       `status = 'CONFIRMED'`, `order_id`, `reviewed_at = nowMs`, `reviewed_by_user_id`,
       `reviewed_by_full_name`. Rồi `await this.outbox.cancelPendingForRequest(request.id, mgr)` —
       gọi **trong cùng transaction** để không có khe hở "đã CONFIRMED nhưng SMS vẫn bắn".

    Sau commit: emit `this.emitter.emit('online_order.reviewed', { request_id, at_ms })` (SSE dùng ở
    plan 09-07) và emit audit `online_order.table_autocreated` nếu có.

    **`reject(requestId, actor, body: RejectOnlineOrderBody)`** — không cần transaction phức tạp,
    nhưng vẫn dùng `ds.transaction` + `SELECT ... FOR UPDATE` trên request để 2 admin không cùng từ chối:
    - `status !== 'WAITING'` → `ORDER_ALREADY_CONFIRMED`.
    - `reject_reason` = `body.reason_code === 'OTHER' ? body.reason_other_text!.trim()
      : REJECT_REASON_TEXT[body.reason_code]` — đây là câu **khách đọc được nguyên văn** (D-08).
    - `internal_reject_note` = `body.internal_note?.trim() ?? null` — **CHỈ vào DB + audit** (D-09).
      Ghi comment cấm mọi mapper công khai đọc cột này.
    - `status='REJECTED'`, `reviewed_at`, `reviewed_by_*`.
    - `cancelPendingForRequest(id, mgr)`.
    - Sau commit: emit `online_order.reviewed` + emit `audit.write` với
      `action_kind: 'online_order.rejected'`, `after_json: { reason_code, reject_reason, internal_note }`
      — ghi chú nội bộ **được phép** nằm trong audit log (D-09 nói rõ: DB + audit log), chỉ cấm ra
      response công khai.
    - **KHÔNG** bắn SMS/thông báo gì cho khách (D-10) — khách chỉ biết qua `/o/:token`.

    **`list(status: 'WAITING')`** — trả `AdminOnlineOrderList`: đọc request theo `status`,
    `ORDER BY submitted_at ASC` (FIFO — 09-UI-SPEC Giả định #1), re-check tồn kho 1 lần cho toàn bộ
    món của tất cả đơn (1 query `In(allIds)`), map sang `AdminOnlineOrderRow`:
    `order_token_masked = order_token.slice(0,4).toUpperCase() + '…'`,
    `waiting_seconds = Math.floor((nowMs - submitted_at) / 1000)`,
    `out_of_stock_count`, và `escalate_sms_after_s` từ `SettingsService.readAll()`.
    ⚠ **Không** map `ip_hash`, `user_agent`, `internal_reject_note`, `order_token` đầy đủ.
    Kết thúc bằng `AdminOnlineOrderList.strict().parse(payload)` — lưới an toàn giống
    `public-menu.mapper.ts` của phase 8.

    **`admin-online-orders.module.ts`**: `imports: [TypeOrmModule.forFeature([OnlineOrderRequest,
    Order, OrderItem, OrderActivityLog, RestaurantTable, MenuItem]), NotificationsModule, SettingsModule]`,
    `providers: [AdminOnlineOrdersService]`, `exports: [AdminOnlineOrdersService]`.
    **Chưa** thêm vào `app.module.ts` ở plan này (controller ở 09-07 mới cần) — hoặc thêm luôn nếu
    module compile sạch; nếu thêm thì ghi vào SUMMARY để 09-07 không thêm lần hai.
  </action>
  <verify>
    <automated>cd apps/api && ../../node_modules/.bin/tsc --noEmit && ./node_modules/.bin/vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "state: 'KITCHEN'" apps/api/src/modules/admin-online-orders/admin-online-orders.service.ts` = 1
    - `grep -c "source: 'ONLINE'" apps/api/src/modules/admin-online-orders/admin-online-orders.service.ts` = 1
    - `grep -c "cancelPendingForRequest" apps/api/src/modules/admin-online-orders/admin-online-orders.service.ts` = 2 (confirm + reject)
    - `grep -c "internal_reject_note" apps/api/src/modules/admin-online-orders/admin-online-orders.service.ts` = 1 (chỉ trong `reject`)
    - `grep -c "AdminOnlineOrderList.strict().parse" apps/api/src/modules/admin-online-orders/admin-online-orders.service.ts` = 1
    - Đọc hàm `list()` và xác nhận **không** có `ip_hash`, `user_agent`, `internal_reject_note`, hay `order_token` đầy đủ trong object trả về
    - `grep -cE "menuRepo|MenuItem" apps/api/src/modules/admin-online-orders/admin-online-orders.service.ts` ≥ 1 (có re-check tồn kho) và trong bước tạo `order_items` KHÔNG đọc `menu.price` (đọc `it.unit_price` của snapshot)
    - `cd apps/api && ../../node_modules/.bin/tsc --noEmit` exit 0
    - `cd apps/api && ./node_modules/.bin/vitest run` full suite exit 0
  </acceptance_criteria>
  <done>`confirm()`/`reject()`/`list()` đủ hành vi criterion 2 + M2.D-61/D-62 + D-08/D-09; bằng chứng row lock và cách ly doanh thu do plan 09-08 cung cấp.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bảng staging `online_order_requests` → bảng thật `orders` | Ranh giới M2.D-01. 48 query doanh thu/history/sơ đồ bàn/bếp tin rằng chỉ đơn đã duyệt mới ở đây |
| 2 admin đồng thời → 1 bàn / 1 đơn | Ranh giới đồng thời (M2.D-06) |
| body request admin → `ship_fee`, `drop_menu_item_ids` | Dữ liệu ảnh hưởng tiền và nội dung đơn của khách |
| ghi chú nội bộ → DB + audit (được) / response (cấm) | D-09 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-26 | Tampering | 2 admin duyệt song song cấp **trùng bàn** → 2 khách chung 1 bàn (**HIGH**) | mitigate | `SELECT ... ORDER BY code ASC LIMIT 1 FOR UPDATE` trên `restaurant_tables` trong cùng transaction với INSERT `orders`; `runWithRetry` cho deadlock (M2.D-06). Bằng chứng 2-connection MySQL thật ở plan 09-08 |
| T-09-27 | Tampering | 2 admin duyệt **cùng 1 đơn** → tạo 2 `orders`, 2 bàn bị chiếm | mitigate | `SELECT ... FROM online_order_requests WHERE id = ? FOR UPDATE` là bước ĐẦU TIÊN của transaction; transaction thứ hai thấy `status='CONFIRMED'` → 409 `ORDER_ALREADY_CONFIRMED` |
| T-09-28 | Elevation of Privilege | Đơn `WAITING` lọt vào `orders` sớm → nhiễm doanh thu/bếp/sơ đồ bàn (vi phạm M2.D-01, core value của dự án) | mitigate | INSERT vào `orders` chỉ nằm trong nhánh confirm sau khi request đã khoá và kiểm `status='WAITING'`; test đếm doanh thu trước/sau 5 đơn WAITING ở plan 09-08 |
| T-09-29 | Tampering | `ship_fee` âm/khổng lồ hoặc client tự đặt giá món khi duyệt | mitigate | `ship_fee` đã bị `z.number().int().min(0).max(1_000_000)` chặn ở hợp đồng (09-01); `menu_item_price` lấy từ `items_snapshot` chứ không từ body — acceptance criteria kiểm bằng đọc code |
| T-09-30 | Denial of Service | Hết bàn → khách bị chặn (vi phạm M2.D-05 "khách không bao giờ bị chặn") | mitigate | Nhánh tự tạo bàn bắt buộc; `NO_TABLE_AVAILABLE` bị cấm xuất hiện (grep = 0); va chạm code bàn trả 409 có hướng dẫn bấm lại |
| T-09-31 | Information Disclosure | `list()` leak `ip_hash`/`user_agent`/`internal_reject_note`/`order_token` đầy đủ ra màn admin | mitigate | Whitelist tường minh + `AdminOnlineOrderList.strict().parse()`; schema (09-01) không có các field đó nên `.strict()` throw nếu ai spread entity |
| T-09-32 | Repudiation | 3 role đều duyệt được (D-02) nhưng không truy được AI đã duyệt — mất lớp bảo vệ mà M2.D-33 từng cung cấp | mitigate | `reviewed_by_user_id`/`reviewed_by_full_name` lấy từ `req.user` (đã qua `JwtAuthGuard`), KHÔNG từ body; thêm audit `online_order.rejected`/`online_order.table_autocreated`; plan 09-07 thêm `online_order.confirmed`; plan 09-08 assert dòng audit tồn tại |
| T-09-33 | Tampering | Món đã hết hàng vẫn được đẩy vào bếp | mitigate | Re-check tồn kho **trong transaction** (M2.D-61); món hết mà không tick bỏ → 409 kèm tên món; bỏ hết món → `ORDER_EMPTY_AFTER_DROP` buộc dùng Từ chối |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Plan này KHÔNG cài package nào |
</threat_model>

<verification>
1. `cd packages/schemas && ../../node_modules/.bin/tsc` — build lại dist
2. `cd apps/api && ../../node_modules/.bin/tsc --noEmit` — sạch
3. `cd apps/api && ./node_modules/.bin/vitest run` — full suite xanh (không hồi quy phase 8)
4. Đọc lại hàm `list()` xác nhận whitelist (không có 4 field cấm)
5. Bằng chứng hành vi DB (row lock, doanh thu, audit) do plan 09-08 cung cấp — plan này chưa đóng criterion 2
</verification>

<success_criteria>
- `confirm()` cấp bàn theo `code` ASC đúng kind, tự tạo bàn khi hết + audit, dùng `FOR UPDATE` + `runWithRetry`
- Items vào `orders`/`order_items` với `state='KITCHEN'`, giá lấy từ snapshot
- `reject()` lưu câu soạn sẵn vào `reject_reason` và ghi chú tự do vào `internal_reject_note` + audit
- Cả 2 nhánh huỷ outbox PENDING trong cùng transaction
- `list()` whitelist qua `.strict().parse()`, không leak 4 field nội bộ
- Typecheck sạch, full suite xanh
</success_criteria>

<output>
Create `.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-06-SUMMARY.md` when done
</output>
