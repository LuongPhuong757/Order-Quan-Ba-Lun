---
phase: 09-duy-t-n-th-ng-b-o-theo-d-i-n
plan: 08
type: execute
wave: 5
depends_on: ["09-06", "09-07"]
files_modified:
  - apps/api/src/modules/admin-online-orders/admin-online-orders.integration.test.ts
  - apps/api/src/modules/orders/orders.service.ts
  - apps/web/src/pages/HistoryPage.tsx
autonomous: true
requirements: [REQ-M]

must_haves:
  truths:
    - "2 transaction MySQL THẬT tranh cùng 1 bàn: transaction thứ hai BỊ CHẶN cho tới khi cái đầu commit, và không cấp trùng bàn (M2.D-06)"
    - "5 đơn `WAITING` không làm đổi doanh thu / số đơn / sơ đồ bàn / hàng bếp — chứng minh bằng đếm trước và sau (M2.D-01)"
    - "Duyệt đơn để lại đúng 1 dòng audit có tên người duyệt (kiểm soát bù trừ của D-02)"
    - "`ship_fee` KHÔNG cộng vào doanh thu món; báo cáo hiện thành dòng riêng (M2.D-62)"
  artifacts:
    - path: "apps/api/src/modules/admin-online-orders/admin-online-orders.integration.test.ts"
      provides: "Bằng chứng duy nhất cho criterion 2 và 3 — mock không chứng minh được hành vi lock của InnoDB"
      contains: "FOR UPDATE"
      min_lines: 200
    - path: "apps/api/src/modules/orders/orders.service.ts"
      provides: "stats() trả thêm ship_fee_total tách khỏi doanh thu món"
      contains: "ship_fee_total"
  key_links:
    - from: "apps/api/src/modules/admin-online-orders/admin-online-orders.integration.test.ts"
      to: "MySQL thật qua 2 QueryRunner"
      via: "dataSourceOptions + synchronize:false"
      pattern: "synchronize: false"
    - from: "apps/web/src/pages/HistoryPage.tsx"
      to: "stats.ship_fee_total"
      via: "StatTile mới"
      pattern: "ship_fee_total"
---

<objective>
Cung cấp **bằng chứng** cho criterion 2 và 3 của ROADMAP bằng MySQL thật, và đóng nốt phần báo cáo
của M2.D-62 (`ship_fee` tách khỏi doanh thu món).

Purpose: Hai tiêu chí này mock không chứng minh được (C-TEST-01, D-21): hành vi record lock của
InnoDB và việc 48 điểm query doanh thu không nhìn thấy bảng staging là **thuộc tính của DB**, không
phải của code Node. Phase 8 đã có tiền lệ chạy 2 connection MySQL thật
(`open-order-lock.integration.test.ts`) — dùng lại đúng khuôn đó.

Output: 1 file integration test 3 nhóm `describe`, 1 trường thống kê mới, 1 ô hiển thị trên báo cáo.
</objective>

<execution_context>
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-CONTEXT.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-RESEARCH.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-PATTERNS.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-VALIDATION.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-06-SUMMARY.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-07-SUMMARY.md

<interfaces>
<!-- Trích sẵn. KHÔNG đi khám phá lại. -->

apps/api/src/modules/public/open-order-lock.integration.test.ts (223 dòng) — KHUÔN MẪU CHÉP GẦN NHƯ NGUYÊN VĂN:
```typescript
import 'dotenv/config';                 // BẮT BUỘC — data-source.ts đọc process.env.MYSQL_PORT thô
import { randomBytes } from 'node:crypto';
import { afterAll, beforeAll, beforeEach, describe, expect, it } from 'vitest';
import { DataSource } from 'typeorm';
import { dataSourceOptions } from '../../data-source.js';

let ds: DataSource;
const sleep = (ms: number) => new Promise<void>((r) => setTimeout(r, ms));

beforeAll(async () => {
  ds = new DataSource({ ...dataSourceOptions, synchronize: false });
  try { await ds.initialize(); }
  catch (err) { throw new Error('Không kết nối được MySQL local — bật MySQL trước khi chạy test này. Lỗi gốc: ' + String(err)); }
}, 20_000);
afterAll(async () => { await cleanupSentinelRows(); await ds.destroy(); }, 20_000);
beforeEach(async () => { await cleanupSentinelRows(); });
```
Khuôn chứng minh "bị chặn thật, không phải chỉ chậm" (dòng 121-153):
```typescript
let bResolved = false;
const bPromise = runnerB.query(`SELECT ... FOR UPDATE`, [param]).then((rows) => { bResolved = true; return rows; });
const raceResult = await Promise.race([bPromise, sleep(500).then(() => 'TIMEOUT' as const)]);
expect(raceResult).toBe('TIMEOUT');
expect(bResolved).toBe(false);
await runnerA.commitTransaction();
const bRows = await bPromise;
await runnerB.commitTransaction();
```
Quy ước dọn dữ liệu: dùng **sentinel** cố định (phase 8 dùng SĐT `0900000001`, xoá bằng
`DELETE ... WHERE customer_phone LIKE '09000000%'` ở cả `beforeEach` lẫn `afterAll`).

apps/api/src/modules/admin-online-orders/admin-online-orders.service.ts (plan 09-06) — câu SQL cần
  chứng minh:
```sql
SELECT t.id, t.code, t.name FROM restaurant_tables t
WHERE t.kind = ? AND t.is_active = 1 AND t.kiotviet_locked = 0
  AND t.id NOT IN (SELECT o.table_id FROM orders o WHERE o.closed_at IS NULL)
ORDER BY t.code ASC LIMIT 1 FOR UPDATE
```

apps/api/src/modules/orders/orders.service.ts:
  - `PAID_SQL = 'o.closed_at IS NOT NULL AND o.is_paid = 1'` (dòng ~77)
  - `stats(opts)` (dòng 1066+) trả `{ revenue_by_day, top_items, revenue_by_cashier, by_hour,
    paid_count, unpaid_count, cancelled_count, paid_revenue }`. Doanh thu tính bằng
    `SUM(CASE WHEN i.state='SERVED' THEN i.menu_item_price*i.qty ELSE 0 END)` từ `order_items`
    → **`ship_fee` đã tự động không nằm trong doanh thu món**, chỉ thiếu dòng tổng riêng.
  - `applyFilters(qb)` — helper dùng chung cho mọi truy vấn thống kê, tái dùng cho truy vấn ship_fee.

apps/web/src/pages/HistoryPage.tsx:
  - `type Stats` khai ở đầu file (khớp shape BE) — phải thêm field mới ở đây trước
  - lưới `StatTile` dòng ~410-418: `<StatTile label=... value=... color=... bg=... border=... />`
  - `fmt(...)` là hàm format tiền đã có trong file

Lệnh MySQL: `mysql -h127.0.0.1 -P3307 -uorder_app -porder_app_pass order_quan_balun`
Lệnh test: `cd apps/api && ./node_modules/.bin/vitest run src/modules/admin-online-orders/admin-online-orders.integration.test.ts`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration test — row lock cấp bàn với 2 connection MySQL thật (criterion 2)</name>
  <files>apps/api/src/modules/admin-online-orders/admin-online-orders.integration.test.ts</files>
  <read_first>
    - apps/api/src/modules/public/open-order-lock.integration.test.ts (TOÀN BỘ 223 dòng — chép cấu trúc)
    - apps/api/src/modules/admin-online-orders/admin-online-orders.service.ts (câu SQL chọn bàn cần chứng minh)
    - apps/api/src/modules/tables/entities/restaurant-table.entity.ts
    - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-CONTEXT.md D-21
  </read_first>
  <action>
    Tạo `admin-online-orders.integration.test.ts` theo đúng khuôn `open-order-lock.integration.test.ts`:
    `DataSource` riêng với `synchronize: false`, `beforeAll` fail RÕ RÀNG nếu không có MySQL
    (**KHÔNG** `it.skip` im lặng — test bị skip âm thầm chính là cách criterion 2 mất bằng chứng).

    **Sentinel dùng chung cho cả 3 describe:** bàn `code LIKE 'ship-9%'` (dải 90-99 dành cho test),
    SĐT `09000000%`. Hàm `cleanupSentinelRows()` xoá theo thứ tự FK-an-toàn: `order_items` của các
    order sentinel → `orders` sentinel → `online_order_requests` sentinel → `restaurant_tables`
    sentinel → `notification_outbox` của request sentinel. Gọi ở `beforeEach` và `afterAll`.

    **`describe('Cấp bàn — row lock chống cấp trùng (M2.D-06)')`:**
    - Setup: xoá sạch, INSERT đúng **1** bàn trống `ship-90` (kind `delivery`, `is_active=1`,
      `kiotviet_locked=0`) và bảo đảm nó không có đơn mở.
    - Test 1 — **bị chặn thật**: runnerA và runnerB là 2 `QueryRunner` riêng, cùng `startTransaction()`.
      A chạy câu chọn bàn (chép nguyên văn từ service, thêm `AND t.code LIKE 'ship-9%'` để không
      đụng dữ liệu thật) → nhận `ship-90`. B chạy **cùng câu** → dùng khuôn `Promise.race` với
      `sleep(500)` để assert `'TIMEOUT'` và `bResolved === false`. A INSERT 1 `orders` mở trên bàn đó
      rồi `commitTransaction()`. B mới chạy tiếp → **trả 0 hàng** (bàn đã bị loại bởi
      `NOT IN (open orders)`) → đúng nghĩa "B phải tự tạo bàn mới thay vì cấp trùng". Commit B.
      Assert cuối: `SELECT COUNT(*) FROM orders WHERE table_id = <ship-90>.id AND closed_at IS NULL` = **1**.
    - Test 2 — **không chặn oan bàn khác kind**: A giữ lock trên `ship-90` (kind delivery); B chạy câu
      chọn bàn với `kind='takeaway'` (đã INSERT sẵn `mang-ve-90`) → B **không** bị chặn, resolve < 500ms.
    - Test 3 — **thứ tự `code` ASC**: INSERT `ship-92`, `ship-90`, `ship-91` (cố ý sai thứ tự chèn) →
      câu chọn bàn trả về `ship-90`.
    - Test 4 — **bàn bị `kiotviet_locked` hoặc `is_active=0` không được cấp**: INSERT `ship-93`
      (`kiotviet_locked=1`) và `ship-94` (`is_active=0`) khi không còn bàn nào khác → câu chọn bàn
      trả 0 hàng (buộc nhánh tự tạo bàn).
    - `testTimeout` 15s cho các test có chờ lock; **không** dùng watch mode.

    Comment đầu file: "File này là **bằng chứng duy nhất** cho criterion 2 của ROADMAP phase 9
    (2 admin duyệt song song không cấp trùng bàn) và cho phần DB của criterion 3. Mock không chứng
    minh được hành vi record lock của InnoDB."
  </action>
  <verify>
    <automated>cd apps/api && ./node_modules/.bin/vitest run src/modules/admin-online-orders/admin-online-orders.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "FOR UPDATE" apps/api/src/modules/admin-online-orders/admin-online-orders.integration.test.ts` ≥ 4
    - `grep -cE "it\.skip|describe\.skip|\.todo" apps/api/src/modules/admin-online-orders/admin-online-orders.integration.test.ts` = 0
    - `grep -c "synchronize: false" apps/api/src/modules/admin-online-orders/admin-online-orders.integration.test.ts` = 1
    - `grep -cE "@nestjs/testing|supertest" apps/api/src/modules/admin-online-orders/admin-online-orders.integration.test.ts` = 0
    - `cd apps/api && ./node_modules/.bin/vitest run src/modules/admin-online-orders/admin-online-orders.integration.test.ts` exit 0 với ≥ 4 test pass
    - Sau khi chạy: `mysql -h127.0.0.1 -P3307 -uorder_app -porder_app_pass order_quan_balun -e "SELECT COUNT(*) FROM restaurant_tables WHERE code LIKE 'ship-9%' OR code LIKE 'mang-ve-9%';"` = 0 (dọn sạch sentinel)
  </acceptance_criteria>
  <done>Criterion 2 có bằng chứng chạy được bằng 1 lệnh: transaction thứ hai bị chặn thật, không chặn oan, đúng thứ tự `code` ASC.</done>
</task>

<task type="auto">
  <name>Task 2: Integration test — đơn WAITING không nhiễm doanh thu/bếp/sơ đồ bàn + audit ghi đúng actor (criterion 3, D-02)</name>
  <files>apps/api/src/modules/admin-online-orders/admin-online-orders.integration.test.ts</files>
  <read_first>
    - apps/api/src/modules/orders/orders.service.ts dòng 60-80 (`PAID_SQL`, `HAS_ANY_ITEM_SQL`) và dòng 1066-1140 (`stats`)
    - apps/api/src/modules/public/entities/online-order-request.entity.ts (cột bắt buộc khi INSERT tay)
    - apps/api/src/modules/audit/entities/audit-log.entity.ts
    - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-CONTEXT.md D-02 (audit là kiểm soát bù trừ)
  </read_first>
  <action>
    Thêm 2 `describe` nữa vào cùng file:

    **`describe('Đơn WAITING không lẫn vào orders/doanh thu (M2.D-01)')`:**
    - Đếm TRƯỚC: `beforeCounts = { orders: SELECT COUNT(*) FROM orders,
      openOrders: COUNT(*) WHERE closed_at IS NULL,
      revenue: SELECT COALESCE(SUM(i.menu_item_price*i.qty),0) FROM order_items i JOIN orders o ON o.id=i.order_id WHERE o.closed_at IS NOT NULL AND o.is_paid=1 AND i.state='SERVED',
      kitchenItems: COUNT(*) FROM order_items WHERE state IN ('KITCHEN','COOKING','READY') }`
      — dùng đúng điều kiện `PAID_SQL` của `orders.service.ts` để phép đếm khớp với truy vấn thật.
    - INSERT **5** dòng `online_order_requests` `status='WAITING'` với SĐT sentinel `0900000001..5`,
      `items_snapshot` có giá thật (vd 3 món × 50000), `subtotal` khác 0.
    - Đếm SAU và assert **cả 4 con số KHÔNG ĐỔI**. Đây chính là "test đếm doanh thu trước và sau khi
      có 5 đơn WAITING" mà ROADMAP criterion 3 yêu cầu.
    - Assert bổ sung: `SELECT COUNT(*) FROM orders WHERE online_request_id IN (<5 id>)` = 0 và
      `SELECT COUNT(*) FROM orders WHERE source='ONLINE' AND online_request_id IS NULL` = 0.

    **`describe('Duyệt đơn để lại vết audit đúng người (D-02)')`:**
    - Vì `AdminOnlineOrdersService` cần Nest DI, test này **không** gọi service; thay vào đó nó kiểm
      **hợp đồng dữ liệu** mà plan 09-07 tạo ra khi chạy thật:
      · INSERT 1 request sentinel WAITING, gọi endpoint thật bằng `fetch` tới
        `http://localhost:3001/admin/online-orders/<id>/confirm` với cookie admin lấy từ biến môi
        trường `TEST_ADMIN_COOKIE`. **Nếu biến này không được đặt → `throw new Error(...)` với hướng
        dẫn cách lấy cookie, KHÔNG skip im lặng.**
      · Assert: response 200; `SELECT status, reviewed_by_full_name, order_id FROM online_order_requests
        WHERE id=?` → `CONFIRMED`, tên khớp, `order_id` khác null;
        `SELECT action_kind, actor_name, target_id FROM audit_log WHERE target_id=? ORDER BY ts_ms DESC LIMIT 1`
        → `online_order.confirmed` + `actor_name` khớp;
        `SELECT status FROM notification_outbox WHERE request_id=? AND level='L2'` → `CANCELLED`
        (duyệt kịp trước 90s thì SMS bị huỷ — criterion 4).
      · Dọn sạch bàn/đơn/items sinh ra.
    - Nếu việc chạy server trong test là quá nặng cho khuôn hiện tại, **được phép** chuyển 4 assert
      này thành phần "chạy thật có ghi output" trong acceptance criteria thay vì code test — nhưng
      phải ghi rõ đã chọn hướng nào trong SUMMARY và output thật phải được dán vào SUMMARY.
      **Không được bỏ hẳn.**
  </action>
  <verify>
    <automated>cd apps/api && ./node_modules/.bin/vitest run src/modules/admin-online-orders/admin-online-orders.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "is_paid = 1" apps/api/src/modules/admin-online-orders/admin-online-orders.integration.test.ts` ≥ 1 (dùng đúng định nghĩa doanh thu của repo)
    - `grep -c "WAITING" apps/api/src/modules/admin-online-orders/admin-online-orders.integration.test.ts` ≥ 3
    - `grep -c "audit_log" apps/api/src/modules/admin-online-orders/admin-online-orders.integration.test.ts` ≥ 1
    - `cd apps/api && ./node_modules/.bin/vitest run src/modules/admin-online-orders/admin-online-orders.integration.test.ts` exit 0 với ≥ 8 test pass
    - Sau khi chạy: `mysql ... -e "SELECT COUNT(*) FROM online_order_requests WHERE customer_phone LIKE '09000000%';"` = 0
    - `cd apps/api && ./node_modules/.bin/vitest run` full suite exit 0
  </acceptance_criteria>
  <done>Criterion 3 có bằng chứng đếm-trước-đếm-sau trên DB thật; kiểm soát bù trừ của D-02 (audit ghi rõ ai duyệt) được khẳng định bằng truy vấn, không phải bằng lời.</done>
</task>

<task type="auto">
  <name>Task 3: `ship_fee` tách khỏi doanh thu món + hiện thành dòng riêng trong báo cáo (M2.D-62)</name>
  <files>apps/api/src/modules/orders/orders.service.ts, apps/web/src/pages/HistoryPage.tsx</files>
  <read_first>
    - apps/api/src/modules/orders/orders.service.ts dòng 1066-1140 (`stats` + `applyFilters` + kiểu trả về)
    - apps/web/src/pages/HistoryPage.tsx dòng 160-250 (khai `type Stats` + chỗ gọi `/orders/stats`) và dòng 400-425 (lưới `StatTile`)
    - docs/MILESTONE-02-ONLINE-ORDERING-SPEC.md dòng 340-344 và dòng 505 (M2.D-62)
  </read_first>
  <action>
    **BE — `orders.service.ts` `stats()`:** thêm `ship_fee_total: number` vào kiểu trả về và tính bằng
    1 truy vấn riêng, tái dùng `applyFilters`:
    `SELECT COALESCE(SUM(o.ship_fee), 0) FROM orders o WHERE <PAID_SQL> <+ filter>` → `Number(...)`.
    ⚠ **KHÔNG** đụng vào truy vấn `perOrder`/`paidRevenue` hiện có — doanh thu món phải giữ nguyên
    nghĩa "tiền món đã SERVED". Ghi comment tại chỗ: "M2.D-62 — `ship_fee` là tiền thu hộ, KHÔNG
    phải doanh thu món. Báo cáo hiển thị 2 dòng tách biệt; ai cộng 2 số này vào `paid_revenue` là
    làm sai quyết định đã chốt."
    Kiểm `orders.controller.ts` có trả thẳng object của `stats()` không (nếu nó whitelist field thì
    phải thêm `ship_fee_total` ở đó nữa).

    **FE — `HistoryPage.tsx`:** thêm `ship_fee_total: number` vào `type Stats`, và thêm 1 `StatTile`
    ngay sau ô "Doanh thu": label **"Phí ship thu hộ"**, `value={fmt(stats?.ship_fee_total ?? 0)}`,
    dùng bộ màu trung tính đã có sẵn trong file (`color="#334155" bg="#f8fafc" border="#e2e8f0"` —
    KHÔNG phát minh hex mới, `apps/web` không có design token và UI-SPEC cấm đưa token sang đây).
    Không đổi ô "Doanh thu" hiện có.

    ⚠ File `apps/web/src/pages/MenuManagementPage.tsx` đang có sửa đổi chưa commit của chủ dự án
    (5MB→10MB) — **không đụng file đó**. `HistoryPage.tsx` không liên quan, nhưng vẫn `git diff` file
    này trước khi sửa để chắc chắn không đè lên thay đổi chưa commit nào.
  </action>
  <verify>
    <automated>cd apps/api && ../../node_modules/.bin/tsc --noEmit && cd ../web && ../../node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ship_fee_total" apps/api/src/modules/orders/orders.service.ts` ≥ 2 (kiểu trả về + truy vấn)
    - `grep -c "ship_fee_total" apps/web/src/pages/HistoryPage.tsx` = 2 (type + StatTile)
    - `grep -c "Phí ship thu hộ" apps/web/src/pages/HistoryPage.tsx` = 1
    - `git diff apps/web/src/pages/MenuManagementPage.tsx` — trống (không đụng file đang sửa dở của chủ dự án)
    - **Chạy thật**: tạo 1 đơn online, duyệt với `ship_fee = 20000`, thanh toán đơn đó, rồi gọi
      `GET /orders/stats?...` → `paid_revenue` = **chỉ tiền món** (không gồm 20000) và
      `ship_fee_total` = 20000. Dán output JSON vào SUMMARY.
    - `cd apps/web && ../../node_modules/.bin/tsc --noEmit` exit 0
    - `cd apps/api && ./node_modules/.bin/vitest run` full suite exit 0
  </acceptance_criteria>
  <done>M2.D-62 đóng trọn: phí ship không lẫn vào doanh thu món và có dòng riêng trên báo cáo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → MySQL của môi trường dev | Test ghi/xoá dữ liệu thật; nếu nhắm nhầm dải sentinel là xoá dữ liệu quán |
| bảng staging → mọi truy vấn tiền | Ranh giới M2.D-01 — task 2 chính là bài kiểm tra ranh giới này |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-41 | Tampering | Test xoá nhầm dữ liệu thật của quán | mitigate | Chỉ thao tác trên dải sentinel cố định (`ship-9%`, `mang-ve-9%`, SĐT `09000000%`); `cleanupSentinelRows` chỉ dùng điều kiện sentinel; acceptance criteria đếm lại = 0 sau khi chạy |
| T-09-42 | Repudiation | Test bị `skip` âm thầm khi thiếu MySQL → criterion 2/3 coi như đã chứng minh mà thực ra chưa chạy | mitigate | `beforeAll` `throw` với thông điệp hướng dẫn; grep `it.skip|describe.skip|.todo` = 0 trong acceptance criteria |
| T-09-43 | Tampering | `ship_fee` bị cộng vào doanh thu món làm sai sổ sách của quán | mitigate | Truy vấn `ship_fee_total` tách hẳn, không đụng `perOrder`; kịch bản chạy thật kiểm 2 con số riêng biệt |
| T-09-44 | Information Disclosure | Cookie admin dùng cho test bị commit vào repo | mitigate | Cookie đọc từ biến môi trường `TEST_ADMIN_COOKIE`, không hardcode; test `throw` nếu thiếu thay vì dùng giá trị mặc định |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Plan này KHÔNG cài package nào (không `supertest`, không `@nestjs/testing` — giữ quyết định "hướng nhẹ" của phase 8) |
</threat_model>

<verification>
1. `cd apps/api && ./node_modules/.bin/vitest run src/modules/admin-online-orders/admin-online-orders.integration.test.ts` — ≥ 8 test xanh
2. `cd apps/api && ./node_modules/.bin/vitest run` — full suite xanh
3. `cd apps/api && ../../node_modules/.bin/tsc --noEmit` và `cd apps/web && ../../node_modules/.bin/tsc --noEmit` — sạch
4. Truy vấn dọn sentinel = 0 sau khi chạy test
5. Kịch bản `ship_fee` chạy thật, dán JSON vào SUMMARY
</verification>

<success_criteria>
- Criterion 2 (không cấp trùng bàn) và criterion 3 (WAITING không nhiễm doanh thu) có bằng chứng chạy bằng 1 lệnh trên MySQL thật
- Audit ghi đúng người duyệt được khẳng định bằng truy vấn `audit_log`
- Outbox L2 chuyển `CANCELLED` khi duyệt kịp
- `ship_fee` tách khỏi doanh thu món, hiện thành dòng riêng trên báo cáo
- Không còn dòng sentinel nào trong DB sau khi test chạy xong
</success_criteria>

<output>
Create `.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-08-SUMMARY.md` when done
</output>
