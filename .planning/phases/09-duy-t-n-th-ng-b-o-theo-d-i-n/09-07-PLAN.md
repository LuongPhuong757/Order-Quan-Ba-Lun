---
phase: 09-duy-t-n-th-ng-b-o-theo-d-i-n
plan: 07
type: execute
wave: 4
depends_on: ["09-06"]
files_modified:
  - apps/api/src/modules/admin-online-orders/admin-online-orders.controller.ts
  - apps/api/src/modules/admin-online-orders/admin-online-orders.module.ts
  - apps/api/src/modules/audit/audit.interceptor.ts
  - apps/api/src/app.module.ts
autonomous: true
requirements: [REQ-M, REQ-N]

must_haves:
  truths:
    - "4 endpoint sống ở `/admin/online-orders*` — KHÔNG có tiền tố `/api` (OD-08)"
    - "Gọi không đăng nhập → 401; đăng nhập nhưng role ngoài 3 role cho phép → 403 ROLE_FORBIDDEN"
    - "Cả 3 role admin/order/kitchen đều GỌI ĐƯỢC confirm và reject (D-02 ghi đè M2.D-33)"
    - "Mọi lần confirm/reject đều để lại dòng audit log có tên người thực hiện — kiểm soát bù trừ của D-02"
    - "SSE `/admin/online-orders/stream` đẩy event tới mọi tab đang mở, KHÔNG giữ connection DB nào cho mỗi subscriber (C-INFRA-01)"
    - "Client ngắt kết nối → subscriber được dọn, không rò listener"
  artifacts:
    - path: "apps/api/src/modules/admin-online-orders/admin-online-orders.controller.ts"
      provides: "GET list · POST :id/confirm · POST :id/reject · GET stream (SSE)"
      contains: "@Sse"
    - path: "apps/api/src/modules/audit/audit.interceptor.ts"
      provides: "action_kind rõ nghĩa cho 2 route duyệt đơn"
      contains: "online_order.confirmed"
  key_links:
    - from: "apps/api/src/modules/admin-online-orders/admin-online-orders.controller.ts"
      to: "RequireRoles('admin','order','kitchen')"
      via: "@UseGuards ở method level, dưới @UseGuards(JwtAuthGuard) ở class level"
      pattern: "RequireRoles\\('admin', ?'order', ?'kitchen'\\)"
    - from: "apps/api/src/modules/admin-online-orders/admin-online-orders.controller.ts"
      to: "EventEmitter2"
      via: "rxjs fromEvent + merge + takeUntil(close$)"
      pattern: "takeUntil"
---

<objective>
Đưa 3 thao tác duyệt đơn ra HTTP và mở kênh SSE đẩy đơn mới tới mọi tab admin đang mở.

Purpose: Criterion 1 của ROADMAP (admin thấy đơn mới < 2s) và phần phân quyền của D-02. Đây cũng là
`@Sse()` đầu tiên của repo — RESEARCH Pattern 1 đã viết sẵn khuôn từ tài liệu chính thức NestJS,
không phải tự nghĩ. Ràng buộc cứng C-INFRA-01: fan-out **in-process**, KHÔNG một DB connection cho
mỗi subscriber (pool 50 đang được size cho poller 2s của POS).

Output: 1 controller 4 route + 2 nhánh `action_kind` audit rõ nghĩa + đăng ký module.
</objective>

<execution_context>
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-CONTEXT.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-RESEARCH.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-PATTERNS.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-VALIDATION.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-06-SUMMARY.md

<interfaces>
<!-- Trích sẵn. KHÔNG đi khám phá lại. -->

apps/api/src/modules/auth/guards/roles.guard.ts (đã tồn tại, CHƯA từng được dùng ở đâu):
```typescript
export function RequireRoles(...allowed: string[]): Type<CanActivate> { /* mixin */ }
// ném ForbiddenException({ code: 'ROLE_FORBIDDEN', message: 'Bạn không có quyền xem mục này.' })
// Chỉ dùng ở METHOD level dưới class-level @UseGuards(JwtAuthGuard) — nó không tự chạy JwtAuthGuard.
```
`req.user` sau `JwtAuthGuard`: `{ sub, full_name, role, is_owner, ... }`.
`ROLE_VALUES = ['admin','order','kitchen'] as const` tại `modules/admin/users.controller.ts:26`.

apps/api/src/modules/settings/settings.controller.ts — KHUÔN MẪU controller admin:
  `@Controller('admin/settings')` (KHÔNG `/api`, OD-08) · `@UseGuards(AdminGuard)` ·
  lấy actor: `const actor = { user_id: req.user!.sub, full_name: req.user!.full_name }`

apps/api/src/modules/admin-online-orders/admin-online-orders.service.ts (plan 09-06):
  `list(status)` → `AdminOnlineOrderList`
  `confirm(requestId, actor: { id, full_name }, body)` → `{ order_id, table_code, table_created, dropped_count }`
  `reject(requestId, actor, body)` → `{ ok: true }`
  Service tự emit `online_order.reviewed` sau commit.

apps/api/src/modules/audit/audit.interceptor.ts:
  - `deriveActionKind(method, path)` — chuỗi `if` theo path, fallback
    `` `${method.toLowerCase()}.${path.replace(/[^a-z0-9]/gi,'_')}` `` (xấu, cần thêm nhánh riêng)
  - Interceptor chỉ ghi khi `MUTATION_METHODS.has(method)` → `GET /stream` KHÔNG bị ghi audit và
    `tap()` KHÔNG chạy trên từng event SSE. Không cần loại trừ thủ công.
  - `AuditEventHandler` `@OnEvent('audit.write', { async: true })` đã có, nuốt lỗi.
  - `EventEmitterModule.forRoot()` đã global ở `app.module.ts:24`.

apps/api/src/main.ts — `apiPrefixes` của SPA fallback **đã có `/admin`**; `pathRequiresCheck()` của
  CSRF guard cũng đã phủ `path.startsWith('/admin/')`. **KHÔNG cần sửa 2 chỗ đó.**

RESEARCH.md § Pattern 1 (dòng 173-198) — khuôn `@Sse()` chính thức: `fromEvent(emitter, name)` +
  `merge` + `map` + heartbeat `timer(15_000, 15_000)` + `takeUntil(fromEvent(req, 'close'))`.

@order/schemas: `AdminOnlineOrderList`, `ConfirmOnlineOrderBody`, `RejectOnlineOrderBody`,
  `OnlineOrderStreamEvent` (payload SSE tối giản — FE tự gọi lại GET list, D-06).

apps/api/src/modules/public/public-orders.controller.ts — khuôn `apiOk()` + zod `safeParse` →
  `BadRequestException({ code: 'VALIDATION_FAILED', message, field_errors })`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Controller 3 route REST + RequireRoles 3 role + audit action_kind rõ nghĩa</name>
  <files>apps/api/src/modules/admin-online-orders/admin-online-orders.controller.ts, apps/api/src/modules/admin-online-orders/admin-online-orders.module.ts, apps/api/src/modules/audit/audit.interceptor.ts, apps/api/src/app.module.ts</files>
  <read_first>
    - apps/api/src/modules/settings/settings.controller.ts (khuôn controller admin, cách lấy actor từ `req.user`)
    - apps/api/src/modules/auth/guards/roles.guard.ts (toàn bộ)
    - apps/api/src/modules/public/public-orders.controller.ts (khuôn `apiOk` + zod safeParse → 400)
    - apps/api/src/modules/audit/audit.interceptor.ts dòng 40-75 (`deriveActionKind` + `extractTargetId`)
    - apps/api/src/app.module.ts
    - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-CONTEXT.md D-02 (và cảnh báo ghi đè M2.D-33)
  </read_first>
  <action>
    Tạo `admin-online-orders.controller.ts`:
    ```
    @Controller('admin/online-orders')        // OD-08 — KHÔNG '/api'. Spec §5.2 ghi '/api/admin/...'
                                              // là SAI với repo này (admin/users, admin/audit đều không có).
    @UseGuards(JwtAuthGuard)                  // class level
    ```
    Docblock đầu file phải ghi rõ: **D-02 ghi đè M2.D-33**. Spec + ROADMAP criterion 1 hiện ghi role
    `order` chỉ được XEM; chủ dự án đã đổi ngày 2026-07-31 thành **cả 3 role đều duyệt được** vì
    không muốn chủ quán thành nút thắt giờ cao điểm. Lớp bảo vệ thay thế là **audit log ghi rõ ai
    duyệt** — ai gỡ audit ở đây là gỡ luôn kiểm soát bù trừ.

    3 route, mỗi route `@UseGuards(RequireRoles('admin', 'order', 'kitchen'))` ở method level:
    - `@Get()` `list(@Query('status') status?: string)` — chỉ chấp nhận `status` là `'WAITING'`
      (thiếu → mặc định `'WAITING'`; giá trị khác → 400 `VALIDATION_FAILED`). Trả
      `apiOk(await svc.list('WAITING'))`. Header `Cache-Control: no-store` (dữ liệu hàng chờ không
      được cache — khoảng cách giữa "đơn tới" và "admin thấy" phải bằng 0).
    - `@Post(':id/confirm')` `@HttpCode(200)` — validate `ConfirmOnlineOrderBody.safeParse(body)`;
      actor `{ id: req.user!.sub, full_name: req.user!.full_name }`; trả
      `apiOk({ order_id, table_code, table_created, dropped_count })`.
    - `@Post(':id/reject')` `@HttpCode(200)` — validate `RejectOnlineOrderBody.safeParse(body)`;
      trả `apiOk({ ok: true })`.
      ⚠ Response của reject **KHÔNG** echo lại `internal_note` (D-09) — chỉ `{ ok: true }`.
    Cả 3 route: `id` param phải là uuid (`z.string().uuid().safeParse`) → 400 nếu không.

    **`audit.interceptor.ts`** — thêm 2 nhánh vào `deriveActionKind`, đặt cạnh nhóm settings phase 8:
    ```
    if (path.match(/^\/admin\/online-orders\/[^/]+\/confirm$/) && method === 'POST') return 'online_order.confirmed';
    if (path.match(/^\/admin\/online-orders\/[^/]+\/reject$/)  && method === 'POST') return 'online_order.rejected';
    ```
    Không có 2 nhánh này thì fallback sinh chuỗi rác kiểu `post._admin_online_orders__id_confirm`,
    và trang `/admin/audit` sẽ không lọc được theo hành động — tức là kiểm soát bù trừ của D-02 tồn
    tại trên giấy mà không tra cứu được. Kiểm `extractTargetId(req)` có lấy đúng `req.params.id`;
    nếu không, bổ sung để `target_id` là id của request đơn.
    ⚠ Service (plan 09-06) cũng emit `audit.write` thủ công cho `online_order.rejected` —
    **giữ cả hai là chấp nhận được nhưng gây trùng dòng**. Chốt tại plan này: **bỏ emit thủ công
    `online_order.rejected` trong service**, chỉ giữ emit thủ công cho `online_order.table_autocreated`
    (interceptor không biết chuyện tự tạo bàn). Sửa lại service cho khớp và ghi vào SUMMARY.
    Đổi lại, `after_json` của interceptor lấy từ response body — nên response reject phải đủ để
    truy vết: đổi thành `apiOk({ ok: true, reason_code, has_internal_note: boolean })`
    (**không** trả nội dung ghi chú), còn nội dung ghi chú đã nằm ở cột `internal_reject_note`.

    **`app.module.ts`**: thêm `AdminOnlineOrdersModule` vào `imports` (nếu plan 09-06 chưa thêm).
    **`admin-online-orders.module.ts`**: thêm `controllers: [AdminOnlineOrdersController]`.
  </action>
  <verify>
    <automated>cd apps/api && ../../node_modules/.bin/tsc --noEmit && ./node_modules/.bin/vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@Controller('admin/online-orders')" apps/api/src/modules/admin-online-orders/admin-online-orders.controller.ts` = 1
    - `grep -c "api/admin" apps/api/src/modules/admin-online-orders/admin-online-orders.controller.ts` = 0
    - `grep -c "RequireRoles('admin', 'order', 'kitchen')" apps/api/src/modules/admin-online-orders/admin-online-orders.controller.ts` = 3
    - `grep -c "online_order.confirmed" apps/api/src/modules/audit/audit.interceptor.ts` = 1 và `grep -c "online_order.rejected" apps/api/src/modules/audit/audit.interceptor.ts` = 1
    - `grep -c "internal_note" apps/api/src/modules/admin-online-orders/admin-online-orders.controller.ts` = 0
    - **Chạy thật — 401**: `curl -i -X POST http://localhost:3001/admin/online-orders/<uuid>/confirm -H 'Content-Type: application/json' -H 'Origin: http://localhost:5173' -d '{}'` (không cookie) → **401**
    - **Chạy thật — 3 role đều duyệt được (D-02)**: đăng nhập lần lượt tài khoản role `order` và role `kitchen`, gọi `POST /admin/online-orders/<id>/confirm` với 1 đơn WAITING thật → **200**, và `SELECT reviewed_by_full_name FROM online_order_requests WHERE id='<id>'` = tên đúng người vừa gọi
    - **Chạy thật — audit ghi đúng**: `SELECT action_kind, actor_name, target_id FROM audit_log ORDER BY ts_ms DESC LIMIT 3` → có dòng `online_order.confirmed` với `actor_name` đúng và `target_id` = id đơn
    - **Chạy thật — 403**: tạo/chuyển 1 user sang role không nằm trong 3 role (nếu hệ thống chỉ có đúng 3 role thì ghi vào SUMMARY là **không kiểm được**, KHÔNG bịa kết quả), gọi confirm → 403 `ROLE_FORBIDDEN`
    - **Chạy thật — CSRF**: `curl -X POST http://localhost:3001/admin/online-orders/<id>/reject` **không** header Origin, có cookie hợp lệ → **403 CSRF_ORIGIN_MISMATCH** (guard có sẵn phủ `/admin/`, không cần sửa gì)
    - `cd apps/api && ./node_modules/.bin/vitest run` full suite exit 0
  </acceptance_criteria>
  <done>3 thao tác duyệt đơn gọi được qua HTTP đúng nếp route của repo, đúng phân quyền D-02, và để lại vết audit tra cứu được.</done>
</task>

<task type="auto">
  <name>Task 2: SSE `/admin/online-orders/stream` — fan-out in-process qua EventEmitter2</name>
  <files>apps/api/src/modules/admin-online-orders/admin-online-orders.controller.ts</files>
  <read_first>
    - apps/api/src/modules/audit/audit.interceptor.ts dòng 100-130 (khuôn `emitter.emit` / `@OnEvent` đã chạy production)
    - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-RESEARCH.md § Pattern 1 (dòng 173-198 — code mẫu đầy đủ) và § Anti-Patterns
    - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-CONTEXT.md D-06, D-07, D-20
    - apps/api/src/modules/admin-online-orders/admin-online-orders.service.ts (tên event đang emit)
  </read_first>
  <action>
    Thêm vào controller:
    ```
    @Sse('stream')
    @UseGuards(RequireRoles('admin', 'order', 'kitchen'))
    stream(@Req() req: Request): Observable<MessageEvent>
    ```
    Cài đúng RESEARCH Pattern 1:
    - `const close$ = fromEvent(req, 'close')` — dọn subscriber khi client ngắt.
    - `events$ = merge(fromEvent(this.emitter, 'online_order.new'), fromEvent(this.emitter, 'online_order.reviewed'))`
      `.pipe(map((payload) => ({ data: <OnlineOrderStreamEvent> } as MessageEvent)))`.
      Payload gửi đi **chỉ** `{ type, request_id, at_ms }` — parse qua `OnlineOrderStreamEvent.strict().parse()`
      trước khi trả để không lỡ tay đẩy cả object đơn ra stream.
    - `heartbeat$ = timer(15_000, 15_000).pipe(map(() => ({ data: { type: 'heartbeat', request_id: null, at_ms: Date.now() } } as MessageEvent)))`
      — heartbeat cần cho D-07: FE đo khoảng lặng để biết SSE đã chết (proxy/mạng đứt im lặng).
      Gửi 1 heartbeat NGAY khi mở stream (dùng `timer(0, 15_000)`) để FE biết kết nối đã lên.
    - `return merge(events$, heartbeat$).pipe(takeUntil(close$))`.
    - Đếm subscriber: field `private subscribers = 0`, tăng/giảm bằng `tap`/`finalize`, log khi
      vượt ngưỡng (vd > 20) — RESEARCH § Security khuyến nghị để phát hiện rò listener.

    **Cấm tuyệt đối trong handler này** (ghi thành comment, C-INFRA-01):
    - KHÔNG query DB (không `this.svc.list()` bên trong stream) — 1 subscriber = 1 connection là
      đúng thứ ràng buộc này cấm. Event chỉ là tín hiệu "có gì đó đổi"; FE tự gọi lại
      `GET /admin/online-orders?status=WAITING` (D-06).
    - KHÔNG `Last-Event-ID`, KHÔNG replay buffer (D-06 đã chốt ngược lại).

    Đảm bảo service (plan 09-06) emit `online_order.reviewed` sau commit và plan 09-09 sẽ emit
    `online_order.new` lúc submit — controller chỉ **nghe**, không tự emit.

    Ghi vào SUMMARY: RESEARCH Pitfall #1 đã xác minh Caddy tự flush `text/event-stream`, **không cần
    sửa `Caddyfile`**; nếu sau này production thấy trễ > 2s thì kiểm `Content-Type` trước, đừng nghi
    Caddy.
  </action>
  <verify>
    <automated>cd apps/api && ../../node_modules/.bin/tsc --noEmit && ./node_modules/.bin/vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@Sse('stream')" apps/api/src/modules/admin-online-orders/admin-online-orders.controller.ts` = 1
    - `grep -c "takeUntil" apps/api/src/modules/admin-online-orders/admin-online-orders.controller.ts` = 1
    - `grep -cE "Last-Event-ID|lastEventId" apps/api/src/modules/admin-online-orders/admin-online-orders.controller.ts` = 0
    - Đọc thân hàm `stream()` và xác nhận **không** có lời gọi service/repository/`ds.` nào bên trong
    - **Chạy thật — stream mở được và có heartbeat**: `curl -N -H 'Cookie: <cookie admin>' http://localhost:3001/admin/online-orders/stream` → header `content-type: text/event-stream`, trong 20s thấy ≥ 1 dòng `data:` có `"type":"heartbeat"`
    - **Chạy thật — event tới < 2s**: giữ `curl -N` mở, ở terminal khác submit 1 đơn thật qua `POST /api/public/orders` → dòng `data:` có `"type":"new"` xuất hiện trong < 2s. (Nếu plan 09-09 chưa xong nên chưa có emit `new`, thay bằng: gọi `POST /admin/online-orders/<id>/reject` và xác nhận nhận được `"type":"reviewed"`; ghi rõ đã dùng biến thể nào.)
    - **Chạy thật — không rò connection DB**: trong lúc giữ 3 `curl -N` song song, `mysql ... -e "SHOW STATUS LIKE 'Threads_connected';"` **không** tăng thêm 3 so với lúc chưa mở stream
    - **Chạy thật — dọn subscriber**: đóng cả 3 `curl`, log API in số subscriber về 0 (hoặc không còn tăng ở lần mở tiếp theo)
    - `cd apps/api && ./node_modules/.bin/vitest run` full suite exit 0
  </acceptance_criteria>
  <done>SSE đẩy được event tới nhiều tab, có heartbeat cho phát hiện đứt kết nối, và không tiêu tốn connection DB nào cho mỗi subscriber.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → `/admin/online-orders*` | 4 endpoint admin mới; trước guard thì mọi request là untrusted |
| SSE stream → nhiều tab trình duyệt | Kênh đẩy dữ liệu một chiều, mở lâu, không đi qua vòng đời request thông thường |
| `req.user` → `reviewed_by_*` + audit | Danh tính người duyệt — nền tảng của kiểm soát bù trừ D-02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-34 | Elevation of Privilege | Người chưa đăng nhập gọi được endpoint duyệt đơn (**HIGH**) | mitigate | `@UseGuards(JwtAuthGuard)` ở class level + `RequireRoles(...)` ở từng method; acceptance criteria có `curl` không cookie → 401 |
| T-09-35 | Information Disclosure | SSE stream đẩy dữ liệu đơn tới client chưa có phiên hợp lệ (**HIGH**) | mitigate | `@Sse` nằm dưới cùng class-level `JwtAuthGuard` + method-level `RequireRoles`; payload lại tối giản (`type`/`request_id`/`at_ms`) nên kể cả rò cũng không lộ PII; `.strict().parse()` chặn nhồi thêm field |
| T-09-36 | Repudiation | Không truy được ai duyệt đơn nào — mất lớp bảo vệ mà M2.D-33 từng cung cấp trước khi D-02 mở cho 3 role | mitigate | 2 nhánh `action_kind` rõ nghĩa trong `AuditInterceptor` + `reviewed_by_*` lấy từ `req.user` (không tin body); acceptance criteria truy vấn `audit_log` thật |
| T-09-37 | Tampering | CSRF trên `POST /admin/online-orders/:id/confirm` | mitigate | `pathRequiresCheck()` hiện có đã phủ `path.startsWith('/admin/')` — không cần sửa `csrf-paths.ts`; acceptance criteria có `curl` thiếu Origin → 403 |
| T-09-38 | Denial of Service | Nhiều tab bỏ quên giữ stream vô hạn → rò listener / cạn pool DB | mitigate | `takeUntil(fromEvent(req,'close'))` dọn subscriber; đếm + log subscriber; **cấm mọi truy vấn DB trong handler stream** (kiểm bằng đọc code + đo `Threads_connected`) |
| T-09-39 | Information Disclosure | Response reject echo lại ghi chú nội bộ (D-09) | mitigate | Response chỉ `{ ok, reason_code, has_internal_note }`; grep `internal_note` trong controller = 0 |
| T-09-40 | Tampering | `id` không phải uuid dùng để dò lỗi/SQL | mitigate | `z.string().uuid()` cho param ở cả 3 route; truy vấn dùng tham số hoá (`mgr.query(sql, [id])`) từ plan 09-06 |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Plan này KHÔNG cài package nào — `rxjs` đã là dependency sẵn có |
</threat_model>

<verification>
1. `cd apps/api && ../../node_modules/.bin/tsc --noEmit` — sạch
2. `cd apps/api && ./node_modules/.bin/vitest run` — full suite xanh
3. 7 kịch bản chạy thật ở acceptance criteria (401 · 3 role duyệt được · audit đúng · 403 CSRF · stream có heartbeat · event < 2s · không tăng connection DB)
4. `SELECT action_kind, COUNT(*) FROM audit_log WHERE action_kind LIKE 'online_order%' GROUP BY 1` — thấy đủ `confirmed`/`rejected`
</verification>

<success_criteria>
- 4 endpoint sống ở `/admin/online-orders*`, không tiền tố `/api`
- Cả 3 role duyệt được; `reviewed_by_*` + audit ghi đúng người
- SSE đẩy event tới mọi tab, có heartbeat, dọn subscriber khi đóng, 0 connection DB thêm
- Response reject không chứa ghi chú nội bộ
- Full suite xanh, typecheck sạch
</success_criteria>

<output>
Create `.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-07-SUMMARY.md` when done
</output>
