---
phase: 09-duy-t-n-th-ng-b-o-theo-d-i-n
plan: 10
type: execute
wave: 5
depends_on: ["09-07"]
files_modified:
  - apps/web/src/lib/queue-clock.ts
  - apps/web/src/lib/queue-clock.test.ts
  - apps/web/src/lib/bell.ts
  - apps/web/src/lib/online-orders-sse.ts
  - apps/web/src/lib/online-orders-sse.test.ts
  - apps/web/src/pages/OnlineOrdersQueuePage.tsx
  - apps/web/src/App.tsx
autonomous: true
requirements: [REQ-M, REQ-N]

must_haves:
  truths:
    - "Trang `/admin/online-orders` mở được bởi cả 3 role admin/order/kitchen và cả 3 đều thấy 2 nút Xác nhận/Từ chối (D-02)"
    - "Đơn mới hiện lên trong < 2s nhờ SSE, kèm chuông và badge số đơn chờ (D-05)"
    - "Chưa mở khoá âm thanh → banner đỏ CHIẾM CHỖ 'Chuông đang tắt', không phải toast biến mất (D-03)"
    - "SSE đứt > 10s → chấm đỏ + banner 'Mất kết nối'; nối lại được thì tự tải lại toàn bộ hàng chờ (D-06, D-07)"
    - "Panel từ chối tách rõ 'Lý do gửi tới khách' (5 radio) và '🔒 Ghi chú nội bộ — khách KHÔNG thấy' (D-08, D-09)"
    - "Đồng hồ đếm giây chờ từng đơn đổi đỏ đúng tại ngưỡng `escalate_sms_after_s` do BE trả về, không hardcode 90"
  artifacts:
    - path: "apps/web/src/lib/bell.ts"
      provides: "Mở khoá âm thanh bằng user gesture + phát chuông lặp 5 phút"
      exports: ["createBell"]
    - path: "apps/web/src/lib/online-orders-sse.ts"
      provides: "EventSource client + phân loại trạng thái kết nối"
      exports: ["subscribeOnlineOrders", "connectionStateFrom", "SSE_DEAD_MS"]
    - path: "apps/web/src/pages/OnlineOrdersQueuePage.tsx"
      provides: "Trang hàng chờ duyệt đầy đủ theo 09-UI-SPEC Mặt A"
      min_lines: 300
    - path: "apps/web/src/App.tsx"
      provides: "Route /admin/online-orders bọc RoleGate 3 role"
      contains: "OnlineOrdersQueuePage"
  key_links:
    - from: "apps/web/src/pages/OnlineOrdersQueuePage.tsx"
      to: "/admin/online-orders (GET, POST confirm, POST reject)"
      via: "api.get/api.post từ lib/api.ts"
      pattern: "admin/online-orders"
    - from: "apps/web/src/pages/OnlineOrdersQueuePage.tsx"
      to: "/admin/online-orders/stream"
      via: "subscribeOnlineOrders (EventSource)"
      pattern: "online-orders/stream"
    - from: "apps/web/src/App.tsx"
      to: "RoleGate allow admin/order/kitchen"
      via: "Route element"
      pattern: "OnlineOrdersQueuePage"
---

<objective>
Dựng `OnlineOrdersQueuePage` — mặt trận A của 09-UI-SPEC. Công cụ nội bộ, nhân viên nhìn cả ca:
tối ưu **tốc độ thao tác + không bỏ sót đơn**, không phải vẻ đẹp.

Purpose: Criterion 1 của ROADMAP sống ở đây (SSE + chuông + badge). Hai loại lỗi im lặng phải nhìn
thấy được: (a) trình duyệt chặn `Audio.play()` → admin ngồi trước trang câm mà tưởng nó đang chạy
(D-03); (b) SSE chết → trang trông bình thường, chỉ là không bao giờ có đơn mới (D-07). Cả hai đều
được xử bằng **banner chiếm chỗ**, không phải toast.

Output: 3 module lib (2 có test) + 1 trang + 1 route.
</objective>

<execution_context>
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-CONTEXT.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-RESEARCH.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-PATTERNS.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-UI-SPEC.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-07-SUMMARY.md

<interfaces>
<!-- Trích sẵn. KHÔNG đi khám phá lại. -->

API (plan 09-07), **không** tiền tố `/api`:
  GET  /admin/online-orders?status=WAITING  → `{ data: AdminOnlineOrderList }`
  POST /admin/online-orders/:id/confirm     body `ConfirmOnlineOrderBody` → `{ data: { order_id, table_code, table_created, dropped_count } }`
  POST /admin/online-orders/:id/reject      body `RejectOnlineOrderBody`  → `{ data: { ok, reason_code, has_internal_note } }`
  GET  /admin/online-orders/stream          SSE, `data:` = `OnlineOrderStreamEvent { type:'new'|'reviewed'|'heartbeat', request_id, at_ms }`

@order/schemas (plan 09-01): `AdminOnlineOrderList`, `AdminOnlineOrderRow`, `AdminOnlineOrderItem`,
  `RejectReasonCode`, `REJECT_REASON_TEXT`, `ConfirmOnlineOrderBody`, `RejectOnlineOrderBody`,
  `OnlineOrderStreamEvent`.
  `AdminOnlineOrderRow` gồm: id · order_token_masked · status · fulfillment_type · customer_name ·
  customer_phone · customer_address · customer_map_link · distance_km · customer_note · items[] ·
  subtotal · submitted_at_ms · waiting_seconds · out_of_stock_count
  `AdminOnlineOrderList` gồm `{ items, escalate_sms_after_s }`.

apps/web/src/lib/api.ts — `api` (axios, `withCredentials: true`, baseURL rỗng) + `extractError(err)`
  → `{ code, message, field_errors? }`. Gọi thẳng `/admin/...` (OD-08).
apps/web/src/components/Toast.tsx — `useToast()`, `toast.push(kind, message)`,
  `ToastKind = 'success'|'error'|'info'|'ready'`.
apps/web/src/App.tsx dòng 38-41 — KHUÔN route cần chép:
```tsx
<Route element={<RoleGate allow={['admin', 'order', 'kitchen']} />}>
  <Route path="/orders" element={<OrdersPage />} />
</Route>
```
`RoleGate` là function cục bộ trong chính `App.tsx` (dòng ~182-199) — không import thêm gì.
Nav bottom theo role ở dòng ~142-169.
apps/web/src/components/NotificationBell.tsx dòng 57-78 — KHUÔN badge đỏ tròn (chép nguyên style).
apps/web/src/pages/AdminSettingsPage.tsx dòng 297-371 — KHUÔN textarea + đếm ký tự còn lại.
apps/web/src/pages/MenuManagementPage.tsx — KHUÔN `<div className="card">` + `<button className="secondary|danger">`.
  ⚠ File này ĐANG có sửa đổi chưa commit của chủ dự án — **chỉ đọc, tuyệt đối không sửa**.
apps/web/src/lib/item-age.ts — **KHÔNG import**: ngưỡng 10/20 phút bị hardcode, ngữ nghĩa khác
  (món ở bếp), còn phase 9 dùng ngưỡng cấu hình được. Chép *hình dạng*, viết module mới.
apps/web/src/styles.css — có sẵn `.card`, `.secondary`, `.danger`, `button { min-height: 44px }`.

09-UI-SPEC.md § "A. OnlineOrdersQueuePage" — bố cục, copy, màu. Bảng màu **đóng**:
  accent `#0f766e` (nút mặc định) · destructive `#dc2626` · warn `#f59e0b` · connected `#059669` ·
  nền trang `#f9fafb` · viền `#d1d5db`/`#e5e7eb` · chữ `#1f2937`/`#6b7280`
  2 nền banner mới: đỏ nhạt `#fee2e2` (viền `#fecaca`, chữ `#991b1b`) · vàng nhạt `#fef3c7`
  (viền `#fde68a`, chữ `#92400e`). **Không hex nào khác.**
  Thang spacing 4px: 4 / 8 / 16 / 24 / 32. Cỡ chữ đúng 4: 24/700 · 16/400 · 13/500 · 12/700.
  ⚠ `apps/web` KHÔNG có design token — hardcode hex/px là ĐÚNG nếp ở đây. **Không** đưa
  `tokens.css` của `apps/shop` sang.

RESEARCH.md § Pattern 4 — autoplay: `NotAllowedError` khi chưa có user gesture.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 3 module lib — đồng hồ chờ, chuông (mở khoá bằng user gesture), client SSE</name>
  <files>apps/web/src/lib/queue-clock.ts, apps/web/src/lib/queue-clock.test.ts, apps/web/src/lib/bell.ts, apps/web/src/lib/online-orders-sse.ts, apps/web/src/lib/online-orders-sse.test.ts</files>
  <read_first>
    - apps/web/src/lib/item-age.ts (toàn bộ 28 dòng — chép *hình dạng*, KHÔNG import)
    - apps/web/src/lib/menu-search.test.ts (khuôn test hàm thuần của `apps/web`)
    - apps/web/src/lib/ready-notifier.ts (xem cách repo đang xử lý polling/diff + dọn dẹp)
    - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-RESEARCH.md § Pattern 4
    - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-CONTEXT.md D-03, D-04, D-06, D-07
  </read_first>
  <behavior>
    `queue-clock.test.ts`:
    - `waitingSeconds(nowMs, submittedAtMs)` = chênh lệch làm tròn xuống theo giây; âm → 0
    - `formatWait(95)` = `"1:35"`; `formatWait(9)` = `"0:09"`; `formatWait(3661)` = `"61:01"`
    - `isOverdue(89, 90)` = false; `isOverdue(90, 90)` = true; `isOverdue(120, 90)` = true
    - `isOverdue(100, 30)` = true — **ngưỡng là tham số**, không hằng số trong module
    `online-orders-sse.test.ts`:
    - `connectionStateFrom({ open: true, lastMessageMs: now - 1_000, nowMs: now })` = `'connected'`
    - `open: true` nhưng `lastMessageMs = now - 40_000` (quá 2 chu kỳ heartbeat 15s) = `'stale'`
    - `open: false, lastMessageMs = now - 3_000` = `'reconnecting'` (chưa quá `SSE_DEAD_MS`)
    - `open: false, lastMessageMs = now - 11_000` = `'dead'` (`SSE_DEAD_MS = 10_000`, D-07)
    - `lastMessageMs = null` (chưa từng nhận gì) và `nowMs - startedMs > SSE_DEAD_MS` = `'dead'`
  </behavior>
  <action>
    **`queue-clock.ts`** — module thuần, không import React:
    `waitingSeconds(nowMs, submittedAtMs)` · `formatWait(seconds): string` (`m:ss`) ·
    `isOverdue(seconds, thresholdSeconds)`. Docblock ghi rõ: ngưỡng đến từ
    `AdminOnlineOrderList.escalate_sms_after_s` do BE trả, **không hardcode 90** — đổi setting ở
    `/admin/settings` phải ăn ngay (09-UI-SPEC yêu cầu).

    **`bell.ts`** — `export function createBell(): { unlock(): Promise<'ok'|'blocked'>; ring(): void; dispose(): void }`.
    ⚠ **Quyết định đã chốt tại plan này, lệch RESEARCH Pattern 4:** dùng **Web Audio API**
    (`AudioContext` + `OscillatorNode` + `GainNode`, 2 tiếng "bíp" ~180ms) thay cho
    `HTMLAudioElement` + file `.mp3`. Lý do: executor không tạo được file nhị phân âm thanh, và
    thêm asset nhị phân vào repo cho 1 tiếng chuông là không đáng. Ngữ nghĩa mở khoá **giống hệt**
    RESEARCH Pattern 4: `AudioContext` khởi tạo ở trạng thái `suspended`, `await ctx.resume()` phải
    được gọi **trong chính** `onClick` handler; sau đó kiểm `ctx.state !== 'running'` → trả
    `'blocked'` (tương đương `NotAllowedError`). Bọc try/catch, không bao giờ throw ra ngoài.
    Ghi lệch này vào SUMMARY để plan 09-13 cân nhắc có cần entry OVERRIDE-DEBT không (đây là lệch
    **cách thi công của RESEARCH**, không lệch quyết định LOCKED nào — D-03/D-04 chỉ nói "chuông" và
    "nút Bật chuông bắt buộc").
    `ring()` phát 2 bíp; gọi khi `ctx.state !== 'running'` thì im lặng bỏ qua.
    `dispose()` đóng `AudioContext`.

    **`online-orders-sse.ts`**:
    - `export const SSE_DEAD_MS = 10_000` (D-07 chốt ~10s; con số chính xác chốt tại đây).
    - `export type SseConnState = 'connected' | 'stale' | 'reconnecting' | 'dead'`
    - `export function connectionStateFrom(input: { open: boolean; lastMessageMs: number | null; startedMs: number; nowMs: number }): SseConnState`
      — hàm **thuần**, đó là phần được test.
    - `export function subscribeOnlineOrders(handlers: { onEvent(ev: OnlineOrderStreamEvent): void;
      onOpen(): void; onError(): void }): () => void` — bọc `EventSource('/admin/online-orders/stream')`
      (same-origin nên cookie phiên tự đi kèm; **không** dùng `withCredentials` vì không cần và
      EventSource không set được header).
      Tự nối lại với backoff 1s → 2s → 5s → 10s (chặn trên 10s); `onmessage` parse JSON rồi
      `OnlineOrderStreamEvent.safeParse` — parse fail thì bỏ qua, không crash trang.
      Trả về hàm cleanup đóng `EventSource` và huỷ timer (React `useEffect` gọi khi unmount —
      thiếu bước này là rò EventSource mỗi lần điều hướng).
    - Docblock: **KHÔNG** dùng `Last-Event-ID`/replay buffer (D-06); mỗi lần mở/nối lại, trang phải
      tự gọi lại `GET /admin/online-orders?status=WAITING` — DB là nguồn sự thật duy nhất.
  </action>
  <verify>
    <automated>cd apps/web && ./node_modules/.bin/vitest run src/lib/queue-clock.test.ts src/lib/online-orders-sse.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "90" apps/web/src/lib/queue-clock.ts` = 0 (ngưỡng không hardcode)
    - `grep -c "SSE_DEAD_MS = 10_000" apps/web/src/lib/online-orders-sse.ts` = 1
    - `grep -cE "Last-Event-ID|lastEventId" apps/web/src/lib/online-orders-sse.ts` = 0
    - `grep -c "AudioContext" apps/web/src/lib/bell.ts` ≥ 1 và `grep -cE "\.mp3|\.wav|\.ogg" apps/web/src/lib/bell.ts` = 0
    - `cd apps/web && ./node_modules/.bin/vitest run` exit 0 với ≥ 13 test pass (test cũ + mới)
    - `cd apps/web && ../../node_modules/.bin/tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>3 mảnh hạ tầng FE sẵn sàng; phần logic thuần (đồng hồ + phân loại trạng thái kết nối) có test, không phải kiểm bằng mắt.</done>
</task>

<task type="auto">
  <name>Task 2: OnlineOrdersQueuePage — header/badge/chấm kết nối, 2 banner chiếm chỗ, danh sách đơn, Xác nhận</name>
  <files>apps/web/src/pages/OnlineOrdersQueuePage.tsx</files>
  <read_first>
    - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-UI-SPEC.md § "A. OnlineOrdersQueuePage" + § Copywriting Contract Mặt A + § Color Mặt A (đọc kỹ, đây là hợp đồng)
    - apps/web/src/components/NotificationBell.tsx dòng 40-90 (badge đỏ)
    - apps/web/src/pages/AdminSettingsPage.tsx dòng 240-380 (khuôn card + form + toast)
    - apps/web/src/components/Toast.tsx (useToast)
    - apps/web/src/lib/api.ts (`api`, `extractError`)
    - apps/web/src/lib/queue-clock.ts + online-orders-sse.ts + bell.ts (Task 1)
  </read_first>
  <action>
    Tạo `OnlineOrdersQueuePage.tsx`. Bố cục **đúng thứ tự** 09-UI-SPEC:

    1. **Header**: `<h1>Hàng chờ duyệt</h1>` (24px/700) + badge đỏ tròn `(N)` cạnh tiêu đề, **ẩn khi
       N = 0** (chép style badge của `NotificationBell.tsx`) + **chấm trạng thái kết nối** 10px
       `border-radius: 999px` căn phải cùng hàng:
       `connected` → `#059669`, `aria-label="Đang kết nối"` · `reconnecting`/`stale` → `#f59e0b`,
       `aria-label="Đang kết nối lại..."` · `dead` → `#dc2626`.
    2. **Banner "🔕 Chuông đang tắt"** (khi chưa unlock audio) — full-width, `padding: 12px 16px`,
       min-height 48px, nền `#fee2e2` viền `#fecaca` chữ `#991b1b`, body "Bấm để bật thông báo đơn
       mới", nút **"Bật chuông"**. Gọi `bell.unlock()` **trực tiếp trong `onClick`** (không `await`
       gì khác trước đó — user gesture hết hiệu lực nếu có async gap).
       ⚠ Banner này đứng **TRƯỚC** banner mất kết nối khi cả hai cùng hiện (mất chuông nghiêm trọng
       hơn: không nghe được = mất luôn 2 kênh cảnh báo).
    3. **Banner "⚠ Mất kết nối"** (khi state = `dead`) — cùng bộ màu đỏ nhạt, body "Đang thử nối lại
       — đơn mới có thể chưa hiện ngay."
    4. **Danh sách** `.card` xếp dọc, gap 24px, **sắp `submitted_at_ms` ASC (FIFO)**.
    5. **Empty state** khi N = 0: card căn giữa, "Không có đơn nào đang chờ 🎉" + "Đơn mới sẽ tự hiện
       ở đây, không cần tải lại trang."
    6. **Error state** khi tải lỗi: "Không tải được hàng chờ. " + nút "Thử lại".

    **Mỗi card đơn** (theo 09-UI-SPEC):
    - Hàng đầu: `order_token_masked` + giờ gửi (13px `#6b7280`) trái · **đồng hồ đếm giây**
      (`formatWait`, 12px/700) phải — `#1f2937` bình thường, `#dc2626` khi
      `isOverdue(waitingSeconds, escalate_sms_after_s)`. Cập nhật mỗi giây bằng **một** `setInterval`
      chung cho cả trang (không 1 interval/đơn), dọn trong `useEffect` cleanup.
    - Emoji phương thức 🛵 DELIVERY / 🏠 PICKUP + tên khách + SĐT (16px).
    - DELIVERY: địa chỉ rút gọn 1 dòng + `distance_km` nếu có.
    - Danh sách món thu gọn với toggle "Xem N món" + tổng tiền.
    - `out_of_stock_count > 0` → banner inline vàng nhạt `#fef3c7`/`#fde68a`/`#92400e`
      "⚠ {N} món đã hết hàng" + **checkbox từng món hết, MẶC ĐỊNH ĐÃ TICK** ("Bỏ món này khỏi đơn").
      Món đã tick đi vào `drop_menu_item_ids` khi bấm Xác nhận.
    - DELIVERY → ô number "Phí ship (nếu có)", placeholder "0", đơn vị "đ" (input 16px, cao ≥ 44px).
    - Hàng cuối: **"Xác nhận"** (nút mặc định, không class) + **"Từ chối"** (`.danger`).
      **Cả 2 nút luôn hiện cho cả 3 role — KHÔNG ẩn theo role** (D-02).

    **Luồng Xác nhận:** disable 2 nút → `POST /admin/online-orders/{id}/confirm` với
    `{ ship_fee, drop_menu_item_ids }` → thành công: hiện icon check + fade `opacity` ~600ms
    (**tôn trọng `prefers-reduced-motion`**: dùng `matchMedia('(prefers-reduced-motion: reduce)')`,
    nếu reduce thì bỏ hẳn transition) rồi bỏ khỏi list + `toast.push('success', 'Đã xác nhận — bàn {table_code}')`.
    Lỗi → banner đỏ nhạt **trong chính card đó** với `extractError(err).message`, bật lại 2 nút,
    **KHÔNG** bỏ khỏi list. Mã `MENU_ITEM_UNAVAILABLE`/`ORDER_EMPTY_AFTER_DROP` hiện message BE trả
    nguyên văn (BE đã soạn câu hướng dẫn hành động).

    **Dữ liệu + SSE:**
    - `loadQueue()` = `api.get('/admin/online-orders?status=WAITING')` → set `items` +
      `escalateSmsAfterS`. Gọi khi mount, **và mỗi lần SSE mở/nối lại** (D-06).
    - `subscribeOnlineOrders({ onEvent, onOpen, onError })`: `type === 'heartbeat'` → chỉ cập nhật
      `lastMessageMs`; `type === 'new'|'reviewed'` → `loadQueue()`. `onOpen` → `loadQueue()`.
    - Chuông: khi `items.length` chuyển từ 0 → >0 hoặc có event `new` → `bell.ring()`. Đồng thời
      `setInterval` **5 phút** lặp chuông chừng nào `items.length > 0`; hết đơn thì im (D-04).
    - Dọn toàn bộ interval + EventSource trong `useEffect` cleanup.

    **Ghi 3 giả định của 09-UI-SPEC thành comment ngay đầu file** để người kiểm nghiệm thu biết đây
    là giả định chứ không phải chỉ đạo của chủ dự án:
    (1) sắp FIFO `submitted_at_ms` ASC, đơn mới không chèn lên đầu;
    (2) sau khi xử lý, dòng đơn biến mất sau ~600ms fade (không giữ lại trạng thái "đã xử lý");
    (3) panel từ chối là inline expand, không modal.
  </action>
  <verify>
    <automated>cd apps/web && ../../node_modules/.bin/tsc --noEmit && ./node_modules/.bin/vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Hàng chờ duyệt" apps/web/src/pages/OnlineOrdersQueuePage.tsx` ≥ 1
    - `grep -c "Chuông đang tắt" apps/web/src/pages/OnlineOrdersQueuePage.tsx` = 1 và `grep -c "Mất kết nối" ...` = 1
    - `grep -c "prefers-reduced-motion" apps/web/src/pages/OnlineOrdersQueuePage.tsx` = 1
    - `grep -c "escalate_sms_after_s\|escalateSmsAfterS" apps/web/src/pages/OnlineOrdersQueuePage.tsx` ≥ 1 và `grep -cE "\b90\b" apps/web/src/pages/OnlineOrdersQueuePage.tsx` = 0
    - `grep -oE "#[0-9a-fA-F]{6}" apps/web/src/pages/OnlineOrdersQueuePage.tsx | sort -u` — mọi hex nằm trong danh sách đóng của 09-UI-SPEC (`#f9fafb #ffffff #d1d5db #e5e7eb #0f766e #dc2626 #f59e0b #059669 #1f2937 #6b7280 #fee2e2 #fecaca #991b1b #fef3c7 #fde68a #92400e #f3f4f6`), không có hex lạ
    - `grep -c "var(--" apps/web/src/pages/OnlineOrdersQueuePage.tsx` = 0 (không mang token của `apps/shop` sang)
    - `grep -c "role ===" apps/web/src/pages/OnlineOrdersQueuePage.tsx` = 0 (không ẩn nút theo role — D-02)
    - `cd apps/web && ../../node_modules/.bin/tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Trang hàng chờ hiển thị đủ, hai loại lỗi im lặng đều có banner chiếm chỗ, luồng Xác nhận hoàn chỉnh.</done>
</task>

<task type="auto">
  <name>Task 3: Panel Từ chối inline (D-08/D-09) + route + nav</name>
  <files>apps/web/src/pages/OnlineOrdersQueuePage.tsx, apps/web/src/App.tsx</files>
  <read_first>
    - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-UI-SPEC.md § Copywriting Contract Mặt A (bảng 5 lý do + nhãn panel)
    - apps/web/src/pages/AdminSettingsPage.tsx dòng 297-371 (textarea + đếm ký tự còn lại — chép nguyên khuôn, panel này CÓ giới hạn 500 ký tự)
    - apps/web/src/App.tsx (route block `/orders` dòng 38-41 + nav theo role dòng 142-169)
  </read_first>
  <action>
    **Panel từ chối** — inline expand **trong chính card đơn** (KHÔNG modal), đẩy các đơn khác xuống:
    - Nhãn **"Lý do gửi tới khách"** + 5 radio, label lấy từ `REJECT_REASON_TEXT` của `@order/schemas`
      (KHÔNG gõ lại chuỗi tiếng Việt trong FE — 1 nguồn sự thật). Mục thứ 5 `OTHER` nhãn
      "Lý do khác (ghi rõ bên dưới)"; chọn nó thì hiện thêm 1 input text bắt buộc, kèm chú thích
      **"Nội dung này khách sẽ đọc được"**.
    - Khối **tách biệt rõ**: nền `#f3f4f6`, viền `1px dashed #d1d5db`, chứa textarea nhãn
      **"🔒 Ghi chú nội bộ — khách KHÔNG thấy"**, `maxLength={500}`, đếm "N ký tự còn lại" theo khuôn
      `AdminSettingsPage`. Ranh giới thị giác giữa 2 khối là **yêu cầu an toàn**, không phải trang trí:
      nhầm ô là gửi chữ nội bộ tới khách (D-09).
    - 2 nút cuối panel: **"Huỷ"** (`.secondary`, đóng panel không gửi gì) và **"Xác nhận từ chối"**
      (`.danger`, **disabled** tới khi đã chọn 1 lý do — và nếu chọn `OTHER` thì tới khi ô text có nội dung).
    - **Không** thêm lớp confirm "Bạn có chắc?" thứ hai — bản thân panel là bước xác nhận (09-UI-SPEC).
    - Gửi `POST /admin/online-orders/{id}/reject` với `{ reason_code, reason_other_text?, internal_note? }`
      → thành công: fade + bỏ khỏi list + `toast.push('success', 'Đã từ chối đơn')`. Lỗi: banner
      trong card, panel giữ nguyên nội dung đã gõ (không bắt admin gõ lại ghi chú).

    **`App.tsx`**: `import { OnlineOrdersQueuePage } from './pages/OnlineOrdersQueuePage.tsx';` và
    thêm block route **ngay sau** block `/orders` (chép đúng khuôn):
    ```tsx
    {/* Hàng chờ duyệt đơn online: admin + order + kitchen — D-02 ghi đè M2.D-33,
        ai đang ở máy thì duyệt. Kiểm soát bù trừ là audit log ghi rõ người duyệt. */}
    <Route element={<RoleGate allow={['admin', 'order', 'kitchen']} />}>
      <Route path="/admin/online-orders" element={<OnlineOrdersQueuePage />} />
    </Route>
    ```
    ⚠ Đặt **ngoài** block `RoleGate allow={['admin']}` đang bọc `/admin/users`, `/admin/audit`,
    `/admin/settings` — nếu nhét vào đó thì role `order`/`kitchen` bị chặn, đúng cái D-02 vừa bỏ.
    Thêm mục nav bottom "Hàng chờ" cho cả 3 role theo đúng khuôn nav hiện có.

    **Badge ở Dashboard**: KHÔNG làm ở phase này. RESEARCH Open Question #2 để ngỏ và D-05 chỉ yêu
    cầu badge **trong trang hàng chờ**. Ghi 1 dòng vào SUMMARY là đã cân nhắc và cố ý bỏ qua.
  </action>
  <verify>
    <automated>cd apps/web && ../../node_modules/.bin/tsc --noEmit && ./node_modules/.bin/vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "REJECT_REASON_TEXT" apps/web/src/pages/OnlineOrdersQueuePage.tsx` = 1
    - `grep -c "Hết nguyên liệu" apps/web/src/pages/OnlineOrdersQueuePage.tsx` = 0 (không gõ lại chuỗi lý do)
    - `grep -c "khách KHÔNG thấy" apps/web/src/pages/OnlineOrdersQueuePage.tsx` = 1
    - `grep -c "dashed" apps/web/src/pages/OnlineOrdersQueuePage.tsx` = 1
    - `grep -cE "<dialog|Modal|createPortal" apps/web/src/pages/OnlineOrdersQueuePage.tsx` = 0 (inline, không modal)
    - `grep -c "/admin/online-orders" apps/web/src/App.tsx` = 1 và `grep -c "OnlineOrdersQueuePage" apps/web/src/App.tsx` = 2
    - Đọc `App.tsx` xác nhận route mới **không** nằm trong block `RoleGate allow={['admin']}`
    - **Chạy thật (3 kịch bản, ghi kết quả vào SUMMARY)**: `pnpm` hỏng nên chạy
      `cd apps/web && ./node_modules/.bin/vite` rồi mở `http://localhost:5173/admin/online-orders`:
      (a) đăng nhập role `kitchen` → vào được trang và thấy đủ 2 nút;
      (b) chưa bấm gì → banner đỏ "Chuông đang tắt" hiện; bấm "Bật chuông" → banner biến mất, submit
      1 đơn từ trang khách → nghe được tiếng và đơn hiện trong < 2s;
      (c) `kill` process API → sau ~10s chấm chuyển đỏ + banner "Mất kết nối"; bật lại API → tự nối
      lại, banner biến mất, danh sách được tải lại đầy đủ.
    - `cd apps/web && ../../node_modules/.bin/tsc --noEmit` exit 0 và `cd apps/web && ./node_modules/.bin/vitest run` exit 0
  </acceptance_criteria>
  <done>Từ chối đơn an toàn (2 khối tách biệt rõ ràng), trang vào được bằng cả 3 role, 3 kịch bản vận hành đã chạy thật.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ô "ghi chú nội bộ" ↔ ô "lý do gửi khách" | Ranh giới nằm trong **nhận thức của nhân viên** — gõ nhầm ô là rò chữ nội bộ tới khách (D-09) |
| trạng thái UI → hành vi thật của hệ thống | Trang trông "đang hoạt động" trong khi SSE chết hoặc chuông bị chặn = lỗi im lặng, đơn bị bỏ quên |
| RoleGate FE | Chỉ là tiện ích UX — chặn thật nằm ở `RequireRoles` phía BE |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-53 | Information Disclosure | Nhân viên gõ chữ nội bộ vào ô "Lý do gửi tới khách" → khách đọc được | mitigate | Ô lý do là **radio 5 lựa chọn soạn sẵn**, không phải free text; nhánh `OTHER` có chú thích "Nội dung này khách sẽ đọc được"; khối ghi chú nội bộ tách bằng nền xám + viền gạch đứt + nhãn 🔒. Hạng mục nghiệm thu tay đã ghi ở 09-VALIDATION § Manual-Only |
| T-09-54 | Repudiation | Đơn bị bỏ quên vì admin ngồi trước trang câm (audio bị chặn) | mitigate | Banner đỏ **chiếm chỗ** khi chưa unlock (D-03), không phải toast; `bell.unlock()` gọi trong chính `onClick`; trạng thái blocked kiểm bằng `ctx.state !== 'running'` |
| T-09-55 | Denial of Service | SSE chết im lặng → không bao giờ có đơn mới mà trang trông bình thường | mitigate | Heartbeat 15s từ BE + `connectionStateFrom` (có test) + chấm màu + banner khi `dead` (>10s); mỗi lần nối lại tự `loadQueue()` (D-06) |
| T-09-56 | Elevation of Privilege | Tin RoleGate của FE là lớp bảo vệ | accept | Chặn thật nằm ở `RequireRoles` phía BE (plan 09-07, có `curl` chứng minh 401/403). RoleGate FE chỉ để không hiện menu vô ích |
| T-09-57 | Denial of Service | Rò `EventSource`/`setInterval` mỗi lần điều hướng → nhiều stream song song, ngốn kết nối | mitigate | `subscribeOnlineOrders` trả hàm cleanup; **một** interval đồng hồ dùng chung cho cả trang; dọn trong `useEffect` cleanup; BE đếm + log subscriber (plan 09-07) |
| T-09-58 | Tampering | Ngưỡng cảnh báo đỏ hardcode 90 → chủ quán đổi setting mà UI không đổi, nhân viên tin sai | mitigate | Ngưỡng luôn lấy từ `escalate_sms_after_s` của response; grep `\b90\b` trong trang = 0 |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Plan này KHÔNG cài package nào cho `apps/web` — chuông dùng Web Audio API có sẵn, icon dùng emoji Unicode đúng quy ước hiện có |
</threat_model>

<verification>
1. `cd apps/web && ../../node_modules/.bin/tsc --noEmit` — sạch
2. `cd apps/web && ./node_modules/.bin/vitest run` — xanh
3. 3 kịch bản chạy thật (3 role vào được · chuông unlock + đơn < 2s · SSE đứt/nối lại)
4. Kiểm bảng hex: không có màu ngoài danh sách đóng của 09-UI-SPEC
5. `cd apps/api && ./node_modules/.bin/vitest run` — full suite BE vẫn xanh (plan này không đụng BE)
</verification>

<success_criteria>
- Trang `/admin/online-orders` vào được bằng cả 3 role, 2 nút luôn hiện
- Đơn mới hiện < 2s, có chuông (lặp 5 phút) và badge
- 2 banner chiếm chỗ cho 2 lỗi im lặng (chuông tắt / mất kết nối)
- Panel từ chối tách rõ 2 khối, nút disabled tới khi chọn lý do
- Ngưỡng đồng hồ đỏ lấy từ setting, không hardcode
- Không hex lạ, không token của `apps/shop`, không package mới
</success_criteria>

<output>
Create `.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-10-SUMMARY.md` when done
</output>
