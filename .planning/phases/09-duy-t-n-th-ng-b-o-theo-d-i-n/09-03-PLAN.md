---
phase: 09-duy-t-n-th-ng-b-o-theo-d-i-n
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/modules/tables/table-kind.ts
  - apps/api/src/modules/tables/tables.controller.ts
  - apps/api/src/common/run-with-retry.ts
  - apps/api/src/common/run-with-retry.test.ts
  - apps/api/src/modules/orders/orders.service.ts
  - apps/api/src/modules/admin-online-orders/table-assign.ts
  - apps/api/src/modules/admin-online-orders/table-assign.test.ts
autonomous: true
requirements: [REQ-M]

must_haves:
  truths:
    - "`KIND_FORMAT` tồn tại đúng MỘT bản trong repo; bàn tự tạo ở phase 9 và bàn tạo tay qua /tables sinh code y hệt nhau"
    - "Bàn tự tạo dùng chữ thường `ship-NN` / `mang-ve-NN` — KHÔNG phải `SHIP-NN`/`TAKE-NN` như văn xuôi spec"
    - "`runWithRetry` là hàm dùng chung export được; `OrdersService` dùng đúng hàm đó, không còn bản private riêng"
    - "Retry chỉ chạy lại với lỗi transient (deadlock / lock wait timeout / ER_LOCK); lỗi khác ném ngay, không nuốt"
    - "PICKUP → kind `takeaway`, DELIVERY → kind `delivery` (M2.D-14) — map ở đúng 1 chỗ"
  artifacts:
    - path: "apps/api/src/modules/tables/table-kind.ts"
      provides: "KIND_FORMAT + formatTableCode/formatTableName dùng chung"
      exports: ["KIND_FORMAT", "TABLE_KINDS", "formatTableCode", "formatTableName", "kindForFulfillment"]
    - path: "apps/api/src/common/run-with-retry.ts"
      provides: "Retry transient DB error dùng chung cho OrdersService và AdminOnlineOrdersService"
      exports: ["runWithRetry", "isTransientDbError"]
    - path: "apps/api/src/modules/admin-online-orders/table-assign.ts"
      provides: "Hàm thuần chọn bàn / sinh code bàn mới"
      exports: ["pickFreeTable", "nextTableCode"]
  key_links:
    - from: "apps/api/src/modules/tables/tables.controller.ts"
      to: "./table-kind.js"
      via: "import KIND_FORMAT (không còn const cục bộ)"
      pattern: "from './table-kind.js'"
    - from: "apps/api/src/modules/orders/orders.service.ts"
      to: "../../common/run-with-retry.js"
      via: "import runWithRetry (không còn private method)"
      pattern: "run-with-retry"
---

<objective>
Dọn 2 "tài sản tưởng dùng lại được nhưng thật ra không" trước khi viết logic duyệt đơn:
`KIND_FORMAT` đang là const **cục bộ** trong `tables.controller.ts`, và `runWithRetry()` đang là
method **private** của `OrdersService`. Rồi viết hàm thuần chọn bàn.

Purpose: RESEARCH Pitfall 2 và Pitfall 3 cảnh báo đúng 2 chỗ này. Nếu không xử lý tường minh, plan
09-06 sẽ hoặc (a) hardcode bản sao thứ hai của chuỗi `'ship'`/`'mang-ve'` — bàn tự tạo lệch bàn tạo
tay, hoặc (b) chép 18 dòng retry — 2 bản logic deadlock lệch nhau khi ai đó sửa 1 bản.

**Quyết định đã chốt tại plan này (RESEARCH để ngỏ cho planner):**
- `KIND_FORMAT`: **tách ra module riêng** `modules/tables/table-kind.ts` rồi import từ cả 2 nơi
  (không export từ controller — controller không phải chỗ chứa hằng số dùng chung).
- `runWithRetry`: **export thành hàm thuần** ở `common/run-with-retry.ts`; `OrdersService` xoá bản
  private và gọi hàm chung. Chọn DRY thay vì copy vì bản copy sẽ phân kỳ; rủi ro regression được
  chặn bằng full suite hiện có (106 test) chạy lại trong acceptance criteria.

Output: 2 module dùng chung + 1 hàm thuần chọn bàn + 2 file test.
</objective>

<execution_context>
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-CONTEXT.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-RESEARCH.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-PATTERNS.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-VALIDATION.md

<interfaces>
<!-- Trích nguyên văn từ code hiện tại. KHÔNG đi khám phá lại. -->

apps/api/src/modules/tables/tables.controller.ts dòng 52-61 — bản gốc cần DI CHUYỂN:
```typescript
const KIND_FORMAT: Record<string, { codePrefix: string; namePrefix: string }> = {
  'dine-in':  { codePrefix: 'ban',     namePrefix: 'Bàn' },
  'takeaway': { codePrefix: 'mang-ve', namePrefix: 'Mang về' },
  'delivery': { codePrefix: 'ship',    namePrefix: 'Ship' },
};
```
Dùng ở `bulkCreate()` (POST /tables/bulk) — sinh `ban-01`, `mang-ve-01`, `ship-01` (số 2 chữ số,
zero-pad) và name `"Bàn 01"`, `"Mang về 01"`, `"Ship 01"`.

apps/api/src/modules/orders/orders.service.ts dòng 187-204 — bản gốc cần DI CHUYỂN:
```typescript
private async runWithRetry<T>(fn: () => Promise<T>, maxAttempts: number): Promise<T> {
  let lastErr: unknown;
  for (let attempt = 1; attempt <= maxAttempts; attempt++) {
    try { return await fn(); }
    catch (err) {
      lastErr = err;
      const msg = (err as Error).message || '';
      const isTransient = /deadlock|lock wait timeout|ER_LOCK/i.test(msg);
      if (!isTransient || attempt === maxAttempts) throw err;
      this.logger.warn(`Transient DB error (attempt ${attempt}/${maxAttempts}): ${msg} — retry`);
      await new Promise((r) => setTimeout(r, 30 + Math.random() * 70));
    }
  }
  throw lastErr;
}
```
Gọi tại dòng 183-185: `getOrCreateOpenOrder(...)` → `return this.runWithRetry(() => this.getOrCreateOpenOrderImpl(...), 2)`.
⚠ `orders.service.ts` là file 1315 dòng đã được ghi nhận "Fragile Area" — chỉ sửa đúng 2 chỗ này,
không refactor gì khác.

apps/api/src/modules/tables/entities/restaurant-table.entity.ts:
  `{ id, code varchar(16) UNIQUE, name varchar(64), kind, x, y, is_active, kiotviet_locked, created_at }`
  ⚠ `code` chỉ **16 ký tự** — `mang-ve-` đã 8 ký tự, nên số thứ tự tối đa an toàn là 8 chữ số.

M2.D-14 (decisions.md dòng 73): PICKUP → cấp bàn `kind = 'takeaway'`; DELIVERY → `kind = 'delivery'`.

apps/api/src/modules/public/store-status.ts — khuôn mẫu module thuần (không Nest/TypeORM, tham số hoá).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tách KIND_FORMAT ra module dùng chung</name>
  <files>apps/api/src/modules/tables/table-kind.ts, apps/api/src/modules/tables/tables.controller.ts</files>
  <read_first>
    - apps/api/src/modules/tables/tables.controller.ts dòng 1-140 (const KIND_FORMAT + toàn bộ `bulkCreate` để biết cách zero-pad hiện tại)
    - apps/api/src/modules/tables/entities/restaurant-table.entity.ts
    - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-RESEARCH.md § Pitfall 3
  </read_first>
  <action>
    Tạo `apps/api/src/modules/tables/table-kind.ts` — module thuần (không Nest, không TypeORM):
    - Di chuyển `KIND_FORMAT` sang đây nguyên giá trị, thêm `export`.
    - `export const TABLE_KINDS = ['dine-in','takeaway','delivery'] as const` + type `TableKind`.
    - `export function formatTableCode(kind: string, num: number): string` — `${codePrefix}-${String(num).padStart(2,'0')}`.
      Chép **đúng** cách zero-pad mà `bulkCreate` đang dùng; nếu bản gốc pad khác thì theo bản gốc,
      mục tiêu là bàn tự tạo TRÙNG KHUÔN với bàn tạo tay.
    - `export function formatTableName(kind: string, num: number): string` — `${namePrefix} ${padStart(2,'0')}`.
    - `export function kindForFulfillment(ft: 'PICKUP'|'DELIVERY'): TableKind` — PICKUP→`'takeaway'`,
      DELIVERY→`'delivery'` (M2.D-14). Đây là map duy nhất trong repo, plan 09-06 gọi hàm này.
    - Docblock đầu file phải ghi: "Quy ước code bàn là CHỮ THƯỜNG `ban-NN`/`mang-ve-NN`/`ship-NN`.
      Văn xuôi `docs/MILESTONE-02-ONLINE-ORDERING-SPEC.md` §7 ghi `SHIP-NN`/`TAKE-NN` — đó là mô tả
      viết trước khi code hoá, KHÔNG phải quy ước thật. Thấy chuỗi chữ hoa trong code mới = dấu hiệu
      đọc nhầm spec." Ghi thêm giới hạn `code` 16 ký tự.

    Sửa `tables.controller.ts`: xoá const cục bộ, `import { KIND_FORMAT, formatTableCode, formatTableName } from './table-kind.js';`
    và dùng 2 hàm mới trong `bulkCreate` thay cho việc nội suy chuỗi tại chỗ. **Không đổi hành vi
    endpoint** — code/name sinh ra phải y hệt trước.
  </action>
  <verify>
    <automated>cd apps/api && ../../node_modules/.bin/tsc --noEmit && ./node_modules/.bin/vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const KIND_FORMAT" apps/api/src/modules/tables/tables.controller.ts` = 0
    - `grep -c "export const KIND_FORMAT" apps/api/src/modules/tables/table-kind.ts` = 1
    - `grep -rn "codePrefix" apps/api/src --include=*.ts | grep -v table-kind.ts | wc -l` = 0 (chỉ 1 bản)
    - `grep -cE "'SHIP-|'TAKE-|\"SHIP-|\"TAKE-" apps/api/src/modules/tables/table-kind.ts` = 0
    - `grep -c "kindForFulfillment" apps/api/src/modules/tables/table-kind.ts` = 1
    - **Không hồi quy**: `curl -X POST http://localhost:3001/tables/bulk -H 'Content-Type: application/json' --cookie "<admin cookie>" -d '{"kind":"delivery","from_num":90,"to_num":91}'` → tạo `ship-90`, `ship-91` (đúng chữ thường), rồi xoá 2 bàn test đó
    - `cd apps/api && ./node_modules/.bin/vitest run` full suite exit 0
  </acceptance_criteria>
  <done>Repo có đúng 1 bản quy ước đặt tên bàn; plan 09-06 import được thay vì hardcode bản sao.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Export runWithRetry thành hàm dùng chung, OrdersService dùng lại</name>
  <files>apps/api/src/common/run-with-retry.ts, apps/api/src/common/run-with-retry.test.ts, apps/api/src/modules/orders/orders.service.ts</files>
  <read_first>
    - apps/api/src/modules/orders/orders.service.ts dòng 175-290 (private runWithRetry + getOrCreateOpenOrder + getOrCreateOpenOrderImpl)
    - apps/api/src/common/origin-allowlist.test.ts (khuôn mẫu test hàm thuần trong `src/common`)
    - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-RESEARCH.md § Pitfall 2
  </read_first>
  <behavior>
    `run-with-retry.test.ts` (không cần DB) phủ:
    - Hàm thành công ngay lần 1 → gọi đúng 1 lần, trả giá trị
    - Ném `Error('Deadlock found when trying to get lock')` lần 1, thành công lần 2 → gọi 2 lần, trả giá trị
    - Ném lỗi transient ở cả 2 lần với `maxAttempts = 2` → ném ra lỗi cuối, gọi đúng 2 lần
    - Ném `Error('ER_LOCK_WAIT_TIMEOUT: Lock wait timeout exceeded')` → coi là transient
    - Ném `Error('Duplicate entry ... for key code')` → **KHÔNG** retry, ném ngay, gọi đúng 1 lần
    - Ném `Error('')` (message rỗng) → không retry, không crash
    - Ném giá trị không phải Error (vd string) → không retry, ném nguyên vật thể đó
    - `isTransientDbError` trả `true`/`false` đúng cho 4 chuỗi mẫu ở trên (test riêng, không qua wrapper)
    - Có `onRetry` callback tuỳ chọn: được gọi đúng 1 lần với `(attempt, message)` khi retry
    - Thời gian chờ giữa 2 lần nằm trong 30–100 ms (dùng fake timer hoặc bơm `sleepFn` giả — chọn
      cách bơm `sleepFn` để test chạy tức thì, không treo 100ms thật)
  </behavior>
  <action>
    Tạo `apps/api/src/common/run-with-retry.ts` — module thuần, không import Nest/TypeORM:
    - `export function isTransientDbError(err: unknown): boolean` — `/deadlock|lock wait timeout|ER_LOCK/i`
      test trên `(err as Error)?.message ?? ''`. Giữ **nguyên regex cũ**: nó đã được chỉnh khớp đúng
      message MySQL2/TypeORM thật trong repo này (RESEARCH § Don't Hand-Roll: không thay bằng `p-retry`).
    - `export async function runWithRetry<T>(fn: () => Promise<T>, maxAttempts: number, opts?: {
      onRetry?: (attempt: number, message: string) => void;
      sleepFn?: (ms: number) => Promise<void>;
      }): Promise<T>` — logic y hệt bản private (xem `<interfaces>`), thay `this.logger.warn` bằng
      `opts?.onRetry`, thay `setTimeout` bằng `opts?.sleepFn ?? defaultSleep`.
    - Docblock: nguồn gốc là `OrdersService` (Milestone 1), tách ra ở phase 9 để
      `AdminOnlineOrdersService` (cấp bàn, M2.D-06) dùng chung — sửa regex ở đây là đổi hành vi của
      cả 2 luồng transaction quan trọng nhất hệ thống.

    Sửa `apps/api/src/modules/orders/orders.service.ts`, **đúng 2 điểm, không đụng gì khác**:
    1. Xoá method `private async runWithRetry(...)` (dòng ~187-204).
    2. `getOrCreateOpenOrder` gọi hàm import:
       `return runWithRetry(() => this.getOrCreateOpenOrderImpl(table_id, creator), 2,
         { onRetry: (a, m) => this.logger.warn(\`Transient DB error (attempt ${a}/2): ${m} — retry\`) })`
       Giữ nguyên câu log để log production không đổi hình dạng.
    Nếu grep thấy `this.runWithRetry(` ở chỗ khác trong file thì sửa hết theo cùng khuôn.

    Viết `run-with-retry.test.ts` phủ đủ `<behavior>`.
  </action>
  <verify>
    <automated>cd apps/api && ./node_modules/.bin/vitest run src/common/run-with-retry.test.ts && ./node_modules/.bin/vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "private async runWithRetry" apps/api/src/modules/orders/orders.service.ts` = 0
    - `grep -c "this.runWithRetry" apps/api/src/modules/orders/orders.service.ts` = 0
    - `grep -c "run-with-retry" apps/api/src/modules/orders/orders.service.ts` = 1
    - `grep -c "deadlock|lock wait timeout|ER_LOCK" apps/api/src/common/run-with-retry.ts` = 1
    - `grep -cE "from '@nestjs|from 'typeorm" apps/api/src/common/run-with-retry.ts` = 0
    - `cd apps/api && ./node_modules/.bin/vitest run src/common/run-with-retry.test.ts` exit 0 với ≥ 10 test pass
    - **Không hồi quy**: `cd apps/api && ./node_modules/.bin/vitest run` full suite exit 0 (106 test cũ + test mới)
  </acceptance_criteria>
  <done>Một bản retry duy nhất, có test riêng lần đầu tiên; `AdminOnlineOrdersService` import được mà không chạm `OrdersService`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: table-assign.ts — hàm thuần chọn bàn trống / sinh code bàn mới</name>
  <files>apps/api/src/modules/admin-online-orders/table-assign.ts, apps/api/src/modules/admin-online-orders/table-assign.test.ts</files>
  <read_first>
    - apps/api/src/modules/tables/table-kind.ts (Task 1 — hàm vừa tạo)
    - apps/api/src/modules/public/store-status.ts (khuôn mẫu module thuần)
    - docs/MILESTONE-02-ONLINE-ORDERING-SPEC.md dòng 472-487 (pseudo-code cấp bàn)
  </read_first>
  <behavior>
    `table-assign.test.ts` (không cần DB) phủ:
    - `pickFreeTable([{code:'ship-02'},{code:'ship-01'}])` → trả bàn `ship-01` (sắp theo `code` ASC,
      KHÔNG tin thứ tự đầu vào)
    - Danh sách rỗng → trả `null` (gọi ý cho service: phải tự tạo bàn)
    - `nextTableCode('delivery', [])` → `'ship-01'`
    - `nextTableCode('delivery', ['ship-01','ship-02'])` → `'ship-03'`
    - `nextTableCode('delivery', ['ship-01','ship-09','ship-10'])` → `'ship-11'` (so sánh theo SỐ,
      không theo chuỗi — `'ship-9' > 'ship-10'` nếu so chuỗi là bug)
    - `nextTableCode('takeaway', ['mang-ve-07'])` → `'mang-ve-08'` (đúng tiền tố chữ thường)
    - `nextTableCode` bỏ qua code không khớp tiền tố (`['ban-01','ship-03']` với kind delivery → `'ship-04'`)
    - `nextTableCode` bỏ qua code khớp tiền tố nhưng phần đuôi không phải số (`['ship-abc']` → `'ship-01'`)
    - `nextTableCode` với 99 bàn (`ship-99`) → `'ship-100'` (không pad quá 2 chữ số khi đã ≥ 100)
    - Code sinh ra luôn ≤ 16 ký tự (giới hạn cột) — có test khẳng định cho cả 2 kind
    - `kindForFulfillment('PICKUP')` = `'takeaway'`, `kindForFulfillment('DELIVERY')` = `'delivery'`
      (test lại ở đây để M2.D-14 có bằng chứng trong đúng module dùng nó)
  </behavior>
  <action>
    Tạo thư mục `apps/api/src/modules/admin-online-orders/` và file `table-assign.ts` — module thuần:
    - `export type FreeTableCandidate = { id: string; code: string; name: string }`
    - `export function pickFreeTable(candidates: FreeTableCandidate[]): FreeTableCandidate | null` —
      sắp `code` ASC bằng `localeCompare` rồi trả phần tử đầu; rỗng → `null`. (Service sẽ đã
      `ORDER BY code ASC LIMIT 1 FOR UPDATE` ở SQL; hàm này là lưới an toàn thứ hai + chỗ để test
      quy tắc "bàn nhỏ nhất trước" mà không cần DB — M2.D-04.)
    - `export function nextTableCode(kind: string, existingCodes: string[]): string` — lọc code khớp
      `^{codePrefix}-(\d+)$`, lấy `max(number)`, `+1`, format qua `formatTableCode`. Không có code nào
      khớp → số 1.
    - Re-export `kindForFulfillment` từ `../tables/table-kind.js` để plan 09-06 chỉ cần import 1 chỗ.
    - Docblock: hàm này KHÔNG tự quyết bàn nào đang bận — đó là việc của câu `FOR UPDATE` trong
      transaction (plan 09-06). Ở đây chỉ có quy tắc sắp xếp + đặt tên.

    Viết `table-assign.test.ts` phủ đủ `<behavior>`.
  </action>
  <verify>
    <automated>cd apps/api && ./node_modules/.bin/vitest run src/modules/admin-online-orders/table-assign.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "from '@nestjs|from 'typeorm" apps/api/src/modules/admin-online-orders/table-assign.ts` = 0
    - `grep -cE "'ship-|'mang-ve-" apps/api/src/modules/admin-online-orders/table-assign.ts` = 0 (chuỗi tiền tố CHỈ ở `table-kind.ts`)
    - `cd apps/api && ./node_modules/.bin/vitest run src/modules/admin-online-orders/table-assign.test.ts` exit 0 với ≥ 11 test pass
    - `cd apps/api && ../../node_modules/.bin/tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Quy tắc "bàn trống nhỏ nhất trước, hết thì sinh số kế tiếp" có bằng chứng tự động, dùng đúng quy ước đặt tên chung.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 2 transaction đồng thời → 1 bàn | Ranh giới đồng thời quan trọng nhất của phase (M2.D-06). Plan này chuẩn bị công cụ; chứng minh ở 09-06/09-08 |
| code bàn sinh tự động → cột `code` UNIQUE 16 ký tự | Sinh sai độ dài/định dạng = insert fail giữa transaction duyệt đơn, khách bị treo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Tampering | Bản sao thứ hai của quy ước đặt tên bàn phân kỳ → bàn tự tạo lệch bàn tạo tay, sơ đồ bàn hỏng | mitigate | `KIND_FORMAT` chỉ tồn tại ở `table-kind.ts`; acceptance criteria đếm `codePrefix` toàn repo = 1 chỗ |
| T-09-11 | Denial of Service | `nextTableCode` sinh code > 16 ký tự → INSERT fail, đơn khách không duyệt được (vi phạm M2.D-05 "khách không bao giờ bị chặn") | mitigate | Test khẳng định độ dài ≤ 16 cho cả 2 kind; docblock ghi giới hạn cột |
| T-09-12 | Tampering | Retry nuốt lỗi không-transient (vd `Duplicate entry`) → lặp lại thao tác ghi và tạo dữ liệu trùng | mitigate | `isTransientDbError` giữ nguyên regex đã khớp thực tế; test có case `Duplicate entry` khẳng định KHÔNG retry |
| T-09-13 | Repudiation | Refactor `orders.service.ts` (Fragile Area) làm hỏng luồng mở đơn của Milestone 1 mà không ai biết | mitigate | Chỉ sửa đúng 2 điểm; acceptance criteria bắt chạy lại toàn bộ 106 test cũ và `POST /tables/bulk` thật |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Plan này KHÔNG cài package nào |
</threat_model>

<verification>
1. `cd apps/api && ./node_modules/.bin/vitest run` — full suite xanh (106 test cũ + ≥ 21 test mới)
2. `cd apps/api && ../../node_modules/.bin/tsc --noEmit` — sạch
3. `grep -rn "codePrefix" apps/api/src --include=*.ts` — chỉ 1 file
4. `POST /tables/bulk` thật vẫn sinh `ship-NN` chữ thường như trước
</verification>

<success_criteria>
- Quy ước đặt tên bàn và logic retry mỗi thứ tồn tại đúng 1 bản, có test riêng
- `OrdersService` không còn method `runWithRetry` private, hành vi log giữ nguyên
- `pickFreeTable`/`nextTableCode`/`kindForFulfillment` sẵn sàng cho plan 09-06
- Không hồi quy: full suite + endpoint tạo bàn hàng loạt vẫn đúng
</success_criteria>

<output>
Create `.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-03-SUMMARY.md` when done
</output>
