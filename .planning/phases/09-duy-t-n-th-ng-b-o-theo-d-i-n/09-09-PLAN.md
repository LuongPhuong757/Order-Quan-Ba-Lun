---
phase: 09-duy-t-n-th-ng-b-o-theo-d-i-n
plan: 09
type: execute
wave: 5
depends_on: ["09-01", "09-04", "09-05", "09-07"]
files_modified:
  - packages/schemas/src/public-orders.ts
  - apps/api/src/modules/public/public-orders.service.ts
  - apps/api/src/modules/public/public.module.ts
  - apps/api/src/modules/public/public-orders.test.ts
autonomous: true
requirements: [REQ-N, REQ-O]

must_haves:
  truths:
    - "`GET /api/public/orders/:token` trả `percent`/`stage`/`stage_label`/`cancelled_count` đúng công thức §6"
    - "`percent` KHÔNG BAO GIỜ tụt giữa 2 lần gọi — đã persist `max_progress_shown`"
    - "Response TUYỆT ĐỐI không chứa `status` của từng item (M2.D-23, điều kiện của G-1) — có test assert"
    - "Response TUYỆT ĐỐI không chứa `internal_reject_note` (D-09) — có test assert"
    - "Sau khi duyệt, danh sách món và tổng tiền phản ánh `order_items` THẬT (admin sửa món thì khách thấy — M2.D-47), món bị huỷ trừ khỏi danh sách và hiện thành dòng cảnh báo (M2.D-21)"
    - "Lúc submit, 3 hàng outbox L1/L2/L3 được ghi TRONG CÙNG transaction với đơn, và event `online_order.new` được emit sau commit"
  artifacts:
    - path: "packages/schemas/src/public-orders.ts"
      provides: "PublicOrderStatus mở rộng đúng whitelist §6"
      contains: "stage_label"
    - path: "apps/api/src/modules/public/public-orders.service.ts"
      provides: "getByToken() tính % + persist đơn điệu; submit() enqueue outbox + emit SSE"
      contains: "computeProgress"
    - path: "apps/api/src/modules/public/public-orders.test.ts"
      provides: "Assert shape + assert 2 field cấm + assert đơn điệu"
  key_links:
    - from: "apps/api/src/modules/public/public-orders.service.ts"
      to: "order-progress.computeProgress"
      via: "import từ ./order-progress.js"
      pattern: "computeProgress"
    - from: "apps/api/src/modules/public/public-orders.service.ts"
      to: "NotificationOutboxService.enqueueForNewRequest"
      via: "gọi trong transaction submit (outbox pattern)"
      pattern: "enqueueForNewRequest"
    - from: "apps/api/src/modules/public/public-orders.service.ts"
      to: "EventEmitter2 'online_order.new'"
      via: "emit sau commit"
      pattern: "online_order.new"
---

<objective>
Hoàn thiện 2 đầu công khai của phase: `GET /api/public/orders/:token` trả đủ % + 5 mốc + cảnh báo món
huỷ (REQ-O), và luồng submit của phase 8 nay xếp hàng thông báo + bắn event SSE (REQ-N).

Purpose: Criterion 5 của ROADMAP nằm trọn ở đây, kèm **hard gate G-1**: response tuyệt đối không
chứa trạng thái từng món (M2.D-23). Đồng thời đóng khe hở "đơn đã vào DB nhưng chưa có lịch SMS" —
outbox phải ghi **trong cùng transaction** với đơn.

Output: `PublicOrderStatus` mở rộng, `getByToken()` viết lại phần dựng payload, `submit()` thêm 2
việc, và test mở rộng có assert whitelist.
</objective>

<execution_context>
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-CONTEXT.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-RESEARCH.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-PATTERNS.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-UI-SPEC.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-VALIDATION.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-01-SUMMARY.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-05-SUMMARY.md

<interfaces>
<!-- Trích sẵn. KHÔNG đi khám phá lại. -->

packages/schemas/src/public-orders.ts — bản HIỆN TẠI (phase 8), cần MỞ RỘNG:
```typescript
export const PublicOrderStatus = z.object({
  order_token: z.string(),
  status: z.enum(['WAITING','CONFIRMED','REJECTED','CANCELLED_BY_CUSTOMER']),
  fulfillment_type: z.enum(['PICKUP','DELIVERY']),
  items: z.array(z.object({ name: z.string(), qty: z.number().int().positive(),
                            unit_price: z.number().int().nonnegative() })),
  subtotal: z.number().int().nonnegative(),
  submitted_at_ms: z.number().int(),
  store_phone: z.string(),
  reject_reason: z.string().nullable(),
});
```
⚠ `.strict().parse()` được gọi ở `getByToken` — thêm field vào schema mà service không cung cấp
  (hoặc ngược lại) là **500 ngay lập tức**. Sửa schema và service trong CÙNG plan này là cố ý.

apps/api/src/modules/public/public-orders.service.ts dòng 112-129 — `getByToken` hiện tại:
```typescript
const row = await this.orderRepo.findOne({ where: { order_token: token } });   // orderRepo = OnlineOrderRequest repo
if (!row) throw new NotFoundException({ code: 'ORDER_TOKEN_NOT_FOUND', message: 'Không tìm thấy đơn này.' });
const settings = await this.settingsSvc.readAll();
const shaped = { order_token, status, fulfillment_type,
  items: row.items_snapshot.map((it) => ({ name: it.name, qty: it.qty, unit_price: it.unit_price })),
  subtotal, submitted_at_ms: row.submitted_at, store_phone, reject_reason };
return PublicOrderStatus.strict().parse(shaped);
```
`submit()` hiện bọc `this.ds.transaction(async (mgr) => submitOrder(this.makeDeps(mgr), input, ctx))`
và `submitOrder` trả `{ order_token, distance_km }` (KHÔNG trả id). Lấy id trong cùng transaction bằng
`SELECT id FROM online_order_requests WHERE order_token = ?`.

apps/api/src/modules/public/order-progress.ts (plan 09-01):
  `computeProgress({ request_status, fulfillment_type, item_states, max_progress_shown })`
    → `{ stage, percent, cancelled_count, cancelled_note, all_done }`
  `stageLabel(stage, fulfillment_type)` · `STAGE_LABEL_CANCELLED_BY_CUSTOMER`
  `EXCLUDED_ITEM_STATES = ['CANCELLED','OUT_OF_STOCK']`

apps/api/src/modules/notifications/notification-outbox.service.ts (plan 09-05):
  `enqueueForNewRequest(requestId, nowMs, mgr?)` — nhận `mgr` để chạy trong transaction

Entity (plan 09-04): `Order.order_token` (unique), `Order.ship_fee`, `Order.updated_at`;
  `OrderItem.state` / `menu_item_name` / `menu_item_price` / `qty` / `is_note`;
  `OnlineOrderRequest.max_progress_shown` / `internal_reject_note`.

SettingsService.readAll(): `eta_pickup_min/max`, `eta_delivery_min/max`, `store_phone`.

apps/api/src/modules/public/public-orders.test.ts (phase 8, 19 test) — MỞ RỘNG, không tạo file mới.
apps/api/src/modules/public/public-menu-shape.test.ts — khuôn assert whitelist bằng `Object.keys`.

M2.D-21: món huỷ **phải** hiện — ngoại lệ bắt buộc của G-1, che là lừa khách.
M2.D-23: response TUYỆT ĐỐI không chứa `status` từng item.
M2.D-47: admin sửa món ở bàn ship → khách thấy món + tổng tiền mới.
M2.D-45/D-46: sau CONFIRMED khách không tự sửa được, chỉ hiện SĐT quán + nút gọi (FE làm ở 09-11 —
  BE chỉ cần luôn trả `store_phone`, đã có).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mở rộng PublicOrderStatus + getByToken tính % / 5 mốc / món huỷ, persist đơn điệu</name>
  <files>packages/schemas/src/public-orders.ts, apps/api/src/modules/public/public-orders.service.ts, apps/api/src/modules/public/public-orders.test.ts</files>
  <read_first>
    - packages/schemas/src/public-orders.ts (toàn bộ)
    - apps/api/src/modules/public/public-orders.service.ts (toàn bộ — đặc biệt `getByToken` và các repo đang inject)
    - apps/api/src/modules/public/order-progress.ts (plan 09-01)
    - apps/api/src/modules/public/public-menu-shape.test.ts (khuôn assert whitelist)
    - apps/api/src/modules/public/public-orders.test.ts (19 test có sẵn — mở rộng, không đập đi)
    - docs/MILESTONE-02-ONLINE-ORDERING-SPEC.md dòng 431-454 (shape response + 5 mốc)
  </read_first>
  <behavior>
    Mở rộng `public-orders.test.ts` (dùng fake repository như 19 test có sẵn, KHÔNG cần MySQL) với:
    - Đơn `WAITING` (chưa có `order_id`) → `stage='RECEIVED'`, `percent=0`, `items` lấy từ
      `items_snapshot`, `subtotal` = subtotal đã chốt
    - Đơn `CONFIRMED` có 2 `order_items` state `KITCHEN` → `percent=15`, `stage='CONFIRMED'`,
      `items` lấy từ **`order_items`** (tên/giá/sl của bản THẬT), không phải snapshot
    - Admin sửa món (order_items khác snapshot: bớt 1 món, thêm 1 món) → `items` và `subtotal` phản
      ánh `order_items` (M2.D-47)
    - 1 item `CANCELLED` → item đó **không** có trong `items`, `cancelled_count=1`,
      `cancelled_note` = "1 món đã huỷ — quán sẽ liên hệ bạn" (M2.D-21)
    - **G-1 hard gate**: với mọi trường hợp trên, `JSON.stringify(response)` **không** chứa chuỗi
      `"state"` và mỗi phần tử `items[i]` có đúng `Object.keys(...)` = `['name','qty','unit_price']`
    - **D-09 gate**: đơn `REJECTED` có `internal_reject_note = 'khách hàng này hay bom hàng'` →
      `JSON.stringify(response)` **không** chứa chuỗi `'bom hàng'`; `reject_reason` = câu soạn sẵn
    - **Đơn điệu + persist**: gọi lần 1 với items ở `READY` (percent 80) → trả 80 và **gọi hàm ghi**
      `max_progress_shown = 80` (assert fake repo nhận lệnh update); gọi lần 2 khi 1 item bị trả về
      `KITCHEN` (percent tính ra 47) → trả **80**, và **KHÔNG** gọi update (không ghi đè xuống thấp hơn)
    - **Không ghi thừa**: percent không đổi giữa 2 lần gọi → không phát sinh lệnh update (chống ghi
      DB mỗi 5-10s khi khách để trang mở)
    - `eta_min`/`eta_max` = cấu hình theo `fulfillment_type`; `status='REJECTED'` hoặc
      `stage='COMPLETED'` → cả hai `null`
    - `updated_at_ms` = `order.updated_at` khi đã có order; = `request.submitted_at` khi còn WAITING
    - Token không tồn tại → vẫn `404 ORDER_TOKEN_NOT_FOUND` (không hồi quy phase 8)
  </behavior>
  <action>
    **`packages/schemas/src/public-orders.ts`** — mở rộng `PublicOrderStatus` đúng whitelist §6, giữ
    nguyên 8 field cũ và thêm:
    `stage: z.enum(['RECEIVED','CONFIRMED','COOKING','DELIVERING','READY_FOR_PICKUP','COMPLETED','REJECTED'])`,
    `stage_label: z.string()`, `percent: z.number().int().min(0).max(100)`,
    `cancelled_count: z.number().int().nonnegative()`, `cancelled_note: z.string().nullable()`,
    `eta_min: z.number().int().nullable()`, `eta_max: z.number().int().nullable()`,
    `updated_at_ms: z.number().int()`.
    ⚠ **KHÔNG** thêm field nào khác. Cập nhật docblock: "`.strict()` là lưới an toàn cuối cùng cho
    G-1 — nếu service lỡ trả `status` từng item hay `internal_reject_note`, parse sẽ throw thay vì
    leak. Đừng bao giờ đổi `.strict()` thành `.passthrough()`."
    Rồi `cd packages/schemas && ../../node_modules/.bin/tsc`.

    **`public-orders.service.ts` — viết lại phần dựng payload của `getByToken`:**
    1. Tìm request theo `order_token` (giữ nguyên 404).
    2. Nếu `request.order_id` khác null → đọc `Order` + `order_items` của nó
       (`mgr`/repo mới: cần inject `@InjectRepository(Order)` và `@InjectRepository(OrderItem)`;
       thêm 2 entity vào `TypeOrmModule.forFeature` của `public.module.ts`).
       Bỏ dòng `is_note = true` khỏi danh sách khách thấy (ghi chú cho bếp, không phải món khách đặt).
    3. `item_states` = `state` của các item (chỉ dùng để TÍNH, không bao giờ đưa ra response).
       `computeProgress({ request_status: request.status, fulfillment_type, item_states,
       max_progress_shown: request.max_progress_shown })`.
    4. **Persist đơn điệu**: nếu `percent > request.max_progress_shown` thì
       `UPDATE online_order_requests SET max_progress_shown = ? WHERE id = ?`. Chỉ ghi khi tăng —
       tránh 1 lệnh UPDATE mỗi 5-10s cho mỗi khách đang mở trang.
    5. Dựng payload **whitelist tường minh** (không spread entity):
       - `items`: nếu có order → map từ `order_items` đã lọc bỏ `EXCLUDED_ITEM_STATES` và `is_note`,
         mỗi dòng `{ name: menu_item_name, qty, unit_price: menu_item_price }`;
         nếu chưa có order → map từ `items_snapshot` như hiện tại.
       - `subtotal`: nếu có order → `Σ unit_price × qty` của chính danh sách vừa dựng (M2.D-47 —
         khách thấy tổng tiền MỚI sau khi quán sửa đơn); nếu chưa → `request.subtotal`.
         ⚠ **KHÔNG** cộng `ship_fee` vào `subtotal` (M2.D-62 — subtotal là tiền món).
       - `stage`, `percent`, `cancelled_count`, `cancelled_note` từ `computeProgress`.
       - `stage_label`: `stageLabel(stage, fulfillment_type)`, riêng
         `status === 'CANCELLED_BY_CUSTOMER'` dùng `STAGE_LABEL_CANCELLED_BY_CUSTOMER`.
       - `eta_min`/`eta_max`: theo `fulfillment_type` từ settings; `null` khi `REJECTED`/`COMPLETED`.
       - `updated_at_ms`: `order?.updated_at ?? request.submitted_at`.
       - `reject_reason`: giữ nguyên `request.reject_reason`.
         ⚠ **TUYỆT ĐỐI KHÔNG** đọc `request.internal_reject_note` ở hàm này. Ghi comment cấm tại chỗ.
    6. `return PublicOrderStatus.strict().parse(shaped)`.

    Mở rộng `public-orders.test.ts` phủ đủ `<behavior>`; giữ nguyên 19 test cũ.
  </action>
  <verify>
    <automated>cd packages/schemas && ../../node_modules/.bin/tsc && cd ../../apps/api && ./node_modules/.bin/vitest run src/modules/public/public-orders.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "stage_label" packages/schemas/src/public-orders.ts` = 1
    - `grep -c "PublicOrderStatus.strict().parse" apps/api/src/modules/public/public-orders.service.ts` = 1
    - `grep -c "internal_reject_note" apps/api/src/modules/public/public-orders.service.ts` = 0
    - `grep -c "computeProgress" apps/api/src/modules/public/public-orders.service.ts` = 1
    - `grep -c "max_progress_shown" apps/api/src/modules/public/public-orders.service.ts` ≥ 2
    - `cd apps/api && ./node_modules/.bin/vitest run src/modules/public/public-orders.test.ts` exit 0 với ≥ 32 test pass (19 cũ + ≥ 13 mới)
    - **Chạy thật — G-1**: `curl -s http://localhost:3001/api/public/orders/<token của 1 đơn đã CONFIRMED> | grep -c '"state"'` = **0**, và body có `percent`/`stage`/`stage_label`
    - **Chạy thật — đơn điệu**: gọi 2 lần cách nhau sau khi bếp trả 1 món về `KITCHEN` → `percent` lần 2 **≥** lần 1; `SELECT max_progress_shown FROM online_order_requests WHERE order_token=?` khớp giá trị cao nhất
    - **Chạy thật — D-09**: từ chối 1 đơn với ghi chú nội bộ chứa chuỗi `ZZTEST`, rồi
      `curl -s http://localhost:3001/api/public/orders/<token> | grep -c ZZTEST` = **0**
    - `cd apps/api && ./node_modules/.bin/vitest run` full suite exit 0
  </acceptance_criteria>
  <done>Criterion 5 có bằng chứng ở cả 3 lớp: hàm thuần (09-01), service test (ở đây), và curl thật; 2 field cấm được chặn bằng test chứ không bằng lời hứa.</done>
</task>

<task type="auto">
  <name>Task 2: Submit đơn — xếp hàng outbox L1/L2/L3 trong cùng transaction + emit `online_order.new`</name>
  <files>apps/api/src/modules/public/public-orders.service.ts, apps/api/src/modules/public/public.module.ts</files>
  <read_first>
    - apps/api/src/modules/public/public-orders.service.ts (hàm `submit` + `makeDeps`)
    - apps/api/src/modules/public/public.module.ts
    - apps/api/src/modules/notifications/notification-outbox.service.ts (plan 09-05 — `enqueueForNewRequest(requestId, nowMs, mgr?)`)
    - apps/api/src/modules/audit/audit.interceptor.ts dòng 100-112 (khuôn `emitter.emit` fire-and-forget)
    - docs/MILESTONE-02-ONLINE-ORDERING-SPEC.md dòng 464-470 (**bỏ L4 theo D-12**)
  </read_first>
  <action>
    Sửa `PublicOrdersService`:
    - Inject thêm `NotificationOutboxService` và `EventEmitter2`. `public.module.ts` thêm
      `NotificationsModule` vào `imports` và `Order`, `OrderItem` vào `TypeOrmModule.forFeature`
      (cần cho Task 1).
    - Trong `submit()`, **bên trong** `this.ds.transaction(...)`, ngay sau khi `submitOrder(...)` trả
      về: `const [{ id }] = await mgr.query('SELECT id FROM online_order_requests WHERE order_token = ?', [result.order_token])`
      rồi `await this.outbox.enqueueForNewRequest(id, ctx.nowMs, mgr)`.
      Ghi comment giải thích **outbox pattern**: hàng thông báo phải commit cùng đơn — nếu enqueue
      nằm ngoài transaction thì có cửa sổ "đơn đã vào DB mà không ai được báo", đúng thứ mà REQ-N
      sinh ra để chống.
      Không đổi chữ ký `submitOrder` (module thuần của phase 8 giữ nguyên — plan 09-12 còn phải sửa
      file đó, tránh đụng độ).
    - **Sau khi transaction commit**, emit fire-and-forget:
      `this.emitter.emit('online_order.new', { type: 'new', request_id: id, at_ms: Date.now() })`.
      Bọc try/catch nuốt lỗi — emit fail KHÔNG được làm hỏng response 201 của khách.
      ⚠ Emit **sau** commit, không trong transaction: nếu emit trong transaction rồi transaction
      rollback, admin sẽ thấy 1 đơn ma.
    - Payload emit phải khớp `OnlineOrderStreamEvent` (plan 09-01) vì controller SSE (plan 09-07)
      `.strict().parse()` nó trước khi đẩy ra stream.

    **KHÔNG** thêm hàng outbox nào cho khách (D-10 — khách không nhận SMS).
  </action>
  <verify>
    <automated>cd apps/api && ../../node_modules/.bin/tsc --noEmit && ./node_modules/.bin/vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "enqueueForNewRequest" apps/api/src/modules/public/public-orders.service.ts` = 1
    - `grep -c "online_order.new" apps/api/src/modules/public/public-orders.service.ts` = 1
    - `grep -c "NotificationsModule" apps/api/src/modules/public/public.module.ts` = 2
    - **Chạy thật — outbox ghi cùng đơn**: submit 1 đơn qua `POST /api/public/orders` →
      `SELECT level, channel, status, TIMESTAMPDIFF(SECOND, (SELECT submitted_at FROM online_order_requests WHERE id = o.request_id), o.scheduled_at) AS delay_s FROM notification_outbox o WHERE o.request_id = '<id>'`
      → có 1 dòng `L1/SSE delay_s=0`, 1 dòng `L3/EMAIL delay_s=0`, và `L2/SMS delay_s=90`
      (đúng `escalate_sms_after_s`). Dán output vào SUMMARY.
    - **Chạy thật — không có L4**: `SELECT COUNT(*) FROM notification_outbox WHERE level='L4'` = 0
    - **Chạy thật — SSE nhận được**: giữ `curl -N .../admin/online-orders/stream` (cookie admin), submit
      1 đơn → dòng `data:` có `"type":"new"` xuất hiện trong **< 2s** (bấm giờ, ghi số đo vào SUMMARY —
      đây là criterion 1 của ROADMAP)
    - **Chạy thật — SMS bắn khi quá 90s**: submit 1 đơn, KHÔNG duyệt, chờ ~110s → log API có dòng
      `[SMS:console]`, `SELECT status FROM notification_outbox WHERE request_id=? AND level='L2'` = `SENT`
    - **Chạy thật — duyệt kịp thì KHÔNG bắn**: submit đơn khác, duyệt trong vòng 30s, chờ thêm 90s →
      `SELECT status ... level='L2'` = `CANCELLED`, log KHÔNG có `[SMS:console]` cho đơn đó
    - `cd apps/api && ./node_modules/.bin/vitest run` full suite exit 0
  </acceptance_criteria>
  <done>Criterion 4 (SMS 90s bắn / duyệt kịp thì huỷ) và criterion 1 (SSE < 2s) có số đo thật; đơn và lịch thông báo luôn commit cùng nhau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dữ liệu nội bộ đơn → response công khai `/o/:token` | Ranh giới G-1. Ai giữ `order_token` là đọc được response này — không có auth nào khác |
| transaction submit → hàng đợi thông báo | Nếu 2 thứ không cùng commit thì có đơn không ai được báo (đúng thứ REQ-N chống) |
| ghi `max_progress_shown` trên request GET | Endpoint công khai ghi DB → bề mặt DoS ghi |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-45 | Information Disclosure | Response chứa `status` từng món — **vi phạm M2.D-23, phá hard gate G-1** (**HIGH**) | mitigate | `items` map tường minh 3 field; `PublicOrderStatus.strict().parse()`; test assert `Object.keys(items[i])` và `JSON.stringify` không chứa `"state"`; `curl` thật grep = 0 |
| T-09-46 | Information Disclosure | `internal_reject_note` (D-09) lọt ra response | mitigate | Hàm `getByToken` bị cấm đọc cột đó (grep = 0); schema `.strict()` không có field; test bơm chuỗi mồi và assert vắng mặt; `curl` thật grep `ZZTEST` = 0 |
| T-09-47 | Information Disclosure | Payload lộ PII của khách khác / `ip_hash` / `reviewed_by_*` | mitigate | Whitelist tường minh 16 field + `.strict()`; giữ nguyên nguyên tắc phase 8 |
| T-09-48 | Tampering | `percent` tụt làm khách mất niềm tin (M2.D-19) | mitigate | `computeProgress` clamp bằng `max_progress_shown` + persist khi tăng; test có case "món bị trả về KITCHEN mà % không tụt"; kiểm bằng curl 2 lần |
| T-09-49 | Denial of Service | Poll 5-10s × nhiều khách × 1 UPDATE mỗi lần gọi → ghi DB liên tục | mitigate | Chỉ UPDATE khi `percent` **tăng**; test khẳng định không phát sinh update khi percent không đổi |
| T-09-50 | Spoofing | Đoán `order_token` để xem đơn người khác | accept | Token là `randomBytes(32)` (256-bit) từ phase 8; `Referrer-Policy: no-referrer` đã đặt ở Caddy block `order.` (OD-02). Không thêm lớp nào mới ở phase này |
| T-09-51 | Tampering | Emit SSE trước khi commit → admin thấy "đơn ma" nếu rollback | mitigate | Emit đặt **sau** `ds.transaction(...)` resolve; ghi comment tại chỗ |
| T-09-52 | Denial of Service | Emit lỗi làm hỏng response 201 của khách | mitigate | Emit bọc try/catch nuốt lỗi (khuôn `AuditEventHandler`) |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Plan này KHÔNG cài package nào |
</threat_model>

<verification>
1. `cd packages/schemas && ../../node_modules/.bin/tsc` — build lại dist
2. `cd apps/api && ./node_modules/.bin/vitest run` — full suite xanh (≥ 32 test trong `public-orders.test.ts`)
3. `curl` thật: G-1 grep `"state"` = 0 · D-09 grep `ZZTEST` = 0 · % không tụt
4. 3 kịch bản outbox chạy thật (lịch L1/L2/L3 đúng · SMS bắn sau 90s · duyệt kịp thì CANCELLED)
5. Đo độ trễ SSE đầu-cuối < 2s, ghi số vào SUMMARY
</verification>

<success_criteria>
- `GET /api/public/orders/:token` trả đủ 16 field theo §6, không thừa không thiếu
- Không có `status` từng item và không có ghi chú nội bộ trong response — có test + có curl chứng minh
- `percent` đơn điệu, chỉ ghi DB khi tăng
- Món bị huỷ trừ khỏi danh sách + hiện dòng cảnh báo; admin sửa món thì khách thấy tổng tiền mới
- Submit ghi 3 hàng outbox đúng lịch trong cùng transaction và bắn SSE sau commit
- Full suite xanh
</success_criteria>

<output>
Create `.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-09-SUMMARY.md` when done
</output>
