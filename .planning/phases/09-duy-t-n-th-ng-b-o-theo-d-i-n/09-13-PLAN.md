---
phase: 09-duy-t-n-th-ng-b-o-theo-d-i-n
plan: 13
type: execute
wave: 8
depends_on: ["09-01", "09-02", "09-03", "09-04", "09-05", "09-06", "09-07", "09-08", "09-09", "09-10", "09-11", "09-12"]
files_modified:
  - OVERRIDE-DEBT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-VERIFICATION.md
  - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-VALIDATION.md
  - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-UAT.md
autonomous: false
requirements: [REQ-M, REQ-N, REQ-O]

must_haves:
  truths:
    - "5 quyết định LOCKED bị ghi đè ở phase 9 đều có entry trong OVERRIDE-DEBT.md theo đúng cấu trúc OD-01..OD-10"
    - "Nửa `PATCH` của M2.D-44 (khách tự SỬA đơn) được ghi là hoãn có chủ ý — entry OD-16 + 1 mục mới trong REQUIREMENTS.md, nêu rõ nửa huỷ ĐÃ làm ở 09-11"
    - "`GET /api/public/orders?customer_token=` (trang /history) được ghi là giữ nguyên empty state, không phải quên"
    - "ROADMAP không còn tiêu chí nào mâu thuẫn với code: Phase 9 criterion 1 và 4 sửa theo D-02/D-12, Phase 8 criterion 3 được chú thích theo D-11"
    - "REQUIREMENTS.md REQ-K không còn ghi 'chặn 2 lớp'; REQ-N không còn ghi 'auto-OFF sau 1800s'; REQ-M không còn ghi 'chỉ role admin'"
    - "08-VERIFICATION.md được chú thích tại chỗ: truth #3 nay không còn đúng vì phase 9 cố ý bỏ chặn"
    - "09-VALIDATION.md § Per-Task Verification Map điền task ID thật, không còn dòng TBD nào"
    - "Chủ dự án tự kiểm và duyệt bằng checkpoint trước khi phase được coi là xong"
  artifacts:
    - path: "OVERRIDE-DEBT.md"
      provides: "OD-11..OD-16 + cập nhật mục 'Chưa được ghi ở đây'"
      contains: "OD-11"
    - path: ".planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-UAT.md"
      provides: "Danh sách hạng mục nghiệm thu hoãn của phase 9"
    - path: ".planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-VALIDATION.md"
      provides: "Bảng Per-Task Verification Map đã điền + sign-off"
  key_links:
    - from: ".planning/ROADMAP.md § Phase 9"
      to: "09-CONTEXT.md D-02b, D-12"
      via: "sửa criterion 1 và 4"
      pattern: "D-02"
    - from: "OVERRIDE-DEBT.md"
      to: "apps/api/src/modules/public/order-guard.ts"
      via: "entry OD-11 dẫn file thi công"
      pattern: "order-guard"
---

<objective>
Đóng phase: ghi đầy đủ 5 lần ghi đè quyết định LOCKED + 1 lần hoãn có chủ ý vào `OVERRIDE-DEBT.md`, sửa mọi tài liệu nay
mâu thuẫn với code (ROADMAP phase 8 **và** phase 9, REQUIREMENTS, 08-VERIFICATION), điền bảng
kiểm chứng của 09-VALIDATION, và để chủ dự án tự kiểm bằng checkpoint.

Purpose: Phase 9 cố ý làm **một tiêu chí đã `passed` của phase 8 trở nên không còn đúng**. Nếu
không ghi lại, người verify phase 9 (hoặc chính chủ dự án 3 tháng sau) sẽ thấy code mâu thuẫn tài
liệu và không biết cái nào là ý định thật. Spec §28/§134 bắt buộc: *"Không đổi mà không ghi vào
`OVERRIDE-DEBT.md`"*.

Output: 6 entry override (5 ghi đè + 1 hoãn), 4 tài liệu được sửa, 1 file UAT mới, 1 checkpoint chặn.
</objective>

<execution_context>
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@OVERRIDE-DEBT.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-CONTEXT.md
@.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-VALIDATION.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-VERIFICATION.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-UAT.md

<interfaces>
<!-- Trích sẵn. -->

OVERRIDE-DEBT.md — cấu trúc BẮT BUỘC của mỗi entry (theo OD-01..OD-10):
  `## OD-NN — <tiêu đề 1 dòng>`
  - **Ngày:** … · **Người quyết:** …
  - **Quyết định gốc:** (trích M2.D-xx + số dòng spec)
  - **Lệch:** (chính xác cái gì đổi)
  - **Vì sao:** (lý do vận hành, không phải lý do kỹ thuật chung chung)
  - **Ghi ở:** (file code + plan thi công)
  - **Quay lại thì sao:** (hệ quả nếu muốn khôi phục)
  Mục cuối file "Chưa được ghi ở đây (nợ tồn từ trước)" hiện liệt kê M2.D-59 và M2.D-60 —
  **M2.D-60 nay bị D-12 xoá hẳn**, phải cập nhật mục đó chứ không để mâu thuẫn.

.planning/ROADMAP.md — các chỗ phải sửa:
  · § Phase 9 criterion 1 (dòng ~112): *"role `order` xem được hàng chờ nhưng gọi API confirm/reject
    trực tiếp vẫn bị chặn (M2.D-32, M2.D-33)"* → sai theo D-02/D-02b
  · § Phase 9 criterion 4 (dòng ~115): *"quá 1800s → tự OFF nhận đơn + audit actor SYSTEM và không
    tự ON lại (M2.D-60)"* → bị D-12 bỏ hẳn
  · § Phase 8 criterion 3 (dòng ~83): *"Tắt công tắc: FE khoá nút và gọi API tay vẫn nhận 409
    ONLINE_ORDERING_DISABLED; ngoài giờ mở cửa cũng bị chặn"* → không còn đúng từ phase 9
  · § Phase 9 danh sách **Plans**: planner đã điền 13 dòng khi lập kế hoạch — **kiểm lại cho khớp
    thực tế thi công**, sửa nếu có plan bị gộp/tách trong lúc chạy
  · Bảng Progress cuối file: dòng Phase 9

.planning/REQUIREMENTS.md — 3 dòng phải sửa:
  · REQ-K (dòng 42): *"chặn **2 lớp** FE và BE"*
  · REQ-M (dòng 52): *"**chỉ role `admin`** được duyệt"*
  · REQ-N (dòng 57): *"leo thang + **auto-OFF sau 1800s**"*

08-VERIFICATION.md — bảng "Observable Truths" hàng #3 (dòng ~25) ghi `✓ VERIFIED` cho tiêu chí chặn
  2 lớp. Frontmatter `status: passed`. **KHÔNG đổi `status`** (phase 8 đã đúng tại thời điểm verify)
  — chỉ **chú thích** rằng phase 9 cố ý làm nó hết đúng.

09-VALIDATION.md § Per-Task Verification Map — 10 dòng `TBD`, cần điền Task ID thật + Threat Ref.
  Frontmatter `nyquist_compliant: false` → `true`; `wave_0_complete: false` → `true`; `status: draft`.

08-UAT.md — khuôn file UAT của phase 8 (5 hạng mục, mỗi hạng mục có `result: pending`).

08-13-PLAN.md Task 3 — khuôn checkpoint đã dùng và được chủ dự án duyệt ở phase 8 (có `<action>`
  chuẩn bị trước khi dừng + `<what-built>` + `<how-to-verify>` đánh số bước).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OVERRIDE-DEBT.md — OD-11..OD-16 + cập nhật mục nợ tồn</name>
  <files>OVERRIDE-DEBT.md</files>
  <read_first>
    - OVERRIDE-DEBT.md (toàn bộ — bắt chước đúng giọng văn và 6 mục của OD-01..OD-10)
    - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-CONTEXT.md D-02, D-11, D-12, D-17
    - .planning/intel/decisions.md các mục M2.D-26, M2.D-27, M2.D-32, M2.D-33, M2.D-36, M2.D-60
    - Toàn bộ 12 file `09-*-SUMMARY.md` (mục "lệch" mà executor đã ghi lại)
  </read_first>
  <action>
    Thêm 5 entry vào `OVERRIDE-DEBT.md`, đúng cấu trúc 6 mục:

    **OD-11 — Công tắc "Đóng cửa" không còn chặn đơn (ghi đè M2.D-26 + M2.D-27)**
    Người quyết: chủ dự án, 2026-07-31 (D-11). Gốc: M2.D-26/27 chốt chặn 2 lớp (FE ẩn nút + BE trả
    `409 ONLINE_ORDERING_DISABLED`). Lệch: còn đúng 2 trạng thái Mở/Đóng cửa, **cả hai đều nhận đơn**;
    "Đóng cửa" chỉ đổi chữ hiển thị. Vì sao: chủ quán muốn không mất đơn ngoài giờ — đơn đặt lúc đóng
    cửa xử lý bằng người (gọi điện xác nhận, D-13). Ghi ở: `order-guard.ts`, `order-guard.test.ts`,
    `submit-order.ts`, `CheckoutPage.tsx`, plan 09-12. Quay lại: code chặn cũ nằm trong lịch sử git
    phase 8; muốn có "TẮT HẲN" thật thì thêm **trạng thái thứ ba**, đừng khôi phục ngữ nghĩa cũ của
    `enabled` (2 mã lỗi `ONLINE_ORDERING_DISABLED`/`STORE_CLOSED` vẫn còn trong `errors.ts` nhưng
    **không còn nơi nào phát ra**).

    **OD-12 — Bỏ hẳn auto-OFF (ghi đè M2.D-60 và phần auto-OFF của M2.D-36)**
    Người quyết: chủ dự án, 2026-07-31 (D-12). Gốc: M2.D-36 (auto-OFF sau 300s) → M2.D-60 ghi đè
    thành 1800s. Lệch: **không còn cơ chế nào tự đổi trạng thái công tắc**; lớp L4 trong
    `notification_outbox` không tồn tại; key `escalate_autooff_after_s` bị xoá khỏi
    `SETTINGS_DEFAULTS`/`StoreSettingsMap`. **Phần SMS 90s của M2.D-36 vẫn còn hiệu lực** (D-15) và
    sau khi bỏ auto-OFF thì **nó là lớp duy nhất còn tới được người không ngồi trước máy**. Ghi ở:
    `outbox-rules.ts`, `settings.defaults.ts`, plan 09-05 + 09-12. Quay lại: phải thêm lại cả level
    `'L4'`, một job đổi setting, và audit actor SYSTEM — và cân nhắc lại D-11 vì auto-OFF chỉ có
    nghĩa khi "OFF" thực sự chặn.

    **OD-13 — Cả 3 role duyệt được đơn online (ghi đè M2.D-33 + phần phân quyền của M2.D-32)**
    Người quyết: chủ dự án, 2026-07-31 (D-02). Gốc: M2.D-33 "chỉ role `admin` được xác nhận/từ chối".
    Lệch: `admin` + `order` + `kitchen` đều duyệt được. Vì sao: ai đang ở máy thì duyệt, không để chủ
    quán thành nút thắt giờ cao điểm. **Kiểm soát bù trừ bắt buộc:** audit log ghi rõ ai duyệt
    (`reviewed_by_*` lấy từ `req.user` + `action_kind` `online_order.confirmed`/`rejected`), và có
    test chứng minh. Ghi ở: `admin-online-orders.controller.ts`, `audit.interceptor.ts`,
    `App.tsx`, plan 09-06/09-07/09-08/09-10. Quay lại: đổi `RequireRoles('admin','order','kitchen')`
    về `AdminGuard` ở 2 route + đổi `RoleGate` ở `App.tsx`; **đừng** gỡ audit khi làm vậy, nó có giá
    trị độc lập.

    **OD-14 — Không dùng `getOrCreateOpenOrder()` khi duyệt đơn (lệch pseudo-code spec §7 dòng 480)**
    Người quyết: phát hiện khi lên plan 09-06. Gốc: spec ghi `order = getOrCreateOpenOrder(bàn.id, creator)`.
    Lệch: tạo `Order` trực tiếp trên `EntityManager` của transaction đang chạy. Vì sao:
    `OrdersService.getOrCreateOpenOrderImpl` tự mở `ds.transaction` riêng (connection khác) — gọi từ
    trong transaction cấp bàn thì nó **không nhìn thấy bàn vừa INSERT chưa commit** (`NOT_FOUND: Bàn
    không tồn tại`) và mở đường deadlock 2 connection. Đây là lệch **pseudo-code minh hoạ**, không
    lệch quyết định LOCKED nào (M2.D-02/04/05/06 vẫn thoả). Ghi ở:
    `admin-online-orders.service.ts`, plan 09-06. Quay lại: muốn dùng lại `getOrCreateOpenOrder` thì
    phải cho nó nhận `EntityManager` từ ngoài — đó là refactor `orders.service.ts` (file 1315 dòng,
    Fragile Area), không phải đổi 1 dòng.

    **OD-15 — Chuông báo đơn dùng Web Audio API thay `HTMLAudioElement`**
    Người quyết: chốt khi lên plan 09-10. Gốc: `09-RESEARCH.md` Pattern 4 mô tả `audio.play()` +
    bắt `NotAllowedError`. Lệch: `AudioContext` + `OscillatorNode`, mở khoá bằng `await ctx.resume()`
    trong chính `onClick`, kiểm `ctx.state !== 'running'` để biết bị chặn. Vì sao: không cần thêm
    file nhị phân `.mp3` vào repo, ngữ nghĩa mở-khoá-bằng-user-gesture giống hệt. D-03/D-04 chỉ chốt
    "có chuông" và "nút Bật chuông bắt buộc", không chốt cách phát. Ghi ở: `apps/web/src/lib/bell.ts`,
    plan 09-10. Quay lại: thay ruột `bell.ts` bằng `HTMLAudioElement` + thêm asset, giao diện hàm
    (`unlock`/`ring`/`dispose`) giữ nguyên nên trang không phải sửa.

    **OD-16 — Hoãn có chủ ý nửa `PATCH` của M2.D-44 (khách tự SỬA đơn)**
    Ngày 2026-07-31, người quyết: chủ dự án, khi planner báo M2.D-44 không được plan nào phủ.
    Quyết định gốc: M2.D-44 (spec §5.1) chốt *"trước xác nhận: khách tự sửa / huỷ đơn thoải mái,
    không cần xin phép"* — gồm cả `PATCH` lẫn `DELETE /api/public/orders/:token`.
    Lệch: **chỉ thi công nửa huỷ** (`DELETE`, plan 09-11 Task 3). Nửa sửa (`PATCH`: chạy lại toàn bộ
    guard, snapshot lại giá, UI quay về giỏ hàng) hoãn sang phase 10.
    Vì sao: nửa huỷ đóng được rủi ro thật (khách đặt nhầm bị kẹt, phải gọi điện) với chi phí ~1 task
    và gần như không ăn vào ~18 kB ngân sách bundle còn lại của `apps/shop`; nửa sửa tốn cả một plan
    và nhiều khả năng buộc phải nới gate bundle lần nữa (đã nới 320→370 kB ở phase 8, xem SUMMARY
    08-11). M2.D-44 không nằm trong 5 success criteria của ROADMAP § Phase 9 nên không chặn phase.
    ⚠ Ghi ở: nửa huỷ nằm ở `apps/api/src/modules/public/cancel-order.ts` + `OrderTrackPage.tsx`
    (plan 09-11). **Nêu rõ trong entry rằng nửa huỷ ĐÃ làm** — người đọc sau không được hiểu nhầm là
    cả M2.D-44 bị bỏ.
    Quay lại thì sao: thêm `PATCH` là thêm endpoint + màn sửa; `CANCELLED_BY_CUSTOMER` và row lock
    của 09-11 dùng lại được nguyên. Phải đo lại bundle trước khi bắt tay.

    **Nếu 12 SUMMARY ghi thêm lệch nào khác** (vd `closed_submit_confirm_text` đặt trên
    `/api/public/store` thay vì theo gợi ý PATTERNS, hay việc tách `runWithRetry`/`KIND_FORMAT`) →
    đánh giá: chỉ tạo entry OD mới nếu nó **ghi đè một quyết định M2.D-xx đã LOCKED**; các lệch
    thuần kỹ thuật thì ghi 1 đoạn trong `09-13-SUMMARY.md` là đủ. Nêu rõ đã cân nhắc và kết luận gì.

    **Cập nhật mục cuối file "Chưa được ghi ở đây (nợ tồn từ trước)":** dòng M2.D-60 nay đã bị
    OD-12 vượt qua — sửa thành ghi chú lịch sử có trỏ tới OD-12, không để 2 chỗ nói ngược nhau.
  </action>
  <verify>
    <automated>test $(grep -c "^## OD-" OVERRIDE-DEBT.md) -eq 16</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## OD-" OVERRIDE-DEBT.md` = 16
    - Mỗi entry OD-11..OD-16 có đủ 6 nhãn: `grep -A25 "^## OD-11 " OVERRIDE-DEBT.md | grep -cE "\*\*Ngày:|\*\*Quyết định gốc:|\*\*Lệch:|\*\*Vì sao:|\*\*Ghi ở:|\*\*Quay lại thì sao:"` = 6 (lặp cho OD-12..OD-16)
    - `grep -c "M2.D-26" OVERRIDE-DEBT.md` ≥ 1 · `M2.D-27` ≥ 1 · `M2.D-33` ≥ 1 · `M2.D-36` ≥ 2 · `M2.D-60` ≥ 2
    - `grep -c "order-guard" OVERRIDE-DEBT.md` ≥ 1 (entry dẫn tới file code thật)
    - Mục "Chưa được ghi ở đây" có trỏ tới `OD-12`
  </acceptance_criteria>
  <done>Mọi lần đi lệch quyết định LOCKED ở phase 9 đều có vết đọc được, đúng khuôn 10 entry đã có.</done>
</task>

<task type="auto">
  <name>Task 2: Sửa ROADMAP (Phase 8 + Phase 9), REQUIREMENTS, và chú thích 08-VERIFICATION</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-VERIFICATION.md</files>
  <read_first>
    - .planning/ROADMAP.md § Phase 8 và § Phase 9 (đọc trọn 2 mục + bảng Progress cuối file)
    - .planning/REQUIREMENTS.md dòng 40-63 và bảng Traceability
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-VERIFICATION.md (frontmatter + bảng Observable Truths hàng #3)
    - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-CONTEXT.md D-02b, D-12, D-17
  </read_first>
  <action>
    **`.planning/ROADMAP.md`:**
    - **§ Phase 9 criterion 1** → viết lại: *"Admin thấy đơn mới trong **< 2s** (SSE + chuông + badge);
      **cả 3 role `admin`/`order`/`kitchen` đều xem và duyệt được** — audit log ghi rõ ai duyệt là
      kiểm soát bù trừ (D-02 **ghi đè M2.D-33** và phần phân quyền M2.D-32, xem `OVERRIDE-DEBT.md` OD-13)"*
    - **§ Phase 9 criterion 4** → viết lại: *"Đơn quá **90s** chưa duyệt → SMS bắn; duyệt trước ngưỡng
      thì outbox L2 bị huỷ; đổi `SMS_DRIVER` console↔esms không sửa logic (M2.D-36 phần SMS, M2.D-63).
      **Auto-OFF sau 1800s đã bị bỏ hẳn** (D-12 ghi đè M2.D-60, xem OD-12) — không còn cơ chế nào tự
      đổi trạng thái công tắc."*
    - **§ Phase 8 criterion 3** → giữ nguyên câu cũ, thêm dòng chú thích ngay dưới:
      *"⚠ **Tiêu chí này đúng tại thời điểm đóng phase 8 (2026-07-31) và đã bị phase 9 cố ý làm hết
      hiệu lực** — D-11 bỏ hẳn việc chặn, công tắc chỉ còn đổi chữ hiển thị. Xem `OVERRIDE-DEBT.md`
      OD-11. Phần *"đơn đang chạy không bị ảnh hưởng"* và cơ chế "OFF đến hết hôm nay" tự-ON 00:00
      (OD-07) **vẫn còn đúng**."* — KHÔNG bỏ tick `[x]` của Phase 8.
    - **§ Phase 9 danh sách Plans**: planner đã điền 13 dòng lúc lập kế hoạch — đối chiếu với thực tế
      thi công, sửa mô tả nếu lệch, và tick `[x]` cho các plan đã xong.
    - **Bảng Progress** cuối file: dòng Phase 9 → `13/13` + `Executed` + ngày thật.

    **`.planning/REQUIREMENTS.md`:**
    - REQ-K: bỏ *"chặn **2 lớp** FE và BE"*, thay bằng *"công tắc 2 trạng thái Mở/Đóng cửa — **cả hai
      đều nhận đơn**, Đóng cửa chỉ đổi chữ hiển thị (D-11 ghi đè M2.D-26/27, xem OD-11)"*.
    - REQ-M: bỏ *"**chỉ role `admin`** được duyệt"* → *"**cả 3 role** `admin`/`order`/`kitchen` duyệt
      được, audit log ghi rõ người duyệt (D-02 ghi đè M2.D-33, xem OD-13)"*.
    - REQ-N: bỏ *"+ **auto-OFF sau 1800s**"* → *"(auto-OFF đã bị bỏ hẳn — D-12 ghi đè M2.D-60, xem OD-12)"*.
    - **KHÔNG** tự tick `[x]` cho REQ-M/N/O — việc đó thuộc `/gsd:verify-work`. (08-VERIFICATION đã ghi
      nhận bài học "tick sớm 1 wave" ở phase 8; đừng lặp lại.)
    - **Thêm mục MỚI `## Deferred Features`** (đặt ngay trước `## Deferred UAT (C-LOCAL-01)`).
      ⚠ KHÔNG nhét vào `## Deferred UAT` — mục đó nói về *hạng mục chưa nghiệm thu được*, còn đây là
      *tính năng cố ý chưa làm*; gộp 2 loại vào một chỗ là tự tạo nhầm lẫn cho người đọc sau. Nội dung:
      · **M2.D-44 nửa `PATCH`** (khách tự SỬA đơn khi còn `WAITING`) — hoãn sang phase 10, chủ dự án
        chốt 2026-07-31, xem `OD-16`. **Nửa huỷ (`DELETE`) ĐÃ thi công ở plan 09-11 Task 3** — ghi rõ
        câu này, đừng để người đọc tưởng cả M2.D-44 bị bỏ.
      · **`GET /api/public/orders?customer_token=`** (danh sách đơn cũ cho trang `/history`) — chủ dự
        án chốt 2026-07-31 giữ nguyên empty state tĩnh của phase 8. Không thuộc REQ-M/N/O nên **không
        cần** entry OVERRIDE-DEBT; ghi ở đây để sau này không ai tưởng là quên. Khách vẫn theo dõi
        được đơn qua link `/o/:token`.

    **`08-VERIFICATION.md`:** thêm khối chú thích ngay **sau** bảng Observable Truths:
    ```
    > ⚠ **Bổ sung 2026-07-31 (phase 9, plan 09-12): truth #3 nay KHÔNG CÒN ĐÚNG — có chủ đích.**
    > D-11/D-12 của `09-CONTEXT.md` bỏ hẳn việc chặn submit khi công tắc tắt và bỏ hẳn auto-OFF.
    > `POST /api/public/orders` nay trả **201** kể cả khi Đóng cửa hoặc ngoài giờ mở cửa.
    > Phần vẫn đúng: "đơn đang chạy không bị ảnh hưởng" và cơ chế "OFF đến hết hôm nay" tự-ON 00:00.
    > Phần vẫn đúng và **phải giữ**: 4 lớp chống lạm dụng ở truth #4 (D-18) — plan 09-12 có test riêng.
    > Xem `OVERRIDE-DEBT.md` OD-11/OD-12. Trạng thái `passed` của phase 8 **không đổi**: nó đúng tại
    > thời điểm verify 2026-07-31, việc thay đổi là quyết định sau đó của chủ dự án.
    ```
    **Không** đổi `status: passed` trong frontmatter, không sửa nội dung bảng cũ.
  </action>
  <verify>
    <automated>test $(grep -c "vẫn bị chặn" .planning/ROADMAP.md) -eq 0 && test $(grep -c "KHÔNG CÒN ĐÚNG" .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-VERIFICATION.md) -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "09-13-PLAN.md" .planning/ROADMAP.md` = 1 (đủ 13 dòng plan trong § Phase 9)
    - `grep -c "vẫn bị chặn" .planning/ROADMAP.md` = 0
    - `grep -c "auto-OFF sau 1800s" .planning/REQUIREMENTS.md` = 0
    - `grep -c 'chỉ role `admin`' .planning/REQUIREMENTS.md` = 0
    - `grep -c "chặn \*\*2 lớp\*\*" .planning/REQUIREMENTS.md` = 0
    - `grep -c "OD-11\|OD-12\|OD-13" .planning/ROADMAP.md` ≥ 3
    - `grep -c "KHÔNG CÒN ĐÚNG" .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-VERIFICATION.md` = 1
    - `grep -c "^status: passed" .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-VERIFICATION.md` = 1 (không đổi trạng thái phase 8)
    - `grep -c "^- \[ \] \*\*REQ-M\*\*" .planning/REQUIREMENTS.md` = 1 (REQ-M vẫn chưa tick)
  </acceptance_criteria>
  <done>Không còn tài liệu nào nói ngược code; người verify phase 9 đọc ROADMAP là thấy đúng ý chủ dự án.</done>
</task>

<task type="auto">
  <name>Task 3: Điền 09-VALIDATION Per-Task Verification Map + tạo 09-UAT.md</name>
  <files>.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-VALIDATION.md, .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-UAT.md</files>
  <read_first>
    - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-VALIDATION.md (toàn bộ — bảng 10 dòng TBD + § Manual-Only + § Sign-Off)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-UAT.md (khuôn file UAT + frontmatter)
    - Toàn bộ 12 file `09-*-SUMMARY.md` (lấy Task ID thật + Threat ID thật + kết quả test)
  </read_first>
  <action>
    **`09-VALIDATION.md`:**
    - Điền bảng **Per-Task Verification Map**: mỗi dòng ghi `Task ID` dạng `09-NN Task M`, `Plan`,
      `Wave`, `Threat Ref` (mã `T-09-NN` thật trong `<threat_model>` của plan tương ứng), và đổi
      `Status` theo kết quả thật (`✅ green` / `❌ red` / `⚠️ flaky`). Ánh xạ tối thiểu:
      · row lock cấp bàn → 09-08 Task 1 (T-09-26)
      · WAITING không nhiễm doanh thu → 09-08 Task 2 (T-09-28)
      · 3 role duyệt được + audit actor → 09-07 Task 1 (T-09-36) và 09-08 Task 2 (T-09-32)
      · ghi chú nội bộ không ra công khai → 09-09 Task 1 (T-09-46)
      · poller outbox quét đúng → 09-05 Task 2 (T-09-22)
      · `SMS_DRIVER` đổi không sửa logic → 09-05 Task 1 (T-09-20)
      · duyệt trước 90s thì huỷ SMS → 09-06 Task 2 + 09-09 Task 2 (T-09-23)
      · `computeProgress` đúng/đơn điệu/95% → 09-01 Task 2 (T-09-02)
      · response không có `status` từng item → 09-09 Task 1 (T-09-45)
      · bỏ chặn 409 mà 4 lớp còn xanh → 09-12 Task 2 (T-09-65)
      **Không được để dòng `TBD` nào.**
    - Bổ sung dòng mới cho hạng mục phát sinh: gate bundle `apps/shop` (09-11 Task 2, T-09-63),
      round-trip 2 key cấu hình (09-12 Task 1, T-09-68), `ship_fee` tách doanh thu (09-08 Task 3, T-09-43),
      2 cron hồi sinh (09-02 Task 2, T-09-06).
    - Tick § Validation Sign-Off, đổi frontmatter `status: complete`, `nyquist_compliant: true`,
      `wave_0_complete: true`.

    **`09-UAT.md`** (mới, theo khuôn `08-UAT.md`: frontmatter có `phase`, `status`, `source`,
    `started`, `updated`; mỗi hạng mục là 1 `### ` với `result: pending`) — hạng mục **không nghiệm
    thu được ở local**, nêu rõ có phải blocker trước deploy hay không:
    1. **Chuông trên Safari/iOS thật** — RESEARCH Assumption A2: Safari dùng Media Engagement Index,
       có thể yêu cầu gesture lặp lại. Không blocker phase, cần thử trước khi giao cho nhân viên
       dùng iPhone.
    2. **SSE xuyên Caddy trên production** — RESEARCH Assumption A1 (Caddy tự flush
       `text/event-stream`) mới xác minh qua tài liệu, chưa chạy thật (máy dev không có Docker/caddy).
       Nếu trễ > 2s thì kiểm `Content-Type` trước, không nghi Caddy.
    3. **`SMS_DRIVER=esms` với brandname thật** — local chỉ kiểm nhánh thiếu key. Cần 1 tin thật
       trước khi tin vào lớp cảnh báo 90s (sau D-12 đây là lớp duy nhất tới được người vắng mặt).
    4. **Nhiều instance API cùng chạy** — poller outbox có `SKIP LOCKED` + guard in-process nhưng
       chưa kiểm với 2 container (T-09-22, rủi ro còn lại đã chấp nhận).
    5. **Kế thừa từ `08-UAT.md` test 1: `docker build` + `sharp` trên alpine** — vẫn là **gate bắt
       buộc trước deploy production**, chưa được đóng. Ghi lại ở đây để không rơi khỏi tầm mắt.
  </action>
  <verify>
    <automated>test $(grep -c "TBD" .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-VALIDATION.md) -eq 0 && test -f .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TBD" .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-VALIDATION.md` = 0
    - `grep -c "T-09-" .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-VALIDATION.md` ≥ 10
    - `grep -c "nyquist_compliant: true" .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-VALIDATION.md` = 1
    - `grep -c "result: pending" .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-UAT.md` = 5
    - `grep -c "sharp" .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-UAT.md` = 1 (gate phase 8 được mang sang, không rơi)
  </acceptance_criteria>
  <done>Bảng kiểm chứng có task ID thật để verifier đối chiếu; những gì không kiểm được ở local được ghi thành danh sách thay vì im lặng bỏ qua.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Chủ dự án tự kiểm phase 9</name>
  <files>(không sửa file mã nguồn — kết quả ghi vào `09-13-SUMMARY.md` và `09-VALIDATION.md` § Approval)</files>
  <read_first>
    - .planning/ROADMAP.md § Phase 9 (5 success criteria SAU khi Task 2 sửa — đây là danh sách phải chứng minh)
    - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-VALIDATION.md § Manual-Only Verifications
    - .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-UAT.md (Task 3 — biết hạng mục nào KHÔNG kiểm ở bước này)
    - docs/MILESTONE-02-ONLINE-ORDERING-SPEC.md dòng 583-599 (checklist nghiệm thu phase 9 của spec)
  </read_first>
  <action>
    Dừng lại và để **chủ dự án tự tay** chạy 14 bước ở `<how-to-verify>`. Trước khi dừng, executor
    phải làm xong 4 việc chuẩn bị (phần tự động, không đẩy sang người):
    1. Khởi động sẵn 3 dev server và xác nhận cả 3 lên được:
       `cd apps/api && node --import @swc-node/register/esm-register src/main.ts` ·
       `cd apps/web && ./node_modules/.bin/vite` (5173) · `cd apps/shop && ./node_modules/.bin/vite` (5174).
    2. Chạy và **dán kết quả thật** vào SUMMARY: `cd apps/api && ../../node_modules/.bin/tsc --noEmit`,
       `cd apps/web && ../../node_modules/.bin/tsc --noEmit`, `cd apps/shop && ../../node_modules/.bin/tsc --noEmit`,
       `cd apps/api && ./node_modules/.bin/vitest run`, `cd apps/web && ./node_modules/.bin/vitest run`,
       `cd apps/shop && ./node_modules/.bin/vitest run`,
       `cd apps/shop && ./node_modules/.bin/vite build && cd ../.. && bash scripts/check-shop-bundle.sh`.
       Bất kỳ lệnh nào đỏ → **không** mở checkpoint, sửa trước.
    3. Bảo đảm có sẵn tài khoản role `kitchen` để làm bước 1 (nếu chưa có thì tạo qua `/admin/users`
       và ghi tên đăng nhập/mật khẩu tạm vào phần chuẩn bị của checkpoint).
    4. Bảo đảm `store_phone` và ít nhất 1 số trong `notify_sms_recipients` đã có giá trị ở
       `/admin/settings` — thiếu thì bước 5 sẽ không thấy dòng `[SMS:console]`.
    Sau khi có phản hồi: nếu "approved" thì cập nhật `09-VALIDATION.md` § Approval thành `approved` +
    ngày; nếu có bước sai thì ghi **số bước + hiện tượng** vào SUMMARY và mở gap để sửa, **không** tự
    đánh dấu đạt.
  </action>
  <what-built>
    Toàn bộ phase 9 đã thi công và tự kiểm bằng test tự động:
    hàng chờ duyệt `/admin/online-orders` (SSE + chuông + badge + đếm giây chờ), duyệt đơn có cấp bàn
    tự động (tự tạo bàn khi hết), từ chối có lý do soạn sẵn + ghi chú nội bộ, thông báo SMS 90s qua
    outbox, trang theo dõi `/o/:token` với % và 5 mốc, và phần **sửa lại công tắc nhận đơn của phase 8**
    (2 trạng thái, cả hai đều nhận đơn).
    Kèm: `OVERRIDE-DEBT.md` OD-11..OD-16, ROADMAP/REQUIREMENTS/08-VERIFICATION đã sửa cho khớp code,
    `09-UAT.md` ghi 5 hạng mục chưa kiểm được ở local.
    Phần dưới đây là những gì **chỉ người thật kiểm được** — máy không thay thế được.
  </what-built>
  <how-to-verify>
    Ba server đã chạy sẵn: API (3001), quản lý (http://localhost:5173), khách (http://localhost:5174).
    Làm lần lượt 14 bước, ghi lại bước nào **không** đúng như mô tả:

    1. Đăng nhập bằng tài khoản **bếp** (role `kitchen`), mở http://localhost:5173/admin/online-orders
       → **vào được**, thấy tiêu đề "Hàng chờ duyệt".
    2. Chưa bấm gì → thấy **banner đỏ "🔕 Chuông đang tắt"** chiếm hẳn một dải, có nút "Bật chuông".
    3. Bấm "Bật chuông" → banner biến mất.
    4. Mở http://localhost:5174/ ở cửa sổ khác, đặt 1 đơn **Đến lấy tại quán** → quay lại màn hàng chờ:
       đơn hiện **trong vòng 2 giây**, **nghe thấy tiếng chuông**, badge đỏ cạnh tiêu đề hiện `(1)`,
       và có đồng hồ đếm giây chạy.
    5. Để đơn đó **không duyệt ~2 phút** → đồng hồ chuyển **đỏ** khi qua 90 giây, và cửa sổ log API in
       một dòng `[SMS:console]`.
    6. Bấm **Xác nhận** → hiện thông báo "Đã xác nhận — bàn mang-ve-XX", dòng đơn mờ đi rồi biến mất.
       Mở `/tables` → thấy bàn đó đang có đơn. Mở `/kitchen` → **thấy món ngay** (không phải bấm gì thêm).
    7. Đặt đơn thứ hai, lần này bấm **Từ chối** → panel mở **ngay trong dòng đơn** (không phải hộp thoại
       che màn hình), có 5 lý do chọn 1 và **một ô riêng nền xám viền gạch đứt** ghi
       "🔒 Ghi chú nội bộ — khách KHÔNG thấy". Nút "Xác nhận từ chối" **mờ** cho tới khi chọn 1 lý do.
    8. Chọn lý do "Quán đang quá tải…", gõ vào ô ghi chú nội bộ một câu bất kỳ, bấm Xác nhận từ chối.
       Mở `/o/<mã đơn>` của khách → thấy **đúng câu lý do soạn sẵn**, và **KHÔNG** thấy câu ghi chú
       vừa gõ ở bất cứ đâu.
    9. Mở `/o/<mã đơn>` của đơn đã xác nhận ở bước 6 trên điện thoại (hoặc thu cửa sổ còn 375px rộng)
       → thấy **số phần trăm to**, **5 chấm tròn nối ngang** (không tràn ra ngoài màn hình), và
       **một dòng chữ** mô tả mốc hiện tại.
    10. Ở `/kitchen`, chuyển món của đơn đó sang "đang nấu" rồi "xong" → trang `/o/...` của khách
        **tự cập nhật** trong ~10 giây và **phần trăm chỉ tăng, không bao giờ tụt**.
    11. Ở màn đơn của bàn đó, **huỷ 1 món** → trang khách hiện **banner xanh dương** báo quán đã cập
        nhật đơn, và danh sách món + tổng tiền đổi theo.
    12. Vào `/admin/settings` tab công tắc → thấy **2 ô chữ mới**. Sửa ô "Câu hiển thị trên trang
        khách khi Đóng cửa" thành một đoạn **rất dài** (~300 ký tự), bấm Lưu.
    13. **Tắt** công tắc nhận đơn → mở trang khách: thấy banner với **đúng đoạn chữ dài vừa nhập**,
        chữ **xuống dòng đủ, không bị cắt cụt**, và **nút đặt hàng vẫn bấm được**. Đặt thử 1 đơn →
        **đặt được bình thường**, màn xác nhận hiện câu "Chúng tôi đã tiếp nhận đơn…".
    14. Vẫn trong lúc công tắc tắt: đặt tiếp 1 đơn nữa **cùng số điện thoại** → **bị từ chối** với
        thông báo đang có đơn chưa xử lý xong (đây là lớp chống lạm dụng, phải còn nguyên).

    Nếu bước nào sai, mô tả **bước số mấy** và **thấy gì thay vì thế**.
  </how-to-verify>
  <verify>
    <human-check>Chủ dự án chạy đủ 14 bước và phản hồi</human-check>
  </verify>
  <resume-signal>Gõ "approved" nếu cả 14 bước đúng, hoặc liệt kê các bước sai</resume-signal>
  <done>Chủ dự án xác nhận 14 bước; `09-VALIDATION.md` § Approval ghi `approved` + ngày, hoặc danh sách bước sai được mở thành gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| code đã thi công ↔ tài liệu kế hoạch | Tài liệu nói ngược code là cách một quyết định của chủ dự án bị "mất" sau vài tháng |
| tiêu chí đã `passed` của phase trước ↔ thay đổi của phase sau | Sửa im lặng làm hồ sơ nghiệm thu mất giá trị |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-72 | Repudiation | 5 quyết định LOCKED bị ghi đè + 1 quyết định LOCKED bị hoãn mà không có vết → người sau tưởng là bug và "sửa lại cho đúng spec", làm hỏng ý chủ dự án | mitigate | OD-11..OD-16 đủ 6 mục, mỗi entry ghi cả "quay lại thì sao"; docblock trong code trỏ ngược tới `OVERRIDE-DEBT.md` |
| T-09-73 | Repudiation | 08-VERIFICATION.md giữ `passed` cho một tiêu chí nay sai → hồ sơ nghiệm thu nói dối | mitigate | Chú thích tại chỗ nêu rõ "đúng tại thời điểm verify, bị phase 9 cố ý thay đổi", có trỏ OD-11/OD-12; **không** sửa trạng thái cũ (sửa lịch sử còn tệ hơn) |
| T-09-74 | Repudiation | ROADMAP criterion 1 giữ nguyên câu cũ → verifier đánh trượt phase dù code đúng ý chủ dự án (D-02b cảnh báo đúng việc này) | mitigate | Sửa criterion 1 và 4 của Phase 9; acceptance criteria grep chuỗi cũ = 0 |
| T-09-75 | Tampering | Tick sớm `[x]` cho REQ-M/N/O trước khi verify (lỗi quy trình đã xảy ra ở phase 8 với REQ-J) | mitigate | Task 2 nói rõ **không** tick; acceptance criteria khẳng định REQ-M còn `[ ]` |
| T-09-76 | Repudiation | Hạng mục không kiểm được ở local (Safari, Caddy, eSMS thật, multi-instance) bị coi như đã xong | mitigate | `09-UAT.md` liệt kê 5 hạng mục `result: pending`, kèm gate `sharp`/Docker kế thừa từ phase 8 |
| T-09-77 | Repudiation | Checkpoint được "tự duyệt" thay cho chủ dự án | mitigate | `gate="blocking"`; `<action>` cấm tự đánh dấu đạt; kết quả phải ghi vào `09-VALIDATION.md § Approval` kèm ngày |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Plan này chỉ sửa tài liệu, KHÔNG cài package nào |
</threat_model>

<verification>
1. `grep -c "^## OD-" OVERRIDE-DEBT.md` = 16
2. `grep -c "TBD" .planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-VALIDATION.md` = 0
3. ROADMAP § Phase 9 có đủ 13 dòng plan, criterion 1 và 4 đã sửa; § Phase 8 có chú thích
4. REQUIREMENTS không còn 3 câu mâu thuẫn; REQ-M/N/O vẫn `[ ]`
5. Trước checkpoint: 3 lệnh `tsc --noEmit` sạch · 3 suite vitest xanh · `bash scripts/check-shop-bundle.sh` OK
6. Checkpoint Task 4 được chủ dự án gõ "approved"
</verification>

<success_criteria>
- OD-11..OD-16 đầy đủ 6 mục mỗi entry; mục "nợ tồn từ trước" đã cập nhật
- ROADMAP (Phase 8 + Phase 9), REQUIREMENTS, 08-VERIFICATION không còn mâu thuẫn với code
- 09-VALIDATION không còn `TBD`, có `T-09-NN` thật, sign-off đã tick
- 09-UAT.md liệt kê 5 hạng mục hoãn, giữ nguyên gate `sharp`/Docker
- Chủ dự án duyệt checkpoint 14 bước
</success_criteria>

<output>
Create `.planning/phases/09-duy-t-n-th-ng-b-o-theo-d-i-n/09-13-SUMMARY.md` when done
</output>
