---
phase: 08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n
plan: 08
type: execute
wave: 3
depends_on: ["08-05"]
files_modified:
  - apps/web/src/pages/AdminSettingsPage.tsx
  - apps/web/src/App.tsx
  - apps/web/src/pages/DashboardPage.tsx
autonomous: true
requirements: [REQ-K, REQ-L]

must_haves:
  truths:
    - "D-13: Chủ quán tắt nhận đơn online trong 1 chạm từ widget Dashboard, không cần điều hướng; kiểu OFF/lý do/giờ mở cửa đặt ở /admin/settings"
    - "Trang /admin/settings đặt được kiểu OFF, lý do tạm ngưng, giờ mở cửa, free_ship_km, SĐT quán, toạ độ quán"
    - "D-15: Giờ mở cửa nhập theo dạng mặc định chung + ngoại lệ theo thứ, không phải form 7 dòng"
    - "D-14: Danh sách blacklist SĐT thêm/xoá được ngay trong /admin/settings, là 1 tab không phải route riêng"
    - "D-16: Trang mới theo đúng pattern hiện có của apps/web (giống AdminUsersPage/AdminAuditPage, kể cả màu hardcode) — KHÔNG tạo tokens.css cho apps/web ở phase này"
    - "Chỉ role admin vào được trang này"
  artifacts:
    - path: "apps/web/src/pages/AdminSettingsPage.tsx"
      provides: "Trang cài đặt 2 tab: công tắc + giờ mở cửa, và blacklist SĐT"
      min_lines: 200
    - path: "apps/web/src/pages/DashboardPage.tsx"
      provides: "Widget công tắc bật/tắt 1 chạm"
      contains: "/admin/settings"
  key_links:
    - from: "apps/web/src/App.tsx"
      to: "/admin/settings"
      via: "Route trong block RoleGate allow={['admin']}"
      pattern: "path=\"/admin/settings\""
    - from: "apps/web/src/pages/AdminSettingsPage.tsx"
      to: "/admin/settings và /admin/phone-blacklist"
      via: "api.get/api.put/api.post/api.delete"
      pattern: "'/admin/(settings|phone-blacklist)"
---

<objective>
Dựng giao diện quản lý cho công tắc nhận đơn và blacklist SĐT: widget bật/tắt 1 chạm ở Dashboard + trang
`/admin/settings` mới với 2 tab.

Purpose: REQ-K chặn 2 lớp — BE đã xong ở plan 08-05, đây là lớp điều khiển của chủ quán. D-13 đặt tiêu chí
rõ: tình huống thật là **hết nguyên liệu giữa giờ cao điểm**, chủ quán không có thời gian đi 3 bước, nên
việc tắt phải xong trong 1 chạm ở Dashboard; phần chọn kiểu OFF / lý do / giờ mở cửa / `free_ship_km` mới
đặt ở trang chi tiết. D-14 chốt blacklist là 1 tab của trang này chứ không phải route + mục menu riêng
(việc làm thỉnh thoảng).

Output: 1 trang mới, 1 route, 1 widget. **Theo đúng pattern hiện có của `apps/web`, kể cả màu hardcode
(D-16)** — không tạo `tokens.css` cho `apps/web` trong phase này.
</objective>

<execution_context>
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-CONTEXT.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-PATTERNS.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-05-SUMMARY.md

<interfaces>
<!-- Hợp đồng API từ plan 08-05 + pattern apps/web. -->

GET /admin/settings → `{ data: { settings: StoreSettingsMap, open_hours_input: { default: {from,to},
  exceptions: [{dow,from,to}] }, open_hours_configured: boolean, ordering_status: { enabled, is_open_now,
  blocking_reason } } }`

PUT /admin/settings — body partial, các field nhận được:
  online_ordering_enabled (bool) · online_ordering_off_mode ('MANUAL'|'UNTIL_TOMORROW')
  · online_ordering_off_reason (string ≤255) · store_phone (≤16) · store_lat / store_lng (number)
  · free_ship_km (int 0..100) · distance_factor (number 1..3) · pickup_enabled / delivery_enabled (bool)
  · eta_pickup_min / eta_pickup_max / eta_delivery_min / eta_delivery_max (int 0..240)
  · open_hours_input ({ default, exceptions })
  ⚠ **KHÔNG** gửi `online_ordering_off_until_ms` — BE tự tính mốc 23:59:59 ICT
  Response giống GET.

GET /admin/phone-blacklist?q=&page=&page_size= → `{ data: { items: [{ phone, reason, created_at,
  expires_at, created_by_full_name }], total } }`
POST /admin/phone-blacklist — body `{ phone, reason }`; 409 CONFLICT nếu đã có; 400 VALIDATION_FAILED nếu SĐT sai
DELETE /admin/phone-blacklist/:phone → 200; 404 NOT_FOUND nếu không có

apps/web/src/lib/api.ts — `api` là axios instance (`baseURL` từ `VITE_API_BASE_URL`, `withCredentials`),
  và `extractError(err)` trả `{ code, message }`. Path gọi thẳng `/admin/...` (KHÔNG `/api/admin/...`).

apps/web/src/pages/AdminUsersPage.tsx — khuôn mẫu:
  `import { useEffect, useRef, useState, FormEvent, ReactNode } from 'react'`
  `import { api, extractError } from '../lib/api.ts'` · `import { useToast } from '../components/Toast.tsx'`
  `import { useConfirm } from '../components/ConfirmDialog.tsx'`
  · `refresh()` async có `setLoading` + `toast.push('error', extractError(err).message)` trong catch
  · `useEffect(() => { refresh(); }, [])`
  · submit form: `e.preventDefault()` → validate tay → `setSubmitting(true)` → `api.post(...)`
    → `toast.push('success', '... ✓')` → `finally setSubmitting(false)`
  · **màu hardcode inline**: `#0f766e` (chủ đạo), `#dc2626` (nguy hiểm), `#6b7280` (chữ phụ),
    `#f59e0b` (cảnh báo) — D-16: giữ nguyên kiểu này, KHÔNG tạo token cho `apps/web`
  · class dùng chung: `container`, `container wide`, `card`, `with-bottom-nav`

apps/web/src/pages/AdminAuditPage.tsx — khuôn mẫu tab/filter qua URL:
  `const [params, setParams] = useSearchParams()` · đọc `params.get('...')`
  · ghi: `const n = new URLSearchParams(params); v ? n.set(k,v) : n.delete(k); setParams(n)`

apps/web/src/App.tsx dòng ~58-63 — block `<Route element={<RoleGate allow={['admin']} />}>` đang chứa
  `/dashboard`, `/tables`, `/admin/users`, `/admin/audit`

apps/web/src/pages/DashboardPage.tsx (45 dòng) — grid `repeat(auto-fit, minmax(220px, 1fr))` chứa các
  `<Link className="card">`; `isAdmin = !!user?.is_owner || user?.role === 'admin'`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AdminSettingsPage — tab "Nhận đơn & giờ mở cửa"</name>
  <files>apps/web/src/pages/AdminSettingsPage.tsx</files>
  <read_first>
    - apps/web/src/pages/AdminUsersPage.tsx (khuôn mẫu form + refresh + toast + màu hardcode; đọc toàn bộ khối import và hàm `refresh`/`submit`)
    - apps/web/src/pages/AdminAuditPage.tsx (khuôn mẫu `useSearchParams` cho tab)
    - apps/web/src/lib/api.ts (chữ ký `api` và `extractError`)
    - apps/web/src/components/Toast.tsx (chữ ký `useToast().push`)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-CONTEXT.md D-13, D-15, D-16 và mục Specific Ideas ("Form giờ mở cửa")
    - docs/MILESTONE-02-ONLINE-ORDERING-SPEC.md §4.1 (ý nghĩa từng key) và M2.D-51/52/53 (`free_ship_km` mặc định 10, không auto-tính tiền ship)
  </read_first>
  <action>
    Tạo `AdminSettingsPage.tsx` export named `AdminSettingsPage`. Khung trang: `<div className="container wide with-bottom-nav">`,
    `<h1>Cài đặt nhận đơn</h1>`, rồi 2 tab điều khiển bằng URL `?tab=ordering|blacklist` qua
    `useSearchParams` (mặc định `ordering`) — theo đúng cách `AdminAuditPage` đọc/ghi query param. Tab là
    2 nút `<button>` đổi param, nút đang chọn có `borderBottom: '2px solid #0f766e'` + chữ `#0f766e`.

    Tab **"Nhận đơn & giờ mở cửa"** (task này). `useEffect` gọi `refresh()` → `api.get('/admin/settings')`,
    lưu vào state. Các khối trong tab, mỗi khối 1 `<div className="card">`:

    1. **Công tắc nhận đơn.** Hiện trạng thái hiện tại đọc từ `ordering_status`: đang nhận đơn (chữ
       `#0f766e`) / đang tạm ngưng (chữ `#dc2626`) / ngoài giờ mở cửa (chữ `#f59e0b`, khi
       `blocking_reason === 'OUTSIDE_HOURS'`). Nút bật/tắt. Khi **tắt**: hiện 2 lựa chọn radio kiểu OFF —
       "Tạm ngưng cho tới khi tôi bật lại" (`MANUAL`) và "Tạm ngưng đến hết hôm nay" (`UNTIL_TOMORROW`,
       kèm chú thích "tự nhận đơn lại từ 00:00 sáng mai") — cộng textarea "Lý do hiện cho khách" (≤255 ký
       tự, đếm ký tự còn lại). Gửi `PUT /admin/settings` với `online_ordering_enabled`,
       `online_ordering_off_mode`, `online_ordering_off_reason`. **Không gửi `online_ordering_off_until_ms`** —
       BE tự tính (nếu FE tự tính thì có 2 nguồn sự thật về múi giờ).
       Khi bật lại: chỉ gửi `{ online_ordering_enabled: true }`.
       Sau mỗi PUT: cập nhật state từ response (BE trả shape giống GET) và `toast.push('success', ...)`.
    2. **Giờ mở cửa (D-15).** 1 dòng mặc định "Mở cửa {from} – {to} mọi ngày" với 2 input `type="time"`,
       rồi danh sách **ngoại lệ theo thứ** thêm/xoá được: mỗi ngoại lệ chọn thứ (Chủ nhật..Thứ bảy ↔ dow
       0..6), `from`, `to`, nút xoá. Nút "Thêm ngoại lệ" chỉ cho chọn thứ **chưa có** ngoại lệ. **Không
       dựng form 7 dòng** — nhập 7 lần cùng một con số dễ sai 1 dòng mà không ai phát hiện. Nếu
       `open_hours_configured === false` thì hiện dòng chú thích "Chưa cấu hình — hiện quán nhận đơn mọi
       giờ trong ngày" để chủ quán biết trạng thái thật, và giá trị trong form là **gợi ý** chứ không phải
       dữ liệu đã lưu. Validate trước khi gửi: `from < to` trên từng dòng, sai thì hiện lỗi cạnh dòng đó và
       không gọi API. Gửi `PUT` với `open_hours_input`.
    3. **Giao hàng & liên hệ.** Input `store_phone` (`type="tel"`), `free_ship_km` (number, gợi ý mặc định
       10), `distance_factor` (number, gợi ý 1.3, chú thích "hệ số đường thực tế so với đường chim bay"),
       `store_lat`/`store_lng` (number, chú thích **"chưa nhập thì không tính được khoảng cách tới khách"**
       — đây là nguyên nhân thật khiến màn checkout không hiện số km), 2 checkbox `pickup_enabled` /
       `delivery_enabled`, và 4 input ETA (`eta_pickup_min/max`, `eta_delivery_min/max`). Ghi rõ dưới khối:
       *"Hệ thống không tự tính tiền ship — chỉ hiện quy tắc cho khách, phí cuối do quán chốt khi gọi lại"*
       (M2.D-52).

    Mọi lỗi API: `toast.push('error', extractError(err).message)`. Nút đang gửi thì `disabled` + đổi nhãn.
    Màu **hardcode inline theo đúng convention `apps/web`** (D-16) — không import token, không tạo file CSS
    mới.
  </action>
  <verify>
    <automated>pnpm --filter @order/web typecheck && pnpm --filter @order/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/pages/AdminSettingsPage.tsx` tồn tại, ≥ 200 dòng, export `AdminSettingsPage`
    - `grep -c "online_ordering_off_until_ms" apps/web/src/pages/AdminSettingsPage.tsx` = 0 (FE không tự tính mốc)
    - `grep -c "open_hours_input" apps/web/src/pages/AdminSettingsPage.tsx` ≥ 2
    - `grep -c "UNTIL_TOMORROW" apps/web/src/pages/AdminSettingsPage.tsx` ≥ 1
    - `grep -c "useSearchParams" apps/web/src/pages/AdminSettingsPage.tsx` = 1
    - `grep -oE 'type="time"' apps/web/src/pages/AdminSettingsPage.tsx | wc -l` ≥ 2
    - `grep -c "extractError" apps/web/src/pages/AdminSettingsPage.tsx` ≥ 1
    - Kiểm tay: đăng nhập admin → `/admin/settings?tab=ordering` → tắt kiểu "đến hết hôm nay" với lý do "Hết nguyên liệu" → `curl -s http://localhost:3001/api/public/store | jq '.data.ordering_enabled, .data.off_reason'` trả `false` và đúng chuỗi lý do
    - Kiểm tay: nhập giờ mở cửa mặc định 10:00–22:00 + 1 ngoại lệ Chủ nhật 11:00–20:00 → lưu → reload trang → form hiện lại đúng 1 dòng mặc định + 1 ngoại lệ (không thành 7 dòng)
    - Kiểm tay: nhập `from` muộn hơn `to` → hiện lỗi cạnh dòng, không gọi API (Network tab không có request PUT)
    - `pnpm --filter @order/web build` exit 0
  </acceptance_criteria>
  <done>Chủ quán đặt được kiểu OFF + lý do + giờ mở cửa dạng mặc định/ngoại lệ + tham số giao hàng; trang khách phản ánh ngay.</done>
</task>

<task type="auto">
  <name>Task 2: AdminSettingsPage — tab "Số điện thoại bị chặn"</name>
  <files>apps/web/src/pages/AdminSettingsPage.tsx</files>
  <read_first>
    - apps/web/src/pages/AdminSettingsPage.tsx (file sẽ bổ sung — Task 1 đã dựng khung tab)
    - apps/web/src/pages/AdminUsersPage.tsx (khuôn mẫu bảng danh sách + form thêm + `useConfirm` cho thao tác xoá)
    - apps/web/src/components/ConfirmDialog.tsx (chữ ký `useConfirm()`)
    - apps/web/src/pages/AdminAuditPage.tsx (khuôn mẫu phân trang + ô lọc qua `useSearchParams`)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-CONTEXT.md D-14, D-21
    - docs/MILESTONE-02-ONLINE-ORDERING-SPEC.md M2.D-59 (thêm/xoá tay, không tự hết hạn)
  </read_first>
  <action>
    Bổ sung tab `?tab=blacklist` vào `AdminSettingsPage.tsx`:
    - Form thêm trên cùng: input SĐT (`type="tel"`, chú thích "nhập kiểu nào cũng được — 0912 345 678,
      +84912345678"), input "Lý do" (bắt buộc, ≤255), nút "Thêm vào danh sách". Gọi
      `api.post('/admin/phone-blacklist', { phone, reason })`. Lỗi 409 → `toast.push('error', ...)` với
      message BE trả ("Số này đã có trong danh sách"), lỗi 400 → message BE trả. Thành công →
      `toast.push('success', 'Đã thêm ... ✓')` + refresh danh sách + xoá nội dung form.
    - Ô lọc `?q=` (ghi vào URL qua `useSearchParams`, cùng cách `AdminAuditPage` làm) + phân trang
      `?page=` với `page_size=50`.
    - Bảng: SĐT · Lý do · Người thêm (`created_by_full_name`) · Ngày thêm (format từ epoch ms) · nút "Xoá".
      Cột "Hết hạn" **không hiện** vì mọi bản ghi là vĩnh viễn — thay vào đó ghi 1 dòng chú thích dưới
      bảng: *"Bản ghi không tự hết hạn. Muốn bỏ chặn thì xoá tay."* (M2.D-59).
    - Nút "Xoá": dùng `useConfirm()` như `AdminUsersPage` (đây là thao tác phá huỷ thật, khác với việc giảm
      số lượng ở giỏ hàng của khách), nội dung hỏi ghi rõ SĐT. Gọi
      `api.delete('/admin/phone-blacklist/' + encodeURIComponent(phone))`.
    - Empty state: "Chưa có số nào bị chặn." + 1 dòng giải thích khi nào nên dùng (bom đơn, số ảo).
    - **Copy trong tab giữ tông trung tính** — tiêu đề tab là "Số điện thoại bị chặn" cho admin (nội bộ),
      nhưng lưu ý D-21: **thông báo phía khách** (`PHONE_BLACKLISTED`) không được nói "bị chặn"/"blacklist";
      message đó do BE build ở plan 08-10, tab này không đặt copy cho khách.
  </action>
  <verify>
    <automated>pnpm --filter @order/web typecheck && pnpm --filter @order/web build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "phone-blacklist" apps/web/src/pages/AdminSettingsPage.tsx` ≥ 3 (GET, POST, DELETE)
    - `grep -c "useConfirm" apps/web/src/pages/AdminSettingsPage.tsx` ≥ 1
    - `grep -c "encodeURIComponent" apps/web/src/pages/AdminSettingsPage.tsx` ≥ 1
    - `grep -ciE "blacklist|bị chặn" apps/web/src/pages/AdminSettingsPage.tsx` ≥ 1 (copy nội bộ cho admin — được phép)
    - Kiểm tay: thêm `0912 345 678` → bảng hiện `0912345678` (BE chuẩn hoá); thêm lại `+84912345678` → toast lỗi "đã có trong danh sách"
    - Kiểm tay: bấm Xoá → có hộp xác nhận → xác nhận → dòng biến mất; `/admin/audit` có entry `phone_blacklist.removed`
    - Kiểm tay: gõ `?q=0912` → danh sách lọc đúng, URL giữ `q` sau reload
    - `pnpm --filter @order/web build` exit 0
  </acceptance_criteria>
  <done>Admin thêm/xoá SĐT chặn ngay trong `/admin/settings`, có xác nhận trước khi xoá, danh sách lọc và phân trang được.</done>
</task>

<task type="auto">
  <name>Task 3: Route /admin/settings + widget công tắc 1 chạm ở Dashboard</name>
  <files>apps/web/src/App.tsx, apps/web/src/pages/DashboardPage.tsx</files>
  <read_first>
    - apps/web/src/App.tsx (file sẽ sửa — đọc khối import trang và block `<Route element={<RoleGate allow={['admin']} />}>` dòng ~58-63)
    - apps/web/src/pages/DashboardPage.tsx (file sẽ sửa — toàn bộ 45 dòng: grid card + `isAdmin`)
    - apps/web/src/lib/api.ts và apps/web/src/components/Toast.tsx (dùng trong widget)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-CONTEXT.md D-13 (tiêu chí "tắt trong 1 chạm", **không** mở modal xác nhận ở Dashboard)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-PATTERNS.md mục "apps/web — Widget công tắc ở DashboardPage"
  </read_first>
  <action>
    Sửa `App.tsx`: import `AdminSettingsPage` và thêm `<Route path="/admin/settings" element={<AdminSettingsPage />} />`
    vào **đúng block** `<Route element={<RoleGate allow={['admin']} />}>` đang chứa `/admin/users` và
    `/admin/audit`. Không tạo block mới, không đổi quyền của route nào khác.

    Sửa `DashboardPage.tsx`: thêm 1 widget `<div className="card">` **đặt trước** grid "Truy cập nhanh"
    (đây là thứ chủ quán cần thấy đầu tiên), chỉ render khi `isAdmin`:
    - `useEffect` gọi `api.get('/admin/settings')` lấy `ordering_status` + `settings.online_ordering_off_reason`.
    - Hiện trạng thái bằng chữ + màu: "Đang nhận đơn online" (`#0f766e`) / "Đã tạm ngưng nhận đơn"
      (`#dc2626`, kèm lý do nếu có) / "Ngoài giờ mở cửa" (`#f59e0b`) khi `blocking_reason === 'OUTSIDE_HOURS'`.
    - **1 nút duy nhất** đổi trạng thái: `PUT /admin/settings` với `{ online_ordering_enabled: <đảo>,
      online_ordering_off_mode: 'MANUAL' }` khi tắt, `{ online_ordering_enabled: true }` khi bật.
      **KHÔNG mở modal xác nhận, KHÔNG hỏi lý do ở Dashboard** (D-13: tình huống thật là hết nguyên liệu
      giữa giờ cao điểm — 1 chạm là yêu cầu, không phải tiện lợi). Trong lúc gửi: nút `disabled` + nhãn
      "Đang cập nhật...". Xong: `toast.push('success', ...)` và cập nhật state từ response.
    - Dưới nút, 1 `<Link to="/admin/settings">` nhãn "Cài đặt chi tiết (lý do, giờ mở cửa, số bị chặn) →"
      — đường vào các thao tác cần suy nghĩ.
    - Nếu `GET` lỗi: hiện dòng chữ `#6b7280` "Không đọc được trạng thái nhận đơn" + nút "Thử lại", **không**
      ẩn widget (ẩn đi thì chủ quán tưởng tính năng không tồn tại).
    Xoá khối `<h2>Thông tin phase 01</h2>` + `<ul>` bên dưới nếu vẫn còn — đó là nội dung tạm của phase 1,
    không phải nội dung sản phẩm; giữ lại làm nhiễu chỗ đắt giá nhất của trang. Màu hardcode theo D-16.
  </action>
  <verify>
    <automated>pnpm --filter @order/web typecheck && pnpm --filter @order/web build && pnpm --filter @order/shop build && sh scripts/check-shop-bundle.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'path="/admin/settings"' apps/web/src/App.tsx` = 1, và dòng đó nằm trong cùng block `RoleGate allow={['admin']}` với `/admin/users` (xác nhận bằng đọc file)
    - `grep -c "AdminSettingsPage" apps/web/src/App.tsx` = 2 (import + Route)
    - `grep -c "/admin/settings" apps/web/src/pages/DashboardPage.tsx` ≥ 2 (gọi API + link)
    - `grep -ciE "useConfirm|confirm\(" apps/web/src/pages/DashboardPage.tsx` = 0 (1 chạm, không modal — D-13)
    - `grep -c "Thông tin phase 01" apps/web/src/pages/DashboardPage.tsx` = 0
    - Kiểm tay: đăng nhập admin → Dashboard hiện widget đúng trạng thái → bấm 1 lần → trong ≤ 2 giây trạng thái đổi và `curl -s http://localhost:3001/api/public/store | jq .data.ordering_enabled` phản ánh đúng
    - Kiểm tay: đăng nhập role `order` → không thấy widget (bị `isAdmin` chặn) và vào `/admin/settings` bị `RoleGate` chặn
    - `sh scripts/check-shop-bundle.sh` in `OK` — bundle khách **không** chứa `AdminSettingsPage` (guard đã có `/admin/` trong FORBIDDEN)
  </acceptance_criteria>
  <done>Widget Dashboard tắt/bật nhận đơn trong 1 chạm; route `/admin/settings` chỉ admin vào được; bundle khách không lây code trang mới.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin đã auth (apps/web) → API `/admin/*` | Cookie JWT + AdminGuard; FE chỉ là lớp tiện lợi, không phải lớp phòng thủ |
| bundle `apps/web` → bundle `apps/shop` | Code trang quản lý không được lây sang app khách (M2.D-64) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-38 | Elevation of Privilege | route `/admin/settings` | mitigate | Đặt trong block `RoleGate allow={['admin']}` sẵn có; **và** BE có `AdminGuard` (plan 08-05) — FE gate chỉ là UX, kiểm tra thật ở BE. Acceptance criteria có case role `order` bị chặn |
| T-08-39 | Information Disclosure | `AdminSettingsPage` lọt vào bundle khách | mitigate | `scripts/check-shop-bundle.sh` có `/admin/` trong FORBIDDEN; verify của Task 3 chạy guard |
| T-08-40 | Tampering | FE tự tính mốc `off_until_ms` theo múi giờ máy admin | mitigate | FE **không gửi** field này (grep = 0); BE tính bằng `endOfTodayIctMs` — 1 nguồn sự thật về múi giờ |
| T-08-41 | Repudiation | đổi setting/blacklist không rõ ai làm | mitigate | `AuditInterceptor` + 3 nhánh action mới (plan 08-05); bảng blacklist lưu `created_by_full_name` và hiện trên UI |
| T-08-42 | Tampering (XSS) | `off_reason` do admin nhập, hiện lại ở trang khách | mitigate | React escape ở cả 2 app; BE lưu nguyên văn không render HTML; giới hạn 255 ký tự |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | Plan này KHÔNG cài package nào |
</threat_model>

<verification>
1. `pnpm --filter @order/web typecheck` + `build` — sạch
2. `sh scripts/check-shop-bundle.sh` — `OK` (bundle khách không lây code admin)
3. Kiểm tay 3 luồng: tắt 1 chạm ở Dashboard · đặt giờ mở cửa dạng mặc định+ngoại lệ · thêm/xoá blacklist
4. Kiểm tay quyền: role `order` không vào được `/admin/settings`, không thấy widget
5. `/admin/audit` có `settings.updated`, `phone_blacklist.added`, `phone_blacklist.removed`
</verification>

<success_criteria>
- Widget Dashboard tắt/bật trong 1 chạm, không modal, phản ánh ra `/api/public/store`
- `/admin/settings` có 2 tab qua `?tab=`, chỉ admin vào được
- Giờ mở cửa nhập dạng mặc định + ngoại lệ (không phải 7 dòng), có validate `from < to`
- Blacklist thêm/xoá/lọc/phân trang được, có xác nhận trước khi xoá
- Màu hardcode theo convention `apps/web` (D-16), không tạo `tokens.css` cho `apps/web`
</success_criteria>

<output>
Create `.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-08-SUMMARY.md` when done
</output>
