---
phase: 08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n
plan: 05
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - apps/api/src/modules/settings/settings.defaults.ts
  - apps/api/src/modules/settings/settings.service.ts
  - apps/api/src/modules/settings/settings.controller.ts
  - apps/api/src/modules/settings/phone-blacklist.controller.ts
  - apps/api/src/modules/settings/settings.module.ts
  - apps/api/src/modules/public/phone.ts
  - apps/api/src/modules/public/phone.test.ts
  - apps/api/src/modules/public/store-status.ts
  - apps/api/src/modules/public/store-status.test.ts
  - apps/api/src/modules/audit/audit.interceptor.ts
  - apps/api/src/app.module.ts
autonomous: true
requirements: [REQ-K, REQ-L]

must_haves:
  truths:
    - "Admin đọc và ghi được toàn bộ store_settings qua 1 endpoint, mọi lần ghi đều có audit log"
    - "Bật/tắt nhận đơn bằng 1 request duy nhất (đủ cho widget 1 chạm ở Dashboard)"
    - "Chọn OFF đến hết hôm nay thì BE tự tính mốc 23:59:59 giờ Việt Nam, FE không phải tự tính"
    - "Admin thêm/xoá SĐT blacklist bằng tay, bản ghi không tự hết hạn"
    - "SĐT được chuẩn hoá trước khi lưu/so khớp — 0912345678, 0912 345 678 và +84912345678 là cùng một số"
    - "Chỉ role admin gọi được 2 nhóm endpoint này"
  artifacts:
    - path: "apps/api/src/modules/settings/settings.defaults.ts"
      provides: "Bảng key mặc định theo spec §4.1 + kiểu parse của từng key"
      contains: "escalate_autooff_after_s"
    - path: "apps/api/src/modules/settings/settings.service.ts"
      provides: "readAll/readOrderingSettings/updateMany có ghi actor"
      exports: ["SettingsService"]
    - path: "apps/api/src/modules/settings/settings.controller.ts"
      provides: "GET + PUT /admin/settings"
      contains: "@Controller('admin/settings')"
    - path: "apps/api/src/modules/settings/phone-blacklist.controller.ts"
      provides: "GET + POST + DELETE /admin/phone-blacklist"
      contains: "@Controller('admin/phone-blacklist')"
    - path: "apps/api/src/modules/public/phone.ts"
      provides: "normalizePhone — chuẩn hoá SĐT Việt Nam"
      exports: ["normalizePhone"]
  key_links:
    - from: "apps/api/src/app.module.ts"
      to: "SettingsModule"
      via: "mảng imports"
      pattern: "SettingsModule"
    - from: "apps/api/src/modules/audit/audit.interceptor.ts"
      to: "'settings.updated' / 'phone_blacklist.added' / 'phone_blacklist.removed'"
      via: "deriveActionKind if-chain"
      pattern: "settings\\.updated"
    - from: "apps/api/src/modules/settings/settings.service.ts"
      to: "evaluateOrderingStatus"
      via: "import từ ../public/store-status.js"
      pattern: "evaluateOrderingStatus"
---

<objective>
Dựng tầng đọc/ghi `store_settings` và toàn bộ API admin của công tắc nhận đơn + blacklist SĐT, kèm audit
log tên đẹp.

Purpose: REQ-K yêu cầu chặn **2 lớp** (FE khoá nút, BE trả 409) — BE là nguồn sự thật duy nhất về "đang
nhận đơn hay không" (M2.D-27), nên tầng này phải có trước cả `GET /api/public/store` (plan 08-07) và cả
endpoint submit (plan 08-10). REQ-L cần bảng blacklist có đường thêm/xoá tay (M2.D-59, không TTL, không
cron dọn). M2.D-25 yêu cầu mọi lần đổi setting ghi audit log — dùng lại `AuditInterceptor` đã chạy
production, không viết cơ chế log mới.

Output: 1 module Nest mới (`settings`), 5 endpoint admin, `normalizePhone` có test, 3 nhánh mới trong
audit interceptor.
</objective>

<execution_context>
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-CONTEXT.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-RESEARCH.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-PATTERNS.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-02-SUMMARY.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-01-SUMMARY.md

<interfaces>
<!-- Hợp đồng cần dùng. Đã trích sẵn — executor không phải đi tìm. -->

apps/api/src/modules/public/store-status.ts (plan 08-01):
  export type OpenHourRule = { dow: 0|1|2|3|4|5|6; from: string; to: string }
  export type StoreOrderingSettings = { online_ordering_enabled: boolean;
    online_ordering_off_mode: 'MANUAL'|'UNTIL_TOMORROW'; online_ordering_off_reason: string;
    online_ordering_off_until_ms: number|null; open_hours: OpenHourRule[] }
  export type OrderingStatus = { enabled: boolean; is_open_now: boolean;
    blocking_reason: 'MANUAL_OFF'|'OUTSIDE_HOURS'|null }
  export function evaluateOrderingStatus(s: StoreOrderingSettings, nowMs: number): OrderingStatus
  export type OpenHoursInput = { default: {from,to}; exceptions: Array<{dow,from,to}> }
  export function expandToWeek(input: OpenHoursInput): OpenHourRule[]
  export function collapseToDefaultExceptions(rules: OpenHourRule[]): OpenHoursInput
  (VN_OFFSET_MS = 7h, ICT cố định không DST)

apps/api/src/modules/settings/entities/store-settings.entity.ts (plan 08-02):
  class StoreSetting — key (PK varchar 64, TỪ KHOÁ MySQL: raw SQL phải bọc backtick) · value (text)
  · updated_at (epoch ms) · updated_by_user_id · updated_by_full_name

apps/api/src/modules/settings/entities/phone-blacklist.entity.ts (plan 08-02):
  class PhoneBlacklist — phone (PK varchar 16) · reason · created_at · expires_at (NULL = vĩnh viễn)
  · created_by_user_id · created_by_full_name

apps/api/src/modules/admin/users.controller.ts — khuôn mẫu controller admin:
  `@Controller('admin/users')` **KHÔNG có `/api`** · `@UseGuards(AdminGuard)` ở **class-level**
  · DTO bằng `class-validator` (`@IsOptional() @IsString() @MinLength() @MaxLength()`)
  · repo qua `@InjectRepository(Entity) private readonly repo: Repository<Entity>`
  · lỗi: `throw new NotFoundException({ code: 'NOT_FOUND', message: '...' })`
  · thành công: `return { data: { ... } }`
  · AdminGuard ở `../auth/guards/admin.guard.js`

apps/api/src/modules/audit/audit.interceptor.ts:
  `deriveActionKind()` (dòng ~19-65) là if-chain match `path`+`method`, kết thúc bằng fallback
  `` return `${method.toLowerCase()}.${path...}` ``
  `extractTargetKind()` (dòng ~125-131) map prefix path → 'auth'|'user'|'audit'|'setup'
  Interceptor đã đăng ký GLOBAL ở `app.module.ts` dòng 45 → controller mới không cần khai lại

apps/api/src/app.module.ts — mảng `imports` có `AdminModule`, `MenuModule`, `PublicModule`...; mỗi module
  có comment ngắn giải thích lý do đặt ở đó (xem dòng 24-29, 37-40 làm mẫu)

docs/MILESTONE-02-ONLINE-ORDERING-SPEC.md §4.1 — 20 key + kiểu + mặc định (nguồn sự thật):
  online_ordering_enabled bool true · online_ordering_off_mode enum MANUAL · online_ordering_off_reason text ''
  · online_ordering_off_until_ms bigint null · open_hours json [] · store_phone string ''
  · store_lat/store_lng decimal null · free_ship_km int 10 · distance_factor decimal 1.3
  · pickup_enabled/delivery_enabled bool true · escalate_sms_after_s int 90
  · escalate_autooff_after_s int 1800 (M2.D-60, KHÔNG phải 300 — dòng 469 spec là stale)
  · notify_sms_recipients json [] · notify_email_recipients json []
  · eta_pickup_min 15 / eta_pickup_max 25 / eta_delivery_min 30 / eta_delivery_max 45
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: settings.defaults + SettingsService + helper endOfTodayIctMs</name>
  <files>apps/api/src/modules/settings/settings.defaults.ts, apps/api/src/modules/settings/settings.service.ts, apps/api/src/modules/public/store-status.ts, apps/api/src/modules/public/store-status.test.ts</files>
  <read_first>
    - apps/api/src/modules/public/store-status.ts (file sẽ bổ sung — đọc `VN_OFFSET_MS` và cách hàm hiện có xử lý múi giờ)
    - apps/api/src/modules/public/store-status.test.ts (file sẽ bổ sung test — giữ nguyên các `describe` đã có)
    - apps/api/src/modules/settings/entities/store-settings.entity.ts (bảng key-value, cột `value` là text)
    - docs/MILESTONE-02-ONLINE-ORDERING-SPEC.md §4.1 dòng ~236-262 (20 key + kiểu + mặc định)
    - apps/api/src/modules/orders/orders.service.ts (nếu có) hoặc apps/api/src/modules/menu/menu.controller.ts (convention service/repo injection của repo này)
  </read_first>
  <behavior>
    Bổ sung vào `store-status.test.ts` (giữ nguyên test cũ):
    - `endOfTodayIctMs(nowMs)` trả mốc 23:59:59.999 ICT của **ngày ICT** chứa `nowMs`
    - `nowMs` = 17:30 UTC (tức 00:30 ICT hôm sau) → mốc trả về là cuối ngày ICT **hôm sau theo UTC**, không phải cuối ngày UTC hiện tại
    - `evaluateOrderingStatus` với `off_until_ms = endOfTodayIctMs(t)`, `nowMs = t` → còn `MANUAL_OFF`
    - cùng dữ liệu, `nowMs = endOfTodayIctMs(t) + 1` → `enabled: true` (đã qua nửa đêm ICT)
  </behavior>
  <action>
    Bổ sung vào `store-status.ts` (không sửa hàm đã có): `export function endOfTodayIctMs(nowMs: number): number`
    — trả epoch ms của 23:59:59.999 giờ ICT trong ngày ICT chứa `nowMs`, tính bằng `VN_OFFSET_MS` đã có,
    KHÔNG gọi `Date.now()`. Đây là mốc lưu vào `online_ordering_off_until_ms` khi admin chọn "OFF đến hết
    hôm nay" (M2.D-28) — tính ở BE để FE không phải biết múi giờ. Thêm test theo `<behavior>`.

    Tạo `settings.defaults.ts`: hằng `SETTINGS_DEFAULTS` liệt kê **đủ 20 key** của spec §4.1 với giá trị
    mặc định đúng như bảng ở `<interfaces>`, và khai kiểu parse cho từng key theo dạng
    `{ key, kind: 'bool'|'int'|'float'|'string'|'json', default: <giá trị> }`. Chú ý 3 điểm dễ sai:
    (1) `escalate_autooff_after_s` = **1800** (M2.D-60 ghi đè M2.D-36; dòng 469 spec ghi 300 là stale, ghi
    comment cảnh báo tại chỗ); (2) `open_hours` default `[]` nghĩa là **chưa cấu hình → không giới hạn
    giờ**, khớp cách `evaluateOrderingStatus` xử lý (plan 08-01); (3) `store_lat`/`store_lng` default
    `null`, chưa có thì không tính được `distance_km`. Export thêm type `StoreSettingsMap` (map key →
    giá trị đã parse) để service và controller dùng chung, không tự khai type trùng.

    Tạo `settings.service.ts` (`@Injectable()`), inject `@InjectRepository(StoreSetting)`:
    - `readAll(): Promise<StoreSettingsMap>` — đọc mọi row, parse `value` theo `kind`, **merge lên
      `SETTINGS_DEFAULTS`** để key chưa có row vẫn trả mặc định. Không cần seed script: DB trống vẫn chạy
      đúng, row chỉ sinh khi admin ghi lần đầu. Parse lỗi (JSON hỏng) → dùng default + log warn, KHÔNG
      throw (một setting hỏng không được làm sập trang menu công khai).
    - `readOrderingSettings(): Promise<StoreOrderingSettings>` — trả đúng shape hàm thuần cần.
    - `getOrderingStatus(nowMs: number): Promise<OrderingStatus>` — gọi `readOrderingSettings()` rồi
      `evaluateOrderingStatus(s, nowMs)`. **Đây là đường duy nhất** để mọi nơi biết "đang nhận đơn không";
      cấm đọc thẳng cột `online_ordering_enabled` ở bất kỳ đâu khác (cột có thể `false` trong khi thực tế
      đã tự-ON qua nửa đêm — D-17).
    - `updateMany(patch: Partial<StoreSettingsMap>, actor: { user_id: string; full_name: string }): Promise<StoreSettingsMap>`
      — upsert từng key (serialize theo `kind`: bool → `'true'`/`'false'`, json → `JSON.stringify`), ghi
      `updated_by_user_id`/`updated_by_full_name`, bỏ qua key không có trong `SETTINGS_DEFAULTS` (chống
      ghi key rác). Trả về map sau khi ghi.
    - Không cache trong bộ nhớ: công tắc phải phản ánh ngay lần đọc kế tiếp (khoảng cách giữa "chủ quán
      tắt" và "khách bị chặn" phải bằng 0).
  </action>
  <verify>
    <automated>pnpm --filter @order/api test -- store-status.test.ts && pnpm --filter @order/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function endOfTodayIctMs" apps/api/src/modules/public/store-status.ts` = 1; `grep -c "Date.now()" apps/api/src/modules/public/store-status.ts` = 0
    - `apps/api/src/modules/settings/settings.defaults.ts` chứa đủ 20 key; `grep -c "escalate_autooff_after_s" ...` = 1 và giá trị cạnh nó là `1800`
    - `grep -c "getOrderingStatus" apps/api/src/modules/settings/settings.service.ts` ≥ 1 và file có import `evaluateOrderingStatus`
    - `pnpm --filter @order/api test -- store-status.test.ts` exit 0 với ≥ 15 test pass (11 cũ + 4 mới)
    - `pnpm --filter @order/api typecheck` exit 0
  </acceptance_criteria>
  <done>Đọc/ghi settings hoạt động trên DB trống (fallback default), `getOrderingStatus` là đường duy nhất suy ra trạng thái công tắc, `endOfTodayIctMs` có test.</done>
</task>

<task type="auto">
  <name>Task 2: GET + PUT /admin/settings, đăng ký SettingsModule, 3 nhánh audit mới</name>
  <files>apps/api/src/modules/settings/settings.controller.ts, apps/api/src/modules/settings/settings.module.ts, apps/api/src/modules/audit/audit.interceptor.ts, apps/api/src/app.module.ts</files>
  <read_first>
    - apps/api/src/modules/admin/users.controller.ts (khuôn mẫu đầy đủ: DTO class-validator, `@UseGuards(AdminGuard)` class-level, shape `{ data: ... }`)
    - apps/api/src/modules/admin/admin.module.ts (khuôn mẫu module nhỏ: `imports: [AuthModule]`, `controllers: [...]`)
    - apps/api/src/modules/audit/audit.interceptor.ts (file sẽ sửa — đọc toàn bộ `deriveActionKind` và `extractTargetKind`, và cách lấy actor từ request)
    - apps/api/src/app.module.ts (file sẽ sửa — mảng `imports` + comment style)
    - apps/api/src/modules/public/store-status.ts (dùng `expandToWeek`/`collapseToDefaultExceptions` cho D-15)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-PATTERNS.md mục "apps/api — Controller admin mới" và "apps/api — Audit interceptor mở rộng" (có sẵn 5 dòng if-chain cần thêm)
  </read_first>
  <action>
    Tạo `settings.controller.ts` với `@Controller('admin/settings')` — **KHÔNG có tiền tố `/api`**. Đây là
    lệch chữ spec §5.2 (spec ghi `/api/admin/settings`) nhưng khớp convention thật của repo
    (`admin/users`, `admin/audit`) và khớp `apps/web/src/lib/api.ts` (baseURL là host, path gọi thẳng
    `/admin/...`). Ghi comment ngay đầu file dẫn về entry `OVERRIDE-DEBT.md` (plan 08-13 tạo entry).
    `@UseGuards(AdminGuard)` ở class-level.
    - `GET /admin/settings` → `{ data: { settings: <StoreSettingsMap>, open_hours_input: <OpenHoursInput>, ordering_status: <OrderingStatus> } }`.
      `open_hours_input` = `collapseToDefaultExceptions(settings.open_hours)` — form D-15 nhập theo dạng
      "mặc định chung + ngoại lệ theo thứ", nên BE trả sẵn dạng đó, FE không phải tự gom. Nếu
      `open_hours` rỗng thì trả `open_hours_input` với `default` gợi ý `{ from: '10:00', to: '22:00' }` và
      `exceptions: []`, kèm cờ `open_hours_configured: false` để FE biết đây là gợi ý chứ không phải dữ
      liệu đã lưu.
    - `PUT /admin/settings` nhận DTO `UpdateSettingsDto` bằng `class-validator`, mọi field `@IsOptional()`:
      `online_ordering_enabled` (bool), `online_ordering_off_mode` (`@IsIn(['MANUAL','UNTIL_TOMORROW'])`),
      `online_ordering_off_reason` (string max 255), `store_phone` (string max 16),
      `store_lat`/`store_lng` (number, range hợp lệ), `free_ship_km` (int 0..100),
      `distance_factor` (number 1..3), `pickup_enabled`/`delivery_enabled` (bool),
      `eta_pickup_min`/`eta_pickup_max`/`eta_delivery_min`/`eta_delivery_max` (int 0..240),
      `open_hours_input` (object `{ default: {from,to}, exceptions: [...] }`, validate `HH:mm` bằng
      `@Matches(/^([01]\d|2[0-3]):[0-5]\d$/)` trên từng field giờ).
      Xử lý bắt buộc trong handler:
      · Nếu có `open_hours_input` → gọi `expandToWeek()` rồi lưu vào key `open_hours` (7 phần tử).
      · Nếu `online_ordering_enabled === false` và `online_ordering_off_mode === 'UNTIL_TOMORROW'` → BE tự
        set `online_ordering_off_until_ms = endOfTodayIctMs(Date.now())`. FE **không được** gửi mốc này.
      · Nếu `online_ordering_enabled === false` và mode là `MANUAL` → set `off_until_ms = null`.
      · Nếu `online_ordering_enabled === true` → xoá `off_reason` về `''` và `off_until_ms` về `null`
        (bật lại thì không còn lý do tạm ngưng treo lại).
      · Lấy actor từ request (theo đúng cách `users.controller.ts` đọc user đã auth) và truyền vào
        `updateMany`.
      · Response giống `GET` để FE cập nhật state trong 1 vòng, không phải gọi lại.
      **Không** thêm endpoint riêng cho việc bật/tắt: widget 1 chạm ở Dashboard (D-13) gọi đúng `PUT` này
      với 1 field — vẫn là 1 request, không cần bề mặt API thứ hai.
    - Khi bị chặn vì công tắc, message lỗi cho khách được build ở plan 08-10; controller này không dựng
      message khách.

    Tạo `settings.module.ts`: `imports: [TypeOrmModule.forFeature([StoreSetting, PhoneBlacklist]), AuthModule]`,
    `controllers: [SettingsController, PhoneBlacklistController]` (controller thứ hai ở Task 3 — khai sẵn
    ở đây và tạo file ở Task 3, hoặc khai ở Task 3; miễn cuối plan module có đủ 2), `providers: [SettingsService]`,
    `exports: [SettingsService]` — plan 08-07 và 08-10 sẽ import `SettingsModule` để dùng service này.
    Thêm `SettingsModule` vào mảng `imports` của `app.module.ts` kèm comment 1-2 dòng: đây là nguồn sự
    thật của công tắc nhận đơn, `PublicModule` và luồng submit đều đọc qua nó.

    Sửa `audit.interceptor.ts` — thêm vào `deriveActionKind()` **trước dòng fallback**:
    `if (path === '/admin/settings' && method === 'PUT') return 'settings.updated';`
    `if (path === '/admin/phone-blacklist' && method === 'POST') return 'phone_blacklist.added';`
    `if (path.match(/^\/admin\/phone-blacklist\/[^/]+$/) && method === 'DELETE') return 'phone_blacklist.removed';`
    Và thêm vào `extractTargetKind()`:
    `if (path.startsWith('/admin/settings')) return 'settings';`
    `if (path.startsWith('/admin/phone-blacklist')) return 'phone_blacklist';`
    Không đổi gì khác trong file — interceptor đã global, chỉ cần route match để tên action đọc được
    trong `/admin/audit` (M2.D-25 vẫn thoả về mặt "có ghi" dù không match, nhưng tên xấu là hồi quy chất
    lượng so với convention hiện có).
  </action>
  <verify>
    <automated>pnpm --filter @order/api typecheck && pnpm --filter @order/api test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "@Controller('admin/settings')" apps/api/src/modules/settings/settings.controller.ts` trả 1 dòng; `grep -c "api/admin" apps/api/src/modules/settings/settings.controller.ts` = 0
    - `grep -c "@UseGuards(AdminGuard)" apps/api/src/modules/settings/settings.controller.ts` ≥ 1
    - `grep -E "settings\.updated|phone_blacklist\.added|phone_blacklist\.removed" apps/api/src/modules/audit/audit.interceptor.ts | wc -l` = 3
    - `grep -c "SettingsModule" apps/api/src/app.module.ts` ≥ 2 (import + phần tử mảng)
    - Chạy thật (dev server + cookie admin): `curl -s http://localhost:3001/admin/settings -H "Cookie: ssp_token=<admin>"` trả JSON có `settings`, `open_hours_input`, `ordering_status`
    - `curl -X PUT http://localhost:3001/admin/settings -H "Content-Type: application/json" -H "Origin: http://localhost:5173" -H "Cookie: ssp_token=<admin>" -d '{"online_ordering_enabled":false,"online_ordering_off_mode":"UNTIL_TOMORROW","online_ordering_off_reason":"Hết nguyên liệu"}'` → 200, và `GET` lại cho `ordering_status.blocking_reason = "MANUAL_OFF"` với `settings.online_ordering_off_until_ms` là mốc 23:59:59 hôm nay ICT
    - Cùng request trên với cookie role `order` (không phải admin) → 403 `ADMIN_REQUIRED`
    - Sau request PUT: `curl -s "http://localhost:3001/admin/audit?..."` (hoặc truy vấn bảng audit) có 1 dòng `action_kind = 'settings.updated'`
    - `pnpm --filter @order/api test` exit 0
  </acceptance_criteria>
  <done>2 endpoint settings chạy đúng, chỉ admin gọi được, mọi lần PUT sinh audit log tên `settings.updated`, mốc OFF-đến-hết-hôm-nay do BE tính.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: normalizePhone (có test) + 3 endpoint /admin/phone-blacklist</name>
  <files>apps/api/src/modules/public/phone.ts, apps/api/src/modules/public/phone.test.ts, apps/api/src/modules/settings/phone-blacklist.controller.ts, apps/api/src/modules/settings/settings.module.ts</files>
  <read_first>
    - apps/api/src/common/origin-allowlist.ts + origin-allowlist.test.ts (khuôn mẫu module thuần + test)
    - apps/api/src/modules/admin/users.controller.ts (khuôn mẫu CRUD admin: DTO, `@Delete(':id')`, NotFoundException shape)
    - apps/api/src/modules/settings/entities/phone-blacklist.entity.ts (PK là `phone`, `expires_at` NULL = vĩnh viễn)
    - apps/api/src/modules/settings/settings.module.ts (file sẽ bổ sung controller thứ hai)
    - docs/MILESTONE-02-ONLINE-ORDERING-SPEC.md M2.D-59 (blacklist thêm/xoá tay, không TTL, không cron dọn — ghi đè M2.D-41)
  </read_first>
  <behavior>
    phone.test.ts phải phủ:
    - `normalizePhone('0912345678')` → `'0912345678'`
    - `normalizePhone('0912 345 678')` → `'0912345678'` (bỏ khoảng trắng)
    - `normalizePhone('091-234-5678')` → `'0912345678'` (bỏ gạch nối)
    - `normalizePhone('+84912345678')` → `'0912345678'` (đổi tiền tố +84 thành 0)
    - `normalizePhone('84912345678')` → `'0912345678'`
    - `normalizePhone('(091) 234 5678')` → `'0912345678'`
    - `normalizePhone('  0912345678  ')` → `'0912345678'`
    - Chuỗi không phải số hợp lệ (rỗng, `'abc'`, `'091'`) → trả `null` (gọi bên ngoài quyết định lỗi gì)
    - Kết quả luôn ≤ 16 ký tự (khớp `varchar(16)` của cả 2 bảng)
  </behavior>
  <action>
    Tạo `apps/api/src/modules/public/phone.ts`: `export function normalizePhone(raw: string): string | null`
    — loại mọi ký tự không phải chữ số ngoại trừ dấu `+` đầu chuỗi, đổi tiền tố `+84`/`84` thành `0`, trả
    `null` nếu kết quả không khớp `/^0\d{8,10}$/`. Module thuần, không import `@nestjs/*`/`typeorm`.
    Comment đầu file: dạng chuẩn hoá này là **khoá so khớp duy nhất** cho blacklist, cho kiểm "1 đơn
    mở/SĐT" và cho đếm rate limit — nếu 2 nơi chuẩn hoá khác nhau thì cả 3 cơ chế chống lạm dụng đều rò.
    Viết `phone.test.ts` theo khuôn `origin-allowlist.test.ts` phủ đủ `<behavior>`.

    Tạo `phone-blacklist.controller.ts` với `@Controller('admin/phone-blacklist')` (không `/api`),
    `@UseGuards(AdminGuard)` class-level, inject `@InjectRepository(PhoneBlacklist)`:
    - `GET /admin/phone-blacklist` — trả `{ data: { items: [...], total } }`, sắp `created_at DESC`, có
      `?q=` lọc theo SĐT (chuẩn hoá `q` bằng `normalizePhone` trước khi so, nếu `null` thì lọc theo
      `LIKE`), phân trang `page`/`page_size` mặc định 1/100 theo đúng kiểu `users.controller.ts`.
    - `POST /admin/phone-blacklist` — DTO `{ phone: string (bắt buộc), reason: string 1..255 (bắt buộc) }`.
      Chuẩn hoá `phone` bằng `normalizePhone`; `null` → `BadRequestException({ code: 'VALIDATION_FAILED', message: 'Số điện thoại không hợp lệ' })`.
      Đã tồn tại → `ConflictException({ code: 'CONFLICT', message: 'Số này đã có trong danh sách' })`.
      Lưu `expires_at = null` (**vĩnh viễn** — M2.D-59) và `created_by_user_id`/`created_by_full_name` từ
      actor. **Không** nhận `expires_at` từ client: cột giữ lại chỉ để sau muốn chặn tạm thời, phase 8
      không mở đường ghi.
    - `DELETE /admin/phone-blacklist/:phone` — chuẩn hoá param trước khi xoá; không tìm thấy →
      `NotFoundException({ code: 'NOT_FOUND', message: 'Số không có trong danh sách' })`. Xoá thật (hard
      delete) vì M2.D-59 mô tả thao tác tay, không cần lịch sử ở bảng này (audit log đã ghi ai xoá).
    Thêm `PhoneBlacklistController` vào `controllers` của `settings.module.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @order/api test -- phone.test.ts && pnpm --filter @order/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function normalizePhone" apps/api/src/modules/public/phone.ts` = 1; `grep -cE "@nestjs|typeorm" apps/api/src/modules/public/phone.ts` = 0
    - `pnpm --filter @order/api test -- phone.test.ts` exit 0 với ≥ 9 test pass
    - `grep -n "@Controller('admin/phone-blacklist')" apps/api/src/modules/settings/phone-blacklist.controller.ts` trả 1 dòng
    - `grep -c "expires_at" apps/api/src/modules/settings/phone-blacklist.controller.ts` ≥ 1 và giá trị ghi là `null` (không có đường nhận từ client — xác nhận bằng đọc DTO không có field `expires_at`)
    - Chạy thật: `curl -X POST http://localhost:3001/admin/phone-blacklist -H "Content-Type: application/json" -H "Origin: http://localhost:5173" -H "Cookie: ssp_token=<admin>" -d '{"phone":"0912 345 678","reason":"Bom đơn 3 lần"}'` → 201/200, rồi `GET` cho thấy `phone = "0912345678"` (đã chuẩn hoá) và `expires_at = null`
    - Gửi lại cùng số → 409 `CONFLICT`; `DELETE /admin/phone-blacklist/+84912345678` → 200 (chuẩn hoá param khớp cùng bản ghi)
    - Audit log có dòng `phone_blacklist.added` và `phone_blacklist.removed`
    - `pnpm --filter @order/api test` full suite exit 0
  </acceptance_criteria>
  <done>Blacklist thêm/xoá được bằng tay qua API admin, SĐT chuẩn hoá 1 dạng duy nhất có test, bản ghi không tự hết hạn.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin đã auth → `store_settings` | Giá trị setting ảnh hưởng trực tiếp việc chặn/cho phép đơn của khách |
| `store_settings.value` (text) → logic nghiệp vụ | Parse từ text; JSON hỏng không được làm sập trang khách |
| SĐT nhập tay bởi admin → khoá so khớp blacklist | Sai chuẩn hoá = blacklist vô hiệu |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-20 | Elevation of Privilege | `/admin/settings`, `/admin/phone-blacklist` | mitigate | `@UseGuards(AdminGuard)` ở class-level (ASVS V4); acceptance criteria có case role `order` → 403 `ADMIN_REQUIRED` |
| T-08-21 | Tampering | `PUT /admin/settings` ghi key rác | mitigate | `updateMany` bỏ qua mọi key không có trong `SETTINGS_DEFAULTS`; DTO `class-validator` chặn kiểu sai |
| T-08-22 | Tampering | FE tự gửi `online_ordering_off_until_ms` | mitigate | Mốc do BE tính bằng `endOfTodayIctMs(Date.now())`; DTO **không có** field này |
| T-08-23 | Repudiation | đổi setting không để lại dấu | mitigate | `AuditInterceptor` global + 3 nhánh `deriveActionKind` mới (M2.D-25); acceptance criteria kiểm dòng audit thật |
| T-08-24 | Denial of Service | JSON `open_hours` hỏng làm sập `GET /api/public/store` | mitigate | `readAll` parse lỗi → dùng default + log warn, không throw |
| T-08-25 | Spoofing | biến thể SĐT lách blacklist (`+84…` vs `0…`) | mitigate | `normalizePhone` là khoá duy nhất, dùng ở cả POST/DELETE/GET và ở luồng submit (08-10); 9 test case cố định hành vi |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | Plan này KHÔNG cài package nào |
</threat_model>

<verification>
1. `pnpm --filter @order/api typecheck` — sạch
2. `pnpm --filter @order/api test` — full suite xanh (bao gồm 4 test mới của `store-status` và 9 của `phone`)
3. `curl` 5 endpoint theo acceptance criteria với cookie admin và cookie role `order` (403)
4. Truy vấn bảng audit xác nhận 3 `action_kind` mới xuất hiện đúng tên
</verification>

<success_criteria>
- `GET`/`PUT /admin/settings` và 3 endpoint blacklist chạy, chỉ admin gọi được
- `SETTINGS_DEFAULTS` đủ 20 key, `escalate_autooff_after_s = 1800`
- DB trống vẫn đọc được settings (fallback default), không cần seed script
- `getOrderingStatus()` là đường duy nhất suy ra trạng thái công tắc
- `normalizePhone` + `endOfTodayIctMs` có test, full suite xanh
- 3 nhánh audit mới hiện đúng tên trong `/admin/audit`
</success_criteria>

<output>
Create `.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-05-SUMMARY.md` when done
</output>
