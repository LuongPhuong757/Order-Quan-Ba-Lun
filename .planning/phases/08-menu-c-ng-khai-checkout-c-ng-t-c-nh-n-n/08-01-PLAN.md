---
phase: 08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/schemas/src/public-store.ts
  - packages/schemas/src/public-menu.ts
  - packages/schemas/src/public-orders.ts
  - packages/schemas/src/errors.ts
  - packages/schemas/src/index.ts
  - apps/api/src/modules/public/store-status.ts
  - apps/api/src/modules/public/store-status.test.ts
  - apps/api/src/modules/public/haversine.ts
  - apps/api/src/modules/public/haversine.test.ts
  - apps/api/src/modules/public/order-guard.ts
  - apps/api/src/modules/public/order-guard.test.ts
  - apps/api/src/modules/public/ip-hash.ts
  - apps/api/src/modules/public/ip-hash.test.ts
autonomous: true
requirements: [REQ-I, REQ-J, REQ-K, REQ-L]

must_haves:
  truths:
    - "Hợp đồng dữ liệu của mọi endpoint /api/public/* tồn tại dưới dạng zod schema dùng chung được cả BE và FE"
    - "Logic công tắc nhận đơn (bật/tắt thủ công, OFF-đến-hết-hôm-nay, giờ mở cửa) chạy được và test được không cần DB"
    - "OFF đến hết hôm nay tự ON lại sau 00:00 giờ Việt Nam — chứng minh bằng test truyền nowMs qua nửa đêm"
    - "Thứ tự ưu tiên 6 mã lỗi khi submit đơn cố định theo spec §7 và có test cho mọi tổ hợp"
    - "IP không bao giờ được lưu dạng thô — hàm hash là HMAC-SHA256 có salt"
  artifacts:
    - path: "packages/schemas/src/public-menu.ts"
      provides: "PublicMenuItem (đúng 7 field, M2.D-43) + PublicMenuGroup"
      exports: ["PublicMenuItem", "PublicMenuGroup"]
    - path: "packages/schemas/src/public-orders.ts"
      provides: "OnlineOrderSubmit + OnlineOrderItemInput (KHÔNG có field giá) + PublicOrderStatus"
      exports: ["OnlineOrderSubmit", "OnlineOrderItemInput", "PublicOrderStatus"]
    - path: "packages/schemas/src/public-store.ts"
      provides: "PublicStoreStatus response schema"
      exports: ["PublicStoreStatus", "OpenHourRule"]
    - path: "apps/api/src/modules/public/store-status.ts"
      provides: "evaluateOrderingStatus / expandToWeek / collapseToDefaultExceptions"
      contains: "nowMs: number"
    - path: "apps/api/src/modules/public/order-guard.ts"
      provides: "checkOrderGuard — thứ tự ưu tiên error code"
      contains: "ORDER_ALREADY_OPEN_FOR_PHONE"
    - path: "apps/api/src/modules/public/ip-hash.ts"
      provides: "hashIp HMAC-SHA256"
      contains: "createHmac"
    - path: "apps/api/src/modules/public/haversine.ts"
      provides: "haversineKm + estimatedRoadDistanceKm (M2.D-50)"
      exports: ["haversineKm", "estimatedRoadDistanceKm"]
  key_links:
    - from: "packages/schemas/src/index.ts"
      to: "./public-menu.js, ./public-store.js, ./public-orders.js"
      via: "barrel export"
      pattern: "export \\* from './public-"
    - from: "apps/api/src/modules/public/store-status.test.ts"
      to: "./store-status.js"
      via: "vitest import"
      pattern: "from './store-status.js'"
---

<objective>
Dựng **hợp đồng dữ liệu** (zod schema dùng chung) và **4 module thuần** chứa toàn bộ logic nghiệp vụ
test được của phase 8, kèm test tự động cho từng module.

Purpose: Đây là Wave 0 của `08-VALIDATION.md`. Repo hiện có ĐÚNG 1 file test (C-TEST-01), và 4/5 success
criteria của phase 8 cần test tự động. Tách logic thành hàm thuần trước khi viết controller là cách duy
nhất phủ được các criteria đó mà không cần MySQL/HTTP harness. Đồng thời mọi plan sau (BE và FE) đều
import schema từ đây, nên plan này là contract-first cho cả phase.

Output: 5 file trong `packages/schemas/src` (3 mới + 2 sửa) và 8 file trong
`apps/api/src/modules/public` (4 module + 4 test).
</objective>

<execution_context>
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-CONTEXT.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-RESEARCH.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-PATTERNS.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-VALIDATION.md

<interfaces>
<!-- Hợp đồng đã có trong repo mà plan này phải khớp. Executor KHÔNG cần đi tìm. -->

packages/schemas/src/index.ts (hiện tại, 6 dòng):
  export * from './errors.js';  './auth.js'  './admin.js'  './menu.js'  './tables.js'  './orders.js'

packages/schemas/src/errors.ts — `ErrorCode` là `z.enum([...])`, nhóm theo comment `// Auth`, `// Admin`,
`// Generic`. `ErrorEnvelope` = `{ error: { code, message, request_id, ts_ms, field_errors? } }` — shape
này ĐÃ khớp `GlobalExceptionFilter`, KHÔNG sửa.

apps/api/src/modules/menu/entities/menu-item.entity.ts — nguồn dữ liệu của PublicMenuItem:
  id: string (uuid) · code: varchar(32) · name: varchar(128) · group: varchar(16) · price: int unsigned
  · unit: varchar(32) · image_url: varchar(512) | null · is_out_of_stock: boolean · is_active: boolean
  · created_at / updated_at: number (epoch ms qua dateToMsTransformer)

apps/api/src/modules/menu/entities/menu-group.entity.ts:
  id · code: varchar(16) · name: varchar(64) · icon: varchar(8) | null · kitchen_type · sort_order: int
  · is_active: boolean · created_at

apps/api/src/common/origin-allowlist.ts + .test.ts — mẫu module thuần + mẫu test DUY NHẤT của repo.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hợp đồng zod dùng chung cho /api/public/* + 9 mã lỗi mới</name>
  <files>packages/schemas/src/public-store.ts, packages/schemas/src/public-menu.ts, packages/schemas/src/public-orders.ts, packages/schemas/src/errors.ts, packages/schemas/src/index.ts</files>
  <read_first>
    - packages/schemas/src/menu.ts (convention 1 file/domain: `z.object` + `z.infer` cùng tên, cùng chỗ)
    - packages/schemas/src/orders.ts (convention DTO + constants label tiếng Việt đi kèm)
    - packages/schemas/src/errors.ts (file sẽ sửa — xem nhóm comment và thứ tự enum hiện có)
    - packages/schemas/src/index.ts (barrel, file sẽ sửa)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-RESEARCH.md mục "Pattern 4" và "Code Examples" (đã có nội dung schema đầy đủ, copy trực tiếp)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-PATTERNS.md mục "packages/schemas — Domain schema file mới"
  </read_first>
  <action>
    Tạo `public-menu.ts` export `PublicMenuItem` với **đúng 7 field** theo M2.D-43: `id` (uuid),
    `code`, `name`, `price` (int nonnegative), `unit`, `images` (`z.array(z.string()).max(1)` — D-09 map
    0..1 phần tử từ `menu_item.image_url`), `is_out_of_stock` (boolean). Thêm `PublicMenuGroup` gồm
    `id`, `code`, `name`, `icon` (nullable), `items` (array của `PublicMenuItem`). Ghi comment đầu file:
    số field là 7 và là hợp đồng đóng — thêm field nào cũng phải sửa `08-RESEARCH.md` M2.D-43 trước.

    Tạo `public-store.ts` export `OpenHourRule` (`{ dow: 0..6, from: 'HH:mm', to: 'HH:mm' }`) và
    `PublicStoreStatus` đúng theo spec §5.1 dòng 361: `ordering_enabled`, `off_reason` (string),
    `store_phone` (string), `open_hours` (array `OpenHourRule`), `is_open_now` (boolean),
    `blocking_reason` (`z.enum(['MANUAL_OFF','OUTSIDE_HOURS']).nullable()`), `pickup_enabled`,
    `delivery_enabled`, `free_ship_km` (int), `distance_factor` (number), `eta` (object
    `{ pickup: {min,max}, delivery: {min,max} }` — int phút). `store_lat`/`store_lng` **KHÔNG** trả cho
    khách (toạ độ quán không cần lộ, khoảng cách tính ở BE).

    Tạo `public-orders.ts` export `OnlineOrderItemInput` (`menu_item_id` uuid, `qty` int 1..99, `note`
    optional max 255) — **CỐ Ý không có `unit_price` hay `name`**, ghi comment nói rõ lý do là chống
    client tự đặt giá. Export `OnlineOrderSubmit` theo đúng đoạn "DTO submit" trong `08-RESEARCH.md`
    §Code Examples (`customer_token` min 32, `customer_name` 1..128, `customer_phone` 9..16,
    `fulfillment_type` enum PICKUP/DELIVERY, `customer_address` optional max 255, `customer_lat`/`lng`
    optional có range, `customer_map_link` optional max 512, `customer_note` optional max 500, `items`
    1..50) + `.refine()` bắt buộc `customer_address` khi `fulfillment_type === 'DELIVERY'` với message
    tiếng Việt và `path: ['customer_address']`. Export thêm `PublicOrderStatus` cho màn xác nhận tối
    giản phase 8: `order_token`, `status` (`z.enum(['WAITING','CONFIRMED','REJECTED','CANCELLED_BY_CUSTOMER'])`),
    `fulfillment_type`, `items` (array `{ name, qty, unit_price }`), `subtotal` int,
    `submitted_at_ms` int, `store_phone`, `reject_reason` nullable. **Tuyệt đối không** có field
    `status` bên trong từng item (M2.D-23 — điều kiện G-1).

    Sửa `errors.ts`: thêm nhóm comment `// Public ordering (M2 phase 08)` rồi 9 code vào `ErrorCode`
    enum theo đúng thứ tự spec §5.1: `ONLINE_ORDERING_DISABLED`, `STORE_CLOSED`, `PHONE_BLACKLISTED`,
    `TOO_MANY_REQUESTS`, `ORDER_ALREADY_OPEN_FOR_PHONE`, `ORDER_ALREADY_CONFIRMED`,
    `ORDER_TOKEN_NOT_FOUND`, `MENU_ITEM_UNAVAILABLE`, `NO_TABLE_AVAILABLE`. Giữ nguyên `ErrorEnvelope`.
    Thêm comment cảnh báo: 9 code này **KHÔNG** được thêm vào dict `FRIENDLY_VN` của
    `global-exception.filter.ts` (Pitfall #6) vì message cần nội suy `off_reason`/`store_phone` động.

    Sửa `index.ts`: thêm 3 dòng `export * from './public-store.js';`, `'./public-menu.js'`,
    `'./public-orders.js'` (giữ đuôi `.js` — pure ESM, C-CONV-01).
  </action>
  <verify>
    <automated>pnpm -r typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/schemas/src/public-menu.ts` tồn tại, chứa `export const PublicMenuItem` và `images:` với `.max(1)`
    - `node -e "const s=require('fs').readFileSync('packages/schemas/src/public-menu.ts','utf8'); process.exit(/unit_cost|created_at|is_active/.test(s)?1:0)"` exit 0 (không leak field nội bộ vào schema công khai)
    - `grep -c "unit_price" packages/schemas/src/public-orders.ts` — chuỗi `unit_price` chỉ xuất hiện trong `PublicOrderStatus`, KHÔNG trong `OnlineOrderItemInput`; xác nhận bằng đọc file
    - `grep -E "'ONLINE_ORDERING_DISABLED'|'MENU_ITEM_UNAVAILABLE'|'NO_TABLE_AVAILABLE'" packages/schemas/src/errors.ts` trả về 3 dòng
    - `grep -c "public-" packages/schemas/src/index.ts` trả về 3
    - `pnpm -r typecheck` exit 0
  </acceptance_criteria>
  <done>5 file schema đúng shape, `pnpm -r typecheck` sạch 5 project, `ErrorCode` có đủ 9 code mới.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Module thuần store-status + haversine, kèm test (Wave 0)</name>
  <files>apps/api/src/modules/public/store-status.ts, apps/api/src/modules/public/store-status.test.ts, apps/api/src/modules/public/haversine.ts, apps/api/src/modules/public/haversine.test.ts</files>
  <read_first>
    - apps/api/src/common/origin-allowlist.ts (khuôn mẫu module thuần: comment đầu file giải thích vì sao thuần, không import `@nestjs/*`)
    - apps/api/src/common/origin-allowlist.test.ts (khuôn mẫu test DUY NHẤT của repo: `import { describe, expect, it } from 'vitest'`, import file cùng thư mục với đuôi `.js`, `describe`/`it` tiếng Việt)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-RESEARCH.md mục "Pattern 1" (code `evaluateOrderingStatus`, `expandToWeek`, `collapseToDefaultExceptions` đã viết sẵn) và mục "Code Examples → Haversine"
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-VALIDATION.md mục "Wave 0 Requirements" (ràng buộc `nowMs` là tham số)
    - docs/MILESTONE-02-ONLINE-ORDERING-SPEC.md §4.1 (bảng key `store_settings`: `online_ordering_off_mode`, `online_ordering_off_until_ms`, `open_hours`, `distance_factor`)
  </read_first>
  <behavior>
    store-status.test.ts phải phủ:
    - ON + trong giờ mở cửa → `{ enabled: true, is_open_now: true, blocking_reason: null }`
    - OFF thủ công `MANUAL` → `enabled: false`, `blocking_reason: 'MANUAL_OFF'` (kể cả khi đang trong giờ)
    - OFF `UNTIL_TOMORROW` với `off_until_ms` = 23:59:59 hôm nay, `nowMs` TRƯỚC mốc đó → vẫn `MANUAL_OFF`
    - OFF `UNTIL_TOMORROW`, `nowMs` SAU mốc (00:30 hôm sau) + trong giờ mở cửa → `enabled: true` (M2.D-28 tự ON lại 00:00, không cần cron)
    - ON nhưng ngoài giờ mở cửa → `enabled: false`, `blocking_reason: 'OUTSIDE_HOURS'`, `is_open_now: false`
    - `open_hours: []` (chưa cấu hình) → `is_open_now: true`, không chặn — quán mới cài không bị khoá oan
    - `open_hours` thiếu rule cho `dow` hiện tại (nghỉ ngày đó) → `OUTSIDE_HOURS`
    - Đúng mốc `from` là mở, đúng mốc `to` là đã đóng (biên `>= from`, `< to`)
    - Múi giờ: `nowMs` = 17:30 UTC ngày X = 00:30 ICT ngày X+1 → `dow` phải là của ngày X+1
    - `expandToWeek({default, exceptions})` trả đúng 7 phần tử, ngoại lệ ghi đè đúng `dow`
    - `collapseToDefaultExceptions` là nghịch đảo: expand rồi collapse ra lại input ban đầu
    haversine.test.ts phải phủ:
    - Khoảng cách 2 điểm trùng nhau = 0
    - Cặp toạ độ đã biết (ví dụ Hà Nội 21.0278,105.8342 → Hải Phòng 20.8449,106.6881) ra ~87–89 km thẳng
    - `estimatedRoadDistanceKm(straight, 1.3)` = straight × 1.3 làm tròn 2 chữ số thập phân (khớp `decimal(6,2)`)
    - `estimatedRoadDistanceKm` với factor 1 trả đúng giá trị gốc đã làm tròn
  </behavior>
  <action>
    Tạo `store-status.ts` theo đúng code mẫu trong `08-RESEARCH.md` §Pattern 1, với 4 điều chỉnh
    BẮT BUỘC so với bản mẫu:
    1. `evaluateOrderingStatus(s: StoreOrderingSettings, nowMs: number)` — `nowMs` là **tham số**, tuyệt
       đối không gọi `Date.now()` bên trong file (ràng buộc của `08-VALIDATION.md`).
    2. `open_hours` rỗng (`length === 0`) → coi là **không giới hạn giờ**, `is_open_now = true`. Bản mẫu
       của research trả `false` khi không tìm thấy rule, sẽ khoá toàn bộ quán mới cài vì spec §4.1 đặt
       default `open_hours = []`. Ghi comment giải thích tại chỗ.
    3. Hằsố `VN_OFFSET_MS = 7 * 60 * 60 * 1000` với comment: ICT cố định, không DST (nguồn ở
       `08-RESEARCH.md` §Sources), nên không thêm thư viện timezone.
    4. Export cả `StoreOrderingSettings`, `OrderingStatus`, `OpenHoursInput` types để plan 08-05 và 08-07
       import lại, không tự khai type trùng.
    Không import gì từ `@nestjs/*`, `typeorm`, `@order/schemas` trong file này.

    Tạo `haversine.ts` theo mẫu `08-RESEARCH.md` §Code Examples: `haversineKm(lat1,lng1,lat2,lng2)` với
    `EARTH_RADIUS_KM = 6371`, và `estimatedRoadDistanceKm(straightKm, distanceFactor)` làm tròn 2 chữ số
    (M2.D-50 hệ số mặc định 1.3 lấy từ setting `distance_factor`, KHÔNG hardcode 1.3 trong hàm).

    Viết 2 file test cặp đôi theo khuôn `origin-allowlist.test.ts`: `describe`/`it` tiếng Việt, import
    `./store-status.js` / `./haversine.js`, mỗi nhóm test có comment ghi mã quyết định liên quan
    (M2.D-28, M2.D-30, M2.D-50).
  </action>
  <verify>
    <automated>pnpm --filter @order/api test -- store-status.test.ts haversine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/modules/public/store-status.ts` chứa `export function evaluateOrderingStatus(` và tham số thứ hai là `nowMs: number`
    - `grep -n "Date.now()" apps/api/src/modules/public/store-status.ts` không trả kết quả nào
    - `grep -nE "@nestjs|typeorm" apps/api/src/modules/public/store-status.ts apps/api/src/modules/public/haversine.ts` không trả kết quả nào
    - `pnpm --filter @order/api test -- store-status.test.ts haversine.test.ts` exit 0, và output có ≥ 11 test pass
    - Test file có ít nhất 1 case đặt tên chứa `00:00` hoặc `nửa đêm` (M2.D-28) và 1 case cho `open_hours` rỗng
    - `pnpm --filter @order/api test` (full suite) exit 0 — 18 test cũ của phase 7 không hồi quy
  </acceptance_criteria>
  <done>2 module thuần + 2 file test xanh; toàn bộ criterion (b) của ROADMAP phase 8 ("OFF đến hết hôm nay tự ON lại 00:00") đã có bằng chứng tự động.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Module thuần order-guard + ip-hash, kèm test (Wave 0)</name>
  <files>apps/api/src/modules/public/order-guard.ts, apps/api/src/modules/public/order-guard.test.ts, apps/api/src/modules/public/ip-hash.ts, apps/api/src/modules/public/ip-hash.test.ts</files>
  <read_first>
    - apps/api/src/common/origin-allowlist.ts và origin-allowlist.test.ts (khuôn mẫu, như Task 2)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-RESEARCH.md mục "Pattern 2" (code `checkOrderGuard` đã viết sẵn) và "Code Examples → Hash IP theo HMAC"
    - docs/MILESTONE-02-ONLINE-ORDERING-SPEC.md §7 dòng 461-463 (thứ tự kiểm tra đã chốt trong spec)
    - apps/api/src/modules/auth/jwt.service.ts (convention đọc secret từ env với default dev — dùng lại cho `IP_HASH_SALT`)
    - apps/api/src/modules/public/store-status.ts (Task 2 — `OrderingStatus` type dùng làm input của guard)
  </read_first>
  <behavior>
    order-guard.test.ts phải phủ đúng thứ tự ưu tiên spec §7 — mỗi case chứng minh code có ưu tiên cao
    "che" code thấp hơn:
    - Tất cả sạch → trả `null`
    - `ordering.enabled=false` + `blocking_reason='MANUAL_OFF'` → `'ONLINE_ORDERING_DISABLED'`
    - `ordering.enabled=false` + `blocking_reason='OUTSIDE_HOURS'` → `'STORE_CLOSED'`
    - `enabled=false` + `isBlacklisted=true` cùng lúc → vẫn trả code của công tắc (công tắc ưu tiên cao nhất)
    - `isBlacklisted=true` (mọi cái khác sạch) → `'PHONE_BLACKLISTED'`
    - `isBlacklisted=true` + `isRateLimited=true` → `'PHONE_BLACKLISTED'` (blacklist trước rate limit)
    - `isRateLimited=true` → `'TOO_MANY_REQUESTS'`
    - `isRateLimited=true` + `hasOpenOrder=true` → `'TOO_MANY_REQUESTS'`
    - `hasOpenOrder=true` → `'ORDER_ALREADY_OPEN_FOR_PHONE'`
    - `hasOpenOrder=true` + `unavailableItemCodes=['X']` → `'ORDER_ALREADY_OPEN_FOR_PHONE'`
    - `unavailableItemCodes=['X']` → `'MENU_ITEM_UNAVAILABLE'`
    ip-hash.test.ts phải phủ:
    - `hashIp('203.0.113.9', salt)` KHÔNG chứa chuỗi `203.0.113.9` và không chứa `203`
    - Output là 64 ký tự hex (`/^[0-9a-f]{64}$/`)
    - Cùng IP + cùng salt → cùng hash (deterministic, để đếm được)
    - Cùng IP + salt KHÁC → hash khác (chứng minh có salt thật, không phải SHA256 trần)
    - IP khác + cùng salt → hash khác
  </behavior>
  <action>
    Tạo `order-guard.ts` theo đúng code mẫu `08-RESEARCH.md` §Pattern 2: type `OrderGuardInput`
    (`ordering`, `isBlacklisted`, `isRateLimited`, `hasOpenOrder`, `unavailableItemCodes: string[]`),
    type `GuardErrorCode` (6 code), hàm `checkOrderGuard(input): GuardErrorCode | null`. Hàm **chỉ nhận
    boolean/array đã fetch sẵn**, không query DB, không import `typeorm`. Import type `OrderingStatus`
    từ `./store-status.js` thay vì khai lại inline. Comment đầu file dẫn nguồn spec §7 dòng 461-463 và
    ghi rõ: đổi thứ tự trong hàm này là đổi hành vi nghiệp vụ, phải sửa test tương ứng.

    Tạo `ip-hash.ts`: `hashIp(ip: string, salt: string): string` dùng `createHmac('sha256', salt)` từ
    `node:crypto`, trả hex. Thêm `resolveIpHashSalt(): string` đọc
    `process.env.IP_HASH_SALT || 'dev-ip-salt-CHANGE-ME'` theo đúng convention `jwt.service.ts` (biến
    env thật sẽ thêm vào `.env.example` ở plan 08-02). Comment đầu file: KHÔNG dùng `sha256(ip)` trần —
    không gian IPv4 chỉ ~4.3 tỷ giá trị nên rainbow table tính trước toàn bộ là khả thi; HMAC + salt bí
    mật là bắt buộc (M2.D-56).

    Viết 2 file test cặp đôi theo khuôn `origin-allowlist.test.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @order/api test -- order-guard.test.ts ip-hash.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/modules/public/order-guard.ts` chứa `export function checkOrderGuard(` và cả 6 chuỗi `ONLINE_ORDERING_DISABLED`, `STORE_CLOSED`, `PHONE_BLACKLISTED`, `TOO_MANY_REQUESTS`, `ORDER_ALREADY_OPEN_FOR_PHONE`, `MENU_ITEM_UNAVAILABLE`
    - `grep -n "createHmac" apps/api/src/modules/public/ip-hash.ts` trả về ≥ 1 dòng; `grep -n "createHash(" apps/api/src/modules/public/ip-hash.ts` không trả kết quả
    - `grep -nE "typeorm|DataSource" apps/api/src/modules/public/order-guard.ts` không trả kết quả nào
    - `pnpm --filter @order/api test -- order-guard.test.ts ip-hash.test.ts` exit 0, output có ≥ 16 test pass
    - `pnpm --filter @order/api test` full suite exit 0
    - `pnpm -r typecheck` exit 0
  </acceptance_criteria>
  <done>Toàn bộ nhánh quyết định 6 mã lỗi và cơ chế hash IP có test tự động; phần logic của criterion (a) và (c) ROADMAP phase 8 đã được phủ.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `/api/public/orders` | Khách chưa auth gửi JSON tự do; mọi field là untrusted |
| DB (`store_settings.value` text) → logic công tắc | Giá trị setting do admin nhập, parse thành số/JSON trước khi dùng |
| IP thô (`req.ip`) → cột `ip_hash` | Dữ liệu định danh cá nhân đi qua ranh giới lưu trữ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `OnlineOrderItemInput` | mitigate | Schema **cố ý không có** `unit_price`/`name`; BE tự lookup giá (thi công ở 08-10). Test hình dạng schema là bằng chứng |
| T-08-02 | Information Disclosure | `PublicMenuItem` | mitigate | Đúng 7 field theo M2.D-43; acceptance criteria grep chặn `unit_cost`/`created_at`/`is_active` lọt vào schema |
| T-08-03 | Information Disclosure | `hashIp` | mitigate | HMAC-SHA256 + salt từ `IP_HASH_SALT`; test khẳng định output không chứa IP gốc và đổi salt ra hash khác |
| T-08-04 | Information Disclosure | `PublicOrderStatus` | mitigate | Không có field `status` trong từng item (M2.D-23, điều kiện G-1) — assert bằng đọc schema |
| T-08-05 | Elevation of Privilege | `checkOrderGuard` thứ tự sai | mitigate | 11 test case chứng minh code ưu tiên cao che code thấp; đổi thứ tự làm test đỏ |
| T-08-06 | Repudiation | logic tính-lúc-đọc D-17 | accept | Không ghi audit cho lần tự-động-ON qua nửa đêm (đúng nghĩa "tính lúc đọc"); rủi ro thấp vì trạng thái luôn suy ra được từ `off_until_ms` + giờ hiện tại |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | Plan này KHÔNG cài package nào (`zod` đã có trong `packages/schemas` và `apps/api`). Audit legitimacy ở `08-RESEARCH.md` §Package Legitimacy Audit — không có `[ASSUMED]`/`[SUS]` nào áp dụng cho plan này |
</threat_model>

<verification>
1. `pnpm -r typecheck` — 5 project sạch
2. `pnpm --filter @order/api test` — full suite xanh (18 test cũ + ≥ 27 test mới)
3. `grep -rn "Date.now()" apps/api/src/modules/public/store-status.ts` — không có kết quả
4. `grep -rn "public-" packages/schemas/src/index.ts` — 3 dòng barrel mới
</verification>

<success_criteria>
- 3 file schema công khai mới + `errors.ts` có 9 code + barrel export đủ 3 dòng
- 4 module thuần không import `@nestjs/*`/`typeorm`, `evaluateOrderingStatus` nhận `nowMs`
- 4 file test xanh, tổng suite `apps/api` xanh
- `pnpm -r typecheck` exit 0
</success_criteria>

<output>
Create `.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-01-SUMMARY.md` when done
</output>
