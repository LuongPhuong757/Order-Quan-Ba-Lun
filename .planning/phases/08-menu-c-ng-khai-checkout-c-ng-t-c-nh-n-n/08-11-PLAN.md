---
phase: 08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n
plan: 11
type: execute
wave: 5
depends_on: ["08-09"]
files_modified:
  - apps/shop/src/components/Stepper.tsx
  - apps/shop/src/components/StickyCta.tsx
  - apps/shop/src/pages/CartPage.tsx
  - apps/shop/src/pages/OrderTrackPage.tsx
  - apps/shop/src/pages/HistoryPage.tsx
autonomous: true
requirements: [REQ-I, REQ-J]

must_haves:
  truths:
    - "Khách xem lại giỏ, tăng/giảm số lượng, xoá dòng, nhập ghi chú đơn, rồi bấm TIẾP TỤC sang bước 2"
    - "Giảm số lượng về 0 xoá dòng ngay, không hỏi xác nhận"
    - "Dòng món hết hàng hiện mờ + khoá và CHẶN nút TIẾP TỤC tới khi khách xoá dòng đó"
    - "Giỏ rỗng hiện empty state có nút Xem menu, ẩn ô ghi chú và card tổng tiền"
    - "Sau khi submit thành công, /o/:token hiện màn xác nhận — luồng không cụt"
    - "/history tồn tại, hiện empty state, không 404 và không gọi BE"
  artifacts:
    - path: "apps/shop/src/pages/CartPage.tsx"
      provides: "Bước 1 giỏ hàng đầy đủ"
      min_lines: 150
    - path: "apps/shop/src/components/Stepper.tsx"
      provides: "Stepper 2 bước dùng chung cho /cart và /checkout"
      contains: "var(--brand-600)"
    - path: "apps/shop/src/components/StickyCta.tsx"
      provides: "Thanh CTA dính đáy mobile dùng chung"
      contains: "var(--sticky-cta-h)"
    - path: "apps/shop/src/pages/OrderTrackPage.tsx"
      provides: "Màn xác nhận tối giản sau submit"
      contains: "slice(0, 4)"
  key_links:
    - from: "apps/shop/src/pages/CartPage.tsx"
      to: "useCart().setQty"
      via: "stepper số lượng từng dòng"
      pattern: "setQty\\("
    - from: "apps/shop/src/pages/CartPage.tsx"
      to: "/checkout"
      via: "nút TIẾP TỤC (bị chặn khi blocksCheckout)"
      pattern: "/checkout"
---

<objective>
Dựng bước 1 của luồng đặt hàng (`/cart`), màn xác nhận tối giản (`/o/:token`) và empty state của
`/history`.

Purpose: `/cart` là nơi D-07 phải hiển thị đúng: dòng món hết hàng **giữ lại nhưng khoá và chặn nút
TIẾP TỤC** cho tới khi khách tự xoá — quy tắc "khách không bao giờ bất ngờ ở bước cuối". `/o/:token` phải
tồn tại ở phase 8 để luồng submit không cụt (nội dung đầy đủ % tiến độ + 5 mốc là REQ-O/phase 9).
`/history` ở phase 8 **chỉ render tĩnh, không gọi BE** — `GET /api/public/orders?customer_token=` đã bị
loại khỏi phạm vi phase này.

Output: 2 component dùng chung + 3 trang.
</objective>

<execution_context>
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-CONTEXT.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-UI-SPEC.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-PATTERNS.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-06-SUMMARY.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-09-SUMMARY.md
@apps/shop/DESIGN.md

<interfaces>
<!-- Hợp đồng cần dùng. -->

apps/shop/src/lib/cart-store.ts (plan 08-06):
  useCart() → { lines, subtotal, count, add, setQty, clear, applyMenuSync }
  CartLine = { menu_item_id, code, name, unit_price, qty, note, image, unavailable? }
  formatVnd(n) — hàm định dạng tiền DUY NHẤT
  ⚠ `subtotal` từ `useCart()` **đã loại** dòng `unavailable` (plan 08-06)

apps/shop/src/lib/customer-token.ts (plan 08-06):
  readLastOrderToken() · saveLastOrderToken(token) · readLastCustomer() · saveLastCustomer(...)

apps/shop/src/components/BannerNotice.tsx (plan 08-09):
  props { tone: 'brand'|'warn'|'danger'; title; body?; action? }

apps/shop/src/components/ImagePlaceholder.tsx (plan 08-09): props { name }

apps/shop/src/pages/CartPage.tsx (hiện tại, placeholder 51 dòng) — GIỮ LẠI khi thay ruột:
  `import type { JSX } from 'react'` + `Link` từ 'react-router-dom'
  · docblock đầu file (**cập nhật** nội dung, không xoá lịch sử quyết định)
  · `data-testid="cart-back-link"` trên link về menu
  · style const `page`/`heading`/`body`/`backLink` ở module scope đọc `var(--...)`

apps/shop/src/pages/OrderTrackPage.tsx (placeholder) — **BẮT BUỘC giữ**: chỉ hiện 4 ký tự đầu token
  (`token.slice(0, 4)`), KHÔNG BAO GIỜ render token đầy đủ (quyết định bảo mật trong docblock dòng 6-11;
  `order_token` là bearer credential — C-INFRA-03) · `data-testid="order-track-back-link"`

apps/shop/src/pages/HistoryPage.tsx (placeholder) — `data-testid="history-back-link"`

08-UI-SPEC.md — copy bắt buộc:
  CTA bước 1→2: **"TIẾP TỤC"** (IN HOA, full-width, dính đáy trên mobile)
  Empty state giỏ: heading **"Giỏ hàng đang trống"** · body **"Xem menu và thêm món bạn thích nhé"**
    · nút **"Xem menu"** → `/`
  Phí ship ở bước 1: *"Chọn phương thức nhận hàng ở bước sau để xem phí ship"* (D-19)
  Ô ghi chú: input kiểu gạch chân, placeholder *"Ví dụ: ít cay, giao giờ trưa..."*, map `customer_note` (≤500)
  Bỏ hẳn card "Tùy chọn" và checkbox "Thu thập thông tin cá nhân" của Lotteria
  Không có thao tác phá huỷ thật ở phase 8: bấm `-` về 0 xoá dòng **không cần xác nhận**

tokens.css: `--sticky-cta-h --safe-bottom --z-sticky-cta --tap-min --content-max --bg-surface`
  `--border-subtle --border-strong --brand-100 --brand-600 --text-price-sm --text-muted --text-strong`
  `--fs-caption --fs-sm --fs-base --fs-lg --fs-2xl --fw-semibold --fw-bold --r-card --r-button --sp-1..--sp-6`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stepper + StickyCta dùng chung</name>
  <files>apps/shop/src/components/Stepper.tsx, apps/shop/src/components/StickyCta.tsx</files>
  <read_first>
    - apps/shop/src/components/Wordmark.tsx (khuôn mẫu bắt buộc: CSSProperties module scope, `var(--...)`)
    - apps/shop/src/components/FloatingCart.tsx (plan 08-06 — khuôn mẫu thanh dính đáy + CSS media query, để 2 thanh không đè nhau)
    - apps/shop/src/styles/tokens.css (`--sticky-cta-h`, `--z-sticky-cta`, `--z-floating-cart`, `--safe-bottom`)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-UI-SPEC.md mục "Giỏ hàng (/cart)" (đoạn header thu gọn + stepper) và bảng Spacing (Exceptions)
  </read_first>
  <action>
    Tạo `Stepper.tsx`: stepper **2 bước ngang** dùng chung cho `/cart` và `/checkout`. Props
    `{ current: 1 | 2 }`. Bước đang active: vòng tròn nền `var(--brand-600)` chữ `var(--text-on-brand)`;
    bước chưa tới: vòng tròn viền `var(--border-strong)` nền trong suốt, chữ `var(--text-muted)`. Nhãn:
    bước 1 "Giỏ hàng", bước 2 "Thông tin nhận hàng". Trên mobile: co lại, nhãn `--fs-caption` (UI-SPEC:
    stepper co lên cùng hàng với logo) — làm bằng CSS media query trong thẻ `<style>`, không JS.
    `aria-current="step"` trên bước active.

    Tạo `StickyCta.tsx`: thanh CTA dính đáy dùng chung cho nút "TIẾP TỤC" (bước 1) và "ĐẶT HÀNG" (bước 2,
    plan 08-12). Props `{ label: string; onClick?: () => void; to?: string; disabled?: boolean; hint?: ReactNode }`.
    - Mobile: `position: fixed`, `bottom: 0`, full width, bo góc 0, `minHeight: var(--sticky-cta-h)`,
      `paddingBottom: var(--safe-bottom)`, `zIndex: var(--z-sticky-cta)`. Nền `var(--brand-600)`, chữ
      `var(--text-on-brand)`, IN HOA, `--fw-semibold`, `letterSpacing: var(--ls-wide)`.
    - Desktop (≥768px): **không fixed** — hiển thị như nút bình thường trong luồng tài liệu (UI-SPEC: "nằm
      trong card trên desktop"). Làm bằng CSS media query.
    - `disabled`: `opacity: var(--opacity-disabled)`, `cursor: 'not-allowed'`, `aria-disabled="true"`, và
      **không** điều hướng. `hint` hiện ngay trên nút (dùng cho dòng giải thích vì sao nút bị khoá) — đây
      là điểm bắt buộc: nút khoá mà không nói lý do là bẫy người dùng.
    - `to` → render `<Link>`; `onClick` → render `<button>`. Không nhận cả hai cùng lúc (nếu có `to` thì
      bỏ qua `onClick`).
    - `zIndex` của `StickyCta` phải **cao hơn** `FloatingCart` (`--z-sticky-cta` > `--z-floating-cart` theo
      tokens.css) và trang dùng nó phải chừa `paddingBottom` tương ứng.
  </action>
  <verify>
    <automated>pnpm --filter @order/shop typecheck && pnpm --filter @order/shop build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "aria-current=\"step\"" apps/shop/src/components/Stepper.tsx` = 1
    - `grep -c "@media" apps/shop/src/components/Stepper.tsx` ≥ 1 và `grep -c "@media" apps/shop/src/components/StickyCta.tsx` ≥ 1
    - `grep -c "var(--sticky-cta-h)" apps/shop/src/components/StickyCta.tsx` = 1 và `grep -c "var(--safe-bottom)" ...` = 1
    - `grep -c "var(--z-sticky-cta)" apps/shop/src/components/StickyCta.tsx` = 1
    - `grep -c "aria-disabled" apps/shop/src/components/StickyCta.tsx` = 1
    - `grep -nE "#[0-9a-fA-F]{3,6}\b" apps/shop/src/components/Stepper.tsx apps/shop/src/components/StickyCta.tsx` không trả kết quả
    - `pnpm --filter @order/shop build` exit 0
  </acceptance_criteria>
  <done>2 component dùng chung sẵn sàng cho cả bước 1 và bước 2, thanh CTA không đè thanh giỏ nổi, nút khoá luôn kèm lý do.</done>
</task>

<task type="auto">
  <name>Task 2: CartPage — bước 1 đầy đủ, chặn TIẾP TỤC khi có món hết hàng</name>
  <files>apps/shop/src/pages/CartPage.tsx</files>
  <read_first>
    - apps/shop/src/pages/CartPage.tsx (file sẽ thay ruột — giữ docblock/testid/style const như mô tả ở `<interfaces>`)
    - apps/shop/src/lib/cart-store.ts (plan 08-06 — `useCart`, `setQty`, `formatVnd`, cờ `unavailable`)
    - apps/shop/src/components/{BannerNotice,ImagePlaceholder,Stepper,StickyCta}.tsx
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-UI-SPEC.md mục "Giỏ hàng (/cart)" (toàn bộ) + bảng Copywriting
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-CONTEXT.md D-07, D-19
  </read_first>
  <action>
    Thay ruột `CartPage.tsx`. Cập nhật docblock: mô tả bước 1, dẫn D-07/D-19, giữ lịch sử quyết định cũ
    (P08.D-04, P08.D-46) trong docblock chứ không xoá.

    Cấu trúc:
    1. `<Stepper current={1} />` ngay đầu trang.
    2. Tiêu đề `"GIỎ HÀNG CỦA BẠN (N món)"` (`--fs-lg`, `--fw-bold`) + link `"+ THÊM MÓN"`
       (`var(--brand-600)`, `--fs-sm`, `--fw-semibold`) → `/`. Giữ `data-testid="cart-back-link"` trên link
       này (hoặc trên link "← Về trang menu" nếu vẫn giữ) — **không đổi tên testid**.
    3. Danh sách dòng giỏ: ảnh 56×56 bo `var(--r-card)` (`objectFit: 'cover'`, không có ảnh →
       `<ImagePlaceholder>` thu nhỏ), tên món (`--fs-base`, `--fw-semibold`, `var(--text-strong)`), đơn giá
       (`var(--text-price-sm)`), stepper số lượng (nút `-` và `+`, mỗi nút `var(--tap-min)` 44px, giữa là
       số), thành tiền cuối dòng in đậm.
       · Nút `-` khi `qty === 1` → gọi `setQty(id, 0)` xoá dòng **ngay, không hộp xác nhận** (dữ liệu chưa
         gửi server, thêm lại được ngay lập tức — UI-SPEC "Destructive confirmation: không có").
       · **Dòng `unavailable`** (D-07): nội dung dòng `opacity: var(--opacity-out-of-stock)`, chip "Hết hàng"
         `opacity: 1`, stepper `+` disable, thành tiền hiện `—` thay vì số tiền, và **có nút "Xoá món này"**
         rõ ràng. Dòng này **không** được cộng vào tổng.
    4. Ô "Ghi chú đơn hàng": `<textarea>` kiểu gạch chân (`border: 'none'`, `borderBottom: '1px solid var(--border-default)'`),
       placeholder "Ví dụ: ít cay, giao giờ trưa...", `maxLength={500}`, hiện số ký tự còn lại khi > 400.
       Lưu vào localStorage khoá `qbl.cart_note` (hằng khai cùng chỗ các khoá `qbl.*` trong
       `customer-token.ts` hoặc `cart-store.ts` — **không** viết chuỗi khoá rải rác) để bước 2 đọc lại và
       để khách reload không mất. Map vào `customer_note` khi submit (plan 08-12).
    5. Card tổng tiền: `Tạm tính` = `subtotal` từ `useCart()`; `Phí giao hàng` = dòng chữ
       *"Chọn phương thức nhận hàng ở bước sau để xem phí ship"* (`--fs-sm`, `var(--text-muted)`) — D-19 đã
       chốt card "Nhận hàng" nằm ở bước 2; `Tổng cộng` (`--fs-2xl`, `--fw-heavy`) = `subtotal` (chưa gồm ship).
    6. `<StickyCta label="TIẾP TỤC" to="/checkout" />`. **Khoá nút khi có ít nhất 1 dòng `unavailable`**
       (`disabled` + `hint` = *"Vui lòng xoá món đã hết trước khi tiếp tục"*) — đây là yêu cầu D-07, không
       phải tuỳ chọn. Cũng khoá khi `count === 0`.
    7. **Empty state** (`count === 0`): heading "Giỏ hàng đang trống", body "Xem menu và thêm món bạn thích
       nhé", nút "Xem menu" → `/`, kèm hình minh hoạ SVG tự vẽ đơn giản tông `var(--brand-100)`/`var(--wood-100)`.
       **Ẩn** ô ghi chú, card tổng tiền và `StickyCta` khi giỏ rỗng.
    8. **Bỏ hẳn** card "Tùy chọn" và checkbox "Thu thập thông tin cá nhân" của bố cục Lotteria (đã chốt).
    9. Chừa `paddingBottom` = `calc(var(--sticky-cta-h) + var(--safe-bottom) + var(--sp-4))` trên mobile để
       CTA không che card tổng tiền.
    Không hardcode hex/px. Không `dangerouslySetInnerHTML`.
  </action>
  <verify>
    <automated>pnpm --filter @order/shop typecheck && pnpm --filter @order/shop test && pnpm --filter @order/shop build && sh scripts/check-shop-bundle.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TIẾP TỤC" apps/shop/src/pages/CartPage.tsx` = 1
    - `grep -c "Giỏ hàng đang trống" apps/shop/src/pages/CartPage.tsx` = 1 và `grep -c "Xem menu và thêm món bạn thích nhé" ...` = 1
    - `grep -c "Chọn phương thức nhận hàng ở bước sau để xem phí ship" apps/shop/src/pages/CartPage.tsx` = 1
    - `grep -c "cart-back-link" apps/shop/src/pages/CartPage.tsx` = 1 (testid không đổi)
    - `grep -c "maxLength={500}" apps/shop/src/pages/CartPage.tsx` = 1
    - `grep -ciE "useConfirm|window.confirm" apps/shop/src/pages/CartPage.tsx` = 0 (không xác nhận khi xoá dòng)
    - `grep -c "var(--opacity-out-of-stock)" apps/shop/src/pages/CartPage.tsx` = 1
    - `grep -nE "#[0-9a-fA-F]{3,6}\b" apps/shop/src/pages/CartPage.tsx` không trả kết quả
    - Kiểm tay: thêm 2 món → `/cart` hiện 2 dòng, tổng đúng; bấm `-` về 0 → dòng biến mất ngay không hỏi
    - Kiểm tay: đánh dấu 1 món trong giỏ là hết hàng ở `/menu` (apps/web) → reload trang khách → dòng đó mờ + chip "Hết hàng" + thành tiền `—`, tổng **không** tính dòng đó, nút "TIẾP TỤC" **bị khoá** và hiện lý do; xoá dòng đó → nút mở lại
    - Kiểm tay: xoá hết món → empty state hiện, ô ghi chú + card tổng tiền + CTA **ẩn**
    - Kiểm tay: gõ ghi chú → reload → ghi chú còn nguyên
    - Kiểm tay (mobile viewport): CTA dính đáy không che card tổng tiền, không đè thanh giỏ nổi
    - `sh scripts/check-shop-bundle.sh` in `OK`
  </acceptance_criteria>
  <done>Bước 1 hoàn chỉnh: sửa số lượng, xoá dòng không hỏi, ghi chú bền, và món hết hàng chặn TIẾP TỤC đúng D-07.</done>
</task>

<task type="auto">
  <name>Task 3: OrderTrackPage (màn xác nhận tối giản) + HistoryPage (empty state tĩnh)</name>
  <files>apps/shop/src/pages/OrderTrackPage.tsx, apps/shop/src/pages/HistoryPage.tsx</files>
  <read_first>
    - apps/shop/src/pages/OrderTrackPage.tsx (file sẽ thay ruột — **đọc kỹ docblock dòng 6-11**: quyết định chỉ hiện 4 ký tự đầu token, và `data-testid="order-track-back-link"`)
    - apps/shop/src/pages/HistoryPage.tsx (file sẽ thay ruột — giữ `data-testid="history-back-link"`)
    - apps/shop/src/lib/use-api.ts (plan 08-06 — `useApi`) và packages/schemas/src/public-orders.ts (`PublicOrderStatus`)
    - apps/shop/src/lib/customer-token.ts (plan 08-06 — `readLastOrderToken`)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-UI-SPEC.md mục "`/o/:token` — màn xác nhận tối giản" và "AppShell & Router" (đoạn nói `/history` chỉ cần tồn tại)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-CONTEXT.md (phạm vi: nội dung đầy đủ của 2 trang này là phase 9)
  </read_first>
  <action>
    Thay ruột `OrderTrackPage.tsx` — **màn xác nhận tối giản, đúng phần thuộc phase 8**:
    - Đọc `token` từ `useParams()`, gọi `useApi('/api/public/orders/' + token, PublicOrderStatus)`.
    - Trạng thái thành công: tiêu đề **"Đã gửi đơn thành công!"** (`--font-display`, `--fs-lg`) + dấu tick
      SVG tự vẽ màu `var(--herb-600)`; dòng "Mã đơn: **{token.slice(0, 4).toUpperCase()}…**" — **CHỈ 4 ký tự
      đầu, KHÔNG BAO GIỜ render token đầy đủ** (quyết định bảo mật đã có trong docblock; `order_token` là
      bearer credential nằm trong URL, C-INFRA-03); dòng "Quán sẽ xác nhận sớm nhất có thể"; danh sách món
      đã gửi (`name × qty` + thành tiền) + `Tổng cộng`; nút gọi quán `tel:{store_phone}`.
    - **Không** thi công % tiến độ, 5 mốc trạng thái, banner "quán vừa cập nhật đơn", trạng thái từng món —
      toàn bộ là REQ-O/phase 9. Ghi comment nói rõ chỗ chèn cho phase 9 để người sau không tưởng là thiếu.
      Đặc biệt: response **không có** trạng thái từng món (M2.D-23) nên đừng đi tìm.
    - `loading` → skeleton đơn giản. `error` → `BannerNotice` tone `danger`: `code === 'ORDER_TOKEN_NOT_FOUND'`
      → *"Không tìm thấy đơn này. Có thể link đã cũ."* + nút "Về menu"; lỗi khác → dùng `error.message` + nút
      "Thử lại" gọi `reload()`.
    - Giữ `data-testid="order-track-back-link"` trên link về menu.

    Thay ruột `HistoryPage.tsx` — **empty state tĩnh, KHÔNG gọi BE**:
    - `GET /api/public/orders?customer_token=` **không thuộc phạm vi phase 8** (dời phase 9/10). Trang này
      chỉ render tĩnh: tiêu đề "Đơn của tôi", dòng giải thích *"Lịch sử đơn sẽ hiện ở đây."*, và:
      · nếu `readLastOrderToken()` có giá trị → 1 nút "Xem đơn gần nhất" → `/o/{token}` (đường ra hữu ích
        ngay từ phase 8, dùng dữ liệu localStorage đã có, **không** gọi API);
      · nếu không có → nút "Xem menu" → `/`.
    - Ghi comment đầu file nêu rõ: danh sách đơn thật là phase 9 và **cố ý** không gọi BE ở phase 8 —
      đây là quyết định phạm vi, không phải trang chưa làm xong.
    - Giữ `data-testid="history-back-link"`.
    - **Không** `useApi` trong file này (grep sẽ kiểm).
  </action>
  <verify>
    <automated>pnpm --filter @order/shop typecheck && pnpm --filter @order/shop build && sh scripts/check-shop-bundle.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "slice(0, 4)" apps/shop/src/pages/OrderTrackPage.tsx` = 1
    - `grep -c "Đã gửi đơn thành công" apps/shop/src/pages/OrderTrackPage.tsx` = 1
    - `grep -c "tel:" apps/shop/src/pages/OrderTrackPage.tsx` = 1
    - `grep -c "ORDER_TOKEN_NOT_FOUND" apps/shop/src/pages/OrderTrackPage.tsx` = 1
    - `grep -c "order-track-back-link" apps/shop/src/pages/OrderTrackPage.tsx` = 1 và `grep -c "history-back-link" apps/shop/src/pages/HistoryPage.tsx` = 1
    - `grep -cE "useApi|fetch\(" apps/shop/src/pages/HistoryPage.tsx` = 0 (phase 8 không gọi BE ở trang này)
    - `grep -c "customer_token=" apps/shop/src/pages/HistoryPage.tsx` = 0
    - `grep -nE "#[0-9a-fA-F]{3,6}\b" apps/shop/src/pages/OrderTrackPage.tsx apps/shop/src/pages/HistoryPage.tsx` không trả kết quả
    - Kiểm tay: gửi 1 đơn thật qua `curl` (plan 08-10) lấy `order_token` → mở `http://localhost:5174/o/<token>` → hiện "Đã gửi đơn thành công!", mã đơn chỉ 4 ký tự, danh sách món + tổng đúng, nút gọi quán hoạt động
    - Kiểm tay: mở `/o/khong-ton-tai` → banner "Không tìm thấy đơn này..." + nút Về menu, **không** trắng trang
    - Kiểm tay: mở `/history` → hiện empty state, DevTools Network **không có** request nào tới `/api/public/orders?...`
    - `sh scripts/check-shop-bundle.sh` in `OK`
  </acceptance_criteria>
  <done>Luồng sau submit không cụt: `/o/:token` hiện màn xác nhận an toàn (4 ký tự token), `/history` tồn tại không 404 và không gọi BE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `order_token` trong URL → DOM / Referer | Token là bearer credential: ai có nó xem được đơn |
| localStorage (khách sửa được) → hiển thị giỏ | Giá trong giỏ chỉ để hiển thị, không phải nguồn giá |
| `store_phone` từ BE → `href="tel:"` | Chuỗi từ DB đi vào thuộc tính link |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-59 | Information Disclosure | render `order_token` đầy đủ trên UI | mitigate | Chỉ `token.slice(0, 4)` (quyết định có sẵn trong docblock file); grep xác nhận. Kết hợp `Referrer-Policy: no-referrer` ở Caddy block `order.` (OD-02) để token không rò qua `Referer` |
| T-08-60 | Information Disclosure | `/o/:token` hiện trạng thái từng món | mitigate | Response BE không có field đó (M2.D-23 / G-1) và trang không tự suy diễn trạng thái món |
| T-08-61 | Tampering | khách sửa `unit_price` trong localStorage rồi TIẾP TỤC | mitigate | `toSubmitItems()` không gửi giá; BE chốt giá lúc submit (plan 08-10). Số tiền ở `/cart` thuần hiển thị |
| T-08-62 | Tampering | bỏ qua chặn "món hết hàng" bằng cách vào trực tiếp `/checkout` | mitigate | Lớp chặn thật ở BE: món hết → 409 `MENU_ITEM_UNAVAILABLE` (plan 08-10). Chặn nút TIẾP TỤC là lớp UX; plan 08-12 kiểm lại `blocksCheckout` ở bước 2 |
| T-08-63 | Tampering (XSS) | `store_phone` vào `href="tel:"` | mitigate | Lọc còn chữ số và dấu `+` trước khi ghép `tel:`; React escape phần text. Không ghép giá trị từ BE vào `href` nào khác |
| T-08-64 | Denial of Service | localStorage lỗi làm trắng trang giỏ | mitigate | Mọi truy cập bọc try/catch từ plan 08-06; giỏ lỗi → hiện empty state, không throw |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | Plan này KHÔNG cài package nào |
</threat_model>

<verification>
1. `pnpm --filter @order/shop typecheck` + `test` + `build` — sạch
2. `sh scripts/check-shop-bundle.sh` — `OK` 2 gate
3. Kiểm tay 6 kịch bản: sửa số lượng · xoá dòng không hỏi · món hết hàng chặn TIẾP TỤC + xoá thì mở · empty state ẩn 3 khối · ghi chú bền qua reload · `/o/<token>` thật + token sai
4. `grep -rn "dangerouslySetInnerHTML" apps/shop/src` — không kết quả
5. DevTools Network ở `/history` — không request nào
</verification>

<success_criteria>
- `/cart` đầy đủ: dòng giỏ có stepper 44px, xoá dòng không hỏi, ghi chú ≤500 ký tự bền qua reload
- Dòng món hết hàng giữ lại + mờ + không tính tổng + **chặn TIẾP TỤC kèm lý do**
- Empty state đúng copy, ẩn ghi chú/tổng tiền/CTA
- `/o/:token` hiện màn xác nhận, **chỉ 4 ký tự token**, có nút gọi quán, xử lý token sai
- `/history` tồn tại, empty state, **không gọi BE**
- 0 hex hardcode, guard bundle xanh
</success_criteria>

<output>
Create `.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-11-SUMMARY.md` when done
</output>
