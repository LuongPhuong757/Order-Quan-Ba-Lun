---
phase: 08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n
plan: 13
type: execute
wave: 7
depends_on: ["08-03", "08-05", "08-07", "08-08", "08-09", "08-10", "08-11", "08-12"]
files_modified:
  - OVERRIDE-DEBT.md
  - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-UAT.md
  - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-VALIDATION.md
autonomous: false
requirements: [REQ-I, REQ-J, REQ-K, REQ-L]

must_haves:
  truths:
    - "3 chỗ phase 8 đi lệch chữ spec đều được ghi vào OVERRIDE-DEBT.md, không có override im lặng"
    - "Hạng mục không nghiệm thu được ở local được ghi thành deferred UAT, trong đó docker build với sharp là gate BẮT BUỘC trước khi deploy production"
    - "Trạng thái từng dòng trong 08-VALIDATION.md phản ánh kết quả thật sau khi thi công"
    - "Chủ dự án tự tay xác nhận luồng đặt hàng đầu-cuối và 4 lớp chống lạm dụng trước khi phase được coi là xong"
  artifacts:
    - path: "OVERRIDE-DEBT.md"
      provides: "OD-06, OD-07, OD-08 cho 3 lệch của phase 8"
      contains: "OD-08"
    - path: ".planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-UAT.md"
      provides: "Deferred UAT phase 8, có gate bắt buộc trước deploy production"
      contains: "sharp"
  key_links:
    - from: "OVERRIDE-DEBT.md"
      to: "docs/MILESTONE-02-ONLINE-ORDERING-SPEC.md §5.2, M2.D-43, M2.D-28"
      via: "mỗi entry dẫn quyết định gốc bị lệch"
      pattern: "M2\\.D-"
---

<objective>
Đóng phase: ghi 3 override vào `OVERRIDE-DEBT.md`, lập `08-UAT.md` cho các hạng mục không nghiệm thu được
ở local, cập nhật trạng thái `08-VALIDATION.md`, và chốt bằng 1 checkpoint chủ dự án tự kiểm.

Purpose: Spec §28/§134 yêu cầu *"không đổi mà không ghi vào `OVERRIDE-DEBT.md`"* — phase 8 có 3 chỗ đi lệch
chữ spec (đều là quyết định có chủ ý). Bên cạnh đó `sharp` là dependency **native đầu tiên** của `apps/api`
và máy dev không có Docker, nên rủi ro build image chỉ lộ ra lúc deploy — hạng mục này phải là **gate bắt
buộc trước khi deploy production thật**, không chỉ trước khi đóng phase. Cuối cùng, 4/5 success criteria có
phần chỉ người thật kiểm được (chặn 2 lớp qua `curl`, Geolocation trên máy thật).

Output: 3 file tài liệu + 1 checkpoint.
</objective>

<execution_context>
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-CONTEXT.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-VALIDATION.md
@.planning/phases/07-shop-infra/07-UAT.md
@OVERRIDE-DEBT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 3 entry override của phase 8 vào OVERRIDE-DEBT.md</name>
  <files>OVERRIDE-DEBT.md</files>
  <read_first>
    - OVERRIDE-DEBT.md (file sẽ bổ sung — đọc toàn bộ OD-01..OD-05 để copy đúng cấu trúc entry: Ngày · Người quyết · Quyết định gốc · Lệch · Vì sao · Ghi ở · Quay lại thì sao)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-CONTEXT.md D-09, D-17
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-RESEARCH.md mục "Pitfall 2" (prefix admin) và §Assumptions Log A5
    - docs/MILESTONE-02-ONLINE-ORDERING-SPEC.md §5.2 (prefix `/api/admin/*`), M2.D-43 (`images[]`), M2.D-28 (OFF đến hết hôm nay)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-05-SUMMARY.md và 08-07-SUMMARY.md (đường dẫn file thật để trích ở phần "Ghi ở")
  </read_first>
  <action>
    Thêm 3 entry vào cuối phần chính của `OVERRIDE-DEBT.md` (**trước** mục "Chưa được ghi ở đây (nợ tồn từ
    trước)"), theo đúng cấu trúc 7 mục của OD-01..OD-05:

    **OD-06 — `images[]` của M2.D-43 map từ cột `image_url`, không thêm bảng ảnh.**
    · Ngày 2026-07-30, người quyết: chủ dự án (D-09).
    · Quyết định gốc: M2.D-43 mô tả `GET /api/public/menu` trả field `images[]`.
    · Lệch: schema DB **không đổi** — `menu_items` vẫn chỉ có `image_url varchar(512)`; BE map
      `image_url ? [image_url] : []` (0..1 phần tử).
    · Vì sao: card món trong `08-UI-SPEC.md` chỉ vẽ 1 ảnh, và nhiều ảnh/món không nằm trong REQ-I..L. Giữ
      **hợp đồng API** dạng mảng để sau thêm bảng `menu_item_images` thì FE không phải sửa gì.
    · Ghi ở: `packages/schemas/src/public-menu.ts`, `apps/api/src/modules/public/public-menu.mapper.ts`,
      test `public-menu-shape.test.ts` có case `image_url = null` → `[]`.
    · Quay lại thì sao: thêm bảng ảnh riêng + sửa mapper là đủ; **không** phải sửa FE vì hợp đồng đã là mảng.
      Ngược lại nếu ai đổi API thành `image_url` đơn lẻ thì mới là phá hợp đồng.

    **OD-07 — "OFF đến hết hôm nay" thi công bằng tính-lúc-đọc, không cron.**
    · Ngày 2026-07-30, người quyết: chủ dự án (D-17).
    · Quyết định gốc: M2.D-28 mô tả hành vi "tự ON lại 00:00 Asia/Ho_Chi_Minh" — cách hiểu tự nhiên là 1 job
      chạy lúc nửa đêm.
    · Lệch: **không có cron**. Lưu `online_ordering_off_until_ms` = 23:59:59.999 ICT hôm nay; mỗi lần đọc
      trạng thái, `evaluateOrderingStatus()` so với giờ hiện tại và tự coi là đã ON. Cột DB vẫn ghi `false`
      trong khi hành vi thực tế là ON — **đây là điểm người sau dễ hiểu sai**, nên cấm đọc thẳng cột
      `online_ordering_enabled` ở bất kỳ đâu ngoài hàm đó.
    · Vì sao: sống sót qua restart container và mất điện VPS; repo đang có **2 cron chết** (xem STATE.md) nên
      thêm cron là thêm điểm chết im lặng thứ ba. Dùng cùng cơ chế cho cả "ngoài giờ mở cửa" (M2.D-30).
    · Ghi ở: `apps/api/src/modules/public/store-status.ts` (+ `store-status.test.ts` có case qua nửa đêm),
      `apps/api/src/modules/settings/settings.service.ts` (`getOrderingStatus`).
    · Quay lại thì sao: muốn dùng cron thì phải đồng thời bỏ nhánh auto-revert trong hàm thuần, nếu không sẽ
      có 2 cơ chế cùng quyết định 1 trạng thái.

    **OD-08 — Route admin mới không có tiền tố `/api`.**
    · Ngày 2026-07-30, người quyết: chủ dự án (chốt trong phiên planning phase 8).
    · Quyết định gốc: spec §5.2 ghi `/api/admin/settings` và `/api/admin/phone-blacklist`.
    · Lệch: thi công `@Controller('admin/settings')` và `@Controller('admin/phone-blacklist')` — **không**
      `/api`.
    · Vì sao: toàn bộ route admin có sẵn từ Milestone 1 đều không có `/api`
      (`admin/users`, `admin/audit`), và `apps/web/src/lib/api.ts` gọi thẳng `/admin/...`. Cả 2 cách đều
      route đúng vì `apiPrefixes` trong `main.ts` liệt kê cả `/api` lẫn `/admin`, nên đây là **lệch chữ,
      không lệch hành vi**.
    · Ghi ở: `apps/api/src/modules/settings/settings.controller.ts`,
      `apps/api/src/modules/settings/phone-blacklist.controller.ts`, `apps/web/src/pages/AdminSettingsPage.tsx`.
    · Quay lại thì sao: đổi 2 decorator + 4 chỗ gọi ở `apps/web`. Nhưng khi đó `admin/users`/`admin/audit`
      thành không nhất quán — nên nếu muốn bám chữ spec thì phải đổi **cả** route cũ, tức là phá API đang
      chạy production.

    Không sửa OD-01..OD-05. Giữ nguyên mục "Chưa được ghi ở đây" ở cuối file.
  </action>
  <verify>
    <automated>grep -c "^## OD-0[678]" OVERRIDE-DEBT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## OD-06" OVERRIDE-DEBT.md` = 1, `grep -c "^## OD-07" OVERRIDE-DEBT.md` = 1, `grep -c "^## OD-08" OVERRIDE-DEBT.md` = 1
    - Mỗi entry mới có đủ 7 nhãn: `**Ngày:**`, `**Quyết định gốc:**`, `**Lệch:**`, `**Vì sao:**`, `**Ghi ở:**`, `**Quay lại thì sao:**` — kiểm bằng `grep -c "Quay lại thì sao" OVERRIDE-DEBT.md` ≥ 8
    - `grep -c "M2.D-43" OVERRIDE-DEBT.md` ≥ 1, `grep -c "M2.D-28" OVERRIDE-DEBT.md` ≥ 1, `grep -c "§5.2" OVERRIDE-DEBT.md` ≥ 1
    - `grep -c "^## OD-01" OVERRIDE-DEBT.md` = 1 (5 entry cũ không bị sửa)
    - `grep -c "Chưa được ghi ở đây" OVERRIDE-DEBT.md` = 1
  </acceptance_criteria>
  <done>3 override của phase 8 được ghi đầy đủ theo đúng cấu trúc file, mỗi entry dẫn được quyết định gốc và đường quay lại.</done>
</task>

<task type="auto">
  <name>Task 2: 08-UAT.md (deferred UAT + gate bắt buộc trước deploy) và cập nhật 08-VALIDATION.md</name>
  <files>.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-UAT.md, .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-VALIDATION.md</files>
  <read_first>
    - .planning/phases/07-shop-infra/07-UAT.md (khuôn mẫu bắt buộc: frontmatter `status/phase/source/started/updated`, mục "Current Test", mục "Tests" với `expected`/`result`/`local_substitute`/`steps`/`depends_on`)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-VALIDATION.md (file sẽ cập nhật — đọc mục "Manual-Only Verifications", "Per-Task Verification Map", "Validation Sign-Off")
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-RESEARCH.md mục "Pitfall 5" (rủi ro sharp trên alpine) và §Environment Availability
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-03-SUMMARY.md (kết quả thật của việc thêm sharp + lockfile)
    - .planning/REQUIREMENTS.md § Deferred UAT (bảng deferred của milestone — thêm dòng cho phase 8 nếu cần)
  </read_first>
  <action>
    Tạo `08-UAT.md` theo đúng khuôn `07-UAT.md`. Frontmatter: `status: testing`, `phase: 08-menu-cong-khai-checkout-cong-tac-nhan-don`,
    `source` liệt kê các SUMMARY của phase 8, `started`/`updated` là ngày thi công. Mở đầu ghi rõ: Milestone 2
    làm LOCAL ONLY (C-LOCAL-01) nên các hạng mục dưới đây nghiệm thu sau; **nhưng test 1 là gate BẮT BUỘC
    trước khi deploy production**, khác hẳn 7 hạng mục "chờ chủ dự án deploy" của phase 7.

    Các test:
    1. **[GATE BẮT BUỘC TRƯỚC KHI DEPLOY PRODUCTION] Docker image build được và `sharp` chạy trên alpine.**
       expected: `docker build -t order-api .` xong không lỗi; chạy container, upload 1 ảnh JPG ≥ 2 MB qua
       trang quản lý menu, xác nhận file ra là `.webp` rộng ≤ 800px và < 300 KB.
       local_substitute: không có — máy dev không có Docker (07-UAT.md test 6). Đã giảm thiểu bằng
       `pnpm.supportedArchitectures` (có `linux` + `musl`) và lockfile đã chứa `sharp-linuxmusl-x64`
       (ghi số liệu thật từ `08-03-SUMMARY.md`).
       steps: nêu rõ nếu lỗi `Could not load the sharp module` thì kiểm 3 thứ theo thứ tự: khối
       `pnpm.supportedArchitectures` ở root `package.json` · lockfile có biến thể musl ·
       `engines.node >= 20.9.0`.
       ⚠ Ghi rõ: **không được deploy production khi test này chưa xanh** — ảnh món là nội dung chính của
       trang khách, `sharp` lỗi nghĩa là admin không upload được ảnh nào.
    2. **`Permissions-Policy: geolocation=(self)` serve thật trên `order.`** (M2.D-69). Ghi
       `depends_on: 07-UAT.md test 3` và nêu hệ quả cụ thể: header sai thì nút "Chia sẻ vị trí" **im lặng
       không chạy** dù code đúng — khách không thấy lỗi gì, chỉ thấy nút không phản ứng.
    3. **Geolocation trong WebView Zalo/Facebook.** steps: mở link `order.<domain>` từ tin nhắn Zalo trên cả
       iPhone và Android thật, bấm "Chia sẻ vị trí". Xác nhận: nếu WebView chặn thì hiện dòng "Không lấy
       được vị trí..." và **vẫn đặt được hàng** bằng địa chỉ tay (đây là điều kiện chấp nhận, không phải
       "geolocation phải chạy").
    4. **Ảnh cũ upload trước phase 8 vẫn xem được.** expected: các `image_url` trỏ file JPG/PNG cũ trong
       `uploads/menu/` vẫn hiện bình thường trên trang khách (bước resize chỉ áp cho ảnh upload **mới**,
       không có migration ảnh cũ). local_substitute: kiểm ở local nếu có ảnh cũ; nếu DB local trống thì
       hạng mục này phải kiểm trên production.
    5. **Kích thước bundle khách trên mạng 3G thật.** expected: trang menu hiện được nội dung đầu trong
       ~5 giây trên 3G. local_substitute: gate `MAX_JS_KB` trong `scripts/check-shop-bundle.sh` (ghi số
       thật từ `08-04-SUMMARY.md`) + throttling "Slow 3G" trong DevTools.

    Cập nhật `08-VALIDATION.md`:
    - Trong **Per-Task Verification Map**, thay 7 ô `_(planner điền)_` bằng Task ID và Plan thật:
      `store-status.test.ts` → `08-01 Task 2` · `order-guard.test.ts` → `08-01 Task 3` ·
      `ip-hash.test.ts` → `08-01 Task 3` · `haversine.test.ts` → `08-01 Task 2` ·
      `public-menu-shape.test.ts` → `08-07 Task 3` · `public-orders.test.ts` → `08-10 Task 1` ·
      `open-order-lock.integration.test.ts` → `08-10 Task 3`.
      Thêm 3 dòng mới cho test phát sinh trong khi thi công: `csrf-paths.test.ts` (`08-07 Task 1`, REQ-L,
      threat "form auto-submit từ site lạ"), `phone.test.ts` (`08-05 Task 3`, REQ-L, threat "biến thể SĐT
      lách blacklist"), `cart-store.test.ts` + `maps-link.test.ts` (`08-06 Task 2` / `08-12 Task 1`, REQ-I/J).
      Cập nhật cột `File Exists` và `Status` theo kết quả thật (`✅ green` cho test đã xanh).
    - Trong **Manual-Only Verifications**, cập nhật hàng "Docker image build được với sharp" để trỏ sang
      `08-UAT.md` test 1 và nhấn lại đây là **gate trước deploy production**.
    - Đặt `nyquist_compliant: true` và `wave_0_complete: true` trong frontmatter **chỉ khi** toàn bộ dòng
      Wave 0 đã `✅ green`; nếu còn dòng đỏ thì để `false` và ghi rõ dòng nào.
    - Ở mục **Validation Sign-Off**, tick các ô đã đạt và ghi ngày. Ô "Approval" để `pending` cho tới khi
      Task 3 (checkpoint) xong.
  </action>
  <verify>
    <automated>test -f .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-UAT.md && ! grep -q "planner điền" .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `08-UAT.md` tồn tại với frontmatter có `status:`, `phase:`, `source:`, `started:`, `updated:`
    - `grep -c "^### " .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-UAT.md` = 5 (5 test)
    - `grep -ci "GATE BẮT BUỘC" .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-UAT.md` ≥ 1 và cùng đoạn có chuỗi `sharp`
    - `grep -c "07-UAT.md test 3" .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-UAT.md` ≥ 1
    - `grep -c "planner điền" .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-VALIDATION.md` = 0
    - `grep -c "08-01 Task 2" .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-VALIDATION.md` ≥ 1 và `grep -c "08-10 Task 3" ...` ≥ 1
    - `grep -cE "csrf-paths.test.ts|phone.test.ts|maps-link.test.ts" .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-VALIDATION.md` ≥ 3
    - `pnpm --filter @order/api test` và `pnpm --filter @order/shop test` đều exit 0 (số liệu điền vào bảng là số liệu thật, không phải dự đoán)
  </acceptance_criteria>
  <done>Có `08-UAT.md` 5 hạng mục với gate `sharp`/Docker bắt buộc trước deploy; `08-VALIDATION.md` không còn ô trống và phản ánh kết quả test thật.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Chủ dự án tự kiểm luồng đặt hàng đầu-cuối và 4 lớp chống lạm dụng</name>
  <files>(không sửa file — checkpoint xác nhận; kết quả ghi vào 08-13-SUMMARY.md và 08-VALIDATION.md § Approval)</files>
  <read_first>
    - .planning/ROADMAP.md § Phase 8 (5 success criteria — đây là danh sách phải chứng minh)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-VALIDATION.md § Manual-Only Verifications
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-UAT.md (Task 2 — biết hạng mục nào KHÔNG kiểm ở bước này)
    - docs/MILESTONE-02-ONLINE-ORDERING-SPEC.md §checklist nghiệm thu dòng ~557-569 (13 mục cho phase 8)
  </read_first>
  <action>
    Dừng lại và để **chủ dự án tự tay** chạy 15 bước ở `<how-to-verify>`. Trước khi dừng, executor phải làm
    xong 3 việc chuẩn bị (đây là phần tự động, không đẩy sang người):
    1. Khởi động sẵn 3 dev server (`@order/api`, `@order/shop`, `@order/web`) và xác nhận cả 3 lên được.
    2. Chạy `pnpm -r typecheck`, `pnpm --filter @order/api test`, `pnpm --filter @order/shop test`,
       `pnpm --filter @order/shop build && sh scripts/check-shop-bundle.sh` — dán kết quả vào SUMMARY. Nếu
       bất kỳ lệnh nào đỏ thì **không** mở checkpoint, sửa trước.
    3. Chuẩn bị sẵn dữ liệu để người kiểm không phải đi tìm: 1 `menu_item_id` thật, 1 `customer_token`
       64 hex, và lệnh `curl` đã điền đủ giá trị cho bước 7/8/9 (in ra để copy-paste).
    Sau khi có phản hồi: nếu "approved" thì cập nhật `08-VALIDATION.md` § Approval thành `approved` + ngày;
    nếu có bước sai thì ghi số bước + hiện tượng vào SUMMARY và mở gap để sửa, **không** tự đánh dấu đạt.
  </action>
  <what-built>
    Toàn bộ phase 8 đã tự động hoá xong:
    - `apps/shop`: router 5 route, trang menu (lưới 1→2→4 cột, dải danh mục, tìm kiếm không dấu, banner OFF),
      giỏ hàng localStorage 24h có đồng bộ giá/hết hàng, checkout 2 bước, màn xác nhận `/o/:token`
    - `apps/api`: `GET /api/public/store`, `GET /api/public/menu` (đúng 7 field), `POST /api/public/orders`
      (6 lớp kiểm tra + gap lock + snapshot giá + HMAC hash IP), `GET /api/public/orders/:token`,
      `GET`/`PUT /admin/settings`, 3 endpoint `/admin/phone-blacklist`, `CsrfOriginGuard` đã phủ
      `/api/public/*`, resize ảnh webp 800px lúc upload
    - `apps/web`: widget công tắc 1 chạm ở Dashboard, trang `/admin/settings` 2 tab
    - Test tự động: toàn bộ suite `apps/api` và `apps/shop` xanh; `scripts/check-shop-bundle.sh` xanh 2 gate
    Phần dưới đây là những gì **chỉ người thật kiểm được** — máy không thay thế được.
  </what-built>
  <how-to-verify>
    Chuẩn bị: `pnpm --filter @order/api dev` và `pnpm --filter @order/shop dev` (khách:
    http://localhost:5174) và `pnpm --filter @order/web dev` (quản lý: http://localhost:5173).

    **A. Luồng khách (điện thoại thật hoặc DevTools mobile viewport)**
    1. Mở http://localhost:5174/ — **không đăng nhập gì**. Xem được toàn bộ menu? Có bị hỏi thông tin cá
       nhân nào không? (Phải: KHÔNG)
    2. Đánh dấu 1 món hết hàng ở http://localhost:5173/menu → reload trang khách. Card món đó phải **mờ +
       có chip "Hết hàng" + nút `+` không bấm được**, và **vẫn hiện** (không bị ẩn).
    3. Thêm 2 món → thanh giỏ nổi hiện "2 món · <tổng>" → bấm vào → `/cart` → TIẾP TỤC → điền tên/SĐT →
       chọn "Đến lấy tại quán" → ĐẶT HÀNG. Phải chuyển sang `/o/<token>` với chữ "Đã gửi đơn thành công!" và
       **mã đơn chỉ 4 ký tự**.
    4. Chọn "Giao tận nơi" ở lần đặt sau: bấm "Chia sẻ vị trí" và **từ chối quyền**. Phải vẫn đặt được hàng
       bằng địa chỉ nhập tay. (Đây là điểm rụng khách nghiêm trọng nhất nếu làm sai.)

    **B. Chặn 2 lớp — criterion 3, phần chỉ `curl` chứng minh được**
    5. Ở http://localhost:5173/dashboard bấm tắt nhận đơn (1 chạm).
    6. Trang khách: reload `/checkout` → phải thấy banner lý do và nút "ĐẶT HÀNG" **bị khoá kèm lý do**.
    7. Gọi API tay (thay `<...>` bằng dữ liệu thật):
       `curl -i -X POST http://localhost:3001/api/public/orders -H 'Content-Type: application/json' -H 'Origin: http://localhost:5174' -d '{"customer_token":"<64 hex>","customer_name":"Test","customer_phone":"0912345670","fulfillment_type":"PICKUP","items":[{"menu_item_id":"<id món>","qty":1}]}'`
       Phải trả **409** và body chứa **`ONLINE_ORDERING_DISABLED`** + đúng lý do đã nhập.
    8. Bật lại nhận đơn, đặt giờ mở cửa thành khoảng **không** chứa giờ hiện tại → gọi lại lệnh trên → phải
       trả **409 `STORE_CLOSED`**. Rồi sửa giờ về khoảng chứa giờ hiện tại.
    9. Chặn Origin: gọi lại lệnh trên **bỏ header `Origin`** → phải trả **403 `CSRF_ORIGIN_MISMATCH`**.

    **C. 4 lớp chống lạm dụng — criterion 4**
    10. Đặt 1 đơn (còn WAITING), rồi đặt lại **cùng SĐT** → phải **409 `ORDER_ALREADY_OPEN_FOR_PHONE`**.
    11. Thêm 1 SĐT vào tab "Số điện thoại bị chặn" ở `/admin/settings` → đặt bằng số đó → phải 409, và
        **thông báo cho khách không được có chữ "chặn" hay "blacklist"**.
    12. Gửi 11 request submit trong 1 phút → request thứ 11 phải trả **429**.
    13. Truy vấn DB: `SELECT ip_hash, LENGTH(ip_hash) FROM online_order_requests LIMIT 5` → phải là 64 ký
        tự hex, **không** thấy IP thô ở bất kỳ cột nào.

    **D. OFF đến hết hôm nay — criterion 3, phần tự ON lại**
    14. Ở `/admin/settings` chọn "Tạm ngưng đến hết hôm nay" → xác nhận `/api/public/store` trả
        `ordering_enabled: false`. (Hành vi tự ON sau 00:00 đã có test tự động trong `store-status.test.ts`
        — không cần đợi tới nửa đêm.)

    **E. Đơn đang chạy không bị ảnh hưởng**
    15. Trong lúc công tắc đang TẮT, mở lại `/o/<token>` của đơn đã gửi trước đó → phải hiện bình thường.

    Nếu bất kỳ bước nào sai, ghi rõ **số bước** + hiện tượng thật thấy được (kèm status code / ảnh chụp nếu
    là UI) để sửa đúng chỗ.
  </how-to-verify>
  <resume-signal>Gõ "approved" nếu 15 bước đều đúng, hoặc liệt kê số bước bị sai kèm hiện tượng thật</resume-signal>
  <verify>
    <automated>pnpm -r typecheck && pnpm --filter @order/api test && pnpm --filter @order/shop test && pnpm --filter @order/shop build && sh scripts/check-shop-bundle.sh</automated>
    <human-check>15 bước ở `<how-to-verify>` — chủ dự án gõ "approved"</human-check>
  </verify>
  <acceptance_criteria>
    - 4 lệnh tự động ở `<verify>` đều exit 0, kết quả dán nguyên văn vào `08-13-SUMMARY.md`
    - Bước 7: `curl` trả HTTP **409** và body chứa `ONLINE_ORDERING_DISABLED` + đúng lý do đã nhập ở `/admin/settings`
    - Bước 8: `curl` trả HTTP **409** và body chứa `STORE_CLOSED`
    - Bước 9: `curl` **không** header Origin trả HTTP **403** và body chứa `CSRF_ORIGIN_MISMATCH`
    - Bước 10: trả **409** `ORDER_ALREADY_OPEN_FOR_PHONE`
    - Bước 11: trả 409 và message hiện cho khách **không chứa** chuỗi `chặn` hay `blacklist`
    - Bước 12: request thứ 11 trong 1 phút trả HTTP **429**
    - Bước 13: `SELECT LENGTH(ip_hash) FROM online_order_requests LIMIT 5` trả toàn bộ = **64**, và không cột nào chứa IP thô
    - Bước 2: card món hết hàng **vẫn hiện** (không bị ẩn) — ảnh chụp hoặc mô tả trong SUMMARY
    - Bước 4: từ chối quyền vị trí mà **vẫn đặt được hàng** — mô tả trong SUMMARY
    - Phản hồi của chủ dự án được dán nguyên văn vào `08-13-SUMMARY.md`; nếu có bước sai thì ghi số bước + hiện tượng, **không** đánh dấu phase đạt
  </acceptance_criteria>
  <done>Chủ dự án gõ "approved" cho cả 15 bước, hoặc mọi bước sai đã được ghi lại kèm số bước + hiện tượng thật để mở gap sửa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tài liệu override → người thi công phase sau | Override không ghi lại = người sau "sửa lỗi" bằng cách phá quyết định có chủ ý |
| local đã xanh → production | `sharp` là native binary; local xanh không chứng minh alpine chạy được |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-73 | Repudiation | 3 lệch spec không có dấu vết | mitigate | OD-06/07/08 trong `OVERRIDE-DEBT.md`, mỗi entry có "Quay lại thì sao" để người sau biết cái giá của việc đảo quyết định |
| T-08-74 | Denial of Service | deploy production với `sharp` chưa build được trên alpine | mitigate | `08-UAT.md` test 1 là **gate bắt buộc trước deploy**, kèm 3 bước chẩn đoán nếu lỗi |
| T-08-75 | Availability | nút "Chia sẻ vị trí" chết im lặng trên production do thiếu `Permissions-Policy` | mitigate | `08-UAT.md` test 2 dẫn `07-UAT.md` test 3 và nêu rõ hiện tượng (nút không phản ứng, khách không thấy lỗi) |
| T-08-76 | Tampering | test bị skip im lặng làm mất bằng chứng criterion | mitigate | `08-VALIDATION.md` điền bằng **số liệu thật** sau khi chạy suite; `nyquist_compliant` chỉ được `true` khi mọi dòng Wave 0 xanh |
| T-08-77 | Elevation of Privilege | chặn 2 lớp chỉ có lớp FE (nguỵ trang là đã xong) | mitigate | Checkpoint bước 7, 8, 9 buộc chứng minh bằng `curl` thật (409/409/403), không chấp nhận "nút bị khoá" là bằng chứng |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | Plan này KHÔNG cài package nào |
</threat_model>

<verification>
1. `grep -c "^## OD-0[678]" OVERRIDE-DEBT.md` = 3
2. `08-UAT.md` tồn tại, 5 test, có gate bắt buộc cho `sharp`/Docker
3. `grep -c "planner điền" 08-VALIDATION.md` = 0
4. `pnpm -r typecheck` · `pnpm --filter @order/api test` · `pnpm --filter @order/shop test` · `sh scripts/check-shop-bundle.sh` — tất cả xanh
5. Checkpoint 15 bước được chủ dự án gõ "approved"
</verification>

<success_criteria>
- 3 entry OD-06/07/08 đầy đủ 7 mục theo khuôn file
- `08-UAT.md` có 5 hạng mục, test 1 là gate bắt buộc trước deploy production
- `08-VALIDATION.md` không còn ô `_(planner điền)_`, trạng thái là kết quả thật, có thêm 3+ dòng test phát sinh
- Chủ dự án xác nhận 15 bước, trong đó 3 bước `curl` chứng minh chặn 2 lớp và chặn Origin
</success_criteria>

<output>
Create `.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-13-SUMMARY.md` when done
</output>
