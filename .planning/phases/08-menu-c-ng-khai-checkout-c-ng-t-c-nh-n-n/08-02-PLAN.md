---
phase: 08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/modules/settings/entities/store-settings.entity.ts
  - apps/api/src/modules/settings/entities/phone-blacklist.entity.ts
  - apps/api/src/modules/public/entities/online-order-request.entity.ts
  - apps/api/src/data-source.ts
  - apps/api/src/cli/verify-schema.ts
  - apps/api/package.json
  - .env.example
  - .env.production.example
autonomous: true
requirements: [REQ-K, REQ-L, REQ-J]

must_haves:
  truths:
    - "3 bảng mới (store_settings, online_order_requests, phone_blacklist) TỒN TẠI THẬT trong MySQL, chứng minh bằng truy vấn DB chứ không bằng typecheck"
    - "Tên cột khớp §4 spec ngay lần đầu — không có lần rename nào sau này (C-SCHEMA-07: rename = mất dữ liệu im lặng)"
    - "Mọi cột datetime của entity mới trả về epoch ms cho tầng application"
    - "Salt hash IP có chỗ khai báo trong cả 2 file env mẫu"
  artifacts:
    - path: "apps/api/src/modules/public/entities/online-order-request.entity.ts"
      provides: "Entity online_order_requests theo spec §4.2"
      contains: "@Entity('online_order_requests')"
    - path: "apps/api/src/modules/settings/entities/store-settings.entity.ts"
      provides: "Entity key-value store_settings theo spec §4.1"
      contains: "@Entity('store_settings')"
    - path: "apps/api/src/modules/settings/entities/phone-blacklist.entity.ts"
      provides: "Entity phone_blacklist theo spec §4.3"
      contains: "@Entity('phone_blacklist')"
    - path: "apps/api/src/cli/verify-schema.ts"
      provides: "Kiểm tra bảng/cột phase 8 tồn tại thật trong MySQL"
      contains: "information_schema"
  key_links:
    - from: "apps/api/src/data-source.ts"
      to: "3 entity mới"
      via: "mảng entities (KHÔNG autoload — thiếu ở đây thì synchronize bỏ qua bảng)"
      pattern: "OnlineOrderRequest|StoreSetting|PhoneBlacklist"
---

<objective>
Tạo 3 entity mới của phase 8 đúng theo §4 spec, đăng ký vào `data-source.ts`, và **chứng minh bằng truy
vấn MySQL thật** rằng `synchronize: true` đã tạo đủ bảng/cột.

Purpose: `data-source.ts` khai `entities: [...]` **tường minh, không autoload** — entity không có trong
mảng đó thì `synchronize` bỏ qua hoàn toàn, mà `tsc` vẫn xanh (type đến từ file entity, không từ DB).
Đó là cái bẫy verification dương tính giả mà plan này phải đóng lại trước khi bất kỳ controller nào
được viết. C-SCHEMA-07: dự án không có migration, nên đặt tên cột sai ở lần đầu = rename sau này = mất
dữ liệu im lặng.

Output: 3 entity, `data-source.ts` cập nhật, 1 CLI kiểm schema chạy lại được, 2 file env mẫu có
`IP_HASH_SALT`.
</objective>

<execution_context>
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-CONTEXT.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-PATTERNS.md

<interfaces>
<!-- Hợp đồng schema đã chốt trong spec §4. Đây là NGUỒN SỰ THẬT về tên cột. -->

store_settings (§4.1) — bảng key-value, KHÔNG phải bảng 1 dòng nhiều cột:
  key varchar(64) PK · value text · updated_at datetime(6)
  · updated_by_user_id varchar(36) NULL · updated_by_full_name varchar(128) NULL

online_order_requests (§4.2):
  id uuid PK · order_token varchar(64) UNIQUE · customer_token varchar(64) INDEX
  · status varchar(16) INDEX · fulfillment_type varchar(16) · customer_name varchar(128)
  · customer_phone varchar(16) INDEX · customer_address varchar(255) NULL
  · customer_lat decimal(10,7) NULL · customer_lng decimal(10,7) NULL
  · customer_map_link varchar(512) NULL · distance_km decimal(6,2) NULL
  · customer_note varchar(500) NULL · items_snapshot json · subtotal int
  · submitted_at datetime(6) · reviewed_at datetime(6) NULL · reviewed_by_user_id varchar(36) NULL
  · reviewed_by_full_name varchar(128) NULL · reject_reason varchar(255) NULL
  · order_id varchar(36) NULL · max_progress_shown int DEFAULT 0 · ip_hash varchar(64)
  · user_agent varchar(255) · created_at datetime(6)
  INDEX idx_oor_status_submitted (status, submitted_at)

phone_blacklist (§4.3):
  phone varchar(16) PK · reason varchar(255) · created_at datetime(6)
  · expires_at datetime(6) NULL INDEX (M2.D-59: NULL = vĩnh viễn) 
  · created_by_user_id varchar(36) NULL · created_by_full_name varchar(128) NULL

apps/api/src/data-source.ts (hiện tại) — mảng entities tường minh 10 phần tử:
  entities: [User, AuditLog, RevokedJti, RecoveryCode, MenuItem, MenuGroup, RestaurantTable, Order, OrderItem, OrderActivityLog]
  synchronize: true · timezone: 'Z' · extra.connectionLimit: 50

apps/api/src/modules/auth/entities/user.entity.ts — export `dateToMsTransformer`
  (to: number|Date → Date · from: Date|string → epoch ms)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 3 entity mới theo §4 spec</name>
  <files>apps/api/src/modules/settings/entities/store-settings.entity.ts, apps/api/src/modules/settings/entities/phone-blacklist.entity.ts, apps/api/src/modules/public/entities/online-order-request.entity.ts</files>
  <read_first>
    - apps/api/src/modules/menu/entities/menu-item.entity.ts (khuôn mẫu entity đơn giản: thứ tự import typeorm, `@Index` ở class-level, comment tiếng Việt cạnh cột)
    - apps/api/src/modules/orders/entities/order.entity.ts (khuôn mẫu entity nhiều cột nullable + nhiều index)
    - apps/api/src/modules/auth/entities/user.entity.ts (nơi export `dateToMsTransformer` — xem chữ ký chính xác)
    - docs/MILESTONE-02-ONLINE-ORDERING-SPEC.md §4.1, §4.2, §4.3 (dòng ~236-302 — nguồn sự thật tên cột)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-PATTERNS.md mục "apps/api — Entity mới"
  </read_first>
  <action>
    Tạo `store-settings.entity.ts`: `@Entity('store_settings')`, class `StoreSetting`. Cột `key`
    (`@PrimaryColumn({ type: 'varchar', length: 64 })`), `value` (`text`), `updated_at`
    (`@UpdateDateColumn({ type: 'datetime', precision: 6, transformer: dateToMsTransformer })`),
    `updated_by_user_id` (`varchar(36)` nullable), `updated_by_full_name` (`varchar(128)` nullable).
    Comment cảnh báo đầu file: `key` là **từ khoá MySQL** — TypeORM tự bọc backtick nên OK qua repository
    API, nhưng mọi raw SQL viết tay phải dùng `` `key` ``. Ghi thêm: đây là bảng key-value theo §4.1, giá
    trị luôn là text, tầng service chịu trách nhiệm parse (plan 08-05).

    Tạo `phone-blacklist.entity.ts`: `@Entity('phone_blacklist')`, class `PhoneBlacklist`. `phone`
    `@PrimaryColumn({ type: 'varchar', length: 16 })`, `reason` `varchar(255)`, `created_at`
    `@CreateDateColumn` + transformer, `expires_at` `datetime(6)` nullable + transformer +
    `@Index('idx_phone_blacklist_expires', ['expires_at'])` ở class-level, `created_by_user_id`
    `varchar(36)` nullable, `created_by_full_name` `varchar(128)` nullable. Comment: M2.D-59 — blacklist
    thêm/xoá **tay**, `expires_at` NULL nghĩa là vĩnh viễn, và **không có cron dọn** (M2.D-59 ghi đè
    M2.D-41, đã bỏ `cron-blacklist-cleanup.ts` khỏi phạm vi).

    Tạo `online-order-request.entity.ts`: `@Entity('online_order_requests')`, class
    `OnlineOrderRequest`, đủ 25 cột đúng tên/kiểu/độ dài như bảng §4.2 ở `<interfaces>`. Chi tiết bắt
    buộc:
    - `id` `@PrimaryGeneratedColumn('uuid')`
    - `order_token` `varchar(64)` + `@Index('idx_oor_token', ['order_token'], { unique: true })`
    - `items_snapshot` `@Column({ type: 'json' })`, type TS là
      `Array<{ menu_item_id: string; code: string; name: string; unit_price: number; qty: number; note: string | null }>`
    - `subtotal` `@Column({ type: 'int', unsigned: true })` (VND, không thập phân — khớp `menu_items.price`)
    - `customer_lat`/`customer_lng` `decimal(10,7)` nullable, `distance_km` `decimal(6,2)` nullable —
      MySQL trả decimal dạng **string** qua mysql2; khai type TS là `string | null` và ghi comment,
      KHÔNG khai `number` rồi tin sai.
    - `submitted_at` `@Column({ type: 'datetime', precision: 6, transformer: dateToMsTransformer })`,
      `reviewed_at` cùng kiểu nhưng nullable, `created_at` `@CreateDateColumn` + transformer
    - `max_progress_shown` `@Column({ type: 'int', default: 0 })` (M2.D-19, phase 9 dùng — tạo cột ngay
      để không phải sửa schema lần hai)
    - `ip_hash` `varchar(64)`, `user_agent` `varchar(255)`
    - 3 index class-level: `@Index('idx_oor_status_submitted', ['status', 'submitted_at'])` (đúng spec),
      `@Index('idx_oor_phone_status', ['customer_phone', 'status'])` (cho gap lock `FOR UPDATE` của
      08-10, khuyến nghị `08-RESEARCH.md` Pattern 3), `@Index('idx_oor_phone_submitted', ['customer_phone', 'submitted_at'])`
      (cho đếm rate limit theo cửa sổ thời gian, D-18), `@Index('idx_oor_customer_token', ['customer_token'])`
    Comment đầu file: bảng này là hàng chờ **trước** khi thành `orders` thật (M2.D-01 — đơn WAITING
    không được xuất hiện ở bếp/sơ đồ bàn/doanh thu), và C-SCHEMA-07 cấm rename cột về sau.
  </action>
  <verify>
    <automated>pnpm --filter @order/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 3 file tồn tại; `grep -h "@Entity(" apps/api/src/modules/settings/entities/*.entity.ts apps/api/src/modules/public/entities/*.entity.ts` in ra đúng `@Entity('store_settings')`, `@Entity('phone_blacklist')`, `@Entity('online_order_requests')`
    - `grep -c "dateToMsTransformer" apps/api/src/modules/public/entities/online-order-request.entity.ts` ≥ 4 (submitted_at, reviewed_at, created_at + import)
    - `grep -n "idx_oor_phone_status" apps/api/src/modules/public/entities/online-order-request.entity.ts` trả về 1 dòng
    - `grep -oE "^\s+[a-z_]+!" apps/api/src/modules/public/entities/online-order-request.entity.ts | wc -l` = 25 (đủ 25 cột §4.2)
    - `pnpm --filter @order/api typecheck` exit 0
  </acceptance_criteria>
  <done>3 entity đúng tên bảng/cột spec §4, typecheck sạch.</done>
</task>

<task type="auto">
  <name>Task 2: Đăng ký entity vào data-source + CLI kiểm schema + IP_HASH_SALT trong 2 file env mẫu</name>
  <files>apps/api/src/data-source.ts, apps/api/src/cli/verify-schema.ts, apps/api/package.json, .env.example, .env.production.example</files>
  <read_first>
    - apps/api/src/data-source.ts (file sẽ sửa — mảng `entities` tường minh, đọc comment về `synchronize`)
    - apps/api/src/cli/cron-jti-cleanup.ts (khuôn mẫu CLI: `import 'reflect-metadata'`, `AppDataSource.initialize()`, in JSON, `destroy()`, `main().catch` thoát code 1)
    - apps/api/package.json (khối `scripts` — thêm entry mới cùng kiểu `cron:jti-cleanup`)
    - .env.example (nhóm comment theo `# === Tên nhóm ===`, có sẵn nhóm CSRF Origin)
    - .env.production.example (giữ cùng bộ biến với `.env.example`; STATE.md ghi nhận nợ `ALLOWED_ORIGIN` từng thiếu ở đây)
  </read_first>
  <action>
    Sửa `data-source.ts`: import 3 entity mới (đuôi `.js`) và thêm `StoreSetting`, `PhoneBlacklist`,
    `OnlineOrderRequest` vào cuối mảng `entities`. Thêm comment ngay trên mảng: mảng này **tường minh,
    không autoload** — entity thiếu ở đây thì `synchronize` bỏ qua bảng mà `tsc` vẫn xanh, nên mỗi entity
    mới phải thêm 2 chỗ (file entity + mảng này).

    Tạo `apps/api/src/cli/verify-schema.ts` theo khuôn `cron-jti-cleanup.ts`. Nội dung: khởi tạo
    `AppDataSource`, rồi với mỗi cặp (bảng, danh sách cột bắt buộc) truy vấn
    `information_schema.COLUMNS` theo `TABLE_SCHEMA = DATABASE()` để lấy tập cột thật, so với danh sách
    mong đợi. Danh sách kiểm bắt buộc:
    - `store_settings`: `key`, `value`, `updated_at`, `updated_by_user_id`, `updated_by_full_name`
    - `phone_blacklist`: `phone`, `reason`, `created_at`, `expires_at`, `created_by_user_id`, `created_by_full_name`
    - `online_order_requests`: `order_token`, `customer_token`, `status`, `fulfillment_type`,
      `customer_phone`, `items_snapshot`, `subtotal`, `submitted_at`, `ip_hash`, `distance_km`,
      `max_progress_shown`
    In JSON `{ tables: [{ table, exists, missing_columns: [...] }], ok: boolean }`. Thoát code **1** nếu
    bất kỳ bảng thiếu hoặc thiếu cột — đây là gate, không phải báo cáo. Thêm script
    `"schema:verify": "node --import @swc-node/register/esm-register src/cli/verify-schema.ts"` vào
    `apps/api/package.json`.

    Thêm vào `.env.example` nhóm mới:
    `# === Hash IP cho đơn online (M2.D-56) ===` kèm comment "salt thật sinh bằng
    `openssl rand -hex 32`; đổi salt = mất khả năng đối chiếu hash cũ" và dòng
    `IP_HASH_SALT=dev-ip-salt-CHANGE-ME`. Thêm cùng biến vào `.env.production.example` với giá trị
    placeholder `replace-with-openssl-rand-hex-32` (không dùng default dev ở file production mẫu).
  </action>
  <verify>
    <automated>pnpm --filter @order/api typecheck && grep -c IP_HASH_SALT .env.example .env.production.example</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "OnlineOrderRequest|StoreSetting|PhoneBlacklist" apps/api/src/data-source.ts | wc -l` ≥ 6 (3 import + 3 phần tử mảng)
    - `apps/api/src/cli/verify-schema.ts` tồn tại và chứa chuỗi `information_schema`
    - `grep -n '"schema:verify"' apps/api/package.json` trả về 1 dòng
    - `grep -c IP_HASH_SALT .env.example` = 1 và `grep -c IP_HASH_SALT .env.production.example` = 1
    - `pnpm --filter @order/api typecheck` exit 0
  </acceptance_criteria>
  <done>3 entity đã nằm trong `entities`, có lệnh `pnpm --filter @order/api schema:verify` chạy được, 2 file env mẫu khai `IP_HASH_SALT`.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Xác nhận bảng mới tồn tại THẬT trong MySQL sau khi synchronize chạy</name>
  <files>apps/api/src/cli/verify-schema.ts</files>
  <read_first>
    - apps/api/src/data-source.ts (`synchronize: true`, `timezone: 'Z'` — đọc lại để biết cấu hình kết nối đang dùng biến env nào)
    - .env.example (MYSQL_HOST/PORT/USER/PASSWORD/DATABASE — máy dev dùng MySQL native cổng 3306)
    - .planning/STATE.md mục "Blockers/Concerns" → C-SCHEMA-07
  </read_first>
  <action>
    **[BLOCKING] — không plan nào sau plan 08-02 được chạy nếu task này chưa xanh.**

    Dự án KHÔNG dùng migration (`npx typeorm migration:run` không áp dụng — `data-source.ts` đặt
    `synchronize: true` cho cả dev và prod, C-SCHEMA-07). Nên bước "đẩy schema" ở đây là: khởi động
    `apps/api` một lần để TypeORM chạy `synchronize`, rồi kiểm bằng truy vấn DB thật.

    Trình tự:
    1. Bảo đảm MySQL local đang chạy và `.env` đã điền (nếu chưa có `.env`, `cp .env.example .env` rồi
       điền mật khẩu MySQL thật của máy). Nếu `@order/utils` chưa build thì chạy
       `pnpm --filter @order/utils build` trước — thiếu bước này `apps/api` không khởi động được.
    2. Chạy `pnpm --filter @order/api dev` (hoặc `pnpm --filter @order/api start` sau khi build), chờ
       tới khi log in ra dòng lắng nghe cổng, rồi tắt. Đây là lúc `synchronize` tạo bảng.
    3. Chạy `pnpm --filter @order/api schema:verify` và đọc JSON output.
    4. Nếu có bảng/cột thiếu: **không sửa CLI cho qua**. Nguyên nhân gần như luôn là entity chưa nằm
       trong mảng `entities` của `data-source.ts` (Task 2) hoặc tên cột trong entity lệch §4 spec
       (Task 1). Sửa nguồn rồi restart lại, đừng nới điều kiện kiểm.
    5. Ghi vào SUMMARY: kết quả JSON đầy đủ + phiên bản MySQL (`SELECT VERSION()`), để phase 9 biết
       schema đã ở trạng thái nào.

    Lý do task này bắt buộc: `pnpm typecheck` và `pnpm build` PASS dù DB chưa có bảng nào — type đến từ
    file entity, không từ DB thật. Bỏ bước này tạo verification dương tính giả, đúng kiểu bug
    `apiPrefixes` của Milestone 1 (OD-03) chỉ lộ ra ở production.
  </action>
  <verify>
    <automated>pnpm --filter @order/api schema:verify</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @order/api schema:verify` exit 0 và JSON output có `"ok": true`
    - Output có 3 phần tử với `exists: true` và `missing_columns: []` cho `store_settings`, `phone_blacklist`, `online_order_requests`
    - Kiểm chéo độc lập không qua CLI: `mysql -h 127.0.0.1 -P 3306 -u order_app -p"$MYSQL_PASSWORD" order_quan_balun -e "SHOW TABLES LIKE 'online_order_requests'"` trả về 1 dòng
    - `mysql ... -e "DESCRIBE online_order_requests"` liệt kê các cột `ip_hash`, `items_snapshot`, `subtotal`, `order_token`, `max_progress_shown`
    - Kết quả JSON được dán nguyên văn vào `08-02-SUMMARY.md`
  </acceptance_criteria>
  <done>3 bảng mới tồn tại thật trong MySQL với đủ cột bắt buộc, có bằng chứng truy vấn DB (không phải typecheck) trong SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| entity TypeScript → schema MySQL thật | `synchronize: true` là đường duy nhất; sai lệch ở đây không bị `tsc` bắt |
| IP thô → cột `ip_hash` varchar(64) | Độ dài cột phải đủ cho 64 hex của HMAC-SHA256, không cắt ngắn |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Information Disclosure | `online_order_requests.ip_hash` | mitigate | Cột đặt tên `ip_hash` varchar(64), **không có** cột nào lưu IP thô. Grep entity xác nhận không có `ip_raw`/`remote_addr` |
| T-08-08 | Tampering | `items_snapshot` json | mitigate | Cột chỉ được ghi bởi BE trong transaction submit (08-10); type TS ràng buộc shape có `unit_price` do BE tính |
| T-08-09 | Denial of Service | thiếu index cho gap lock + đếm rate limit | mitigate | 4 index class-level (`idx_oor_phone_status`, `idx_oor_phone_submitted`, `idx_oor_status_submitted`, `idx_oor_customer_token`) — thiếu index thì `FOR UPDATE` khoá rộng hơn cần thiết, dễ thành điểm nghẽn pool 50 connection |
| T-08-10 | Repudiation | không có bằng chứng schema đã áp dụng | mitigate | Task 3 BLOCKING + CLI `schema:verify` chạy lại được, exit 1 khi thiếu |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | Plan này KHÔNG cài package nào. `08-RESEARCH.md` §Package Legitimacy Audit không có `[SUS]`/`[SLOP]` |
</threat_model>

<verification>
1. `pnpm --filter @order/api typecheck` — sạch
2. `pnpm --filter @order/api schema:verify` — exit 0, `"ok": true`
3. `mysql ... -e "SHOW TABLES LIKE 'store_settings'"` — 1 dòng (kiểm chéo không qua CLI của repo)
4. `pnpm --filter @order/api test` — 18 test cũ không hồi quy
</verification>

<success_criteria>
- 3 entity mới đúng tên bảng/cột §4 spec, có đủ index
- `data-source.ts` khai đủ 13 entity
- `pnpm --filter @order/api schema:verify` exit 0 với `ok: true`
- `IP_HASH_SALT` có trong `.env.example` và `.env.production.example`
</success_criteria>

<output>
Create `.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-02-SUMMARY.md` when done
</output>
