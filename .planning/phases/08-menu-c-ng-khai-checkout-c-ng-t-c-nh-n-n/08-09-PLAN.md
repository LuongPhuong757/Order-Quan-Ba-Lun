---
phase: 08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n
plan: 09
type: execute
wave: 4
depends_on: ["08-06", "08-07"]
files_modified:
  - apps/shop/src/components/ImagePlaceholder.tsx
  - apps/shop/src/components/CardItem.tsx
  - apps/shop/src/components/CategoryRail.tsx
  - apps/shop/src/components/BannerNotice.tsx
  - apps/shop/src/pages/MenuPage.tsx
autonomous: true
requirements: [REQ-I, REQ-K]

must_haves:
  truths:
    - "Khách mở order.<domain> xem được toàn bộ menu không cần login và không bị hỏi thông tin cá nhân nào"
    - "Món hết hàng hiện mờ kèm chip Hết hàng, nút + bị khoá, và KHÔNG bị ẩn khỏi lưới"
    - "D-03: Menu tải 1 lần toàn bộ; đổi tab nhóm hàng và gõ tìm kiếm đều lọc client-side, phản hồi tức thì, không spinner, không gọi lại BE"
    - "D-10: Món không có ảnh hiện placeholder nền gỗ có chủ ý, không phải khung trống hay ảnh lỗi"
    - "D-11: Ảnh món dùng object-fit cover trên khung aspect-ratio 4/3 — cắt phần thừa, lưới đều, không viền trống"
    - "D-20: Khi quán tắt nhận đơn hoặc ngoài giờ, banner hiện ngay ở trang menu kèm lý do và SĐT quán, nhưng nút + vẫn bấm được"
    - "Lỗi mạng khi tải menu hiện banner tại chỗ + nút Thử lại, header và wordmark vẫn nguyên"
  artifacts:
    - path: "apps/shop/src/components/CardItem.tsx"
      provides: "Card món theo components.card-item của DESIGN.md"
      contains: "var(--opacity-out-of-stock)"
    - path: "apps/shop/src/components/CategoryRail.tsx"
      provides: "Dải danh mục sticky cuộn ngang + dot pagination mobile"
      contains: "var(--z-category-rail)"
    - path: "apps/shop/src/components/BannerNotice.tsx"
      provides: "Banner dùng chung cho OFF / ngoài giờ / lỗi tải / giá đổi"
      contains: "var(--brand-100)"
    - path: "apps/shop/src/pages/MenuPage.tsx"
      provides: "Trang menu hoàn chỉnh: tải 1 lần, lọc client-side, thêm vào giỏ"
      min_lines: 120
  key_links:
    - from: "apps/shop/src/pages/MenuPage.tsx"
      to: "/api/public/menu và /api/public/store"
      via: "useApi + zod schema"
      pattern: "/api/public/(menu|store)"
    - from: "apps/shop/src/pages/MenuPage.tsx"
      to: "useCart().add"
      via: "nút + trên CardItem"
      pattern: "\\.add\\("
    - from: "apps/shop/src/pages/MenuPage.tsx"
      to: "applyMenuSync"
      via: "đồng bộ giỏ với menu mới lúc tải (D-07)"
      pattern: "applyMenuSync"
---

<objective>
Dựng trang menu công khai hoàn chỉnh: lưới món, dải danh mục sticky, tìm kiếm client-side, banner trạng
thái quán, và nút thêm vào giỏ.

Purpose: Đây là success criterion 1 của phase 8 — khách xem toàn bộ menu **không cần login** và **trước
khi** bị hỏi bất kỳ thông tin cá nhân nào (M2.D-08), món hết hàng **làm mờ không ẩn** (M2.D-31). Cũng là
nơi D-07 phát huy tác dụng: giỏ hàng cũ được đối chiếu với menu vừa tải và khách được **báo rõ** nếu giá
đổi hoặc món hết.

Output: 4 component + `MenuPage` hoàn chỉnh, đúng hợp đồng thị giác `08-UI-SPEC.md`.
</objective>

<execution_context>
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-CONTEXT.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-UI-SPEC.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-PATTERNS.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-06-SUMMARY.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-07-SUMMARY.md
@apps/shop/DESIGN.md
@docs/design-refs/lotteria/README.md

<interfaces>
<!-- Hợp đồng cần dùng. -->

apps/shop/src/lib/use-api.ts (plan 08-06):
  useApi<T>(path, schema, opts?) → { data, loading, error, reload }
  ApiError = { code: string; message: string; kind: 'http'|'network'|'schema' }

apps/shop/src/lib/cart-store.ts (plan 08-06):
  useCart() → { lines, subtotal, count, add, setQty, clear, applyMenuSync }
  applyMenuSync(groups) → { priceChanged: boolean; blocksCheckout: boolean }
  formatVnd(n) — hàm định dạng tiền DUY NHẤT, dùng lại, không format tay chỗ khác

@order/schemas: PublicMenuGroup[] (từ `data.groups`) · PublicStoreStatus
  PublicMenuItem = { id, code, name, price, unit, images: string[], is_out_of_stock }
  PublicStoreStatus.blocking_reason = 'MANUAL_OFF' | 'OUTSIDE_HOURS' | null

apps/shop/src/components/Header.tsx (plan 08-04) — ô tìm kiếm ghi vào URL query `q` bằng `useSearchParams`;
  MenuPage đọc lại `q` từ URL để lọc (không cần context)

apps/shop/src/components/Wordmark.tsx — khuôn mẫu: CSSProperties module scope, `var(--...)`

tokens.css — token cần dùng:
  `--bg-surface --bg-sunken --bg-overlay --border-subtle --border-brand --brand-100 --brand-500 --brand-600`
  `--wood-100 --wood-500 --wood-700 --text-strong --text-muted --text-price --text-on-brand --danger-100`
  `--danger-600 --warn-100 --warn-600 --cat-1..--cat-7 --font-display --font-body`
  `--fs-caption --fs-sm --fs-base --fs-md --fs-xl --fw-semibold --fw-bold --fw-heavy --lh-snug`
  `--sp-1..--sp-6 --pad-card --r-card --r-button --r-badge --r-category --r-input`
  `--z-category-rail --tap-min --opacity-out-of-stock --opacity-disabled --dur-base --ease-out`
  ⚠ `--wood-400`/`--wood-500` CHỈ trang trí, **không dùng cho chữ** (2.02–2.43:1, fail AA).
  Chữ trên nền `--wood-500` phải là `--text-strong` (6.30:1 ✓AA).

apps/shop/DESIGN.md — `components.card-item`, `components.banner-notice`
  ⚠ frontmatter `cat-1..7` của DESIGN.md đang lệch `tokens.css` (pastel lạnh cũ). **`tokens.css` là nguồn
  sự thật (C-UI-01)** — dùng `var(--cat-N)`, không copy hex từ DESIGN.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ImagePlaceholder + CardItem (D-10, D-11, món hết hàng)</name>
  <files>apps/shop/src/components/ImagePlaceholder.tsx, apps/shop/src/components/CardItem.tsx</files>
  <read_first>
    - apps/shop/src/components/Wordmark.tsx (khuôn mẫu bắt buộc)
    - apps/shop/src/styles/tokens.css (đọc kỹ ghi chú tương phản của `--wood-*` và `--opacity-*`)
    - apps/shop/DESIGN.md (`components.card-item` — nền, viền, bo, padding, **không đổ bóng**)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-UI-SPEC.md mục "Trang Menu (/)" (đặc tả card đầy đủ) và bảng Typography
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-CONTEXT.md D-10, D-11, D-20
    - docs/design-refs/lotteria/README.md mục "Lệch có chủ ý so với Lotteria (đã chốt)"
  </read_first>
  <action>
    Tạo `ImagePlaceholder.tsx` (D-10): khối `aspectRatio: '4 / 3'` nền `var(--wood-100)`, bo
    `var(--r-card)`, giữa là **icon bát SVG tự vẽ** (`stroke="currentColor"`, `strokeWidth={1.75}`) màu
    `var(--wood-700)` + tên món dưới icon (`--fs-sm`, `--wood-700`, tối đa 2 dòng bằng
    `-webkit-line-clamp: 2`). Props `{ name: string }`. Yêu cầu chất lượng: **phải trông có chủ ý** —
    không được giống ảnh lỗi hay khung trống. Tuyệt đối **không** dùng 1 ảnh mặc định chung cho mọi món
    (nhiều món cùng 1 ảnh trông như lỗi dữ liệu), và **không** ẩn vùng ảnh (lưới sẽ so le).

    Tạo `CardItem.tsx` theo `components.card-item` + mục "Trang Menu" của UI-SPEC. Props:
    `{ item: PublicMenuItem; onAdd: (item: PublicMenuItem) => void }`.
    - Khung: nền `var(--bg-surface)`, viền `1px solid var(--border-subtle)`, bo `var(--r-card)`, padding
      `var(--pad-card)`, **không `boxShadow`**.
    - Ảnh: `item.images[0]` nếu có → `<img>` với `aspectRatio: '4 / 3'`, `objectFit: 'cover'`,
      `width: '100%'`, `loading="lazy"`, `decoding="async"`, `alt={item.name}` (D-11: cắt phần thừa, ảnh
      món chủ thể ở giữa nên cắt vẫn ổn; `aspect-ratio` cố định để không giật layout khi tải). Không có
      ảnh → `<ImagePlaceholder name={item.name} />`.
    - Tên món: `--fs-md`, `--fw-bold`, `var(--font-display)`, `--lh-snug`, clamp 2 dòng.
    - Giá + nút `+` **cùng một hàng** (`display: flex`, `justifyContent: 'space-between'`,
      `alignItems: 'center'`). Giá: `--fs-xl`, `--fw-heavy`, `var(--text-price)`, dùng `formatVnd`. Nút
      `+`: vuông `var(--tap-min)` × `var(--tap-min)`, nền `var(--brand-600)`, bo `var(--r-button)`, icon
      `+` SVG tự vẽ màu `var(--text-on-brand)`, `aria-label={"Thêm " + item.name + " vào giỏ"}`.
    - **Món hết hàng** (`is_out_of_stock`): toàn bộ nội dung card `opacity: var(--opacity-out-of-stock)`,
      **trừ** 1 chip "Hết hàng" (nền `var(--bg-overlay)`, chữ trắng, `opacity: 1`, `--fs-caption`,
      `--fw-semibold`) đặt giữa vùng ảnh để luôn đọc được. Nút `+` `disabled` +
      `opacity: var(--opacity-disabled)` + `cursor: 'not-allowed'` + `aria-disabled="true"`.
      **KHÔNG ẩn card** (M2.D-31).
    - **Không** giá gạch ngang, không combo, không coupon, không nhãn "Khuyến mãi" (quán chưa có khuyến
      mãi). Mô tả thành phần: `menu_items` không có cột mô tả nên card **không** có dòng mô tả — ghi
      comment nói rõ để người sau không tưởng là thiếu sót.
    - Badge "Bán chạy": **không thi công ở phase 8** — cần dữ liệu bán thật để suy ra (UI-SPEC ghi "suy ra
      từ dữ liệu bán thật, không gắn tay") và `GET /api/public/menu` chỉ trả 7 field theo M2.D-43. Ghi
      comment 1 dòng dẫn quyết định này để phase sau biết chỗ chèn.
  </action>
  <verify>
    <automated>pnpm --filter @order/shop typecheck && pnpm --filter @order/shop build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var(--opacity-out-of-stock)" apps/shop/src/components/CardItem.tsx` = 1
    - `grep -c "aspectRatio" apps/shop/src/components/CardItem.tsx` ≥ 1 và `grep -c "objectFit" ...` = 1 với giá trị `cover`
    - `grep -c "var(--tap-min)" apps/shop/src/components/CardItem.tsx` ≥ 2
    - `grep -c "aria-label" apps/shop/src/components/CardItem.tsx` ≥ 1 và `grep -c "aria-disabled" ...` = 1
    - `grep -c "boxShadow" apps/shop/src/components/CardItem.tsx` = 0 (card không đổ bóng)
    - `grep -c "var(--wood-100)" apps/shop/src/components/ImagePlaceholder.tsx` = 1
    - `grep -nE "#[0-9a-fA-F]{3,6}\b" apps/shop/src/components/CardItem.tsx apps/shop/src/components/ImagePlaceholder.tsx` không trả kết quả
    - `grep -cE "color: 'var\(--wood-400\)'|color: 'var\(--wood-500\)'" apps/shop/src/components/*.tsx` = 0 (2 màu này fail AA cho chữ)
    - `grep -c "loading=\"lazy\"" apps/shop/src/components/CardItem.tsx` = 1
    - `pnpm --filter @order/shop build` exit 0
  </acceptance_criteria>
  <done>Card món đúng hợp đồng thị giác: giá + nút `+` cùng hàng, ảnh 4:3 cover, món hết hàng mờ + chip + nút khoá, món không ảnh có placeholder gỗ.</done>
</task>

<task type="auto">
  <name>Task 2: CategoryRail (sticky, cuộn ngang, dot pagination) + BannerNotice dùng chung</name>
  <files>apps/shop/src/components/CategoryRail.tsx, apps/shop/src/components/BannerNotice.tsx</files>
  <read_first>
    - apps/shop/src/components/Wordmark.tsx (khuôn mẫu)
    - apps/shop/src/components/Header.tsx (plan 08-04 — `--z-sticky-header` để đặt rail thấp hơn đúng 1 bậc)
    - apps/shop/DESIGN.md (`components.banner-notice`: nền `--brand-100`, icon trái, tiêu đề đậm + dòng phụ `--text-muted`)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-UI-SPEC.md mục "Dải danh mục (category rail)", "Banner thông báo", bảng Copywriting
    - apps/shop/src/styles/tokens.css (`--cat-1..--cat-7`, `--z-category-rail`, `--r-category`)
  </read_first>
  <action>
    Tạo `CategoryRail.tsx`. Props: `{ groups: PublicMenuGroup[]; activeCode: string | null; onSelect: (code: string | null) => void }`.
    - `position: sticky`, `top` bằng chiều cao header, `zIndex: var(--z-category-rail)` (thấp hơn header 1
      bậc), nền `var(--bg-page)`, cuộn ngang (`overflowX: 'auto'`, `scrollSnapType: 'x proximity'`,
      ẩn scrollbar bằng CSS trong thẻ `<style>`).
    - Tile đầu là "Tất cả" (`onSelect(null)`), sau đó 1 tile mỗi nhóm. Mỗi tile: khối vuông bo
      `var(--r-category)` nền `var(--cat-N)` với `N = (index % 7) + 1` (gán theo index nhóm, lặp lại sau 7
      nhóm), giữa là icon nhóm (`group.icon` là emoji nếu có, không có thì chữ cái đầu tên nhóm), tên nhóm
      dưới ảnh (`--fs-sm`, `--fw-semibold`). Nhóm chưa có icon → dùng **ảnh món đầu tiên của nhóm** làm đại
      diện nếu món đó có ảnh (theo §8-bis mục 2); không có ảnh nào thì giữ nền `--cat-N` trơn.
    - Tile đang chọn: `border: 2px solid var(--border-brand)` + chữ `var(--brand-600)`.
    - `minHeight/minWidth: var(--tap-min)` cho vùng bấm.
    - **Mobile:** thêm **dot pagination** dưới rail (6–8px, dot active `var(--brand-500)`, dot thường
      `var(--border-default)`) — số dot = `ceil(groups.length / 3)`, dot active suy từ `scrollLeft` của
      container (theo ảnh ref mobile thật). Chỉ hiện dot khi `groups.length > 3` (rail không cuộn thì dot
      là nhiễu). Ẩn dot từ ~768px bằng CSS media query.

    Tạo `BannerNotice.tsx` — **đúng 1 component dùng cho 4 tình huống**, phân biệt bằng prop
    `tone: 'brand' | 'warn' | 'danger'`:
    - `brand` → nền `var(--brand-100)`, chữ `var(--text-strong)`: dùng cho OFF thủ công và cho banner
      "Giá một vài món đã được cập nhật" (D-07).
    - `warn` → nền `var(--warn-100)`, chữ `var(--warn-600)`: ngoài giờ mở cửa.
    - `danger` → nền `var(--danger-100)`, chữ `var(--danger-600)`: lỗi tải menu / lỗi submit.
    Props: `{ tone; title: string; body?: ReactNode; action?: { label: string; onClick?: () => void; href?: string } }`.
    Layout: icon trái (SVG tự vẽ, chọn theo `tone`), tiêu đề `--fw-bold`, dòng phụ `--text-muted`
    `--fs-sm`, action bên phải hoặc dưới trên mobile. Full width trong `var(--content-max)`, **không
    sticky** (cuộn theo trang). `role="status"` cho tone `brand`/`warn`, `role="alert"` cho `danger`.
    Nếu `action.href` là số điện thoại thì render `<a href={"tel:" + phone}>` — nút gọi 1 chạm mà bảng
    Copywriting yêu cầu cho `ONLINE_ORDERING_DISABLED`.
  </action>
  <verify>
    <automated>pnpm --filter @order/shop typecheck && pnpm --filter @order/shop build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var(--z-category-rail)" apps/shop/src/components/CategoryRail.tsx` = 1
    - `grep -oE "var\(--cat-" apps/shop/src/components/CategoryRail.tsx | wc -l` ≥ 1 và code tính `(index % 7) + 1` có mặt
    - `grep -c "var(--border-brand)" apps/shop/src/components/CategoryRail.tsx` = 1
    - `grep -c "@media" apps/shop/src/components/CategoryRail.tsx` ≥ 1 (ẩn dot ở desktop)
    - `grep -oE "'brand'|'warn'|'danger'" apps/shop/src/components/BannerNotice.tsx | sort -u | wc -l` = 3
    - `grep -c "tel:" apps/shop/src/components/BannerNotice.tsx` = 1
    - `grep -cE "role=\"status\"|role=\"alert\"" apps/shop/src/components/BannerNotice.tsx` ≥ 2
    - `grep -nE "#[0-9a-fA-F]{3,6}\b" apps/shop/src/components/CategoryRail.tsx apps/shop/src/components/BannerNotice.tsx` không trả kết quả
    - `pnpm --filter @order/shop build` exit 0
  </acceptance_criteria>
  <done>Dải danh mục sticky cuộn ngang có dot pagination mobile; 1 component banner phục vụ cả 4 tình huống với nút gọi quán 1 chạm.</done>
</task>

<task type="auto">
  <name>Task 3: MenuPage hoàn chỉnh — tải 1 lần, lọc client-side, đồng bộ giỏ, banner trạng thái</name>
  <files>apps/shop/src/pages/MenuPage.tsx</files>
  <read_first>
    - apps/shop/src/pages/MenuPage.tsx (file sẽ thay ruột — plan 08-04 đã dựng khung + style const)
    - apps/shop/src/lib/use-api.ts và apps/shop/src/lib/cart-store.ts (plan 08-06 — chữ ký `useApi`, `useCart`, `applyMenuSync`, `formatVnd`)
    - apps/shop/src/components/{CardItem,CategoryRail,BannerNotice}.tsx (Task 1, 2)
    - apps/shop/src/components/Header.tsx (ô tìm kiếm ghi query `q` — MenuPage đọc lại bằng `useSearchParams`)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-CONTEXT.md D-03, D-04, D-07, D-20 và mục Specific Ideas
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-UI-SPEC.md mục "Trang Menu (/)" (skeleton loading) và bảng Copywriting (banner OFF)
  </read_first>
  <action>
    Thay ruột `MenuPage.tsx`:
    - Tải dữ liệu bằng 2 lần gọi `useApi`: `/api/public/store` (schema `PublicStoreStatus`) và
      `/api/public/menu` (schema là `z.object({ groups: PublicMenuGroup.array() })` hoặc tương đương theo
      shape plan 08-07 trả về). **Tải 1 lần toàn bộ menu** (D-03) — không phân trang, không gọi lại khi
      đổi tab hay khi gõ tìm kiếm.
    - Lọc **hoàn toàn client-side** (D-03): `activeCode` là state cục bộ (tab nhóm), `q` đọc từ
      `useSearchParams()` (Header ghi vào). Tìm kiếm khớp **không phân biệt hoa thường và không phân biệt
      dấu** trên `name` và `code` — dùng `String.prototype.normalize('NFD').replace(/\p{Diacritic}/gu,'')`
      để khách gõ "bun bo" cũng tìm ra "Bún bò". Không debounce (mảng đã ở trong bộ nhớ, phản hồi tức thì
      là mục tiêu).
    - Khi đang tìm kiếm (`q` không rỗng): hiện **1 lưới phẳng kết quả** thay vì tách theo nhóm, kèm dòng
      "{N} món khớp «{q}»" và nút "Xoá tìm kiếm". Không kết quả → dòng "Không tìm thấy món nào" +
      nút "Xem tất cả món".
    - Lưới món: `display: grid`, `gridTemplateColumns: 'repeat(auto-fill, minmax(280px, 1fr))'`,
      `gap: var(--sp-5)` — **không media query** (CONFLICT-DESIGN-01 / OD-05: ~360px → 1 cột, ~768px → 2,
      ~1200px → 4).
    - Skeleton loading (`loading === true`): render 6 card giữ **đúng kích thước** card thật, nội dung là
      khối `var(--bg-sunken)` với animation chỉ trên `opacity`, bọc trong
      `@media (prefers-reduced-motion: reduce)` để tắt animation.
    - **Banner trạng thái quán** (dùng `BannerNotice`, đặt **dưới** dải danh mục): nếu
      `store.ordering_enabled === false` thì hiện — `blocking_reason === 'OUTSIDE_HOURS'` → tone `warn`,
      tiêu đề *"Quán đang ngoài giờ mở cửa hôm nay"*, dòng phụ *"Gọi {store_phone} nếu cần hỗ trợ."*;
      ngược lại tone `brand`, tiêu đề *"Quán tạm ngưng nhận đơn online"*, dòng phụ
      *"{off_reason || 'Vui lòng gọi để đặt trực tiếp'} — gọi {store_phone} để đặt trực tiếp"* + action
      gọi `tel:`. **Nút `+` vẫn bấm được khi banner hiện** (D-20 / M2.D-26: khách vẫn xây giỏ để biết tổng
      tiền rồi gọi điện; chỉ nút "ĐẶT HÀNG" ở bước 2 bị khoá).
    - **Lỗi tải menu** (D-04): `BannerNotice` tone `danger`, tiêu đề *"Không tải được menu"*, dòng phụ theo
      `error.message`, action "Thử lại" gọi `reload()`. **Giữ nguyên header/wordmark và dải danh mục nếu
      có dữ liệu cũ** — không error boundary toàn trang, không tự retry ngầm. Nếu `error.kind === 'schema'`
      thì dòng phụ nói rõ dữ liệu không đúng định dạng (đây là lỗi triển khai, không phải lỗi của khách).
    - **Đồng bộ giỏ với menu mới** (D-07): khi menu tải xong, gọi `applyMenuSync(groups)` **đúng một lần
      cho mỗi lần dữ liệu menu đổi** (dùng ref/`useEffect` phụ thuộc dữ liệu menu, không gọi mỗi render).
      Nếu trả `priceChanged` → hiện `BannerNotice` tone `brand` với tiêu đề
      *"Giá một vài món đã được cập nhật"* (đóng được). Tuyệt đối **không** im lặng đổi giá hay im lặng
      xoá món.
    - Nút `+` gọi `useCart().add(item, 1)`; món `is_out_of_stock` thì `CardItem` đã tự khoá nên `MenuPage`
      không cần lọc lại.
    - **Trang không hỏi bất kỳ thông tin cá nhân nào** (M2.D-08) — không form email, không popup thu thập,
      không checkbox. Ghi comment nói rõ đây là ràng buộc, không phải "chưa làm".
  </action>
  <verify>
    <automated>pnpm --filter @order/shop typecheck && pnpm --filter @order/shop test && pnpm --filter @order/shop build && sh scripts/check-shop-bundle.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/api/public/menu" apps/shop/src/pages/MenuPage.tsx` = 1 và `grep -c "/api/public/store" ...` = 1
    - `grep -c "applyMenuSync" apps/shop/src/pages/MenuPage.tsx` = 1
    - `grep -c "minmax(280px, 1fr)" apps/shop/src/pages/MenuPage.tsx` = 1; `grep -c "@media (max-width" apps/shop/src/pages/MenuPage.tsx` = 0 (lưới không dùng media query)
    - `grep -c "prefers-reduced-motion" apps/shop/src/pages/MenuPage.tsx` = 1
    - `grep -c "normalize('NFD')" apps/shop/src/pages/MenuPage.tsx` ≥ 1 (tìm kiếm bỏ dấu)
    - `grep -cE "debounce|setTimeout" apps/shop/src/pages/MenuPage.tsx` = 0 (D-03 phản hồi tức thì)
    - `grep -c "dangerouslySetInnerHTML" apps/shop/src/pages/MenuPage.tsx` = 0
    - `grep -nE "#[0-9a-fA-F]{3,6}\b" apps/shop/src/pages/MenuPage.tsx` không trả kết quả
    - Kiểm tay (dev server `apps/api` + `apps/shop`): mở http://localhost:5174/ **không đăng nhập** → thấy toàn bộ menu; đánh dấu 1 món hết hàng ở `/menu` (apps/web) → reload trang khách → card món đó mờ + chip "Hết hàng" + nút `+` không bấm được, **card vẫn hiện**
    - Kiểm tay: gõ "bun bo" (không dấu) vào ô tìm kiếm → ra món "Bún bò"; URL có `?q=bun+bo`; reload giữ nguyên kết quả
    - Kiểm tay: tắt công tắc ở Dashboard → reload trang khách → banner "Quán tạm ngưng nhận đơn online" + lý do + nút gọi; nút `+` **vẫn bấm được** và giỏ vẫn tăng
    - Kiểm tay: tắt `apps/api` → reload trang khách → banner "Không tải được menu" + nút "Thử lại"; bật API rồi bấm "Thử lại" → menu hiện, không phải reload trang
    - Kiểm tay: thêm món vào giỏ → đổi giá món đó ở `/menu` (apps/web) → reload trang khách → banner "Giá một vài món đã được cập nhật" và dòng giỏ mang giá mới
    - `sh scripts/check-shop-bundle.sh` in `OK` (2 gate, kể cả ngưỡng kích thước)
  </acceptance_criteria>
  <done>Trang menu công khai hoàn chỉnh: xem không cần login, món hết hàng mờ không ẩn, lọc/tìm tức thì client-side, banner OFF/ngoài giờ/lỗi/giá đổi đúng copy, giỏ được đồng bộ có báo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dữ liệu menu + `off_reason` từ BE → DOM | Chuỗi do admin nhập được render lại cho khách vô danh |
| `?q=` do khách nhập → filter + render | Chuỗi tuỳ ý |
| `images[0]` → thuộc tính `src` | URL đến từ DB, do admin upload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-43 | Tampering (XSS) | tên món, `off_reason`, `q` render vào DOM | mitigate | React escape mặc định; **không** `dangerouslySetInnerHTML` ở bất kỳ file nào (grep = 0); `group.icon` render như text, không như HTML |
| T-08-44 | Information Disclosure | trang menu hỏi thông tin cá nhân trước khi xem | mitigate | M2.D-08: trang không có form/popup thu thập nào; acceptance criteria kiểm tay xác nhận xem được menu khi chưa đăng nhập và không bị hỏi gì |
| T-08-45 | Tampering | `images[0]` là URL ngoài (`javascript:` hoặc host lạ) | mitigate | Giá trị đến từ `menu_items.image_url` do admin upload qua `/menu/upload-image` (đường dẫn tương đối `/uploads/menu/...`); React chặn scheme `javascript:` trong `src`. Không render `images[0]` vào `href` |
| T-08-46 | Denial of Service | menu rất nhiều món làm treo thiết bị yếu | accept | D-03 đã đánh giá: menu 1 quán lẩu vài chục món. `loading="lazy"` trên ảnh giới hạn tải; nếu sau này menu lên hàng nghìn món thì phải xét lại D-03 |
| T-08-47 | Tampering | khách bấm `+` khi quán đã OFF rồi submit | mitigate | Đây là hành vi **cố ý** (D-20/M2.D-26); lớp chặn thật là BE trả 409 lúc submit (plan 08-10) — FE khoá nút chỉ là lớp UX |
| T-08-48 | Information Disclosure | bundle khách lây code quản lý | mitigate | `check-shop-bundle.sh` chạy trong verify của task này |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | Plan này KHÔNG cài package nào (icon là SVG tự vẽ — D-22 giả định #2) |
</threat_model>

<verification>
1. `pnpm --filter @order/shop typecheck` + `test` + `build` — sạch
2. `sh scripts/check-shop-bundle.sh` — `OK` 2 gate
3. 6 kịch bản kiểm tay ở acceptance criteria Task 3 (xem không login · món hết hàng · tìm không dấu · banner OFF · lỗi mạng + Thử lại · giá đổi)
4. `grep -rn "dangerouslySetInnerHTML" apps/shop/src` — không kết quả
5. `grep -rn "#[0-9a-fA-F]\{3,6\}" apps/shop/src --include=*.tsx` — không kết quả (C-UI-01)
</verification>

<success_criteria>
- Xem toàn bộ menu không cần login, không bị hỏi thông tin cá nhân nào
- Món hết hàng mờ + chip "Hết hàng" + nút `+` khoá, **không ẩn card**
- Đổi tab và tìm kiếm (bỏ dấu) phản hồi tức thì, không gọi lại BE
- Banner OFF / ngoài giờ / lỗi tải / giá đổi đúng copy `08-UI-SPEC.md`, có nút gọi `tel:`
- Món không ảnh có placeholder gỗ có chủ ý
- 0 hex hardcode, 0 `dangerouslySetInnerHTML`, guard bundle xanh
</success_criteria>

<output>
Create `.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-09-SUMMARY.md` when done
</output>
