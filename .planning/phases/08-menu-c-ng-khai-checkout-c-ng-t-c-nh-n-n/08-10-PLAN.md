---
phase: 08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n
plan: 10
type: execute
wave: 4
depends_on: ["08-02", "08-07"]
files_modified:
  - apps/api/src/modules/public/submit-order.ts
  - apps/api/src/modules/public/public-orders.test.ts
  - apps/api/src/modules/public/public-orders.service.ts
  - apps/api/src/modules/public/public-orders.controller.ts
  - apps/api/src/modules/public/open-order-lock.integration.test.ts
  - apps/api/src/modules/public/public.module.ts
autonomous: true
requirements: [REQ-J, REQ-K, REQ-L]

must_haves:
  truths:
    - "Khách gửi được đơn và nhận order_token; đơn vào bảng online_order_requests với status WAITING"
    - "Giá trong items_snapshot và subtotal do BE tự lookup từ DB tại thời điểm submit — client không đặt được giá 0đ"
    - "Công tắc OFF: gọi API tay vẫn nhận 409 ONLINE_ORDERING_DISABLED; ngoài giờ mở cửa nhận 409 STORE_CLOSED"
    - "1 SĐT không mở được 2 đơn cùng lúc, kể cả 2 request gửi đồng thời"
    - "D-18: SĐT trong blacklist bị chặn; quá 3 đơn/giờ theo SĐT bị chặn (đếm trong DB, không throttler in-memory, không Redis); quá 10 request/phút theo IP bị chặn"
    - "IP lưu dạng HMAC hash, không có IP thô ở bất kỳ cột nào"
    - "Đơn đang chạy không bị ảnh hưởng khi công tắc tắt"
  artifacts:
    - path: "apps/api/src/modules/public/submit-order.ts"
      provides: "Orchestrator submit nhận port (test được bằng fake-repository)"
      exports: ["submitOrder", "SubmitDeps"]
    - path: "apps/api/src/modules/public/public-orders.service.ts"
      provides: "Transaction + gap lock + cài port thật lên DataSource"
      contains: "FOR UPDATE"
    - path: "apps/api/src/modules/public/public-orders.controller.ts"
      provides: "POST /api/public/orders (có @Throttle) + GET /api/public/orders/:token"
      contains: "@Throttle"
    - path: "apps/api/src/modules/public/public-orders.test.ts"
      provides: "Test service với fake-repository — 6 nhánh mã lỗi + snapshot giá"
    - path: "apps/api/src/modules/public/open-order-lock.integration.test.ts"
      provides: "Test gap lock với MySQL thật"
      contains: "FOR UPDATE"
  key_links:
    - from: "apps/api/src/modules/public/public-orders.service.ts"
      to: "online_order_requests (SELECT ... FOR UPDATE)"
      via: "transaction + gap lock trên (customer_phone, status)"
      pattern: "FOR UPDATE"
    - from: "apps/api/src/modules/public/submit-order.ts"
      to: "checkOrderGuard"
      via: "import từ ./order-guard.js"
      pattern: "checkOrderGuard"
    - from: "apps/api/src/modules/public/public-orders.service.ts"
      to: "hashIp"
      via: "import từ ./ip-hash.js"
      pattern: "hashIp\\("
---

<objective>
Dựng endpoint submit đơn — trái tim của phase 8 — với đầy đủ 6 lớp kiểm tra theo thứ tự spec §7, snapshot
giá do BE tự lookup, gap lock chống race "1 đơn mở/SĐT", và hash IP bằng HMAC. Kèm màn xác nhận
`GET /api/public/orders/:token` tối giản.

Purpose: Đây là success criteria 2, 3, 4 của ROADMAP phase 8. Ba rủi ro bảo mật severity HIGH tập trung ở
đây: (1) client tự đặt giá 0đ nếu BE tin field giá gửi lên; (2) 2 request cùng SĐT lách kiểm "1 đơn mở"
nếu không có gap lock; (3) IP thô rò qua log/backup nếu không hash. Guard Origin cho `/api/public/*` đã
được đóng ở plan 08-07 — endpoint này **không được** merge trước plan đó.

Output: 1 orchestrator test được bằng fake-repository, 1 service có transaction, 1 controller 2 route, 2
file test (unit + integration MySQL thật).
</objective>

<execution_context>
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-CONTEXT.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-RESEARCH.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-VALIDATION.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-PATTERNS.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-05-SUMMARY.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-07-SUMMARY.md

<interfaces>
<!-- Hợp đồng cần dùng. Đã trích sẵn. -->

apps/api/src/modules/public/order-guard.ts (plan 08-01):
  type OrderGuardInput = { ordering: OrderingStatus; isBlacklisted: boolean; isRateLimited: boolean;
    hasOpenOrder: boolean; unavailableItemCodes: string[] }
  type GuardErrorCode = 'ONLINE_ORDERING_DISABLED'|'STORE_CLOSED'|'PHONE_BLACKLISTED'
    |'TOO_MANY_REQUESTS'|'ORDER_ALREADY_OPEN_FOR_PHONE'|'MENU_ITEM_UNAVAILABLE'
  function checkOrderGuard(input): GuardErrorCode | null      // thứ tự ưu tiên đã cố định + có test

apps/api/src/modules/public/ip-hash.ts (plan 08-01): hashIp(ip, salt) · resolveIpHashSalt()
apps/api/src/modules/public/haversine.ts (plan 08-01): haversineKm(lat1,lng1,lat2,lng2)
  · estimatedRoadDistanceKm(straightKm, distanceFactor)
apps/api/src/modules/public/phone.ts (plan 08-05): normalizePhone(raw): string | null
apps/api/src/modules/settings/settings.service.ts (plan 08-05): readAll() · getOrderingStatus(nowMs)

packages/schemas (plan 08-01):
  OnlineOrderSubmit = { customer_token(≥32), customer_name, customer_phone, fulfillment_type,
    customer_address?, customer_lat?, customer_lng?, customer_map_link?, customer_note?,
    items: [{ menu_item_id, qty, note? }] }   ← **KHÔNG có unit_price/name**
  PublicOrderStatus = { order_token, status, fulfillment_type, items: [{name,qty,unit_price}],
    subtotal, submitted_at_ms, store_phone, reject_reason }

apps/api/src/modules/public/entities/online-order-request.entity.ts (plan 08-02) — 25 cột §4.2;
  `items_snapshot` json: [{menu_item_id, code, name, unit_price, qty, note}]
  index `idx_oor_phone_status` (customer_phone, status) · `idx_oor_phone_submitted`
  `customer_lat/lng` decimal(10,7) và `distance_km` decimal(6,2) → mysql2 trả **string**

apps/api/src/modules/menu/menu.controller.ts method `toggleStock` (dòng ~296-357) — khuôn mẫu
  transaction: `await this.ds.transaction(async (mgr) => { const repo = mgr.getRepository(X); ... })`

apps/api/src/modules/auth/auth.controller.ts method `login` (dòng ~1-31) — khuôn mẫu `@Throttle`:
  `import { Throttle } from '@nestjs/throttler'` · `@Throttle({ default: { limit: 5, ttl: 300_000 } })`

apps/api/src/modules/public/public.controller.ts — khuôn mẫu response: `apiOk<T>()`, error là
  `HttpException` con với body `{ code, message }`

apps/api/src/common/filters/global-exception.filter.ts — dict `FRIENDLY_VN[code]` **GHI ĐÈ** message
  động. ⚠ KHÔNG thêm 9 code phase 8 vào dict đó; build message hoàn chỉnh ngay tại nơi throw.

08-UI-SPEC.md bảng Copywriting — copy chính xác cho từng mã lỗi (BE build message, FE hiện nguyên văn):
  ONLINE_ORDERING_DISABLED: "Quán vừa tắt nhận đơn online. {off_reason nếu có, else 'Vui lòng gọi {store_phone} để đặt trực tiếp.'}"
  STORE_CLOSED: "Quán đang ngoài giờ mở cửa hôm nay. Gọi {store_phone} nếu cần hỗ trợ."
  PHONE_BLACKLISTED: "Không thể gửi đơn với số điện thoại này lúc này. Vui lòng gọi {store_phone} để được hỗ trợ."  ← tông TRUNG TÍNH, KHÔNG nói "bị chặn"/"blacklist" (D-21)
  TOO_MANY_REQUESTS: "Bạn thao tác hơi nhanh, vui lòng thử lại sau ít phút."
  ORDER_ALREADY_OPEN_FOR_PHONE: "Số điện thoại này đang có 1 đơn chưa xử lý xong. Vui lòng chờ quán xác nhận, hoặc gọi {store_phone}."
  MENU_ITEM_UNAVAILABLE: "Một vài món trong giỏ hàng vừa hết. Vui lòng quay lại giỏ hàng để cập nhật."
  ORDER_TOKEN_NOT_FOUND: dùng cho GET /:token không tìm thấy

docs/MILESTONE-02-ONLINE-ORDERING-SPEC.md §7 dòng 461-463 — thứ tự kiểm tra (ĐÃ CHỐT, không tự nghĩ lại):
  ordering_enabled → is_open_now → phone not blacklisted → rate limit → no open order for phone → món còn hàng
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: submit-order.ts — orchestrator nhận port, test được bằng fake-repository</name>
  <files>apps/api/src/modules/public/submit-order.ts, apps/api/src/modules/public/public-orders.test.ts</files>
  <read_first>
    - apps/api/src/modules/public/order-guard.ts (plan 08-01 — `checkOrderGuard` và thứ tự ưu tiên đã có test)
    - apps/api/src/modules/public/haversine.ts và phone.ts và ip-hash.ts (plan 08-01/08-05 — chữ ký hàm)
    - apps/api/src/common/origin-allowlist.test.ts (khuôn mẫu test)
    - packages/schemas/src/public-orders.ts (plan 08-01 — `OnlineOrderSubmit`)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-RESEARCH.md mục "Pattern 2" và §Anti-Patterns ("đừng tin giá client gửi")
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-VALIDATION.md (dòng `public-orders.test.ts` — hướng nhẹ: fake-repository, không `@nestjs/testing`/`supertest`)
    - docs/MILESTONE-02-ONLINE-ORDERING-SPEC.md §7 dòng 455-470
  </read_first>
  <behavior>
    public-orders.test.ts dùng **fake `SubmitDeps`** (object literal trả dữ liệu định trước, không bootstrap
    Nest, không DB) và phải phủ:
    - Công tắc OFF thủ công → throw có `code = 'ONLINE_ORDERING_DISABLED'`, và message **chứa** `off_reason` khi có, chứa `store_phone` khi `off_reason` rỗng
    - Ngoài giờ mở cửa → `code = 'STORE_CLOSED'`, message chứa `store_phone`
    - SĐT blacklist → `code = 'PHONE_BLACKLISTED'`, và message **không chứa** chuỗi `chặn` hay `blacklist` (D-21 tông trung tính)
    - Vượt 3 đơn/giờ theo SĐT → `code = 'TOO_MANY_REQUESTS'`
    - Đã có đơn `WAITING` cùng SĐT → `code = 'ORDER_ALREADY_OPEN_FOR_PHONE'`
    - 1 món `is_out_of_stock` hoặc `is_active = false` hoặc id không tồn tại → `code = 'MENU_ITEM_UNAVAILABLE'`
    - **Snapshot giá**: client gửi `items` chỉ có `{menu_item_id, qty}`; fake menu trả `price = 45000` → `items_snapshot` có `unit_price = 45000` và `subtotal = 45000 × qty`
    - **Không tin client**: gọi `submitOrder` với input bị nhồi thêm `unit_price: 0` (cast qua `as any`) → `items_snapshot` **vẫn** dùng giá từ fake menu, `subtotal > 0`
    - PICKUP: `customer_address` bỏ trống vẫn thành công; `customer_lat/lng` không truyền → `distance_km = null`
    - DELIVERY có toạ độ khách + toạ độ quán → `distance_km` ≈ `haversineKm × distance_factor`, làm tròn 2 chữ số
    - DELIVERY có toạ độ khách nhưng **thiếu** `store_lat/store_lng` → `distance_km = null`, **không throw** (thiếu cấu hình không được chặn khách đặt hàng)
    - `ip_hash` trong row insert khớp `hashIp(ip, salt)` và **không chứa** IP gốc
    - `order_token` là 64 ký tự hex và **khác nhau** giữa 2 lần gọi
    - SĐT `'0912 345 678'` và `'+84912345678'` cùng map về 1 khoá khi kiểm blacklist/open-order (gọi `normalizePhone` đúng chỗ)
    - `status` của row insert là `'WAITING'`
  </behavior>
  <action>
    Tạo `submit-order.ts` — tách "quyết định + dựng dữ liệu" khỏi "truy cập DB", để test được không cần
    MySQL. Khai `export type SubmitDeps` là port với đúng các phương thức sau (không hơn):
    - `getOrderingStatus(nowMs: number): Promise<OrderingStatus>`
    - `readSettings(): Promise<{ store_phone: string; store_lat: number|null; store_lng: number|null; distance_factor: number; free_ship_km: number; online_ordering_off_reason: string; pickup_enabled: boolean; delivery_enabled: boolean }>`
    - `isPhoneBlacklisted(phone: string): Promise<boolean>`
    - `countRecentByPhone(phone: string, sinceMs: number): Promise<number>`
    - `hasOpenOrderForPhoneLocked(phone: string): Promise<boolean>` — bản cài thật dùng `FOR UPDATE` (Task 2)
    - `findMenuItemsByIds(ids: string[]): Promise<Array<{ id, code, name, price, unit, is_active, is_out_of_stock }>>`
    - `insertRequest(row: OnlineOrderRequestInsert): Promise<void>`
    - `hashIpFn(ip: string): string` — bọc salt sẵn, để test không cần env var

    `export async function submitOrder(deps: SubmitDeps, input: OnlineOrderSubmit, ctx: { ip: string; userAgent: string; nowMs: number }): Promise<{ order_token: string; distance_km: string | null }>`
    thực hiện đúng trình tự:
    1. `const phone = normalizePhone(input.customer_phone)`; `null` → throw `VALIDATION_FAILED` với message
       "Số điện thoại không hợp lệ".
    2. Fetch song song 5 dữ liệu quyết định: `getOrderingStatus(nowMs)`, `isPhoneBlacklisted(phone)`,
       `countRecentByPhone(phone, nowMs - PHONE_WINDOW_MS)`, `hasOpenOrderForPhoneLocked(phone)`,
       `findMenuItemsByIds(ids)`. Hằng ở đầu file, dễ chỉnh: `PHONE_MAX_ORDERS_PER_WINDOW = 3`,
       `PHONE_WINDOW_MS = 3_600_000` (**tối đa 3 đơn/giờ theo SĐT** — đếm mọi trạng thái, đã chốt).
       ⚠ `hasOpenOrderForPhoneLocked` phải gọi **tuần tự sau** các bước khác trong bản cài thật nếu cần giữ
       thứ tự lock; ưu tiên đúng thứ tự lock hơn là song song hoá (ghi comment nói rõ).
    3. `unavailableItemCodes` = mã của mọi món: không tìm thấy id, `is_active = false`, hoặc
       `is_out_of_stock = true` (dùng `menu_item_id` khi không tìm thấy code).
    4. `checkOrderGuard({...})` → có code → throw exception mang `{ code, message }` với message **build tại
       chỗ** theo bảng Copywriting ở `<interfaces>` (nội suy `off_reason`, `store_phone`). **KHÔNG** thêm
       code nào vào `FRIENDLY_VN` (Pitfall #6 — dict đó sẽ ghi đè message động bằng chuỗi tĩnh).
       Chọn đúng lớp exception để ra status code: `TOO_MANY_REQUESTS` → 429
       (`HttpException` với `HttpStatus.TOO_MANY_REQUESTS`), 5 code còn lại → 409 `ConflictException`.
    5. `fulfillment_type = 'PICKUP'` mà `pickup_enabled = false` (hoặc DELIVERY mà `delivery_enabled = false`)
       → throw 409 `CONFLICT` với message nói rõ phương thức đó đang tạm ngưng.
    6. **Dựng `items_snapshot` từ dữ liệu DB**, không từ input: mỗi dòng
       `{ menu_item_id, code, name, unit_price: <price từ DB>, qty: <từ input>, note: <từ input ?? null> }`.
       `subtotal = Σ unit_price × qty`. Ghi comment: input **cố ý không có** `unit_price`; nếu tương lai ai
       thêm field giá vào DTO thì đây là chỗ phải từ chối nó.
    7. `distance_km`: chỉ tính khi `fulfillment_type === 'DELIVERY'` **và** có `customer_lat/lng` **và** có
       `store_lat/store_lng` → `estimatedRoadDistanceKm(haversineKm(...), distance_factor).toFixed(2)`;
       mọi trường hợp khác → `null`. **Không auto-tính tiền ship** (M2.D-52) — chỉ ra số km.
    8. `order_token = randomBytes(32).toString('hex')` (64 ký tự, khớp `varchar(64)`).
    9. `insertRequest({ order_token, customer_token, status: 'WAITING', fulfillment_type, customer_name,
       customer_phone: phone (đã chuẩn hoá), customer_address, customer_lat, customer_lng,
       customer_map_link, distance_km, customer_note, items_snapshot, subtotal, submitted_at: nowMs,
       ip_hash: deps.hashIpFn(ctx.ip), user_agent: ctx.userAgent.slice(0, 255), max_progress_shown: 0 })`.
    10. Trả `{ order_token, distance_km }`.

    Viết `public-orders.test.ts` với fake `SubmitDeps` phủ đủ `<behavior>`. **Không** thêm
    `@nestjs/testing`/`supertest`/`@types/supertest` (quyết định "hướng nhẹ" đã chốt).
  </action>
  <verify>
    <automated>pnpm --filter @order/api test -- public-orders.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function submitOrder" apps/api/src/modules/public/submit-order.ts` = 1
    - `grep -c "checkOrderGuard" apps/api/src/modules/public/submit-order.ts` = 1
    - `grep -cE "input\.items\[.\]\.unit_price|body\.unit_price" apps/api/src/modules/public/submit-order.ts` = 0 (không đọc giá từ input)
    - `grep -c "PHONE_MAX_ORDERS_PER_WINDOW = 3" apps/api/src/modules/public/submit-order.ts` = 1 và `grep -c "PHONE_WINDOW_MS = 3_600_000" ...` = 1
    - `grep -c "randomBytes(32)" apps/api/src/modules/public/submit-order.ts` = 1
    - `grep -ciE "blacklist|bị chặn" apps/api/src/modules/public/submit-order.ts` — chuỗi này CHỈ được xuất hiện trong tên biến/comment, **không** trong message gửi khách (xác nhận bằng đọc nhánh `PHONE_BLACKLISTED`)
    - `grep -cE "@nestjs/testing|supertest" apps/api/src/modules/public/public-orders.test.ts` = 0
    - `pnpm --filter @order/api test -- public-orders.test.ts` exit 0 với ≥ 15 test pass
    - `pnpm --filter @order/api typecheck` exit 0
  </acceptance_criteria>
  <done>Toàn bộ nhánh quyết định của submit (6 mã lỗi + snapshot giá + khoảng cách + hash IP) có test tự động chạy không cần DB — phủ phần logic của criteria 2, 3, 4.</done>
</task>

<task type="auto">
  <name>Task 2: PublicOrdersService (transaction + gap lock) và controller (POST có @Throttle, GET /:token)</name>
  <files>apps/api/src/modules/public/public-orders.service.ts, apps/api/src/modules/public/public-orders.controller.ts, apps/api/src/modules/public/public.module.ts</files>
  <read_first>
    - apps/api/src/modules/public/submit-order.ts (Task 1 — port `SubmitDeps` phải cài đủ, không thêm phương thức ngoài)
    - apps/api/src/modules/menu/menu.controller.ts method `toggleStock` dòng ~296-357 (khuôn mẫu `this.ds.transaction`)
    - apps/api/src/modules/auth/auth.controller.ts dòng 1-31 (khuôn mẫu `@Throttle`)
    - apps/api/src/modules/public/public.controller.ts (khuôn mẫu `@Controller('api/public')` + `apiOk`)
    - apps/api/src/modules/public/public.module.ts (file sẽ sửa — plan 08-07 đã thêm `SettingsModule` + `forFeature`)
    - apps/api/src/modules/public/entities/online-order-request.entity.ts (plan 08-02 — tên cột, index `idx_oor_phone_status`)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-RESEARCH.md mục "Pattern 3" (gap lock `FOR UPDATE`, có câu SQL mẫu)
    - packages/schemas/src/public-orders.ts (`OnlineOrderSubmit` + `PublicOrderStatus`)
  </read_first>
  <action>
    Tạo `public-orders.service.ts` (`@Injectable()`), inject `@InjectDataSource() ds: DataSource` và
    `SettingsService`:
    - `submit(input: OnlineOrderSubmit, ctx): Promise<{ order_token, distance_km }>` — bọc **toàn bộ**
      validate + insert trong `this.ds.transaction(async (mgr) => submitOrder(this.makeDeps(mgr), input, ctx))`.
      `makeDeps(mgr)` cài `SubmitDeps` lên `mgr`:
      · `hasOpenOrderForPhoneLocked(phone)` → **raw query có gap lock**:
        `SELECT id FROM online_order_requests WHERE customer_phone = ? AND status = 'WAITING' FOR UPDATE`.
        Đây là điểm chống race: MySQL không có unique-partial-index, nên dưới `REPEATABLE READ` mặc định,
        câu này giữ next-key/gap lock trên khoảng của index `idx_oor_phone_status` → transaction thứ 2 cùng
        SĐT **bị chặn** cho tới khi transaction đầu COMMIT/ROLLBACK. Ghi comment nêu rõ: **không** thay bằng
        `findOne` thường, **không** dùng `GET_LOCK()` hay bảng lock phụ.
      · `countRecentByPhone(phone, sinceMs)` →
        `SELECT COUNT(*) FROM online_order_requests WHERE customer_phone = ? AND submitted_at >= ?`
        (đếm trong DB — D-18: bộ đếm in-memory reset khi restart là vô nghĩa với chống bom đơn; không thêm Redis).
      · `isPhoneBlacklisted(phone)` → có row trong `phone_blacklist` với `phone = ?` **và**
        (`expires_at IS NULL` **hoặc** `expires_at > NOW()`). Cột `expires_at` hiện luôn NULL (M2.D-59) nhưng
        điều kiện viết sẵn để sau bật chặn tạm thời không phải sửa logic.
      · `findMenuItemsByIds(ids)` → `mgr.getRepository(MenuItem).find({ where: { id: In(ids) } })`, chọn đúng
        7 field cần (`id, code, name, price, unit, is_active, is_out_of_stock`).
      · `insertRequest(row)` → `mgr.getRepository(OnlineOrderRequest).insert(row)`.
      · `readSettings()` / `getOrderingStatus(nowMs)` → uỷ quyền cho `SettingsService`.
      · `hashIpFn(ip)` → `hashIp(ip, resolveIpHashSalt())`.
    - `getByToken(token: string): Promise<PublicOrderStatus>` — tìm theo `order_token`; không có → throw
      `NotFoundException({ code: 'ORDER_TOKEN_NOT_FOUND', message: 'Không tìm thấy đơn này.' })`. Trả đúng
      `PublicOrderStatus`: `items` map từ `items_snapshot` chỉ lấy `{ name, qty, unit_price }` — **tuyệt đối
      không** trả trạng thái từng món (M2.D-23, điều kiện của G-1), **không** trả `ip_hash`, `user_agent`,
      `customer_*`, `reviewed_by_*`. Gọi `PublicOrderStatus.strict().parse()` trước khi trả.
      Phase 8 chỉ cần màn xác nhận tối giản; % tiến độ + 5 mốc + banner cập nhật là REQ-O/phase 9.

    Tạo `public-orders.controller.ts` với `@Controller('api/public')`:
    - `@Post('orders')` + `@HttpCode(201)` + **`@Throttle({ default: { limit: 10, ttl: 60_000 } })`**
      (10 request/phút/IP riêng cho endpoint này; throttler global 600/phút vẫn áp song song — đã chốt).
      Validate body bằng `OnlineOrderSubmit.safeParse()` của zod (cần `.refine()` cho điều kiện chéo
      PICKUP/DELIVERY mà `class-validator` không diễn đạt gọn) → lỗi → `BadRequestException` với
      `{ code: 'VALIDATION_FAILED', message, field_errors }` map từ `issues` của zod (khớp
      `ErrorEnvelope.field_errors`). Lấy IP theo cách Nest/Express cho ra (`req.ip`) và
      `req.headers['user-agent'] ?? ''`. Trả `apiOk({ order_token })` — **không** trả lại `items_snapshot`
      hay `subtotal` (khách đã biết; giảm bề mặt rò).
    - `@Get('orders/:token')` → `apiOk(await service.getByToken(token))`. Không `@Throttle` riêng.
    - Cả 2 route: header `Cache-Control: no-store`.
    Thêm `PublicOrdersController` vào `controllers` và `PublicOrdersService` vào `providers` của
    `public.module.ts`; bổ sung `OnlineOrderRequest`, `PhoneBlacklist` vào `TypeOrmModule.forFeature`.
  </action>
  <verify>
    <automated>pnpm --filter @order/api typecheck && pnpm --filter @order/api test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "FOR UPDATE" apps/api/src/modules/public/public-orders.service.ts` = 1
    - `grep -c "ds.transaction" apps/api/src/modules/public/public-orders.service.ts` = 1
    - `grep -c "@Throttle({ default: { limit: 10, ttl: 60_000 } })" apps/api/src/modules/public/public-orders.controller.ts` = 1
    - `grep -cE "ip_hash|user_agent|reviewed_by|customer_phone" apps/api/src/modules/public/public-orders.service.ts` — các chuỗi này xuất hiện ở phần insert/query nhưng **không** trong hàm `getByToken` (xác nhận bằng đọc hàm)
    - `grep -c "PublicOrderStatus.strict().parse" apps/api/src/modules/public/public-orders.service.ts` = 1
    - Chạy thật, công tắc ON: `curl -i -X POST http://localhost:3001/api/public/orders -H 'Content-Type: application/json' -H 'Origin: http://localhost:5174' -d '{"customer_token":"<64hex>","customer_name":"Test","customer_phone":"0912345678","fulfillment_type":"PICKUP","items":[{"menu_item_id":"<id món thật>","qty":2}]}'` → **201** + `{"data":{"order_token":"<64hex>"}}`
    - `mysql ... -e "SELECT status, subtotal, ip_hash, LENGTH(ip_hash), items_snapshot FROM online_order_requests ORDER BY created_at DESC LIMIT 1"` → `status = WAITING`, `subtotal` = `price × 2` đúng theo DB, `LENGTH(ip_hash) = 64`, `items_snapshot` có `unit_price` bằng giá DB
    - `mysql ... -e "SELECT COUNT(*) FROM online_order_requests WHERE ip_hash LIKE '%127.0.0.1%' OR ip_hash LIKE '%::1%'"` = 0 (không có IP thô)
    - **Chống đặt giá 0đ**: gửi lại request trên với `items: [{"menu_item_id":"<id>","qty":1,"unit_price":0}]` → vẫn 201 và `subtotal` = giá DB (field lạ bị bỏ), **không** phải 0
    - **Chặn 2 lớp**: tắt công tắc ở Dashboard rồi gửi lại → **409** + body chứa `ONLINE_ORDERING_DISABLED` và message chứa lý do đã nhập
    - **1 đơn mở/SĐT**: bật công tắc, gửi lại cùng SĐT (đơn WAITING cũ còn đó) → **409** `ORDER_ALREADY_OPEN_FOR_PHONE`
    - **Blacklist**: thêm `0987654321` vào blacklist rồi submit với số đó → 409 `PHONE_BLACKLISTED`, message **không** chứa chữ "chặn"/"blacklist"
    - **Rate limit IP**: gửi 11 request trong 1 phút → request thứ 11 trả **429**
    - `GET /api/public/orders/<token>` → 200, body **không** chứa `ip_hash`/`user_agent`/`customer_phone`, và mỗi item chỉ có `name`, `qty`, `unit_price`
    - `GET /api/public/orders/khong-ton-tai` → 404 `ORDER_TOKEN_NOT_FOUND`
    - **Đơn đang chạy không bị ảnh hưởng**: tắt công tắc → `GET /api/public/orders/<token>` của đơn đã gửi vẫn trả 200 bình thường
  </acceptance_criteria>
  <done>Endpoint submit hoạt động đầy đủ 6 lớp kiểm tra, giá do BE chốt, IP hash 64 hex, rate limit IP + SĐT, và màn xác nhận không leak field nội bộ.</done>
</task>

<task type="auto">
  <name>Task 3: Integration test gap lock với MySQL thật (criterion 4, phần DB)</name>
  <files>apps/api/src/modules/public/open-order-lock.integration.test.ts</files>
  <read_first>
    - apps/api/src/modules/public/public-orders.service.ts (Task 2 — câu `FOR UPDATE` cần chứng minh)
    - apps/api/src/data-source.ts (`AppDataSource`, `synchronize: true`, `extra.connectionLimit: 50`)
    - apps/api/src/cli/cron-jti-cleanup.ts (khuôn mẫu dùng `AppDataSource` ngoài context Nest)
    - apps/api/src/common/origin-allowlist.test.ts (khuôn mẫu test)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-RESEARCH.md mục "Pattern 3" và §Environment Availability (MySQL local)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-VALIDATION.md (dòng `open-order-lock.integration.test.ts` — dùng thẳng `DataSource`, KHÔNG bootstrap Nest)
  </read_first>
  <action>
    Tạo `open-order-lock.integration.test.ts` dùng **thẳng `DataSource`**, không bootstrap Nest, không
    `supertest`.
    - `beforeAll`: khởi tạo một `DataSource` riêng từ `dataSourceOptions` (đặt `synchronize: false` cho
      instance test — bảng đã tồn tại từ plan 08-02, test không được đổi schema). Nếu kết nối thất bại thì
      **fail rõ ràng** với message hướng dẫn bật MySQL local — KHÔNG `it.skip` im lặng (test bị skip âm thầm
      chính là cách criterion 4 mất bằng chứng).
    - Dữ liệu test: dùng 1 SĐT sentinel không đụng dữ liệu thật (ví dụ `'0900000001'`), xoá sạch row của SĐT
      đó ở `beforeEach` và `afterAll`.
    - Test 1 — **gap lock chặn race**: mở 2 `QueryRunner` riêng (2 connection), cùng
      `startTransaction()`. Runner A chạy
      `SELECT id FROM online_order_requests WHERE customer_phone = ? AND status = 'WAITING' FOR UPDATE`
      (rỗng) rồi `INSERT` 1 row WAITING. Runner B chạy **cùng câu `FOR UPDATE`** trước khi A commit —
      assert B **bị chặn** (dùng `Promise.race` với timer ~500ms: promise của B chưa resolve). Sau khi A
      `commitTransaction()`, B mới đọc được và thấy **1 row** → nếu logic đúng, B kết luận "đã có đơn mở" và
      không insert. Cuối cùng assert `SELECT COUNT(*) WHERE customer_phone = ? AND status = 'WAITING'` = **1**.
    - Test 2 — **không chặn oan SĐT khác**: runner A giữ lock cho SĐT X, runner B chạy cùng câu cho SĐT Y →
      B **không** bị chặn (resolve trong < 500ms). Chứng minh lock ở khoảng index chứ không khoá cả bảng.
    - Test 3 — **cửa sổ đếm rate limit**: insert 3 row cho SĐT sentinel với `submitted_at` trong 1 giờ gần
      nhất, `COUNT(*) WHERE customer_phone = ? AND submitted_at >= now - 3600000` = 3; thêm 1 row
      `submitted_at` = 2 giờ trước → count vẫn 3 (row cũ ngoài cửa sổ).
    - `afterAll`: `destroy()` cả 2 runner + DataSource. Không để connection treo (pool 50 dùng chung với
      dev server).
    - Đặt `testTimeout` đủ cho phần chờ lock (ví dụ 15s) qua tham số của `it`, không dùng watch mode.
    - Comment đầu file ghi rõ: file này là **bằng chứng duy nhất** cho phần DB của criterion 4 ROADMAP phase 8
      ("2 request cùng SĐT không tạo được 2 đơn mở"); mock không chứng minh được hành vi lock của InnoDB.
  </action>
  <verify>
    <automated>pnpm --filter @order/api test -- open-order-lock.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "FOR UPDATE" apps/api/src/modules/public/open-order-lock.integration.test.ts` ≥ 2
    - `grep -cE "@nestjs/testing|supertest" apps/api/src/modules/public/open-order-lock.integration.test.ts` = 0
    - `grep -cE "it\.skip|describe\.skip" apps/api/src/modules/public/open-order-lock.integration.test.ts` = 0
    - `grep -c "synchronize: false" apps/api/src/modules/public/open-order-lock.integration.test.ts` = 1
    - `pnpm --filter @order/api test -- open-order-lock.integration.test.ts` exit 0 với 3 test pass
    - Sau khi test xong: `mysql ... -e "SELECT COUNT(*) FROM online_order_requests WHERE customer_phone LIKE '09000000%'"` = 0 (dọn sạch dữ liệu sentinel)
    - `pnpm --filter @order/api test` full suite exit 0
  </acceptance_criteria>
  <done>Gap lock được chứng minh bằng 2 connection thật trên MySQL: request thứ hai bị chặn, không chặn oan SĐT khác, cửa sổ đếm rate limit đúng.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| khách vô danh → `POST /api/public/orders` | Mutation công khai đầu tiên của hệ thống; mọi field untrusted |
| body `items[]` → giá tiền lưu vào DB | Ranh giới quan trọng nhất: giá **không được** đến từ client |
| `req.ip` → cột `ip_hash` | PII đi qua ranh giới lưu trữ |
| 2 request đồng thời → 1 quy tắc nghiệp vụ | Ranh giới đồng thời (transaction/lock) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-49 | Tampering | client tự đặt giá 0đ/âm (**HIGH**) | mitigate | `OnlineOrderSubmit` không có `unit_price`; `submitOrder` build `items_snapshot` từ `findMenuItemsByIds`; test có case nhồi `unit_price: 0` và curl thật xác nhận `subtotal` = giá DB |
| T-08-50 | Tampering | race 2 request cùng SĐT lách "1 đơn mở" (**HIGH**) | mitigate | `SELECT ... FOR UPDATE` trong cùng transaction với INSERT + index `idx_oor_phone_status`; `open-order-lock.integration.test.ts` chứng minh bằng 2 connection thật |
| T-08-51 | Information Disclosure | IP thô rò qua log/backup (**HIGH**) | mitigate | `hashIp` HMAC-SHA256 + `IP_HASH_SALT` từ env; không cột nào lưu IP thô; acceptance criteria query DB xác nhận |
| T-08-52 | Tampering | form auto-submit từ site lạ (**HIGH**) | mitigate | `CsrfOriginGuard` đã phủ `/api/public/*` từ plan 08-07 (**dependency cứng**) + `@Throttle` 10/phút/IP + rate limit SĐT 3/giờ + blacklist |
| T-08-53 | Information Disclosure | `GET /orders/:token` leak trạng thái từng món (vi phạm G-1) | mitigate | `items` map chỉ `{name, qty, unit_price}` + `PublicOrderStatus.strict().parse()`; acceptance criteria kiểm body thật |
| T-08-54 | Information Disclosure | `GET /orders/:token` leak PII/`ip_hash`/`reviewed_by_*` | mitigate | Whitelist tường minh + `.strict().parse()`; `order_token` là bearer credential nên `Referrer-Policy: no-referrer` đã đặt ở Caddy block `order.` (OD-02, phase 7) |
| T-08-55 | Spoofing | đoán `order_token` để xem đơn người khác | mitigate | `randomBytes(32)` = 256 bit CSPRNG; test xác nhận 64 hex và khác nhau mỗi lần |
| T-08-56 | Denial of Service | bom đơn làm đầy bảng / khoá pool | mitigate | `@Throttle` 10/phút/IP + 3 đơn/giờ/SĐT đếm trong DB (sống sót restart, D-18) + `items` tối đa 50 dòng, `qty` ≤ 99 trong schema |
| T-08-57 | Elevation of Privilege | đơn WAITING lọt vào bếp/doanh thu | accept (phạm vi phase 9) | Phase 8 chỉ insert vào `online_order_requests`, **không** ghi gì vào `orders`/`order_items` — M2.D-01 tự động thoả; test đếm doanh thu là criterion của phase 9 |
| T-08-58 | Tampering | message lỗi bị dict `FRIENDLY_VN` ghi đè bằng chuỗi tĩnh | mitigate | Không thêm 9 code phase 8 vào dict; message nội suy `off_reason`/`store_phone` build tại nơi throw; test assert message chứa `off_reason` |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | Plan này KHÔNG cài package nào — quyết định "hướng nhẹ" loại bỏ `@nestjs/testing`/`supertest`/`@types/supertest`; grep trong acceptance criteria xác nhận không lén import |
</threat_model>

<verification>
1. `pnpm --filter @order/api test` — full suite xanh (bao gồm ≥ 15 test `public-orders` + 3 test integration)
2. `pnpm -r typecheck` — sạch
3. 10 kịch bản `curl` ở acceptance criteria Task 2 (201 · giá 0đ bị bỏ · 409 OFF · 409 open-order · 409 blacklist · 429 rate limit · GET token · 404 token · đơn cũ không bị ảnh hưởng)
4. Truy vấn MySQL xác nhận `LENGTH(ip_hash) = 64` và không có IP thô
5. `curl -i -X POST http://localhost:3001/api/public/orders` **không** header Origin → 403 (guard plan 08-07 còn hiệu lực)
</verification>

<success_criteria>
- Submit thành công trả `order_token`, row `WAITING` với `items_snapshot` + `subtotal` giá từ DB
- 6 mã lỗi đúng thứ tự spec §7, message nội suy `off_reason`/`store_phone`, tông `PHONE_BLACKLISTED` trung tính
- Gap lock chứng minh bằng 2 connection MySQL thật; không chặn oan SĐT khác
- Rate limit IP 10/phút (429) và SĐT 3/giờ (409) hoạt động
- `ip_hash` 64 hex HMAC, không IP thô trong DB
- `GET /api/public/orders/:token` không leak trạng thái từng món và không leak PII
- Không thêm devDependency nào cho test
</success_criteria>

<output>
Create `.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-10-SUMMARY.md` when done
</output>
