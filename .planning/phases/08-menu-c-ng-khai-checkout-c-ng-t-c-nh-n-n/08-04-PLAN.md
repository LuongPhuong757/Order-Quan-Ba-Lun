---
phase: 08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/shop/package.json
  - apps/shop/src/main.tsx
  - apps/shop/src/components/AppShell.tsx
  - apps/shop/src/components/Header.tsx
  - apps/shop/src/components/CartIcon.tsx
  - apps/shop/src/pages/MenuPage.tsx
  - scripts/check-shop-bundle.sh
autonomous: true
requirements: [REQ-I]

must_haves:
  truths:
    - "Mở http://localhost:5174/ hiện trang menu thật (không phải BrandPreview), header có wordmark + ô tìm kiếm + icon giỏ hàng"
    - "5 đường dẫn /, /cart, /checkout, /o/:token, /history đều render đúng trang, không 404, reload trực tiếp cũng được"
    - "4 trang trong src/pages/ không còn là dead code — chúng có mặt trong bundle đã build"
    - "Guard M2.D-64 vẫn xanh sau khi bundle có router thật, và từ nay có thêm ngưỡng kích thước ghi rõ lý do"
    - "Không hardcode hex/px nào trong .tsx mới — mọi giá trị đọc qua var(--...)"
  artifacts:
    - path: "apps/shop/src/main.tsx"
      provides: "BrowserRouter + 5 route, không còn import BrandPreview"
      contains: "BrowserRouter"
    - path: "apps/shop/src/components/AppShell.tsx"
      provides: "Layout chung: Header + <Outlet/>"
      contains: "Outlet"
    - path: "apps/shop/src/components/Header.tsx"
      provides: "2 biến thể desktop/mobile bằng CSS media query"
      contains: "@media"
    - path: "apps/shop/src/pages/MenuPage.tsx"
      provides: "Trang menu (khung + trạng thái tải), nội dung thật ở plan 08-09"
      min_lines: 30
  key_links:
    - from: "apps/shop/src/main.tsx"
      to: "./components/AppShell.tsx"
      via: "Route element bọc 5 route con"
      pattern: "element=\\{<AppShell"
    - from: "apps/shop/src/components/Header.tsx"
      to: "apps/shop/src/styles/tokens.css"
      via: "var(--...) trong CSSProperties"
      pattern: "var\\(--"
---

<objective>
Dựng router thật cho `apps/shop`: `BrowserRouter` + `AppShell` + `Header` 2 biến thể, gắn 5 trang vào
route, thêm `zod` làm direct dependency, và xác nhận lại guard bundle khách sau khi bundle có route thật.

Purpose: **Đây là việc đầu tiên của phase 8.** `main.tsx` hiện render `BrandPreview` (trang xem màu tạm);
4 trang `CartPage`/`CheckoutPage`/`HistoryPage`/`OrderTrackPage` tồn tại nhưng **không được import ở đâu**
→ dead code, bị tree-shake, không vào bundle. Mọi UI khác của phase 8 phụ thuộc việc này. Kéo theo:
criterion M2.D-64 (bundle khách không chứa code quản lý) từ phase 7 tới giờ pass gần như vô nghĩa vì
bundle chưa có route nào — guard chỉ có giá trị thật từ plan này.

Output: `http://localhost:5174/` chạy app thật 5 route; `sh scripts/check-shop-bundle.sh` in `OK` với cả
gate chuỗi cấm và gate kích thước mới.
</objective>

<execution_context>
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-CONTEXT.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-UI-SPEC.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-PATTERNS.md
@apps/shop/DESIGN.md

<interfaces>
<!-- Hợp đồng UI và mã hiện có. Executor KHÔNG cần đi tìm. -->

apps/shop/src/main.tsx (22 dòng hiện tại) — render `<BrandPreview />`, import `fonts.css` TRƯỚC
  `tokens.css` (thứ tự này bắt buộc: @font-face phải khai trước khi token dùng tên font).
  Có `TODO(phase-08)` mô tả đúng việc plan này làm.

apps/shop/src/components/Wordmark.tsx — khuôn mẫu BẮT BUỘC cho mọi component mới:
  `export function Wordmark({ variant = 'plaque', size = 'var(--fs-md)' }: Props): JSX.Element`
  · object `CSSProperties` khai ở **module scope** (không trong thân component)
  · mọi giá trị đọc `var(--...)`, không hardcode hex/px (C-UI-01)
  · 2 biến thể `plaque` (nền `--bg-wood`) và `bare`

apps/shop/src/pages/CartPage.tsx (và 3 trang còn lại) — placeholder, mỗi file có:
  `import type { JSX } from 'react'` + `Link` từ 'react-router-dom' · docblock ghi mã quyết định
  · `data-testid` trên link back (`cart-back-link`, `checkout-back-link`, `history-back-link`,
    `order-track-back-link`) · style const `page`/`heading`/`body`/`backLink` đọc `var(--...)`

apps/shop/src/styles/tokens.css — token dùng trong plan này:
  `--bg-page --bg-surface --border-subtle --brand-600 --text-strong --text-muted --text-on-brand`
  `--font-display --font-body --fs-caption --fs-sm --fs-base --fs-md --fw-semibold --fw-bold`
  `--sp-1..--sp-16 --gutter --gutter-lg --content-max --r-button --r-input --r-badge`
  `--shadow-sticky --z-sticky-header --tap-min --safe-top --focus-ring`

apps/shop/vite.config.ts — port 5174 strict; proxy CHỈ `/api` và `/uploads` (khách không gọi `/admin/*`)

scripts/check-shop-bundle.sh — gate M2.D-64 hiện tại là **grep chuỗi cấm**, danh sách FORBIDDEN gồm
  `/dashboard`, `/kitchen`, `/admin/`, `DashboardPage`, `KitchenPage`, `TablesManagementPage`,
  `MenuManagementPage`, `AdminAuditPage`, `AdminUsersPage`, `SetupPage`, `RecoverPage`.
  ⚠ Script **chưa có** gate kích thước nào — Task 3 thêm.
  ⚠ Chuỗi `/admin/` nằm trong danh sách cấm → **không được viết chuỗi `/admin/` vào bất kỳ file nào
  của `apps/shop`, kể cả comment**, nếu không guard báo LEAK oan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: zod thành direct dependency + BrowserRouter 5 route trong main.tsx + MenuPage</name>
  <files>apps/shop/package.json, apps/shop/src/main.tsx, apps/shop/src/pages/MenuPage.tsx</files>
  <read_first>
    - apps/shop/src/main.tsx (file sẽ thay ruột — đọc thứ tự import CSS và cách lấy `#root`)
    - apps/shop/package.json (dependencies hiện chỉ có `@order/schemas`, `react`, `react-dom`, `react-router-dom`)
    - apps/shop/src/pages/CartPage.tsx (khuôn mẫu style const + docblock cho MenuPage mới)
    - apps/web/src/App.tsx dòng 1 và 27-40 (cú pháp khai `Routes`/`Route`/element — **chỉ mượn cú pháp**, KHÔNG mượn `AuthProvider`/`RoleGate`/`ProtectedShell` vì `apps/shop` không có auth, M2.D-09)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-PATTERNS.md mục "apps/shop — main.tsx (router + AppShell mount)" (đã có sẵn đoạn thay thế đầy đủ)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-UI-SPEC.md mục "AppShell & Router"
  </read_first>
  <action>
    Chạy `pnpm --filter @order/shop add zod@^3.23.0` — D-02 yêu cầu `apps/shop` parse runtime mọi response
    `/api/public/*`; hiện `zod` chỉ là transitive qua `@order/schemas` nên bundler không đảm bảo resolve.
    Version khớp `apps/web` và `packages/schemas` đang dùng (`^3.23.0`), không nâng major.

    Tạo `apps/shop/src/pages/MenuPage.tsx`: component `MenuPage` export named (khớp convention 4 trang
    hiện có), docblock ghi "REQ-I · nội dung lưới món + dải danh mục + banner ở plan 08-09". Nội dung
    plan này: heading "Menu" (`--font-display`, `--fs-lg`) + 1 vùng `<section>` có `data-testid="menu-grid"`
    dùng `grid-template-columns: repeat(auto-fill, minmax(280px, 1fr))` và `gap: var(--sp-5)` (CONFLICT-DESIGN-01
    / OD-05 — **không dùng media query**), hiện dòng chữ `--text-muted` cho biết đang tải. Style const ở
    module scope theo khuôn `CartPage.tsx`.

    Thay ruột `main.tsx` theo đúng đoạn mẫu trong `08-PATTERNS.md`: giữ 2 dòng import CSS đúng thứ tự
    (`fonts.css` trước `tokens.css`), giữ `const root = document.getElementById('root')` + throw nếu
    thiếu, bọc `<StrictMode>` → `<BrowserRouter>` → `<Routes>` → `<Route element={<AppShell />}>` chứa 5
    route con: `/` → `MenuPage`, `/cart` → `CartPage`, `/checkout` → `CheckoutPage`, `/o/:token` →
    `OrderTrackPage`, `/history` → `HistoryPage`. Thêm route `path="*"` render lại `MenuPage` (khách gõ
    sai URL thì về menu, không thấy trang trắng).

    **Bỏ import `BrandPreview` khỏi `main.tsx`** — giữ nguyên file `BrandPreview.tsx` trên đĩa để tham
    khảo màu, nhưng nó không được nằm trên đường import nào từ `main.tsx` nữa. Xoá luôn khối
    `TODO(phase-08)` đã hoàn thành và thay bằng 1 docblock ngắn mô tả 5 route hiện có.
  </action>
  <verify>
    <automated>pnpm --filter @order/shop typecheck && pnpm --filter @order/shop build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"zod"' apps/shop/package.json` trả về 1 dòng trong khối `dependencies`
    - `grep -c "BrandPreview" apps/shop/src/main.tsx` = 0
    - `grep -cE "BrowserRouter|Routes|Route " apps/shop/src/main.tsx` ≥ 3
    - `grep -oE 'path="[^"]+"' apps/shop/src/main.tsx` in ra đúng 6 dòng: `/`, `/cart`, `/checkout`, `/o/:token`, `/history`, `*`
    - `grep -c "minmax(280px, 1fr)" apps/shop/src/pages/MenuPage.tsx` = 1 và `grep -c "@media" apps/shop/src/pages/MenuPage.tsx` = 0
    - `grep -nE "#[0-9a-fA-F]{3,6}\b" apps/shop/src/main.tsx apps/shop/src/pages/MenuPage.tsx` không trả kết quả (C-UI-01)
    - `pnpm --filter @order/shop build` exit 0; `grep -rl "OrderTrackPage\|CheckoutPage" apps/shop/dist` trả về ≥ 1 file (4 trang đã vào bundle, không còn dead code)
  </acceptance_criteria>
  <done>5 route hoạt động, `zod` là direct dependency, 4 trang cũ đã có mặt trong bundle build.</done>
</task>

<task type="auto">
  <name>Task 2: AppShell + Header 2 biến thể (CSS-only) + CartIcon</name>
  <files>apps/shop/src/components/AppShell.tsx, apps/shop/src/components/Header.tsx, apps/shop/src/components/CartIcon.tsx</files>
  <read_first>
    - apps/shop/src/components/Wordmark.tsx (khuôn mẫu BẮT BUỘC: object CSSProperties module scope, `var(--...)`, 2 biến thể `plaque`/`bare`)
    - apps/shop/src/styles/tokens.css (đọc kỹ ghi chú tương phản: `--wood-400/500` CHỈ trang trí, không dùng cho chữ)
    - apps/shop/DESIGN.md (design system — `components.banner-notice`, quy ước đặt tên)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-UI-SPEC.md mục "Header — biến thể Desktop", "Header — biến thể Mobile", "Ô tìm kiếm", "Giỏ hàng nổi"
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-CONTEXT.md D-22 (giả định #2 không dùng package icon, #8 header 2 biến thể bằng CSS media query, #9 giỏ hàng nổi mobile-only)
  </read_first>
  <action>
    Tạo `CartIcon.tsx`: SVG **tự vẽ tay** (D-22 giả định #2 — không `lucide-react`, không `react-icons`),
    `stroke="currentColor"`, `strokeWidth={1.75}`, mặc định 20px (24px ở header) qua prop `size`. Kèm
    badge tròn: `borderRadius: var(--r-badge)`, nền `var(--brand-600)`, chữ `var(--text-on-brand)`, tối
    thiểu 18px, hiện số món; **ẩn hoàn toàn badge khi count = 0** (không hiện vòng tròn rỗng). Props:
    `{ count: number; size?: number }`.

    Tạo `Header.tsx`. Kiến trúc bắt buộc: **2 khối DOM riêng, ẩn/hiện bằng CSS media query ~768px** qua
    một thẻ `<style>` chứa 2 class (`shop-hd-desktop`, `shop-hd-mobile`) — KHÔNG dùng `useMediaQuery`,
    KHÔNG JS resize listener (D-22 giả định #8: tránh nhấp nháy giữa 2 layout lúc mount). Sticky:
    `position: sticky; top: 0; zIndex: var(--z-sticky-header)`, nền `var(--bg-surface)`, viền dưới
    `var(--border-subtle)`, `paddingTop: var(--safe-top)`.
    - Biến thể desktop: trái `<Wordmark variant="bare"/>`; giữa nav ngang IN HOA 2 mục "TRANG CHỦ" (`/`)
      và "ĐƠN CỦA TÔI" (`/history`) bằng `NavLink`, mục đang xem gạch chân + chữ `var(--brand-600)`;
      phải ô tìm kiếm inline rộng ~240px (placeholder "Tìm món...", `borderRadius: var(--r-input)`,
      focus → viền `var(--border-brand)` + `var(--focus-ring)`) rồi `CartIcon` bọc trong `Link to="/cart"`.
      **Không có icon tài khoản** (không login, M2.D-09).
    - Biến thể mobile: trái `<Wordmark variant="bare" size="var(--fs-md)"/>`; phải icon kính lúp (SVG tự
      vẽ) → `CartIcon` + badge → hamburger. Kính lúp mở overlay tìm kiếm full-width **phủ dưới header**
      (không phủ dải danh mục), input tự focus, nút "Huỷ" đóng overlay. Hamburger mở overlay nav dọc 2
      mục như trên.
    - **Ô tìm kiếm ghi trạng thái vào URL** qua `useSearchParams` với key `q` (mẫu: cách
      `apps/web/src/pages/AdminAuditPage.tsx` đọc/ghi `useSearchParams`). Lý do: `MenuPage` (plan 08-09)
      đọc lại `q` từ URL để lọc client-side (D-03) mà không cần context hay prop drilling xuyên
      `<Outlet/>`. Gõ ô tìm kiếm ở trang `/cart` thì điều hướng về `/?q=...`.
    - Input **luôn `fontSize: var(--fs-base)`** (16px) — nhỏ hơn là Safari iOS tự zoom.

    Tạo `AppShell.tsx`: `<div>` nền `var(--bg-page)`, `minHeight: 100vh`, `fontFamily: var(--font-body)`,
    `color: var(--text-strong)`; bên trong `<Header cartCount={...} cartTotal={...} />` rồi `<main>` có
    `maxWidth: var(--content-max)`, `margin: '0 auto'`, `padding: '0 var(--gutter)'` bọc `<Outlet/>`.
    Ở plan này truyền `cartCount={0}` và `cartTotal={0}` **kèm comment 1 dòng chỉ rõ plan 08-06 nối vào
    `useCart()`** — hợp đồng prop được chốt ngay ở đây để 08-06 chỉ cần đổi nguồn dữ liệu, không phải
    sửa cấu trúc.

    Không hardcode hex/px trong 3 file này. Mọi khoảng cách dùng `--sp-*`, mọi nút bấm ngón tay có
    `minHeight/minWidth: var(--tap-min)`.
  </action>
  <verify>
    <automated>pnpm --filter @order/shop typecheck && pnpm --filter @order/shop build</automated>
  </verify>
  <acceptance_criteria>
    - 3 file tồn tại; `grep -c "Outlet" apps/shop/src/components/AppShell.tsx` ≥ 1
    - `grep -c "@media" apps/shop/src/components/Header.tsx` ≥ 1 và `grep -cE "useMediaQuery|addEventListener\('resize'" apps/shop/src/components/Header.tsx` = 0
    - `grep -c "useSearchParams" apps/shop/src/components/Header.tsx` ≥ 1
    - `grep -cE "lucide|react-icons" apps/shop/src/components/*.tsx` = 0
    - `grep -nE "#[0-9a-fA-F]{3,6}\b" apps/shop/src/components/AppShell.tsx apps/shop/src/components/Header.tsx apps/shop/src/components/CartIcon.tsx` không trả kết quả
    - `grep -c "var(--tap-min)" apps/shop/src/components/Header.tsx` ≥ 1
    - `grep -rn "/admin/" apps/shop/src/` không trả kết quả (chuỗi này nằm trong FORBIDDEN của guard bundle)
    - `pnpm --filter @order/shop build` exit 0
  </acceptance_criteria>
  <done>Header sticky 2 biến thể CSS-only, ô tìm kiếm ghi `?q=` vào URL, CartIcon SVG tự vẽ có badge, AppShell bọc `<Outlet/>`.</done>
</task>

<task type="auto">
  <name>Task 3: Guard bundle khách — xác nhận lại + thêm gate kích thước có ghi lý do</name>
  <files>scripts/check-shop-bundle.sh</files>
  <read_first>
    - scripts/check-shop-bundle.sh (file sẽ sửa — đọc toàn bộ: hiện là gate grep chuỗi cấm, KHÔNG có gate kích thước; đọc kỹ ghi chú về false positive `HistoryPage`)
    - .planning/phases/07-shop-infra/07-04-SUMMARY.md (bối cảnh guard được dựng ở phase 7 khi `apps/shop` chưa có route nào)
    - .planning/STATE.md mục Blockers (giải thích vì sao M2.D-64 chỉ có giá trị thật từ phase 8)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-VALIDATION.md mục "Sampling Rate" (cảnh báo bundle guard sẽ đổi)
  </read_first>
  <action>
    Chạy `pnpm --filter @order/shop build` rồi `sh scripts/check-shop-bundle.sh` và ghi kết quả thật.

    Nếu guard báo `LEAK`: **không tắt guard, không xoá chuỗi khỏi FORBIDDEN**. Nguyên nhân thực tế duy
    nhất có thể xảy ra ở plan này là chuỗi `/admin/` xuất hiện trong source `apps/shop` (kể cả trong
    comment) — sửa source, đừng sửa guard.

    Sau đó **thêm gate kích thước** vào cùng script (giữ nguyên toàn bộ gate grep đang có):
    1. Đo tổng kích thước JS đã build: `du -k apps/shop/dist/assets/*.js` cộng lại (làm tròn kB).
    2. Thêm vào script biến `MAX_JS_KB` đặt bằng **số đo thật vừa lấy + 30%** (làm tròn lên chục), và
       khối kiểm: nếu tổng vượt thì in `FAIL: bundle JS ... kB > ngưỡng ... kB` và `exit 1`.
    3. Ghi ngay trên biến đó khối comment nêu rõ: (a) phase 07 chưa có route nào nên guard cũ chỉ kiểm
       chuỗi cấm; (b) phase 08 thêm `BrowserRouter` thật + `zod` runtime parse + 5 trang nên kích thước
       tăng là **đúng dự kiến, không phải hồi quy**; (c) con số đo được tại thời điểm đóng plan 08-04 là
       bao nhiêu kB; (d) chừa ~30% cho phase 09 (SSE + trang tracking đầy đủ); (e) khách vào bằng 3G nên
       ngưỡng này là hợp đồng hiệu năng, muốn nâng thì phải ghi lý do vào file này chứ không sửa lặng.
    4. Cập nhật dòng in kết quả cuối để nói rõ đã kiểm **2 gate** (chuỗi cấm + kích thước).
    Giữ nguyên `set -e`, giữ nguyên cảnh báo về `HistoryPage` false positive, giữ nguyên `exit $FAIL`
    cho phần grep.
  </action>
  <verify>
    <automated>pnpm --filter @order/shop build && sh scripts/check-shop-bundle.sh</automated>
  </verify>
  <acceptance_criteria>
    - `sh scripts/check-shop-bundle.sh` exit 0 và output chứa `OK`
    - `grep -c "MAX_JS_KB" scripts/check-shop-bundle.sh` ≥ 2 (khai báo + dùng)
    - `grep -c "FORBIDDEN" scripts/check-shop-bundle.sh` ≥ 2 (gate cũ giữ nguyên, không bị thay thế)
    - Khối comment cạnh `MAX_JS_KB` chứa số kB đo được thật và cụm từ `phase 08`
    - Thử nghịch: tạm hạ `MAX_JS_KB` xuống 1 rồi chạy lại → script `exit 1` (chứng minh gate có hiệu lực, sau đó khôi phục giá trị đúng)
    - Số kB đo được ghi vào `08-04-SUMMARY.md`
  </acceptance_criteria>
  <done>Guard xanh với 2 gate; ngưỡng kích thước có số đo thật + lý do ghi trong file; không gate nào bị tắt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundle JS gửi cho khách vô danh | Bất kỳ chuỗi/route quản lý lọt vào đây là lộ bề mặt tấn công (M2.D-64) |
| tham số URL `?q=` do khách nhập | Chuỗi tuỳ ý được render lại trong input và dùng để lọc |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-16 | Information Disclosure | bundle `apps/shop/dist` | mitigate | `scripts/check-shop-bundle.sh` giữ nguyên 11 chuỗi cấm + thêm gate kích thước; chạy trong verify của mọi task FE |
| T-08-17 | Tampering (XSS) | `?q=` render lại vào input | mitigate | React escape mặc định; không dùng `dangerouslySetInnerHTML` ở bất kỳ file nào của `apps/shop` — acceptance criteria grep xác nhận |
| T-08-18 | Denial of Service | bundle phình làm khách 3G bỏ trang | mitigate | Gate `MAX_JS_KB` với số đo thật + lý do; muốn nâng phải ghi lý do vào script |
| T-08-19 | Spoofing | route `*` catch-all | accept | Render lại `MenuPage` cho URL sai, không tiết lộ route nào tồn tại; không có dữ liệu nhạy cảm sau bất kỳ route nào của `apps/shop` |
| T-08-SC | Tampering | `pnpm --filter @order/shop add zod` | mitigate | `08-RESEARCH.md` §Package Legitimacy Audit: `zod` npm, tạo 2020, repo `github.com/colinhacks/zod`, slopcheck OK, **đã dùng trong repo** (`apps/web`, `packages/schemas`, `apps/api`) → Approved, không cần checkpoint chặn |
</threat_model>

<verification>
1. `pnpm --filter @order/shop typecheck` — sạch
2. `pnpm --filter @order/shop build` — thành công
3. `sh scripts/check-shop-bundle.sh` — in `OK`, 2 gate
4. `pnpm --filter @order/shop dev` → mở http://localhost:5174/ và bấm qua đủ 5 route, reload trực tiếp `/cart` và `/o/abc` (SPA fallback của vite config phải trả `index.html`)
5. `grep -rn "BrandPreview" apps/shop/src/main.tsx` — không kết quả
</verification>

<success_criteria>
- 5 route + catch-all hoạt động, reload trực tiếp không 404
- 4 trang cũ có mặt trong `apps/shop/dist` (không còn dead code)
- Header 2 biến thể bằng CSS media query, ô tìm kiếm ghi `?q=` vào URL
- 0 hex/px hardcode trong `.tsx` mới; 0 chuỗi `/admin/` trong `apps/shop/src`
- `check-shop-bundle.sh` xanh với gate chuỗi cấm + gate `MAX_JS_KB` có lý do ghi rõ
</success_criteria>

<output>
Create `.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-04-SUMMARY.md` when done
</output>
