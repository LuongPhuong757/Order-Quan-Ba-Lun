---
phase: 08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n
plan: 07
type: execute
wave: 3
depends_on: ["08-02", "08-05"]
files_modified:
  - apps/api/src/common/csrf-paths.ts
  - apps/api/src/common/csrf-paths.test.ts
  - apps/api/src/common/middleware/csrf-origin.middleware.ts
  - apps/api/src/modules/public/public-store.controller.ts
  - apps/api/src/modules/public/public-menu.controller.ts
  - apps/api/src/modules/public/public-menu.mapper.ts
  - apps/api/src/modules/public/public-menu-shape.test.ts
  - apps/api/src/modules/public/public.module.ts
autonomous: true
requirements: [REQ-I, REQ-K, REQ-L]

must_haves:
  truths:
    - "Mutation tới /api/public/* bị kiểm Origin — site lạ không auto-submit form vào API công khai được"
    - "GET /api/public/menu và /api/public/store gọi được không cần login, không bị chặn Origin"
    - "GET /api/public/menu chỉ trả 7 field mỗi món, không leak field nội bộ nào"
    - "D-09: field images là mảng 0..1 phần tử map từ menu_item.image_url (có ảnh → [url], NULL → []), không đổi schema DB"
    - "GET /api/public/store trả đúng trạng thái công tắc do BE tính, kể cả trường hợp đã tự-ON qua nửa đêm"
    - "Món hết hàng vẫn có trong response (làm mờ ở FE, không ẩn ở BE)"
  artifacts:
    - path: "apps/api/src/common/csrf-paths.ts"
      provides: "pathRequiresCheck thuần, phủ /api/public/*"
      exports: ["pathRequiresCheck"]
    - path: "apps/api/src/modules/public/public-menu.mapper.ts"
      provides: "toPublicMenuItem — whitelist 7 field + assert bằng zod strict"
      contains: "strict()"
    - path: "apps/api/src/modules/public/public-store.controller.ts"
      provides: "GET /api/public/store"
      contains: "apiOk"
    - path: "apps/api/src/modules/public/public-menu.controller.ts"
      provides: "GET /api/public/menu"
      contains: "apiOk"
  key_links:
    - from: "apps/api/src/common/middleware/csrf-origin.middleware.ts"
      to: "apps/api/src/common/csrf-paths.ts"
      via: "import pathRequiresCheck"
      pattern: "from '\\.\\./csrf-paths.js'"
    - from: "apps/api/src/modules/public/public-store.controller.ts"
      to: "SettingsService.getOrderingStatus"
      via: "DI qua SettingsModule"
      pattern: "getOrderingStatus"
---

<objective>
Đóng lỗ hổng CSRF-adjacent ở `/api/public/*` và dựng 2 endpoint đọc mà trang khách gọi đầu tiên:
`GET /api/public/store` (trạng thái công tắc) và `GET /api/public/menu` (cây nhóm hàng + món, đúng 7
field).

Purpose: **Lỗ hổng bảo mật severity HIGH có sẵn trong repo**: `pathRequiresCheck()` trong
`csrf-origin.middleware.ts` chỉ phủ `/admin/` và `/auth/`. Khi `POST /api/public/orders` ra đời (plan
08-10), bất kỳ website nào cũng auto-submit form tới nó mà không bị kiểm Origin — phòng thủ tụt từ 2 lớp
xuống 1 lớp (chỉ còn rate limit + blacklist). Guard phải sửa **trước** endpoint submit, không để endpoint
đó sống một mình dù chỉ 1 commit. Đồng thời M2.D-43 (chỉ 7 field) là success criterion số 5 của phase 8 và
cần được bảo đảm **bằng schema chứ không bằng kỷ luật tay**.

Output: `pathRequiresCheck` tách thành module thuần có test, 2 endpoint GET công khai, mapper 2 lớp
(whitelist thủ công + assert `.strict()` runtime).
</objective>

<execution_context>
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/m1macbook/Desktop/OrderQuanBaLun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-CONTEXT.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-RESEARCH.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-PATTERNS.md
@.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-05-SUMMARY.md
@.planning/phases/07-shop-infra/07-03-SUMMARY.md

<interfaces>
<!-- Hợp đồng cần dùng. -->

apps/api/src/common/middleware/csrf-origin.middleware.ts (hiện tại, 56 dòng):
  const MUTATION_METHODS = new Set(['POST','PUT','PATCH','DELETE'])
  const ALLOWED_ORIGINS = parseAllowedOrigins(process.env.ALLOWED_ORIGIN || 'http://localhost:5173,http://localhost:5174')
  function pathRequiresCheck(path: string): boolean   // ← dòng 21-31, CHƯA export, chỉ phủ /admin/ + /auth/
  use = (req,res,next) => { if (!MUTATION_METHODS.has(req.method)) return next();
    if (!pathRequiresCheck(req.path)) return next(); ... isOriginAllowed(origin, ALLOWED_ORIGINS) ... }
  Lỗi: `ForbiddenException({ code: 'CSRF_ORIGIN_MISMATCH', message })`

apps/api/src/modules/public/public.controller.ts — **khuôn mẫu bắt buộc** cho mọi route `/api/public/*`:
  `@Controller('api/public')` (không `setGlobalPrefix`) · success = `apiOk<T>(data)` từ `@order/utils`
  · error = giữ shape compact của `GlobalExceptionFilter` · không `@Throttle` cho GET (global 600/phút đã áp)

apps/api/src/modules/public/public.module.ts — hiện `controllers: [PublicController]`, chưa có
  `TypeOrmModule.forFeature` (docblock ghi phase 08 mới cần)

apps/api/src/modules/settings/settings.service.ts (plan 08-05):
  readAll(): Promise<StoreSettingsMap> · readOrderingSettings() · getOrderingStatus(nowMs): Promise<OrderingStatus>
  · updateMany(patch, actor) — SettingsModule đã `exports: [SettingsService]`

@order/schemas (plan 08-01): PublicMenuItem (7 field) · PublicMenuGroup · PublicStoreStatus

apps/api/src/modules/menu/entities/menu-item.entity.ts — 11 field, trong đó `group`, `is_active`,
  `created_at`, `updated_at`, `image_url` **không** được lọt ra response công khai
apps/api/src/modules/menu/entities/menu-group.entity.ts — có `kitchen_type`, `sort_order`, `is_active`
  (nội bộ) và `id`, `code`, `name`, `icon` (công khai)

apps/api/src/main.ts:42-44 — `useStaticAssets` serve `/uploads/`; `image_url` trong DB là đường dẫn
  tương đối dạng `/uploads/menu/<file>` → `images[]` trả nguyên đường dẫn tương đối này, KHÔNG ghép host
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: [SECURITY] Tách pathRequiresCheck thành module thuần có test và phủ /api/public/*</name>
  <files>apps/api/src/common/csrf-paths.ts, apps/api/src/common/csrf-paths.test.ts, apps/api/src/common/middleware/csrf-origin.middleware.ts</files>
  <read_first>
    - apps/api/src/common/middleware/csrf-origin.middleware.ts (file sẽ sửa — đọc toàn bộ 56 dòng, đặc biệt `pathRequiresCheck` dòng 21-31 và thứ tự lọc trong `use()` dòng 35-37)
    - apps/api/src/common/origin-allowlist.ts (khuôn mẫu: logic thuần tách khỏi middleware, middleware chỉ gọi vào — làm y hệt cho `csrf-paths.ts`)
    - apps/api/src/common/origin-allowlist.test.ts (khuôn mẫu test)
    - .planning/phases/07-shop-infra/07-03-SUMMARY.md (bối cảnh: phase 7 đã nối allow-list vào guard này, đừng regress)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-RESEARCH.md mục "Pitfall 1" (mô tả đầy đủ lỗ hổng + cách sửa)
  </read_first>
  <behavior>
    csrf-paths.test.ts phải phủ:
    - `pathRequiresCheck('/admin/users')` → `true` (hành vi cũ không hồi quy)
    - `pathRequiresCheck('/auth/logout')` → `true`
    - `pathRequiresCheck('/auth/login')` → `false` và `pathRequiresCheck('/auth/recover')` → `false` (2 ngoại lệ pre-auth phải giữ nguyên)
    - `pathRequiresCheck('/api/public/orders')` → `true` (**lỗ hổng đang đóng**)
    - `pathRequiresCheck('/api/public/orders/abc123')` → `true`
    - `pathRequiresCheck('/api/public/menu')` → `true` (hàm này không phân biệt method; `use()` đã lọc `MUTATION_METHODS` trước nên GET không bị chặn oan — ghi comment nói rõ trong test)
    - `pathRequiresCheck('/health')` → `false` và `pathRequiresCheck('/menu')` → `false` (không siết oan route nội bộ đã có)
    - `pathRequiresCheck('/api/publicfoo')` → `false` (không khớp prefix lỏng — phải có dấu `/` sau `public`)
  </behavior>
  <action>
    Tạo `apps/api/src/common/csrf-paths.ts`: chuyển nguyên `pathRequiresCheck` từ middleware sang đây,
    **export**, và thêm nhánh mới `if (path.startsWith('/api/public/')) return true;`. Giữ nguyên 2 ngoại
    lệ `/auth/login` và `/auth/recover`. Comment đầu file phải ghi 3 điều: (1) hàm chỉ trả lời "path này
    có cần kiểm Origin không", việc lọc method do `use()` làm trước nên `GET /api/public/menu` không bao
    giờ tới đây; (2) trước phase 8 hàm này không phủ `/api/public/*`, nên `POST /api/public/orders` sẽ
    sống mà không có lớp phòng thủ Origin — đây là lý do nhánh mới tồn tại (dẫn `08-RESEARCH.md` Pitfall
    #1); (3) `apps/shop` gọi API **cùng origin** nên request thật từ trình duyệt luôn có header `Origin`
    hợp lệ trên mutation — không lo chặn nhầm khách thật.

    Sửa `csrf-origin.middleware.ts`: xoá định nghĩa `pathRequiresCheck` cục bộ, import từ
    `../csrf-paths.js`. Không đổi gì khác — `MUTATION_METHODS`, `ALLOWED_ORIGINS`, 2 nhánh throw
    `CSRF_ORIGIN_MISMATCH` giữ nguyên nguyên văn (phase 7 đã siết `isOriginAllowed` so khớp chính xác
    host, C-SEC-01 — **đừng revert về `startsWith`**).

    Viết `csrf-paths.test.ts` theo khuôn `origin-allowlist.test.ts` phủ đủ `<behavior>`.
  </action>
  <verify>
    <automated>pnpm --filter @order/api test -- csrf-paths.test.ts origin-allowlist.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function pathRequiresCheck" apps/api/src/common/csrf-paths.ts` = 1
    - `grep -c "/api/public/" apps/api/src/common/csrf-paths.ts` ≥ 1
    - `grep -c "function pathRequiresCheck" apps/api/src/common/middleware/csrf-origin.middleware.ts` = 0 (đã chuyển đi, không còn bản sao)
    - `grep -c "csrf-paths.js" apps/api/src/common/middleware/csrf-origin.middleware.ts` = 1
    - `grep -c "startsWith(allowed)" apps/api/src/common/middleware/csrf-origin.middleware.ts` = 0 (không hồi quy C-SEC-01)
    - `pnpm --filter @order/api test -- csrf-paths.test.ts` exit 0 với ≥ 9 test pass; `origin-allowlist.test.ts` vẫn 18/18 xanh
    - Kiểm chéo bằng curl trên dev server: `curl -i -X POST http://localhost:3001/api/public/orders -H 'Content-Type: application/json' -d '{}'` (**không** set Origin) → **403 `CSRF_ORIGIN_MISMATCH`**, KHÔNG phải 404/400
    - `curl -i -X POST http://localhost:3001/api/public/orders -H 'Origin: https://evil.com' -H 'Content-Type: application/json' -d '{}'` → 403 `CSRF_ORIGIN_MISMATCH`
    - `curl -i http://localhost:3001/api/public/health -H 'Origin: https://evil.com'` → **200** (GET không bị siết oan)
  </acceptance_criteria>
  <done>Mutation tới `/api/public/*` không còn đường lách kiểm Origin; hành vi `/admin/*`, `/auth/*` không hồi quy; có 9+ test khoá hành vi.</done>
</task>

<task type="auto">
  <name>Task 2: GET /api/public/store — trạng thái công tắc do BE tính</name>
  <files>apps/api/src/modules/public/public-store.controller.ts, apps/api/src/modules/public/public.module.ts</files>
  <read_first>
    - apps/api/src/modules/public/public.controller.ts (khuôn mẫu bắt buộc: `@Controller('api/public')`, `apiOk()`, docblock; đọc kỹ đoạn nói phase 08 phải dùng lại đúng cặp success/error này)
    - apps/api/src/modules/public/public.module.ts (file sẽ sửa)
    - apps/api/src/modules/settings/settings.service.ts (plan 08-05 — `getOrderingStatus(nowMs)`, `readAll()`)
    - packages/schemas/src/public-store.ts (plan 08-01 — shape response phải khớp `PublicStoreStatus`)
    - docs/MILESTONE-02-ONLINE-ORDERING-SPEC.md §5.1 dòng 361 (danh sách field của `GET /api/public/store`)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-RESEARCH.md §Anti-Patterns (cấm đọc thẳng cột `online_ordering_enabled`)
  </read_first>
  <action>
    Tạo `public-store.controller.ts` với `@Controller('api/public')`, method `@Get('store')`. Inject
    `SettingsService`. Luồng: `const settings = await this.settings.readAll()`;
    `const status = await this.settings.getOrderingStatus(Date.now())`; rồi build response đúng
    `PublicStoreStatus`:
    `ordering_enabled = status.enabled` (**không** phải `settings.online_ordering_enabled` — cột đó có thể
    `false` trong khi thực tế đã tự-ON qua nửa đêm, D-17), `is_open_now = status.is_open_now`,
    `blocking_reason = status.blocking_reason`, `off_reason = settings.online_ordering_off_reason`,
    `store_phone`, `open_hours`, `pickup_enabled`, `delivery_enabled`, `free_ship_km`, `distance_factor`,
    và `eta = { pickup: { min: eta_pickup_min, max: eta_pickup_max }, delivery: { min: eta_delivery_min, max: eta_delivery_max } }`.
    **Không trả** `store_lat`/`store_lng`, `escalate_*`, `notify_*_recipients` — đó là dữ liệu nội bộ,
    khách không cần và không nên biết. Trước khi `return apiOk(...)`, gọi
    `PublicStoreStatus.strict().parse(payload)` — nếu ai đó sau này spread thêm field nội bộ thì test/dev
    throw ngay chứ không âm thầm leak ra production.

    Response phải **không cache**: đặt header `Cache-Control: no-store` (qua `@Header('Cache-Control', 'no-store')`).
    Công tắc phải phản ánh ngay — khoảng cách giữa "chủ quán tắt" và "khách bị chặn" phải bằng 0, mà
    trình duyệt/proxy cache 60s là đủ để mất một đơn.

    Không `@Throttle` riêng (global `default` 600/phút/IP đã áp cho GET — theo docblock của
    `public.controller.ts`).

    Sửa `public.module.ts`: thêm `imports: [SettingsModule, TypeOrmModule.forFeature([MenuItem, MenuGroup])]`
    (entity cho Task 3) và thêm `PublicStoreController` vào `controllers`. Cập nhật docblock của module: liệt
    kê endpoint hiện có sau phase 8 và ghi rõ `SettingsService` là **đường duy nhất** để biết trạng thái
    công tắc.
  </action>
  <verify>
    <automated>pnpm --filter @order/api typecheck && pnpm --filter @order/api test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@Controller('api/public')" apps/api/src/modules/public/public-store.controller.ts` = 1
    - `grep -c "getOrderingStatus" apps/api/src/modules/public/public-store.controller.ts` = 1
    - `grep -c "online_ordering_enabled" apps/api/src/modules/public/public-store.controller.ts` = 0 (không đọc thẳng cột)
    - `grep -cE "store_lat|store_lng|escalate_|notify_" apps/api/src/modules/public/public-store.controller.ts` = 0
    - `grep -c "strict()" apps/api/src/modules/public/public-store.controller.ts` = 1
    - `grep -c "no-store" apps/api/src/modules/public/public-store.controller.ts` = 1
    - Chạy thật: `curl -s http://localhost:3001/api/public/store | jq '.data | keys'` liệt kê đúng 11 key (`blocking_reason, delivery_enabled, distance_factor, eta, free_ship_km, is_open_now, off_reason, open_hours, ordering_enabled, pickup_enabled, store_phone`) — không nhiều hơn
    - Tắt công tắc qua `PUT /admin/settings` với mode `UNTIL_TOMORROW` rồi gọi lại → `ordering_enabled: false`, `blocking_reason: "MANUAL_OFF"`, `off_reason` đúng chuỗi đã nhập
    - Gọi **không** cần cookie/login → 200 (endpoint công khai)
  </acceptance_criteria>
  <done>Trang khách có 1 endpoint duy nhất để biết quán có nhận đơn không, kết quả do BE tính (kể cả tự-ON qua nửa đêm), không cache, không leak field nội bộ.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GET /api/public/menu — mapper whitelist 7 field + test hình dạng (criterion 5)</name>
  <files>apps/api/src/modules/public/public-menu.mapper.ts, apps/api/src/modules/public/public-menu-shape.test.ts, apps/api/src/modules/public/public-menu.controller.ts, apps/api/src/modules/public/public.module.ts</files>
  <read_first>
    - apps/api/src/modules/public/public.controller.ts (khuôn mẫu response)
    - apps/api/src/modules/menu/menu.controller.ts method `list` dòng ~102-137 (cách query `menu_items` bằng `createQueryBuilder`, cách lọc `is_active`, thứ tự sort)
    - apps/api/src/modules/menu/entities/menu-item.entity.ts và menu-group.entity.ts (đầy đủ field — để biết cái gì phải loại)
    - packages/schemas/src/public-menu.ts (plan 08-01 — `PublicMenuItem` 7 field, `PublicMenuGroup`)
    - apps/api/src/common/origin-allowlist.test.ts (khuôn mẫu test)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-RESEARCH.md mục "Pattern 4" (chiến lược 2 lớp: whitelist thủ công + assert runtime)
    - .planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-CONTEXT.md D-09, D-10, D-11 (images[] map 0..1, placeholder khi không ảnh)
  </read_first>
  <behavior>
    public-menu-shape.test.ts phải phủ (chạy thuần trên mapper, không cần DB):
    - `toPublicMenuItem(entityGiả)` trả object có **đúng 7 key**: assert `Object.keys(result).sort()` bằng `['code','id','images','is_out_of_stock','name','price','unit']`
    - Entity có `image_url = '/uploads/menu/x.webp'` → `images` = `['/uploads/menu/x.webp']` (D-09)
    - Entity có `image_url = null` → `images` = `[]` (D-09, mảng rỗng chứ không phải `null`)
    - Entity có thêm field nội bộ (`unit_cost`, `created_at`, `is_active`, `group`) → chúng **không** xuất hiện trong output
    - `PublicMenuItem.strict().parse(mapperOutput)` không throw
    - `PublicMenuItem.strict().parse({ ...mapperOutput, created_at: 123 })` **throw** — chứng minh lớp assert bắt được leak nếu ai đó sửa mapper thành spread
    - `toPublicMenuGroup(groupEntity, items)` trả đúng 5 key `['code','icon','id','items','name']` — không có `kitchen_type`, `sort_order`, `is_active`
    - Món `is_out_of_stock: true` **vẫn có** trong output của mapper (M2.D-31: làm mờ ở FE, không ẩn ở BE)
  </behavior>
  <action>
    Tạo `public-menu.mapper.ts` với 2 hàm thuần (không import `typeorm`/`@nestjs/*`, chỉ import type entity
    và schema):
    - `toPublicMenuItem(m: MenuItem): PublicMenuItem` — dựng **object literal 7 field tường minh**, tuyệt
      đối KHÔNG `{...m}`. `images: m.image_url ? [m.image_url] : []` (D-09 — không đổi schema DB, giữ hợp
      đồng `images[]` mà M2.D-43 mô tả để sau thêm nhiều ảnh thì FE không phải sửa). Cuối hàm
      `return PublicMenuItem.strict().parse(out)` — **2 lớp**: whitelist thủ công + assert runtime, đúng
      yêu cầu "đảm bảo bằng schema chứ không bằng kỷ luật tay".
    - `toPublicMenuGroup(g: MenuGroup, items: PublicMenuItem[]): PublicMenuGroup` — 5 field tường minh +
      `.strict().parse()` tương tự.
    Comment đầu file: đây là **hàng rào duy nhất** giữa entity nội bộ và response công khai (success
    criterion 5 của ROADMAP phase 8 / M2.D-43); mọi field mới thêm vào `menu_items` sẽ tự động **không**
    lọt ra ngoài, và nếu ai đó sửa thành spread thì `public-menu-shape.test.ts` đỏ ngay.

    Tạo `public-menu.controller.ts` với `@Controller('api/public')`, `@Get('menu')`. Inject
    `@InjectRepository(MenuItem)` và `@InjectRepository(MenuGroup)`. Luồng:
    - Lấy group `is_active = true` sắp theo `sort_order ASC, name ASC`.
    - Lấy item `is_active = true` sắp theo `name ASC` (**không** lọc `is_out_of_stock` — M2.D-31 làm mờ
      không ẩn), gom theo `item.group === group.code`.
    - Map qua mapper, bỏ group không còn món nào (nhóm rỗng làm dải danh mục có tile chết).
    - Item có `group` không khớp group nào đang active → gom vào cuối dưới một nhóm `{ code: 'other', name: 'Khác' }`
      nếu group `other` không tồn tại; không được **rơi mất món** (menu thiếu món là lỗi khách thấy ngay).
    - Trả `apiOk({ groups: [...] })`. Header `Cache-Control: no-store` — menu đổi giá/hết hàng phải hiện
      ngay, và D-03 đã chọn tải 1 lần toàn bộ nên mỗi lần vào trang là 1 request duy nhất.
    - **Tải 1 lần toàn bộ, không phân trang, không tham số `q`** (D-03: lọc nhóm và tìm kiếm đều
      client-side; menu 1 quán lẩu chỉ vài chục món). Không tạo endpoint search.
    Thêm `PublicMenuController` vào `controllers` của `public.module.ts`.
    Viết `public-menu-shape.test.ts` phủ đủ `<behavior>`.
  </action>
  <verify>
    <automated>pnpm --filter @order/api test -- public-menu-shape.test.ts && pnpm --filter @order/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.\.\." apps/api/src/modules/public/public-menu.mapper.ts` = 0 (không spread entity ở bất kỳ đâu)
    - `grep -c "strict()" apps/api/src/modules/public/public-menu.mapper.ts` = 2
    - `pnpm --filter @order/api test -- public-menu-shape.test.ts` exit 0 với ≥ 8 test pass
    - `grep -cE "is_out_of_stock: false|is_out_of_stock = false|oos" apps/api/src/modules/public/public-menu.controller.ts` = 0 (không lọc món hết hàng ra khỏi response)
    - Chạy thật: `curl -s http://localhost:3001/api/public/menu | jq '.data.groups[0].items[0] | keys'` in ra đúng `["code","id","images","is_out_of_stock","name","price","unit"]`
    - `curl -s http://localhost:3001/api/public/menu | jq '[.data.groups[].items[] | keys] | flatten | unique'` — kết quả **chỉ** gồm 7 key trên, không có `group`/`created_at`/`is_active`/`image_url`
    - `curl -s http://localhost:3001/api/public/menu | jq '[.data.groups[].items[] | select(.is_out_of_stock)] | length'` ≥ 1 sau khi đánh dấu 1 món hết hàng qua `/menu/:id/toggle-stock` (món hết hàng vẫn có trong response)
    - Gọi không cần login → 200
    - `pnpm --filter @order/api test` full suite exit 0
  </acceptance_criteria>
  <done>`GET /api/public/menu` trả cây nhóm hàng + món chỉ 7 field, món hết hàng vẫn có mặt, có 8+ test khoá hình dạng — success criterion 5 của phase 8 đã có bằng chứng tự động.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bất kỳ site nào trên internet → `POST /api/public/*` | Không có cookie nhưng vẫn spam/auto-submit được nếu không kiểm Origin |
| entity `MenuItem`/`MenuGroup` nội bộ → response công khai | Ranh giới rò rỉ dữ liệu quan trọng nhất của phase 8 |
| trình duyệt/proxy cache → trạng thái công tắc | Cache làm khách thấy trạng thái cũ và gửi đơn khi quán đã tắt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-32 | Tampering | `pathRequiresCheck` không phủ `/api/public/*` (**HIGH, lỗ hổng có sẵn**) | mitigate | Thêm nhánh `startsWith('/api/public/')`; 9 test khoá hành vi; kiểm chéo bằng curl không-Origin → 403. Phải xanh **trước** khi plan 08-10 chạy |
| T-08-33 | Information Disclosure | leak field nội bộ qua `/api/public/menu` (ASVS V13) | mitigate | 2 lớp: object literal 7 field (cấm spread, grep xác nhận) + `PublicMenuItem.strict().parse()`; test có case input "bẩn" phải throw |
| T-08-34 | Information Disclosure | `/api/public/store` trả toạ độ quán / cấu hình leo thang | mitigate | Response whitelist 11 field + `.strict().parse()`; grep chặn `store_lat`/`escalate_`/`notify_` |
| T-08-35 | Spoofing | so khớp Origin lỏng (`quanbalun.site.evil.com`) | mitigate | Giữ nguyên `isOriginAllowed` so khớp chính xác host của phase 7 (C-SEC-01); grep chặn `startsWith(allowed)` quay lại |
| T-08-36 | Tampering | khách thấy trạng thái công tắc cũ do cache | mitigate | `Cache-Control: no-store` trên cả `/store` và `/menu`; BE vẫn kiểm lại lúc submit (08-10) nên cache không phải lớp phòng thủ duy nhất |
| T-08-37 | Denial of Service | GET công khai bị spam | accept | Throttler global 600 req/phút/IP đã áp từ phase 7; GET là read-only, không tốn transaction |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | Plan này KHÔNG cài package nào |
</threat_model>

<verification>
1. `pnpm --filter @order/api test` — full suite xanh (18 cũ + csrf-paths + public-menu-shape + các test plan trước)
2. `pnpm -r typecheck` — sạch
3. curl 403 cho `POST /api/public/orders` không Origin và Origin lạ; 200 cho GET với Origin lạ
4. `jq` xác nhận đúng 7 key mỗi món và 11 key ở `/store`
5. Bật/tắt công tắc qua `/admin/settings` → `/api/public/store` phản ánh ngay lần gọi kế tiếp
</verification>

<success_criteria>
- `pathRequiresCheck` là module thuần có test, phủ `/api/public/*`, không hồi quy `/admin/*` `/auth/*`
- `POST /api/public/*` không Origin hoặc Origin lạ → 403 `CSRF_ORIGIN_MISMATCH`
- `GET /api/public/store` trả đúng 11 field, `ordering_enabled` lấy từ `getOrderingStatus`
- `GET /api/public/menu` trả đúng 7 field/món, món hết hàng vẫn có mặt
- 2 endpoint gọi được không cần login, có `Cache-Control: no-store`
</success_criteria>

<output>
Create `.planning/phases/08-menu-c-ng-khai-checkout-c-ng-t-c-nh-n-n/08-07-SUMMARY.md` when done
</output>
