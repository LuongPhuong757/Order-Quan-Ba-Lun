---
phase: 07-shop-infra
plan: 03
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - apps/api/src/common/middleware/csrf-origin.middleware.ts
  - .env.example
  - .env.production.example
autonomous: true
requirements: [REQ-Q]
user_setup: []

must_haves:
  truths:
    - "Submit từ origin `order.<domain>` không bị CSRF chặn"
    - "Origin lạ vẫn bị chặn với code CSRF_ORIGIN_MISMATCH"
    - "Origin dạng prefix-spoof bị chặn (trước đây lọt)"
    - "ALLOWED_ORIGIN đọc được dạng danh sách phân tách dấu phẩy ở cả 2 file env mẫu"
  artifacts:
    - path: "apps/api/src/common/middleware/csrf-origin.middleware.ts"
      provides: "CSRF guard dùng allow-list so khớp chính xác"
      contains: "isOriginAllowed"
    - path: ".env.example"
      provides: "ALLOWED_ORIGIN dạng danh sách 2 origin"
      contains: "ALLOWED_ORIGIN"
    - path: ".env.production.example"
      provides: "ALLOWED_ORIGIN cho production (hiện đang thiếu hoàn toàn)"
      contains: "ALLOWED_ORIGIN"
  key_links:
    - from: "apps/api/src/common/middleware/csrf-origin.middleware.ts"
      to: "apps/api/src/common/origin-allowlist.ts"
      via: "import { parseAllowedOrigins, isOriginAllowed } từ '../origin-allowlist.js'"
      pattern: "origin-allowlist"
---

<objective>
Nối module allow-list đã test vào `CsrfOriginGuard`, và đưa `ALLOWED_ORIGIN` dạng danh sách vào cả 2 file env mẫu.

Purpose: M2.D-67 chốt "ALLOWED_ORIGIN thành danh sách 2 origin". C-SEC-01 mở rộng: giữ `startsWith` thì thêm origin vẫn để hở prefix-spoofing. Plan 07-02 đã có hàm đúng + test; plan này chỉ còn nối dây và bỏ code cũ.
Output: middleware dùng so khớp chính xác, `.env.example` + `.env.production.example` khai báo 2 origin.
</objective>

<execution_context>
@C:/Users/Admin/Desktop/QuanBaLun/Order-Quan-Ba-Lun/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/Desktop/QuanBaLun/Order-Quan-Ba-Lun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-shop-infra/07-CONTEXT.md
@.planning/phases/07-shop-infra/07-02-SUMMARY.md
@apps/api/src/common/middleware/csrf-origin.middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thay startsWith bằng allow-list so khớp chính xác</name>
  <files>apps/api/src/common/middleware/csrf-origin.middleware.ts</files>
  <read_first>apps/api/src/common/middleware/csrf-origin.middleware.ts, apps/api/src/common/origin-allowlist.ts</read_first>
  <action>
1. Import module thuần: `import { parseAllowedOrigins, isOriginAllowed } from '../origin-allowlist.js';`

2. Parse allow-list **một lần** khi khởi tạo, không parse mỗi request:
   ```ts
   const ALLOWED = parseAllowedOrigins(
     process.env.ALLOWED_ORIGIN || 'http://localhost:5173,http://localhost:5174',
   );
   ```
   Default local đổi thành **2 origin** (5173 = web admin, 5174 = shop, đúng port strict của `apps/shop`).
   Đặt ở module scope hoặc constructor — không đặt trong `use`.

3. Xoá hẳn khối so sánh cũ ở dòng 34–40 (`if (!origin.startsWith(allowed))`) và thay bằng:
   ```ts
   if (!isOriginAllowed(origin, ALLOWED)) {
     throw new ForbiddenException({
       code: 'CSRF_ORIGIN_MISMATCH',
       message: `Origin ${origin} not in allowed list`,
     });
   }
   ```
   Giữ nguyên `code` và `message` — đừng đổi, FE/log đang dựa vào `CSRF_ORIGIN_MISMATCH`.

4. Giữ nguyên `MUTATION_METHODS`, `pathRequiresCheck`, và nhánh `if (!origin)`. Phase 7 **không** mở check sang `/api/public/*` — đó là phase 8 (D-10).

5. Cập nhật comment đầu file: ghi rõ M2.D-67 + C-SEC-01 và **vì sao** không dùng `startsWith` nữa, để người sau không revert.
  </action>
  <verify>cd apps/api &amp;&amp; pnpm test &amp;&amp; pnpm typecheck</verify>
  <acceptance_criteria>
    - `csrf-origin.middleware.ts` chứa `isOriginAllowed` và `parseAllowedOrigins`
    - `csrf-origin.middleware.ts` KHÔNG còn chuỗi `startsWith(allowed)`
    - `parseAllowedOrigins` được gọi ở module/constructor scope, KHÔNG bên trong hàm `use`
    - default fallback chứa cả `5173` và `5174`
    - vẫn còn `code: 'CSRF_ORIGIN_MISMATCH'`
    - `pathRequiresCheck` không đổi (vẫn chỉ `/admin/` và `/auth/`)
    - `pnpm test` + `pnpm typecheck` pass
  </acceptance_criteria>
  <done>Middleware dùng so khớp chính xác; test của 07-02 vẫn xanh</done>
</task>

<task type="auto">
  <name>Task 2: ALLOWED_ORIGIN dạng danh sách trong cả 2 file env mẫu</name>
  <files>.env.example, .env.production.example</files>
  <read_first>.env.example, .env.production.example</read_first>
  <action>
1. `.env.example` dòng 25: đổi
   `ALLOWED_ORIGIN=http://localhost:5173`
   thành
   `ALLOWED_ORIGIN=http://localhost:5173,http://localhost:5174`
   và thêm comment ngay trên:
   `# Danh sách phân tách dấu phẩy (M2.D-67). 5173 = web admin, 5174 = shop khách.`
   `# So khớp CHÍNH XÁC protocol+host, không phải prefix (C-SEC-01).`

2. `.env.production.example` — file này hiện **KHÔNG có** key `ALLOWED_ORIGIN` (đã verify bằng grep). Thêm mới, đặt cạnh các biến bảo mật khác, dùng biến `DOMAIN` nếu file đó có:
   `# === CSRF Origin check (P01.D-12, M2.D-67) ===`
   `# Danh sách phân tách dấu phẩy: apex (admin) + order. (khách). So khớp chính xác.`
   `ALLOWED_ORIGIN=https://quanbalun.site,https://order.quanbalun.site`

3. Đọc `.env.production.example` trước để khớp đúng style comment và cách đặt tên miền mà file đó đang dùng (hardcode `quanbalun.site` vs `${DOMAIN}`). Theo style có sẵn, đừng áp style mới.

4. **KHÔNG** sửa `.env` thật (đang gitignore, là config máy của chủ dự án). Nếu `.env` local cần 2 origin để test thì ghi hướng dẫn vào SUMMARY, để chủ dự án tự sửa.
  </action>
  <verify>grep -n "ALLOWED_ORIGIN" .env.example .env.production.example</verify>
  <acceptance_criteria>
    - `.env.example` chứa `ALLOWED_ORIGIN=http://localhost:5173,http://localhost:5174`
    - `.env.production.example` chứa dòng `ALLOWED_ORIGIN=` với 2 origin phân tách dấu phẩy
    - cả 2 file có comment nhắc M2.D-67 và việc so khớp chính xác
    - `git diff` KHÔNG chứa file `.env` (không có đuôi `.example`)
  </acceptance_criteria>
  <done>Cả 2 file env mẫu khai báo danh sách 2 origin, có comment giải thích</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd apps/api && pnpm test` pass (test của 07-02 vẫn xanh sau khi nối dây)
- [ ] `cd apps/api && pnpm typecheck` pass
- [ ] `grep -rn "startsWith(allowed)" apps/api/src` không có kết quả
- [ ] `git diff --stat` không có `.env` (chỉ `.env.example`, `.env.production.example`)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Lỗ prefix-spoofing đóng lại, có test chứng minh
- `pathRequiresCheck` không bị nới rộng ngoài phạm vi phase 7
</success_criteria>

<output>
After completion, create `.planning/phases/07-shop-infra/07-03-SUMMARY.md`
</output>
